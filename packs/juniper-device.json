{
  "schemaVersion": 1,
  "packVersion": "1.0.0",
  "id": "juniper-device",
  "name": "Juniper Device",
  "collection": "juniper.device",
  "collectionVersion": "2.0.1",
  "minAppVersion": "1.0.0",
  "testedWithAppVersion": "0.1.0",
  "publishedAt": "2026-07-09T05:24:16Z",
  "qualityLevel": "generated",
  "source": "generated",
  "categoryColor": "#84B135",
  "blockStyleName": "juniper_device_blocks",
  "categoryStyleName": "juniper_device_category",
  "modules": [
    {
      "fqcn": "juniper.device._tbrdevice_hostname",
      "blockType": "juniper_device__tbrdevice_hostname",
      "label": "_tbrdevice_hostname",
      "tooltip": "Manage Hostname server configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/_tbrdevice_hostname_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system hostname). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device. The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result. For state I(rendered) active connection to remote host is not required. The states I(merged), I(replaced) and I(overridden) have identical behaviour for this module. The state I(gathered) will fetch the running configuration from device and transform it into structured data in the format as per the resource module argspec and the value is returned in the I(gathered) key within the result. The state I(parsed) reads the configuration from C(running_config) option and transforms it into JSON format as per the resource module parameters and the value is returned in the I(parsed) key within the result. The value of C(running_config) option should be the same format as the output of command I(show system hostname) executed on device. For state I(parsed) active connection to remote host is not required.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "Specify the hostname."
        }
      ]
    },
    {
      "fqcn": "juniper.device.command",
      "blockType": "juniper_device_command",
      "label": "command",
      "tooltip": "Execute one or more CLI commands on a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/command_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "commands",
          "fieldId": "COMMANDS",
          "type": "text",
          "label": "commands",
          "yamlType": "list",
          "default": "none",
          "description": "A list of one or more CLI commands to execute on the Junos device.",
          "required": true,
          "aliases": [
            "cli",
            "command",
            "cmd",
            "cmds"
          ]
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": "None",
          "description": "The path to a file, on the Ansible control machine, where the output of the cli command will be saved. The file must be writeable. If the file already exists, it is overwritten. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the value of the I(dest) option. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(dest_dir) option in new playbooks. The I(dest) and I(dest_dir) options are mutually exclusive.",
          "aliases": [
            "destination"
          ]
        },
        {
          "key": "dest_dir",
          "fieldId": "DEST_DIR",
          "type": "text",
          "label": "dest dir",
          "default": "None",
          "description": "The path to a directory, on the Ansible control machine, where the output of the cli command will be saved. The output will be logged to a file named C({{ inventory_hostname }}_)I(command)C(.)I(format) in the directory specified by the value of the I(dest_dir) option. The destination file must be writeable. If the file already exists, it is overwritten. It is the users responsibility to ensure a unique I(dest_dir) value is provided for each execution of this module within a playbook. The I(dest_dir) and I(dest) options are mutually exclusive. The I(dest_dir) option is recommended for all new playbooks.",
          "aliases": [
            "destination_dir",
            "destdir"
          ]
        },
        {
          "key": "formats",
          "fieldId": "FORMATS",
          "type": "dropdown",
          "label": "formats",
          "default": "text",
          "description": "The format of the reply for the CLI command(s) specified by the I(commands) option. The specified format(s) must be supported by the target Junos device. The value of this option can either be a single format, or a list of formats. If a single format is specified, it applies to all command(s) specified by the I(commands) option. If a list of formats are specified, there must be one value in the list for each command specified by the I(commands) option. Specifying the value C(xml) for the I(formats) option is similar to appending C(| display xml) to a CLI command, and specifying the value C(json) for the I(formats) option is similar to appending C(| display json) to a CLI command.",
          "options": [
            [
              "text",
              "text"
            ],
            [
              "xml",
              "xml"
            ],
            [
              "json",
              "json"
            ]
          ],
          "aliases": [
            "format",
            "display",
            "output"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "return_output",
          "fieldId": "RETURN_OUTPUT",
          "type": "boolean",
          "label": "return output",
          "default": true,
          "description": "Indicates if the output of the command should be returned in the module's response. You might want to set this option to C(false), and set the I(dest_dir) option, if the command output is very large and you only need to save the output rather than using it's content in subsequent tasks/plays of your playbook."
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.config",
      "blockType": "juniper_device_config",
      "label": "config",
      "tooltip": "Manipulate the configuration of a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/config_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "check",
          "fieldId": "CHECK",
          "type": "boolean",
          "label": "check",
          "default": true,
          "description": "Perform a commit check operation.",
          "aliases": [
            "check_commit",
            "commit_check"
          ]
        },
        {
          "key": "check_commit_wait",
          "fieldId": "CHECK_COMMIT_WAIT",
          "type": "number",
          "label": "check commit wait",
          "description": "The number of seconds to wait between check and commit operations. This option is only valid if I(check) is C(true) and I(commit) is C(true). This option should not normally be needed. It works around an issue in some versions of Junos."
        },
        {
          "key": "comment",
          "fieldId": "COMMENT",
          "type": "text",
          "label": "comment",
          "default": "none",
          "description": "Provide a comment to be used with the commit operation. This option is only valid if the I(commit) option is true."
        },
        {
          "key": "commit",
          "fieldId": "COMMIT",
          "type": "boolean",
          "label": "commit",
          "default": true,
          "description": "Perform a commit operation."
        },
        {
          "key": "commit_empty_changes",
          "fieldId": "COMMIT_EMPTY_CHANGES",
          "type": "boolean",
          "label": "commit empty changes",
          "default": false,
          "description": "Perform a commit operation, even if there are no changes between the candidate configuration and the committed configuration."
        },
        {
          "key": "commit_force_sync",
          "fieldId": "COMMIT_FORCE_SYNC",
          "type": "boolean",
          "label": "commit force sync",
          "default": false,
          "description": "On dual control plane systems, forces the candidate configuration on one control plane to be copied to the other control plane."
        },
        {
          "key": "commit_full",
          "fieldId": "COMMIT_FULL",
          "type": "boolean",
          "label": "commit full",
          "default": false,
          "description": "Coupled with commit=True, this will perform a C(commit full) operation."
        },
        {
          "key": "commit_sync",
          "fieldId": "COMMIT_SYNC",
          "type": "boolean",
          "label": "commit sync",
          "default": false,
          "description": "On dual control plane systems, requests that the candidate configuration on one control plane be copied to the other control plane, checked for correct syntax, and committed on both Routing engines."
        },
        {
          "key": "config_mode",
          "fieldId": "CONFIG_MODE",
          "type": "dropdown",
          "label": "config mode",
          "default": "exclusive",
          "description": "The mode used to access the candidate configuration database.",
          "options": [
            [
              "exclusive",
              "exclusive"
            ],
            [
              "private",
              "private"
            ],
            [
              "dynamic",
              "dynamic"
            ],
            [
              "batch",
              "batch"
            ],
            [
              "ephemeral",
              "ephemeral"
            ]
          ],
          "aliases": [
            "config_access",
            "edit_mode",
            "edit_access"
          ]
        },
        {
          "key": "confirmed",
          "fieldId": "CONFIRMED",
          "type": "number",
          "label": "confirmed",
          "description": "Provide a confirmed timeout, in minutes, to be used with the commit operation. This option is only valid if the I(commit) option is C(true). The value of this option is the number of minutes to wait for another commit operation before automatically rolling back the configuration change performed by this task. In other words, this option causes the module to perform a C(commit confirmed )I(min) where I(min) is the value of the I(confirmed) option. This option DOES NOT confirm a previous C(commit confirmed )I(min) operation. To confirm a previous commit operation, invoke this module with the I(check) or I(commit) option set to C(true).",
          "aliases": [
            "confirm"
          ]
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": "none",
          "description": "The path to a file, on the local Ansible control machine, where the configuration will be saved if the I(retrieve) option is specified. The file must be writeable. If the file already exists, it is overwritten. This option is only valid if the I(retrieve) option is not C(none). When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(dest) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(dest_dir) option in new playbooks. The I(dest) and I(dest_dir) options are mutually exclusive.",
          "aliases": [
            "destination"
          ]
        },
        {
          "key": "dest_dir",
          "fieldId": "DEST_DIR",
          "type": "text",
          "label": "dest dir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine. This is the directory where the configuration will be saved if the I(retrieve) option is specified. It is also the directory where the configuration diff will be specified if the I(diff) option is C(true). This option is only valid if the I(retrieve) option is not C(none) or the I(diff) option is C(true). The retrieved configuration will be saved to a file named C({{ inventory_hostname }}.)I(format_extension) in the I(dest_dir) directory. Where I(format_extension) is C(conf) for text format, C(xml) for XML format, C(json) for JSON format, and C(set) for set format. If the I(diff) option is C(true), the configuration diff will be saved to a file named C({{ inventory_hostname }}.diff) in the I(dest_dir) directory. The destination file must be writeable. If the file already exists, it is overwritten. It is the users responsibility to ensure a unique I(dest_dir) value is provided for each execution of this module within a playbook. The I(dest_dir) and I(dest) options are mutually exclusive. The I(dest_dir) option is recommended for all new playbooks. The I(dest_dir) and I(diff_file) options are mutually exclusive. The I(dest_dir) option is recommended for all new playbooks.",
          "aliases": [
            "destination_dir",
            "destdir",
            "savedir",
            "save_dir"
          ]
        },
        {
          "key": "diff",
          "fieldId": "DIFF",
          "type": "boolean",
          "label": "diff",
          "default": true,
          "description": "Perform a configuration compare (aka diff) operation.",
          "aliases": [
            "compare",
            "diffs"
          ]
        },
        {
          "key": "diffs_file",
          "fieldId": "DIFFS_FILE",
          "type": "text",
          "label": "diffs file",
          "default": "None",
          "description": "The path to a file, on the Ansible control machine, where the configuration differences will be saved if the I(diff) option is specified. The file must be writeable. If the file already exists, it is overwritten. This option is only valid if the I(diff) option is C(true). When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(diffs_file) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(dest_dir) option in new playbooks. The I(diffs_file) and I(dest_dir) options are mutually exclusive."
        },
        {
          "key": "ephemeral_instance",
          "fieldId": "EPHEMERAL_INSTANCE",
          "type": "text",
          "label": "ephemeral instance",
          "default": "None",
          "description": "To open a user-defined instance of the ephemeral database"
        },
        {
          "key": "filter",
          "fieldId": "FILTER",
          "type": "text",
          "label": "filter",
          "default": "none",
          "description": "A string of XML, or '/'-separated configuration hierarchies, which specifies a filter used to restrict the portions of the configuration which are retrieved. See U(PyEZ's get_config method documentation|http://junos-pyez.readthedocs.io/en/stable/jnpr.junos.html#jnpr.junos.rpcmeta._RpcMetaExec.get_config) for details on the value of this option.",
          "aliases": [
            "filter_xml"
          ]
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "none (auto-detect on load, text on retrieve)",
          "description": "Specifies the format of the configuration retrieved, if I(retrieve) is not C(none). Specifies the format of the configuration to be loaded, if I(load) is not C(none). The specified format must be supported by the target Junos device.",
          "options": [
            [
              "xml",
              "xml"
            ],
            [
              "set",
              "set"
            ],
            [
              "text",
              "text"
            ],
            [
              "json",
              "json"
            ]
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "ignore_warning",
          "fieldId": "IGNORE_WARNING",
          "type": "text",
          "label": "ignore warning",
          "default": "none",
          "description": "A boolean, string or list of strings. If the value is C(true), ignore all warnings regardless of the warning message. If the value is a string, it will ignore warning(s) if the message of each warning matches the string. If the value is a list of strings, ignore warning(s) if the message of each warning matches at least one of the strings in the list. The value of the I(ignore_warning) option is applied to the load and commit operations performed by this module."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "lines",
          "fieldId": "LINES",
          "type": "text",
          "label": "lines",
          "yamlType": "list",
          "default": "none",
          "description": "Used with the I(load) option. Specifies a list of list of configuration strings containing the configuration to be loaded. The I(src), I(lines), I(template), and I(url) options are mutually exclusive. By default, the format of the configuration data is auto-dectected by the content of the first line in the I(lines) list. If the I(format) option is specified, the I(format) value overrides the format auto-detection."
        },
        {
          "key": "load",
          "fieldId": "LOAD",
          "type": "dropdown",
          "label": "load",
          "default": "none",
          "description": "Specifies the type of load operation to be performed. The I(load) and I(rollback) options are mutually exclusive. The choices have the following meanings:\n B(none) - Do not perform a load operation. B(merge) - Combine the new configuration with the existing configuration. If statements in the new configuration conflict with statements in the existing configuration, the statements in the new configuration replace those in the existing configuration. B(replace) - This option is a superset of the B(merge) option. It combines the new configuration with the existing configuration. If the new configuration is in text format and a hierarchy level in the new configuartion is prefixed with the string C(replace:), then the hierarchy level in the new configuration replaces the entire corresponding hierarchy level in the existing configuration, regardles of the existence or content of that hierarchy level in the existing configuration. If the configuration is in XML format, the XML attribute C(replace = \"replace\") is equivalent to the text format's C(replace:) prefix. If a configuration hierarchy in the new configuration is not prefixed with C(replace:), then the B(merge) behavior is used. Specifically, for any statements in the new configuration which conflict with statements in the existing configuration, the statements in the new configuration replace those in the existing configuration. B(override) - Discard the entire existing configuration and replace it with the new configuration. When the configuration is later committed, all system processes are notified and the entire new configuration is marked as 'changed' even if some statements previously existed in the configuration. The value B(overwrite) is a synonym for B(override). B(update) - This option is similar to the B(override) option. The new configuration completely replaces the existing configuration. The difference comes when the configuration is later committed. This option performs a 'diff' between the new candidate configuration and the existing committed configuration. It then only notifies system processes repsonsible for the changed portions of the configuration, and only marks the actual configuration changes as 'changed'. B(set) - This option is used when the new configuration data is in set format (a series of configuration mode commands). The new configuration data is loaded line by line and may contain any configuration mode commands, such as set, delete, edit, or deactivate. This value must be specified if the new configuration is in set format.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "set",
              "set"
            ],
            [
              "merge",
              "merge"
            ],
            [
              "update",
              "update"
            ],
            [
              "replace",
              "replace"
            ],
            [
              "override",
              "override"
            ],
            [
              "overwrite",
              "overwrite"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "model",
          "fieldId": "MODEL",
          "type": "dropdown",
          "label": "model",
          "default": "none",
          "description": "Specifies yang model openconfig/custom/ietf to fetch. When model is True and filter_xml is None, xml is enclosed under <data> so that we get junos as well as other model configurations. In case of custom, user will have to provide the namespace to be fetched using I(namespace) option.",
          "options": [
            [
              "openconfig",
              "openconfig"
            ],
            [
              "ietf",
              "ietf"
            ],
            [
              "custom",
              "custom"
            ]
          ]
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "default": "none",
          "description": "Used with I(model) option. Specifies the custom namespace to be fetched from the database."
        },
        {
          "key": "options",
          "fieldId": "OPTIONS",
          "type": "text",
          "label": "options",
          "yamlType": "dict",
          "default": "None",
          "description": "Additional options, specified as a dictionary of key/value pairs, used when retrieving the configuration. See the U(<get-configuration> RPC documentation| https://www.juniper.net/documentation/en_US/junos/topics/reference/tag-summary/junos-xml-protocol-get-configuration.html) for information on available options."
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "retrieve",
          "fieldId": "RETRIEVE",
          "type": "dropdown",
          "label": "retrieve",
          "default": "none",
          "description": "The configuration database to be retrieved.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "candidate",
              "candidate"
            ],
            [
              "committed",
              "committed"
            ]
          ]
        },
        {
          "key": "return_output",
          "fieldId": "RETURN_OUTPUT",
          "type": "boolean",
          "label": "return output",
          "default": true,
          "description": "Indicates if the output of the I(diff) and I(retreive) options should be returned in the module's response. You might want to set this option to C(false), and set the I(dest_dir) option, if the configuration or diff output is very large and you only need to save the output rather than using it's content in subsequent tasks/plays of your playbook."
        },
        {
          "key": "rollback",
          "fieldId": "ROLLBACK",
          "type": "dropdown",
          "label": "rollback",
          "default": "none",
          "description": "Populate the candidate configuration from a previously committed configuration. This value can be a configuration number between 0 and 49, or the keyword C(rescue) to load the previously saved rescue configuration. By default, some Junos platforms store fewer than 50 previous configurations. Specifying a value greater than the number of previous configurations available, or specifying C(rescue) when no rescue configuration has been saved, will result in an error when the module attempts to perform the rollback. The I(rollback) and I(load) options are mutually exclusive.",
          "options": [
            [
              "0-49",
              "0-49"
            ],
            [
              "rescue",
              "rescue"
            ]
          ]
        },
        {
          "key": "src",
          "fieldId": "SRC",
          "type": "text",
          "label": "src",
          "default": "none",
          "description": "Used with the I(load) option. Specifies the path to a file, on the local Ansible control machine, containing the configuration to be loaded. The I(src), I(lines), I(template), and I(url) options are mutually exclusive. By default, the format of the configuration data is determined by the file extension of this path name. If the file has a C(.conf) extension, the content is treated as text format. If the file has a C(.xml) extension, the content is treated as XML format. If the file has a C(.set) extension, the content is treated as Junos B(set) commands. If the I(format) option is specified, the I(format) value overrides the file-extension based format detection.",
          "aliases": [
            "source",
            "file"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "template",
          "fieldId": "TEMPLATE",
          "type": "text",
          "label": "template",
          "default": "none",
          "description": "The path to a Jinja2 template file, on the local Ansible control machine. This template file, along with the I(vars) option, is used to generate the configuration to be loaded on the target Junos device. The I(src), I(lines), I(template), and I(url) options are mutually exclusive. The I(template) and I(vars) options are required together. If one is specified, the other must be specified.",
          "aliases": [
            "template_path"
          ]
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "default": "none",
          "description": "A URL which specifies the configuration data to load on the target Junos device. The Junos device uses this URL to load the configuration, therefore this URL must be reachable by the target Junos device. The possible formats of this value are documented in the 'url' section of the U(<load-configuration> RPC documentation| https://www.juniper.net/documentation/en_US/junos/topics/reference/tag-summary/junos-xml-protocol-load-configuration.html). The I(src), I(lines), I(template), and I(url) options are mutually exclusive."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        },
        {
          "key": "vars",
          "fieldId": "VARS",
          "type": "text",
          "label": "vars",
          "yamlType": "dict",
          "default": "none",
          "description": "A dictionary of keys and values used to render the Jinja2 template specified by the I(template) option. The I(template) and I(vars) options are required together. If one is specified, the other must be specified.",
          "aliases": [
            "template_vars"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.facts",
      "blockType": "juniper_device_facts",
      "label": "facts",
      "tooltip": "Retrieve facts from a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/facts_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "config_format",
          "fieldId": "CONFIG_FORMAT",
          "type": "dropdown",
          "label": "config format",
          "default": "none",
          "description": "The format of the configuration returned. The specified format must be supported by the target Junos device.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "xml",
              "xml"
            ],
            [
              "set",
              "set"
            ],
            [
              "text",
              "text"
            ],
            [
              "json",
              "json"
            ]
          ]
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "savedir",
          "fieldId": "SAVEDIR",
          "type": "text",
          "label": "savedir",
          "default": "none",
          "description": "A path to a directory, on the Ansible control machine, where facts will be stored in a JSON file. The resulting JSON file is saved in I(savedir)C(/)I(hostname)C(-facts.json). The I(savedir) directory is the value of the I(savedir) option. The I(hostname)C(-facts.json) filename begins with the value of the C(hostname) fact returned from the Junos device, which might be different than the value of the I(host) option passed to the module. If the value of the I(savedir) option is C(none), the default, then facts are NOT saved to a file."
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.file_copy",
      "blockType": "juniper_device_file_copy",
      "label": "file_copy",
      "tooltip": "File put and get over SCP/FTP module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/file_copy_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "action",
          "fieldId": "ACTION",
          "type": "text",
          "label": "action",
          "description": "Type of operation to execute, currently only support get and put",
          "required": true
        },
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "checksum",
          "fieldId": "CHECKSUM",
          "type": "boolean",
          "label": "checksum",
          "description": "Validate the file using MD5 algorithm check"
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "file",
          "fieldId": "FILE",
          "type": "text",
          "label": "file",
          "description": "Name of the file to copy to/from the remote device",
          "required": true
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "local_dir",
          "fieldId": "LOCAL_DIR",
          "type": "text",
          "label": "local dir",
          "description": "path of the local directory where the file is located or needs to be copied to",
          "required": true
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "protocol",
          "fieldId": "PROTOCOL",
          "type": "dropdown",
          "label": "protocol",
          "default": "scp",
          "description": "Set the protocols to transfer the file",
          "options": [
            [
              "scp",
              "scp"
            ],
            [
              "ftp",
              "ftp"
            ]
          ]
        },
        {
          "key": "remote_dir",
          "fieldId": "REMOTE_DIR",
          "type": "text",
          "label": "remote dir",
          "description": "path of the directory on the remote device where the file is located or needs to be copied to",
          "required": true
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "transfer_filename",
          "fieldId": "TRANSFER_FILENAME",
          "type": "text",
          "label": "transfer filename",
          "description": "Name of the transfer file to copy to/from the remote device"
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.jsnapy",
      "blockType": "juniper_device_jsnapy",
      "label": "jsnapy",
      "tooltip": "Execute JSNAPy tests on a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/jsnapy_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "action",
          "fieldId": "ACTION",
          "type": "dropdown",
          "label": "action",
          "default": "none",
          "description": "The JSNAPy action to perform.",
          "options": [
            [
              "check",
              "check"
            ],
            [
              "snapcheck",
              "snapcheck"
            ],
            [
              "snap_pre",
              "snap_pre"
            ],
            [
              "snap_post",
              "snap_post"
            ]
          ],
          "required": true
        },
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "config_file",
          "fieldId": "CONFIG_FILE",
          "type": "text",
          "label": "config file",
          "default": "none",
          "description": "The filename of a JSNAPy configuration file (in YAML format). The I(test_files) option and the I(config_file) option are mutually exclusive. Either the I(test_files) option or the I(config_file) option is required."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dir",
          "fieldId": "DIR",
          "type": "text",
          "label": "dir",
          "default": "/etc/jsnapy/testfiles",
          "description": "The path to the directory containing the JSNAPy test file(s) specified by the I(test_files) option or the JSNAPy configuration file specified by the I(config_file) option.",
          "aliases": [
            "directory"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "test_files",
          "fieldId": "TEST_FILES",
          "type": "text",
          "label": "test files",
          "default": "none",
          "description": "The filename of file(s) in the I(dir) directory. Each file contains JSNAPy test case definitions. The I(test_files) option and the I(config_file) option are mutually exclusive. Either the I(test_files) option or the I(config_file) option is required."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos",
      "blockType": "juniper_device_junos",
      "label": "junos",
      "tooltip": "Run arbitrary commands on an Juniper JUNOS device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "commands",
          "fieldId": "COMMANDS",
          "type": "text",
          "label": "commands",
          "yamlType": "list",
          "description": "The commands to send to the remote junos device.  The resulting output from the command is returned.  If the I(wait_for) argument is provided, the module is not returned until the condition is satisfied or the number of I(retries) has been exceeded."
        },
        {
          "key": "display",
          "fieldId": "DISPLAY",
          "type": "dropdown",
          "label": "display",
          "description": "Encoding scheme to use when serializing output from the device. This handles how to properly understand the output and apply the conditionals path to the result set. For I(rpcs) argument default display is C(xml) and for I(commands) argument default display is C(text). Value C(set) is applicable only for fetching configuration from device.",
          "options": [
            [
              "text",
              "text"
            ],
            [
              "json",
              "json"
            ],
            [
              "xml",
              "xml"
            ],
            [
              "set",
              "set"
            ]
          ],
          "aliases": [
            "format",
            "output"
          ]
        },
        {
          "key": "interval",
          "fieldId": "INTERVAL",
          "type": "number",
          "label": "interval",
          "default": 1,
          "description": "Configures the interval in seconds to wait between retries of the command.  If the command does not pass the specified conditional, the interval indicates how to long to wait before trying the command again."
        },
        {
          "key": "match",
          "fieldId": "MATCH",
          "type": "dropdown",
          "label": "match",
          "default": "all",
          "description": "The I(match) argument is used in conjunction with the I(wait_for) argument to specify the match policy.  Valid values are C(all) or C(any).  If the value is set to C(all) then all conditionals in the I(wait_for) must be satisfied.  If the value is set to C(any) then only one of the values must be satisfied.",
          "options": [
            [
              "any",
              "any"
            ],
            [
              "all",
              "all"
            ]
          ]
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "number",
          "label": "retries",
          "default": 10,
          "description": "Specifies the number of retries a command should be tried before it is considered failed.  The command is run on the target device every retry and evaluated against the I(wait_for) conditionals."
        },
        {
          "key": "rpcs",
          "fieldId": "RPCS",
          "type": "text",
          "label": "rpcs",
          "yamlType": "list",
          "description": "The C(rpcs) argument accepts a list of RPCs to be executed over a netconf session and the results from the RPC execution is return to the playbook via the modules results dictionary."
        },
        {
          "key": "wait_for",
          "fieldId": "WAIT_FOR",
          "type": "text",
          "label": "wait for",
          "yamlType": "list",
          "description": "Specifies what to evaluate from the output of the command and what conditionals to apply.  This argument will cause the task to wait for a particular conditional to be true before moving forward.   If the conditional is not true by the configured retries, the task fails.  See examples.",
          "aliases": [
            "waitfor"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_acl_interfaces",
      "blockType": "juniper_device_junos_acl_interfaces",
      "label": "junos_acl_interfaces",
      "tooltip": "ACL interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_acl_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interfaces). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name/Identifier for the interface."
        }
      ],
      "unmappedParams": [
        "config.access_groups (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_acls",
      "blockType": "juniper_device_junos_acls",
      "label": "junos_acls",
      "tooltip": "ACLs resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_acls_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show firewall). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "afi",
          "fieldId": "AFI",
          "type": "dropdown",
          "label": "afi",
          "description": "Protocol family to use by the acl filter",
          "options": [
            [
              "ipv4",
              "ipv4"
            ],
            [
              "ipv6",
              "ipv6"
            ]
          ],
          "required": true
        }
      ],
      "unmappedParams": [
        "config.acls (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_banner",
      "blockType": "juniper_device_junos_banner",
      "label": "junos_banner",
      "tooltip": "Manage multiline banners on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_banner_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "active",
          "fieldId": "ACTIVE",
          "type": "boolean",
          "label": "active",
          "default": true,
          "description": "Specifies whether or not the configuration is active or deactivated"
        },
        {
          "key": "banner",
          "fieldId": "BANNER",
          "type": "dropdown",
          "label": "banner",
          "description": "Specifies which banner that should be configured on the remote device. Value C(login) indicates system login message prior to authenticating, C(motd) is login announcement after successful authentication.",
          "options": [
            [
              "login",
              "login"
            ],
            [
              "motd",
              "motd"
            ]
          ],
          "required": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Specifies whether or not the configuration is present in the current devices active running configuration.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "text",
          "fieldId": "TEXT",
          "type": "text",
          "label": "text",
          "description": "The banner text that should be present in the remote device running configuration.  This argument accepts a multiline string, with no empty lines. Requires I(state=present)."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_bgp_address_family",
      "blockType": "juniper_device_junos_bgp_address_family",
      "label": "junos_bgp_address_family",
      "tooltip": "Manage BGP Address Family attributes of interfaces on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_bgp_address_family_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols bgp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. State I(deleted) only removes BGP address family attributes that this modules manages and does not negate the BGP neighbor address family completely. Thereby, preserving address-family related configurations under BGP group neighbor context. To delete the address family associated to neighbor use M(junipernetworks.junos.junos_bgp_neighbor_address_family) modules for prior cleanup. Refer to examples for more details.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        }
      ],
      "unmappedParams": [
        "config.address_family (has suboptions)",
        "config.groups (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_bgp_global",
      "blockType": "juniper_device_junos_bgp_global",
      "label": "junos_bgp_global",
      "tooltip": "Manages BGP Global configuration on devices running Juniper JUNOS.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_bgp_global_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols bgp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. State I(purged) removes all (routing-options autonomous-system, bgp global, bgp groups, bgp neighbors, bgp family and bgp group and neighbor family) the BGP configurations from the target device. Use caution with this state. State I(deleted) only removes BGP attributes that this modules manages and does not negate the BGP process completely. Thereby, preserving address-family related configurations under BGP context. Running states I(deleted) and I(replaced) will result in an error if there are address-family configuration lines present under a neighbor.Please use the M(junipernetworks.junos.junos_bgp_address_family) modules for prior cleanup. Refer to examples for more details.",
          "options": [
            [
              "purged",
              "purged"
            ],
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "accept_remote_nexthop",
          "fieldId": "ACCEPT_REMOTE_NEXTHOP",
          "type": "boolean",
          "label": "accept remote nexthop",
          "description": "Allow import policy to specify a non-directly connected next-hop."
        },
        {
          "key": "add_path_display_ipv4_address",
          "fieldId": "ADD_PATH_DISPLAY_IPV4_ADDRESS",
          "type": "boolean",
          "label": "add path display ipv4 address",
          "description": "Display add-path path-id in IPv4 address format."
        },
        {
          "key": "advertise_from_main_vpn_tables",
          "fieldId": "ADVERTISE_FROM_MAIN_VPN_TABLES",
          "type": "boolean",
          "label": "advertise from main vpn tables",
          "description": "Advertise VPN routes from bgp.Xvpn.0 tables in master instance."
        },
        {
          "key": "advertise_inactive",
          "fieldId": "ADVERTISE_INACTIVE",
          "type": "boolean",
          "label": "advertise inactive",
          "description": "Advertise inactive routes."
        },
        {
          "key": "advertise_peer_as",
          "fieldId": "ADVERTISE_PEER_AS",
          "type": "boolean",
          "label": "advertise peer as",
          "description": "Advertise routes received from the same autonomous system."
        },
        {
          "key": "as_number",
          "fieldId": "AS_NUMBER",
          "type": "text",
          "label": "as number",
          "description": "Specify Autonomous system number."
        },
        {
          "key": "asdot_notation",
          "fieldId": "ASDOT_NOTATION",
          "type": "boolean",
          "label": "asdot notation",
          "description": "Enable AS-Dot notation to display true 4 byte AS numbers."
        },
        {
          "key": "authentication_algorithm",
          "fieldId": "AUTHENTICATION_ALGORITHM",
          "type": "dropdown",
          "label": "authentication algorithm",
          "description": "Specify authentication algorithm name.",
          "options": [
            [
              "aes-128-cmac-96",
              "aes-128-cmac-96"
            ],
            [
              "hmac-sha-1-96",
              "hmac-sha-1-96"
            ],
            [
              "md5",
              "md5"
            ]
          ]
        },
        {
          "key": "authentication_key",
          "fieldId": "AUTHENTICATION_KEY",
          "type": "text",
          "label": "authentication key",
          "description": "Specify MD5 authentication key."
        },
        {
          "key": "authentication_key_chain",
          "fieldId": "AUTHENTICATION_KEY_CHAIN",
          "type": "text",
          "label": "authentication key chain",
          "description": "Specify authentication key chain name."
        },
        {
          "key": "cluster_id",
          "fieldId": "CLUSTER_ID",
          "type": "text",
          "label": "cluster id",
          "description": "Specify cluster identifier."
        },
        {
          "key": "damping",
          "fieldId": "DAMPING",
          "type": "boolean",
          "label": "damping",
          "description": "Enable route flap damping."
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "Specify text description."
        },
        {
          "key": "disable",
          "fieldId": "DISABLE",
          "type": "boolean",
          "label": "disable",
          "description": "Disable BGP."
        },
        {
          "key": "egress_te_sid_stats",
          "fieldId": "EGRESS_TE_SID_STATS",
          "type": "boolean",
          "label": "egress te sid stats",
          "description": "Create BGP-Peer-SID sensor."
        },
        {
          "key": "enforce_first_as",
          "fieldId": "ENFORCE_FIRST_AS",
          "type": "boolean",
          "label": "enforce first as",
          "description": "Enforce neighbor AS is the first AS in AS-PATH attribute (EBGP)."
        },
        {
          "key": "export",
          "fieldId": "EXPORT",
          "type": "text",
          "label": "export",
          "description": "Specify export policy."
        },
        {
          "key": "forwarding_context",
          "fieldId": "FORWARDING_CONTEXT",
          "type": "text",
          "label": "forwarding context",
          "description": "Specify routing-instance used for data-forwarding and transport-session."
        },
        {
          "key": "hold_time",
          "fieldId": "HOLD_TIME",
          "type": "number",
          "label": "hold time",
          "description": "Specify hold time used when negotiating with a peer."
        },
        {
          "key": "holddown_all_stale_labels",
          "fieldId": "HOLDDOWN_ALL_STALE_LABELS",
          "type": "boolean",
          "label": "holddown all stale labels",
          "description": "Hold all BGP stale-labels, facilating make-before-break for new label advertisements."
        },
        {
          "key": "import",
          "fieldId": "IMPORT",
          "type": "text",
          "label": "import",
          "yamlType": "list",
          "description": "Specify import policy."
        },
        {
          "key": "include_mp_next_hop",
          "fieldId": "INCLUDE_MP_NEXT_HOP",
          "type": "boolean",
          "label": "include mp next hop",
          "description": "Include NEXT-HOP attribute in multiprotocol updates."
        },
        {
          "key": "ipsec_sa",
          "fieldId": "IPSEC_SA",
          "type": "text",
          "label": "ipsec sa",
          "description": "Specify IPSec SA name."
        },
        {
          "key": "keep",
          "fieldId": "KEEP",
          "type": "dropdown",
          "label": "keep",
          "description": "Specify how to retain routes in the routing table.",
          "options": [
            [
              "all",
              "all"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "local_address",
          "fieldId": "LOCAL_ADDRESS",
          "type": "text",
          "label": "local address",
          "description": "Specify Address of local end of BGP session."
        },
        {
          "key": "local_interface",
          "fieldId": "LOCAL_INTERFACE",
          "type": "text",
          "label": "local interface",
          "description": "Specify Local interface for IPv6 link local EBGP peering."
        },
        {
          "key": "local_preference",
          "fieldId": "LOCAL_PREFERENCE",
          "type": "text",
          "label": "local preference",
          "description": "Specify value of LOCAL_PREF path attribute."
        },
        {
          "key": "log_updown",
          "fieldId": "LOG_UPDOWN",
          "type": "boolean",
          "label": "log updown",
          "description": "Enable log a message for peer state transitions."
        },
        {
          "key": "loops",
          "fieldId": "LOOPS",
          "type": "number",
          "label": "loops",
          "description": "Specify maximum number of times this AS can be in an AS path."
        },
        {
          "key": "mtu_discovery",
          "fieldId": "MTU_DISCOVERY",
          "type": "boolean",
          "label": "mtu discovery",
          "description": "Enable TCP path MTU discovery."
        },
        {
          "key": "multipath_build_priority",
          "fieldId": "MULTIPATH_BUILD_PRIORITY",
          "type": "dropdown",
          "label": "multipath build priority",
          "description": "Configure the multipath build priority.",
          "options": [
            [
              "low",
              "low"
            ],
            [
              "medium",
              "medium"
            ]
          ]
        },
        {
          "key": "no_advertise_peer_as",
          "fieldId": "NO_ADVERTISE_PEER_AS",
          "type": "boolean",
          "label": "no advertise peer as",
          "description": "Allows to not advertise routes received from the same autonomous system."
        },
        {
          "key": "no_aggregator_id",
          "fieldId": "NO_AGGREGATOR_ID",
          "type": "boolean",
          "label": "no aggregator id",
          "description": "Set router ID in aggregator path attribute to 0."
        },
        {
          "key": "no_client_reflect",
          "fieldId": "NO_CLIENT_REFLECT",
          "type": "boolean",
          "label": "no client reflect",
          "description": "Disable intracluster route redistribution."
        },
        {
          "key": "no_precision_timers",
          "fieldId": "NO_PRECISION_TIMERS",
          "type": "boolean",
          "label": "no precision timers",
          "description": "Specify not to use precision timers for scheduling keepalives."
        },
        {
          "key": "out_delay",
          "fieldId": "OUT_DELAY",
          "type": "number",
          "label": "out delay",
          "description": "Specify how long before exporting routes from routing table."
        },
        {
          "key": "passive",
          "fieldId": "PASSIVE",
          "type": "boolean",
          "label": "passive",
          "description": "Specify to not send open messages to a peer."
        },
        {
          "key": "peer_as",
          "fieldId": "PEER_AS",
          "type": "text",
          "label": "peer as",
          "description": "Specify Autonomous system number in plain number or 'higher 16bits'.'Lower 16 bits' format."
        },
        {
          "key": "precision_timers",
          "fieldId": "PRECISION_TIMERS",
          "type": "boolean",
          "label": "precision timers",
          "description": "Use precision timers for scheduling keepalives."
        },
        {
          "key": "preference",
          "fieldId": "PREFERENCE",
          "type": "text",
          "label": "preference",
          "description": "Specify preference value."
        },
        {
          "key": "rfc6514_compliant_safi129",
          "fieldId": "RFC6514_COMPLIANT_SAFI129",
          "type": "boolean",
          "label": "rfc6514 compliant safi129",
          "description": "Specify Compliance with RFC6514 SAFI129 format."
        },
        {
          "key": "route_server_client",
          "fieldId": "ROUTE_SERVER_CLIENT",
          "type": "boolean",
          "label": "route server client",
          "description": "Enable route server client behavior."
        },
        {
          "key": "send_addpath_optimization",
          "fieldId": "SEND_ADDPATH_OPTIMIZATION",
          "type": "boolean",
          "label": "send addpath optimization",
          "description": "Enable BGP addpath advertisement optimization."
        },
        {
          "key": "sr_preference_override",
          "fieldId": "SR_PREFERENCE_OVERRIDE",
          "type": "text",
          "label": "sr preference override",
          "description": "Replace received segment routing traffic engineering preference value with override value."
        },
        {
          "key": "stale_labels_holddown_period",
          "fieldId": "STALE_LABELS_HOLDDOWN_PERIOD",
          "type": "number",
          "label": "stale labels holddown period",
          "description": "Specify duration (sec) MPLS labels allocated by BGP are kept after they go stale."
        },
        {
          "key": "tcp_aggressive_transmission",
          "fieldId": "TCP_AGGRESSIVE_TRANSMISSION",
          "type": "boolean",
          "label": "tcp aggressive transmission",
          "description": "Enable aggressive transmission of pure TCP ACKs and retransmissions."
        },
        {
          "key": "tcp_mss",
          "fieldId": "TCP_MSS",
          "type": "number",
          "label": "tcp mss",
          "description": "Specify maximum TCP segment size."
        },
        {
          "key": "ttl",
          "fieldId": "TTL",
          "type": "number",
          "label": "ttl",
          "description": "Specify TTL value for the single-hop peer."
        },
        {
          "key": "unconfigured_peer_graceful_restart",
          "fieldId": "UNCONFIGURED_PEER_GRACEFUL_RESTART",
          "type": "boolean",
          "label": "unconfigured peer graceful restart",
          "description": "Specify BGP unconfigured peer graceful restart options."
        },
        {
          "key": "vpn_apply_export",
          "fieldId": "VPN_APPLY_EXPORT",
          "type": "boolean",
          "label": "vpn apply export",
          "description": "Apply BGP export policy when exporting VPN routes."
        }
      ],
      "unmappedParams": [
        "config.advertise_bgp_static (has suboptions)",
        "config.advertise_external (has suboptions)",
        "config.bfd_liveness_detection (has suboptions)",
        "config.bgp_error_tolerance (has suboptions)",
        "config.bmp (has suboptions)",
        "config.egress_te (has suboptions)",
        "config.egress_te_backup_paths (has suboptions)",
        "config.egress_te_set_segment (has suboptions)",
        "config.graceful_restart (has suboptions)",
        "config.groups (has suboptions)",
        "config.idle_after_switch_over (has suboptions)",
        "config.local_as (has suboptions)",
        "config.metric_out (has suboptions)",
        "config.multihop (has suboptions)",
        "config.multipath (has suboptions)",
        "config.outbound_route_filter (has suboptions)",
        "config.output_queue_priority (has suboptions)",
        "config.path_selection (has suboptions)",
        "config.remove_private (has suboptions)",
        "config.snmp_options (has suboptions)",
        "config.traceoptions (has suboptions)",
        "config.traffic_statistics_labeled_path (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_command",
      "blockType": "juniper_device_junos_command",
      "label": "junos_command",
      "tooltip": "Run arbitrary commands on an Juniper JUNOS device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_command_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "commands",
          "fieldId": "COMMANDS",
          "type": "text",
          "label": "commands",
          "yamlType": "list",
          "description": "The commands to send to the remote junos device.  The resulting output from the command is returned.  If the I(wait_for) argument is provided, the module is not returned until the condition is satisfied or the number of I(retries) has been exceeded."
        },
        {
          "key": "display",
          "fieldId": "DISPLAY",
          "type": "dropdown",
          "label": "display",
          "description": "Encoding scheme to use when serializing output from the device. This handles how to properly understand the output and apply the conditionals path to the result set. For I(rpcs) argument default display is C(xml) and for I(commands) argument default display is C(text). Value C(set) is applicable only for fetching configuration from device.",
          "options": [
            [
              "text",
              "text"
            ],
            [
              "json",
              "json"
            ],
            [
              "xml",
              "xml"
            ],
            [
              "set",
              "set"
            ]
          ],
          "aliases": [
            "format",
            "output"
          ]
        },
        {
          "key": "interval",
          "fieldId": "INTERVAL",
          "type": "number",
          "label": "interval",
          "default": 1,
          "description": "Configures the interval in seconds to wait between retries of the command.  If the command does not pass the specified conditional, the interval indicates how to long to wait before trying the command again."
        },
        {
          "key": "match",
          "fieldId": "MATCH",
          "type": "dropdown",
          "label": "match",
          "default": "all",
          "description": "The I(match) argument is used in conjunction with the I(wait_for) argument to specify the match policy.  Valid values are C(all) or C(any).  If the value is set to C(all) then all conditionals in the I(wait_for) must be satisfied.  If the value is set to C(any) then only one of the values must be satisfied.",
          "options": [
            [
              "any",
              "any"
            ],
            [
              "all",
              "all"
            ]
          ]
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "number",
          "label": "retries",
          "default": 10,
          "description": "Specifies the number of retries a command should be tried before it is considered failed.  The command is run on the target device every retry and evaluated against the I(wait_for) conditionals."
        },
        {
          "key": "rpcs",
          "fieldId": "RPCS",
          "type": "text",
          "label": "rpcs",
          "yamlType": "list",
          "description": "The C(rpcs) argument accepts a list of RPCs to be executed over a netconf session and the results from the RPC execution is return to the playbook via the modules results dictionary."
        },
        {
          "key": "wait_for",
          "fieldId": "WAIT_FOR",
          "type": "text",
          "label": "wait for",
          "yamlType": "list",
          "description": "Specifies what to evaluate from the output of the command and what conditionals to apply.  This argument will cause the task to wait for a particular conditional to be true before moving forward.   If the conditional is not true by the configured retries, the task fails.  See examples.",
          "aliases": [
            "waitfor"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_config",
      "blockType": "juniper_device_junos_config",
      "label": "junos_config",
      "tooltip": "Manage configuration on devices running Juniper JUNOS",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_config_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "This argument will cause the module to create a full backup of the current C(running-config) from the remote device before any changes are made. If the C(backup_options) value is not given, the backup file is written to the C(backup) folder in the playbook root directory or role root directory, if playbook is part of an ansible role. If the directory does not exist, it is created."
        },
        {
          "key": "check_commit",
          "fieldId": "CHECK_COMMIT",
          "type": "boolean",
          "label": "check commit",
          "default": false,
          "description": "This argument will check correctness of syntax; do not apply changes. Note that this argument can be used to confirm verified configuration done via commit confirmed operation"
        },
        {
          "key": "comment",
          "fieldId": "COMMENT",
          "type": "text",
          "label": "comment",
          "default": "configured by junos_config",
          "description": "The C(comment) argument specifies a text string to be used when committing the configuration.  If the C(confirm) argument is set to False, this argument is silently ignored."
        },
        {
          "key": "confirm",
          "fieldId": "CONFIRM",
          "type": "number",
          "label": "confirm",
          "default": 0,
          "description": "The C(confirm) argument will configure a time out value in minutes for the commit to be confirmed before it is automatically rolled back. If the value for this argument is set to 0, the commit is confirmed immediately which is also the default behaviour."
        },
        {
          "key": "confirm_commit",
          "fieldId": "CONFIRM_COMMIT",
          "type": "boolean",
          "label": "confirm commit",
          "default": false,
          "description": "This argument will execute commit operation on remote device. It can be used to confirm a previous commit."
        },
        {
          "key": "lines",
          "fieldId": "LINES",
          "type": "text",
          "label": "lines",
          "yamlType": "list",
          "description": "This argument takes a list of C(set) or C(delete) configuration lines to push into the remote device.  Each line must start with either C(set) or C(delete).  This argument is mutually exclusive with the I(src) argument.",
          "aliases": [
            "commands"
          ]
        },
        {
          "key": "replace",
          "fieldId": "REPLACE",
          "type": "boolean",
          "label": "replace",
          "description": "The C(replace) argument will instruct the remote device to replace the current configuration hierarchy with the one specified in the corresponding hierarchy of the source configuration loaded from this module. Note this argument should be considered deprecated.  To achieve the equivalent, set the I(update) argument to C(replace). This argument will be removed in a future release. The C(replace) and C(update) argument is mutually exclusive."
        },
        {
          "key": "rollback",
          "fieldId": "ROLLBACK",
          "type": "number",
          "label": "rollback",
          "description": "The C(rollback) argument instructs the module to rollback the current configuration to the identifier specified in the argument.  If the specified rollback identifier does not exist on the remote device, the module will fail.  To rollback to the most recent commit, set the C(rollback) argument to 0."
        },
        {
          "key": "src",
          "fieldId": "SRC",
          "type": "text",
          "label": "src",
          "description": "The I(src) argument provides a path to the configuration file to load into the remote system. The path can either be a full system path to the configuration file if the value starts with / or relative to the root of the implemented role or playbook. This argument is mutually exclusive with the I(lines) argument."
        },
        {
          "key": "src_format",
          "fieldId": "SRC_FORMAT",
          "type": "dropdown",
          "label": "src format",
          "description": "The I(src_format) argument specifies the format of the configuration found int I(src).  If the I(src_format) argument is not provided, the module will attempt to determine the format of the configuration file specified in I(src).",
          "options": [
            [
              "xml",
              "xml"
            ],
            [
              "set",
              "set"
            ],
            [
              "text",
              "text"
            ],
            [
              "json",
              "json"
            ]
          ]
        },
        {
          "key": "update",
          "fieldId": "UPDATE",
          "type": "dropdown",
          "label": "update",
          "default": "merge",
          "description": "This argument will decide how to load the configuration data particularly when the candidate configuration and loaded configuration contain conflicting statements. Following are accepted values. C(merge) combines the data in the loaded configuration with the candidate configuration. If statements in the loaded configuration conflict with statements in the candidate configuration, the loaded statements replace the candidate ones. C(override) discards the entire candidate configuration and replaces it with the loaded configuration. C(replace) substitutes each hierarchy level in the loaded configuration for the corresponding level. C(update) is similar to the override option. The new configuration completely replaces the existing configuration. The difference comes when the configuration is later committed. This option performs a 'diff' between the new candidate configuration and the existing committed configuration. It then only notifies system processes responsible for the changed portions of the configuration, and only marks the actual configuration changes as 'changed'.",
          "options": [
            [
              "merge",
              "merge"
            ],
            [
              "override",
              "override"
            ],
            [
              "replace",
              "replace"
            ],
            [
              "update",
              "update"
            ]
          ]
        },
        {
          "key": "zeroize",
          "fieldId": "ZEROIZE",
          "type": "boolean",
          "label": "zeroize",
          "default": false,
          "description": "The C(zeroize) argument is used to completely sanitize the remote device configuration back to initial defaults.  This argument will effectively remove all current configuration statements on the remote device."
        }
      ],
      "unmappedParams": [
        "backup_options (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_facts",
      "blockType": "juniper_device_junos_facts",
      "label": "junos_facts",
      "tooltip": "Collect facts from remote devices running Juniper Junos",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_facts_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "available_network_resources",
          "fieldId": "AVAILABLE_NETWORK_RESOURCES",
          "type": "boolean",
          "label": "available network resources",
          "default": false,
          "description": "When 'True' a list of network resources for which resource modules are available will be provided."
        },
        {
          "key": "config_format",
          "fieldId": "CONFIG_FORMAT",
          "type": "dropdown",
          "label": "config format",
          "default": "text",
          "description": "The I(config_format) argument specifies the format of the configuration when serializing output from the device. This argument is applicable only when C(config) value is present in I(gather_subset). The I(config_format) should be supported by the junos version running on device. This value is not applicable while fetching old style facts that is when C(ofacts) value is present in value if I(gather_subset) value. This option is valid only for C(gather_subset) values.",
          "options": [
            [
              "xml",
              "xml"
            ],
            [
              "text",
              "text"
            ],
            [
              "set",
              "set"
            ],
            [
              "json",
              "json"
            ]
          ]
        },
        {
          "key": "gather_network_resources",
          "fieldId": "GATHER_NETWORK_RESOURCES",
          "type": "text",
          "label": "gather network resources",
          "yamlType": "list",
          "description": "When supplied, this argument will restrict the facts collected to a given subset. Possible values for this argument include all and the resources like interfaces, vlans etc. Can specify a list of values to include a larger subset. Values can also be used with an initial C(!) to specify that a specific subset should not be collected. Valid subsets are 'all', 'interfaces', 'lacp', 'lacp_interfaces', 'lag_interfaces', 'l2_interfaces', 'l3_interfaces', 'lldp_global', 'lldp_interfaces', 'vlans'."
        },
        {
          "key": "gather_subset",
          "fieldId": "GATHER_SUBSET",
          "type": "text",
          "label": "gather subset",
          "yamlType": "list",
          "description": "When supplied, this argument will restrict the facts collected to a given subset.  Possible values for this argument include C(all), C(hardware), C(config), C(interfaces) and C(min). Can specify a list of values to include a larger subset.  Values can also be used with an initial C(!) to specify that a specific subset should not be collected. To maintain backward compatibility old style facts can be retrieved by explicitly adding C(ofacts)  to value, this requires junos-eznc to be installed as a prerequisite. Valid value of gather_subset are default, hardware, config, interfaces, ofacts. If C(ofacts) is present in the list it fetches the old style facts (fact keys without 'ansible_' prefix) and it requires junos-eznc library to be installed."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_hostname",
      "blockType": "juniper_device_junos_hostname",
      "label": "junos_hostname",
      "tooltip": "Manage Hostname server configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_hostname_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system hostname). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device. The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result. For state I(rendered) active connection to remote host is not required. The states I(merged), I(replaced) and I(overridden) have identical behaviour for this module. The state I(gathered) will fetch the running configuration from device and transform it into structured data in the format as per the resource module argspec and the value is returned in the I(gathered) key within the result. The state I(parsed) reads the configuration from C(running_config) option and transforms it into JSON format as per the resource module parameters and the value is returned in the I(parsed) key within the result. The value of C(running_config) option should be the same format as the output of command I(show system hostname) executed on device. For state I(parsed) active connection to remote host is not required.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "Specify the hostname."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_interfaces",
      "blockType": "juniper_device_junos_interfaces",
      "label": "junos_interfaces",
      "tooltip": "Junos Interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interfaces). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "Interface description."
        },
        {
          "key": "duplex",
          "fieldId": "DUPLEX",
          "type": "dropdown",
          "label": "duplex",
          "description": "Interface link status. Applicable for Ethernet interfaces only, either in half duplex, full duplex or in automatic state which negotiates the duplex automatically.",
          "options": [
            [
              "automatic",
              "automatic"
            ],
            [
              "full-duplex",
              "full-duplex"
            ],
            [
              "half-duplex",
              "half-duplex"
            ]
          ]
        },
        {
          "key": "enabled",
          "fieldId": "ENABLED",
          "type": "boolean",
          "label": "enabled",
          "default": true,
          "description": "Administrative state of the interface. Set the value to C(true) to administratively enabled the interface or C(false) to disable it."
        },
        {
          "key": "mtu",
          "fieldId": "MTU",
          "type": "number",
          "label": "mtu",
          "description": "MTU for a specific interface. Applicable for Ethernet interfaces only."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Full name of interface, e.g. ge-0/0/0.",
          "required": true
        },
        {
          "key": "speed",
          "fieldId": "SPEED",
          "type": "text",
          "label": "speed",
          "description": "Interface link speed. Applicable for Ethernet interfaces only."
        }
      ],
      "unmappedParams": [
        "config.hold_time (has suboptions)",
        "config.units (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_l2_interfaces",
      "blockType": "juniper_device_junos_l2_interfaces",
      "label": "junos_l2_interfaces",
      "tooltip": "L2 interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_l2_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interfaces). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "enhanced_layer",
          "fieldId": "ENHANCED_LAYER",
          "type": "boolean",
          "label": "enhanced layer",
          "description": "True if your device has Enhanced Layer 2 Software (ELS). If the l2 configuration is under C(interface-mode) the value is True else if the l2 configuration is under C(port-mode) value is False"
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Full name of interface, e.g. ge-0/0/1.",
          "required": true
        },
        {
          "key": "unit",
          "fieldId": "UNIT",
          "type": "number",
          "label": "unit",
          "description": "Logical interface number. Value of C(unit) should be of type integer."
        }
      ],
      "unmappedParams": [
        "config.access (has suboptions)",
        "config.trunk (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_l3_interfaces",
      "blockType": "juniper_device_junos_l3_interfaces",
      "label": "junos_l3_interfaces",
      "tooltip": "L3 interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_l3_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interfaces). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "mtu",
          "fieldId": "MTU",
          "type": "number",
          "label": "mtu",
          "description": "Protocol family maximum transmission unit."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Full name of interface, e.g. ge-0/0/1",
          "required": true
        },
        {
          "key": "unit",
          "fieldId": "UNIT",
          "type": "number",
          "label": "unit",
          "default": 0,
          "description": "Logical interface number. Value of C(unit) should be of type integer"
        }
      ],
      "unmappedParams": [
        "config.ipv4 (has suboptions)",
        "config.ipv6 (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_lacp",
      "blockType": "juniper_device_junos_lacp",
      "label": "junos_lacp",
      "tooltip": "Global Link Aggregation Control Protocol (LACP) Junos resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_lacp_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show chassis aggregated-devices ethernet lacp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "link_protection",
          "fieldId": "LINK_PROTECTION",
          "type": "dropdown",
          "label": "link protection",
          "description": "Enable LACP link-protection for the system. If the value is set to C(non-revertive) it will not revert links when a better priority link comes up. By default the link will be reverted.",
          "options": [
            [
              "revertive",
              "revertive"
            ],
            [
              "non-revertive",
              "non-revertive"
            ]
          ]
        },
        {
          "key": "system_priority",
          "fieldId": "SYSTEM_PRIORITY",
          "type": "number",
          "label": "system priority",
          "description": "LACP priority for the system."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_lacp_interfaces",
      "blockType": "juniper_device_junos_lacp_interfaces",
      "label": "junos_lacp_interfaces",
      "tooltip": "LACP interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_lacp_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interface). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "force_up",
          "fieldId": "FORCE_UP",
          "type": "boolean",
          "label": "force up",
          "description": "This is a boolean argument to control if the port should be up in absence of received link Aggregation Control Protocol Data Unit (LACPDUS). This value is applicable for member interfaces only."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name Identifier of the interface or link aggregation group."
        },
        {
          "key": "period",
          "fieldId": "PERIOD",
          "type": "dropdown",
          "label": "period",
          "description": "Timer interval for periodic transmission of LACP packets. If the value is set to C(fast) the packets are received every second and if the value is C(slow) the packets are received every 30 seconds. This value is applicable for aggregate interface only.",
          "options": [
            [
              "fast",
              "fast"
            ],
            [
              "slow",
              "slow"
            ]
          ]
        },
        {
          "key": "port_priority",
          "fieldId": "PORT_PRIORITY",
          "type": "number",
          "label": "port priority",
          "description": "Priority of the member port. This value is applicable for member interfaces only. Refer to vendor documentation for valid values."
        },
        {
          "key": "sync_reset",
          "fieldId": "SYNC_RESET",
          "type": "dropdown",
          "label": "sync reset",
          "description": "The argument notifies minimum-link failure out of sync to peer. If the value is C(disable) it disables minimum-link failure handling at LACP level and if value is C(enable) it enables minimum-link failure handling at LACP level. This value is applicable for aggregate interface only.",
          "options": [
            [
              "disable",
              "disable"
            ],
            [
              "enable",
              "enable"
            ]
          ]
        }
      ],
      "unmappedParams": [
        "config.system (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_lag_interfaces",
      "blockType": "juniper_device_junos_lag_interfaces",
      "label": "junos_lag_interfaces",
      "tooltip": "Link Aggregation Juniper JUNOS resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_lag_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show interfaces). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "link_protection",
          "fieldId": "LINK_PROTECTION",
          "type": "boolean",
          "label": "link protection",
          "description": "This boolean option indicates if link protection should be enabled for the LAG interface. If value is C(True) link protection is enabled on LAG and if value is C(False) link protection is disabled."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "description": "LAG mode. A value of C(passive) will enable LACP in C(passive) mode that is it will respond to LACP packets and C(active) configures the link to initiate transmission of LACP packets.",
          "options": [
            [
              "active",
              "active"
            ],
            [
              "passive",
              "passive"
            ]
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name of the link aggregation group (LAG).",
          "required": true
        }
      ],
      "unmappedParams": [
        "config.members (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_lldp_global",
      "blockType": "juniper_device_junos_lldp_global",
      "label": "junos_lldp_global",
      "tooltip": "LLDP resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_lldp_global_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols lldp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "address",
          "fieldId": "ADDRESS",
          "type": "text",
          "label": "address",
          "description": "This argument sets the management address from LLDP."
        },
        {
          "key": "enabled",
          "fieldId": "ENABLED",
          "type": "boolean",
          "label": "enabled",
          "description": "This argument is a boolean value to enabled or disable LLDP."
        },
        {
          "key": "hold_multiplier",
          "fieldId": "HOLD_MULTIPLIER",
          "type": "number",
          "label": "hold multiplier",
          "description": "Specify the number of seconds that LLDP information is held before it is discarded. The multiplier value is used in combination with the C(interval) value."
        },
        {
          "key": "interval",
          "fieldId": "INTERVAL",
          "type": "number",
          "label": "interval",
          "description": "Frequency at which LLDP advertisements are sent (in seconds)."
        },
        {
          "key": "transmit_delay",
          "fieldId": "TRANSMIT_DELAY",
          "type": "number",
          "label": "transmit delay",
          "description": "Specify the number of seconds the device waits before sending advertisements to neighbors after a change is made in local system."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_lldp_interfaces",
      "blockType": "juniper_device_junos_lldp_interfaces",
      "label": "junos_lldp_interfaces",
      "tooltip": "LLDP interfaces resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_lldp_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols lldp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "enabled",
          "fieldId": "ENABLED",
          "type": "boolean",
          "label": "enabled",
          "description": "This is a boolean value to control disabling of LLDP on the interface C(name)"
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name of the interface LLDP needs to be configured on.",
          "required": true
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_logging_global",
      "blockType": "juniper_device_junos_logging_global",
      "label": "junos_logging_global",
      "tooltip": "Manage logging configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_logging_global_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system syslog). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. Refer to examples for more details.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "allow_duplicates",
          "fieldId": "ALLOW_DUPLICATES",
          "type": "boolean",
          "label": "allow duplicates",
          "description": "Do not suppress the repeated message for all targets."
        },
        {
          "key": "log_rotate_frequency",
          "fieldId": "LOG_ROTATE_FREQUENCY",
          "type": "number",
          "label": "log rotate frequency",
          "description": "Specify Rotate log frequency (1..59 minutes)."
        },
        {
          "key": "routing_instance",
          "fieldId": "ROUTING_INSTANCE",
          "type": "text",
          "label": "routing instance",
          "description": "Specify Routing routing-instance."
        },
        {
          "key": "source_address",
          "fieldId": "SOURCE_ADDRESS",
          "type": "text",
          "label": "source address",
          "description": "Specify address as source address."
        }
      ],
      "unmappedParams": [
        "config.archive (has suboptions)",
        "config.console (has suboptions)",
        "config.files (has suboptions)",
        "config.hosts (has suboptions)",
        "config.server (has suboptions)",
        "config.time_format (has suboptions)",
        "config.users (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_netconf",
      "blockType": "juniper_device_junos_netconf",
      "label": "junos_netconf",
      "tooltip": "Configures the Junos Netconf system service",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_netconf_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "netconf_port",
          "fieldId": "NETCONF_PORT",
          "type": "number",
          "label": "netconf port",
          "default": 830,
          "description": "This argument specifies the port the netconf service should listen on for SSH connections.  The default port as defined in RFC 6242 is 830.",
          "aliases": [
            "listens_on"
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Specifies the state of the C(junos_netconf) resource on the remote device.  If the I(state) argument is set to I(present) the netconf service will be configured.  If the I(state) argument is set to I(absent) the netconf service will be removed from the configuration.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_ntp_global",
      "blockType": "juniper_device_junos_ntp_global",
      "label": "junos_ntp_global",
      "tooltip": "Manage NTP configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_ntp_global_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system syslog). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. The states I(replaced) and I(overridden) have identical behaviour for this module. Refer to examples for more details.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "boot_server",
          "fieldId": "BOOT_SERVER",
          "type": "text",
          "label": "boot server",
          "description": "Server to query during boot sequence."
        },
        {
          "key": "broadcast_client",
          "fieldId": "BROADCAST_CLIENT",
          "type": "boolean",
          "label": "broadcast client",
          "description": "Listen to broadcast NTP."
        },
        {
          "key": "interval_range",
          "fieldId": "INTERVAL_RANGE",
          "type": "number",
          "label": "interval range",
          "description": "Set the minpoll and maxpoll interval range."
        },
        {
          "key": "multicast_client",
          "fieldId": "MULTICAST_CLIENT",
          "type": "text",
          "label": "multicast client",
          "description": "Listen to multicast NTP address."
        }
      ],
      "unmappedParams": [
        "config.authentication_keys (has suboptions)",
        "config.broadcasts (has suboptions)",
        "config.peers (has suboptions)",
        "config.servers (has suboptions)",
        "config.source_addresses (has suboptions)",
        "config.threshold (has suboptions)",
        "config.trusted_keys (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_ospf_interfaces",
      "blockType": "juniper_device_junos_ospf_interfaces",
      "label": "junos_ospf_interfaces",
      "tooltip": "OSPF Interfaces Resource Module.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_ospf_interfaces_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols ospf). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name/Identifier of the interface.",
          "required": true
        },
        {
          "key": "router_id",
          "fieldId": "ROUTER_ID",
          "type": "text",
          "label": "router id",
          "description": "The OSPF router id."
        }
      ],
      "unmappedParams": [
        "config.address_family (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_ospfv2",
      "blockType": "juniper_device_junos_ospfv2",
      "label": "junos_ospfv2",
      "tooltip": "OSPFv2 resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_ospfv2_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols ospf). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "external_preference",
          "fieldId": "EXTERNAL_PREFERENCE",
          "type": "number",
          "label": "external preference",
          "description": "Preference of external routes."
        },
        {
          "key": "preference",
          "fieldId": "PREFERENCE",
          "type": "number",
          "label": "preference",
          "description": "Preference of internal routes."
        },
        {
          "key": "prefix_export_limit",
          "fieldId": "PREFIX_EXPORT_LIMIT",
          "type": "number",
          "label": "prefix export limit",
          "description": "Maximum number of external prefixes that can be exported."
        },
        {
          "key": "reference_bandwidth",
          "fieldId": "REFERENCE_BANDWIDTH",
          "type": "dropdown",
          "label": "reference bandwidth",
          "description": "Bandwidth for calculating metric defaults.",
          "options": [
            [
              "1g",
              "1g"
            ],
            [
              "10g",
              "10g"
            ]
          ]
        },
        {
          "key": "rfc1583compatibility",
          "fieldId": "RFC1583COMPATIBILITY",
          "type": "boolean",
          "label": "rfc1583compatibility",
          "description": "Set RFC1583 compatibility"
        },
        {
          "key": "router_id",
          "fieldId": "ROUTER_ID",
          "type": "text",
          "label": "router id",
          "description": "The OSPFv2 router id."
        }
      ],
      "unmappedParams": [
        "config.areas (has suboptions)",
        "config.overload (has suboptions)",
        "config.spf_options (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_ospfv3",
      "blockType": "juniper_device_junos_ospfv3",
      "label": "junos_ospfv3",
      "tooltip": "OSPFv3 resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_ospfv3_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show protocols ospf). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "external_preference",
          "fieldId": "EXTERNAL_PREFERENCE",
          "type": "number",
          "label": "external preference",
          "description": "Preference of external routes."
        },
        {
          "key": "preference",
          "fieldId": "PREFERENCE",
          "type": "number",
          "label": "preference",
          "description": "Preference of internal routes."
        },
        {
          "key": "prefix_export_limit",
          "fieldId": "PREFIX_EXPORT_LIMIT",
          "type": "number",
          "label": "prefix export limit",
          "description": "Maximum number of external prefixes that can be exported."
        },
        {
          "key": "reference_bandwidth",
          "fieldId": "REFERENCE_BANDWIDTH",
          "type": "dropdown",
          "label": "reference bandwidth",
          "description": "Bandwidth for calculating metric defaults.",
          "options": [
            [
              "1g",
              "1g"
            ],
            [
              "10g",
              "10g"
            ]
          ]
        },
        {
          "key": "rfc1583compatibility",
          "fieldId": "RFC1583COMPATIBILITY",
          "type": "boolean",
          "label": "rfc1583compatibility",
          "description": "Set RFC1583 compatibility"
        },
        {
          "key": "router_id",
          "fieldId": "ROUTER_ID",
          "type": "text",
          "label": "router id",
          "description": "The OSPFv3 router id."
        }
      ],
      "unmappedParams": [
        "config.areas (has suboptions)",
        "config.overload (has suboptions)",
        "config.spf_options (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_package",
      "blockType": "juniper_device_junos_package",
      "label": "junos_package",
      "tooltip": "Installs packages on remote devices running Junos",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_package_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "The I(force) argument instructs the module to bypass the package version check and install the packaged identified in I(src) on the remote device."
        },
        {
          "key": "force_host",
          "fieldId": "FORCE_HOST",
          "type": "boolean",
          "label": "force host",
          "default": false,
          "description": "The I(force_host) argument controls the way software package or bundle is added on remote JUNOS host and is applicable for JUNOS QFX5100 device. If the value is set to C(True) it will ignore any warnings while adding the host software package or bundle."
        },
        {
          "key": "issu",
          "fieldId": "ISSU",
          "type": "boolean",
          "label": "issu",
          "default": false,
          "description": "The I(issu) argument is a boolean flag when set to C(True) allows unified in-service software upgrade (ISSU) feature which enables you to upgrade between two different Junos OS releases with no disruption on the control plane and with minimal disruption of traffic."
        },
        {
          "key": "no_copy",
          "fieldId": "NO_COPY",
          "type": "boolean",
          "label": "no copy",
          "default": false,
          "description": "The I(no_copy) argument is responsible for instructing the remote device on where to install the package from.  When enabled, the package is transferred to the remote device prior to installing."
        },
        {
          "key": "reboot",
          "fieldId": "REBOOT",
          "type": "boolean",
          "label": "reboot",
          "default": true,
          "description": "In order for a package to take effect, the remote device must be restarted.  When enabled, this argument will instruct the module to reboot the device once the updated package has been installed. If disabled or the remote package does not need to be changed, the device will not be started."
        },
        {
          "key": "src",
          "fieldId": "SRC",
          "type": "text",
          "label": "src",
          "description": "The I(src) argument specifies the path to the source package to be installed on the remote device in the advent of a version mismatch. The I(src) argument can be either a localized path or a full path to the package file to install.",
          "required": true,
          "aliases": [
            "package"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The C(ssh_config) argument is path to the SSH configuration file. This can be used to load SSH information from a configuration file. If this option is not given by default ~/.ssh/config is queried."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "description": "The C(ssh_private_key_file) argument is path to the SSH private key file. This can be used if you need to provide a private key rather than loading the key into the ssh-key-ring/environment"
        },
        {
          "key": "unlink",
          "fieldId": "UNLINK",
          "type": "boolean",
          "label": "unlink",
          "default": false,
          "description": "The I(unlink) argument is responsible for instructing the remote device to remove the installation packages after installation."
        },
        {
          "key": "validate",
          "fieldId": "VALIDATE",
          "type": "boolean",
          "label": "validate",
          "default": true,
          "description": "The I(validate) argument is responsible for instructing the remote device to skip checking the current device configuration compatibility with the package being installed. When set to false validation is not performed."
        },
        {
          "key": "version",
          "fieldId": "VERSION",
          "type": "text",
          "label": "version",
          "description": "The I(version) argument can be used to explicitly specify the version of the package that should be installed on the remote device.  If the I(version) argument is not specified, then the version is extracts from the I(src) filename."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_ping",
      "blockType": "juniper_device_junos_ping",
      "label": "junos_ping",
      "tooltip": "Tests reachability using ping from devices running Juniper JUNOS",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_ping_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "count",
          "fieldId": "COUNT",
          "type": "number",
          "label": "count",
          "default": 5,
          "description": "Number of packets to send to check reachability."
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "description": "The IP Address or hostname (resolvable by the device) of the remote node.",
          "required": true
        },
        {
          "key": "df_bit",
          "fieldId": "DF_BIT",
          "type": "boolean",
          "label": "df bit",
          "default": false,
          "description": "Determines whether to set the DF bit."
        },
        {
          "key": "interface",
          "fieldId": "INTERFACE",
          "type": "text",
          "label": "interface",
          "description": "The source interface to use while sending the ping packet(s)."
        },
        {
          "key": "interval",
          "fieldId": "INTERVAL",
          "type": "number",
          "label": "interval",
          "description": "Determines the interval (in seconds) between consecutive pings."
        },
        {
          "key": "rapid",
          "fieldId": "RAPID",
          "type": "boolean",
          "label": "rapid",
          "default": false,
          "description": "Determines whether to send the packets rapidly."
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "description": "Determines the size (in bytes) of the ping packet(s)."
        },
        {
          "key": "source",
          "fieldId": "SOURCE",
          "type": "text",
          "label": "source",
          "description": "The IP Address to use while sending the ping packet(s)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Determines if the expected result is success or fail.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "ttl",
          "fieldId": "TTL",
          "type": "number",
          "label": "ttl",
          "description": "The time-to-live value for the ICMP packet(s)."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_prefix_lists",
      "blockType": "juniper_device_junos_prefix_lists",
      "label": "junos_prefix_lists",
      "tooltip": "Manage prefix-lists attributes of interfaces on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_prefix_lists_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show policy-options). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "address_prefixes",
          "fieldId": "ADDRESS_PREFIXES",
          "type": "text",
          "label": "address prefixes",
          "yamlType": "list",
          "description": "Specify address prefixes."
        },
        {
          "key": "dynamic_db",
          "fieldId": "DYNAMIC_DB",
          "type": "boolean",
          "label": "dynamic db",
          "description": "Enable object to exist in dynamic DB."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Specify the name of the prefix-list.",
          "required": true
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_routing_instances",
      "blockType": "juniper_device_junos_routing_instances",
      "label": "junos_routing_instances",
      "tooltip": "Manage routing instances on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_routing_instances_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show routing-instances). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "connector_id_advertise",
          "fieldId": "CONNECTOR_ID_ADVERTISE",
          "type": "boolean",
          "label": "connector id advertise",
          "description": "Advertise connector-id attribute."
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "Specify text description of routing instance."
        },
        {
          "key": "instance_role",
          "fieldId": "INSTANCE_ROLE",
          "type": "dropdown",
          "label": "instance role",
          "description": "Primary role of L2Backhaul-vpn router.",
          "options": [
            [
              "access",
              "access"
            ],
            [
              "nni",
              "nni"
            ]
          ]
        },
        {
          "key": "l2vpn_id",
          "fieldId": "L2VPN_ID",
          "type": "text",
          "label": "l2vpn id",
          "description": "Layer-2 vpn-id for this instance."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Specify routing instance name."
        },
        {
          "key": "no_irb_layer_2_copy",
          "fieldId": "NO_IRB_LAYER_2_COPY",
          "type": "boolean",
          "label": "no irb layer 2 copy",
          "description": "Disable transmission of layer-2 copy of packets of irb routing-interface."
        },
        {
          "key": "no_local_switching",
          "fieldId": "NO_LOCAL_SWITCHING",
          "type": "boolean",
          "label": "no local switching",
          "description": "Disable vlan id normalization for interfaces."
        },
        {
          "key": "no_normalization",
          "fieldId": "NO_NORMALIZATION",
          "type": "boolean",
          "label": "no normalization",
          "description": "Disable vlan id normalization for interfaces."
        },
        {
          "key": "no_vrf_advertise",
          "fieldId": "NO_VRF_ADVERTISE",
          "type": "boolean",
          "label": "no vrf advertise",
          "description": "Disable vlan id normalization for interfaces."
        },
        {
          "key": "no_vrf_propagate_ttl",
          "fieldId": "NO_VRF_PROPAGATE_TTL",
          "type": "boolean",
          "label": "no vrf propagate ttl",
          "description": "Disable TTL propagation from IP to MPLS (on push) and MPLS to IP (on pop)."
        },
        {
          "key": "qualified_bum_pruning_mode",
          "fieldId": "QUALIFIED_BUM_PRUNING_MODE",
          "type": "boolean",
          "label": "qualified bum pruning mode",
          "description": "Enable BUM pruning for VPLS instance."
        },
        {
          "key": "route_distinguisher",
          "fieldId": "ROUTE_DISTINGUISHER",
          "type": "text",
          "label": "route distinguisher",
          "description": "Route distinguisher for this instance"
        },
        {
          "key": "routing_interface",
          "fieldId": "ROUTING_INTERFACE",
          "type": "text",
          "label": "routing interface",
          "yamlType": "list",
          "description": "Routing interface name for this routing-instance."
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "description": "Specify instance type.",
          "options": [
            [
              "evpn",
              "evpn"
            ],
            [
              "evpn-vpws",
              "evpn-vpws"
            ],
            [
              "forwarding",
              "forwarding"
            ],
            [
              "l2backhaul-vpn",
              "l2backhaul-vpn"
            ],
            [
              "l2vpn",
              "l2vpn"
            ],
            [
              "layer2-control",
              "layer2-control"
            ],
            [
              "mac-vrf",
              "mac-vrf"
            ],
            [
              "mpls-forwarding",
              "mpls-forwarding"
            ],
            [
              "mpls-internet-multicast",
              "mpls-internet-multicast"
            ],
            [
              "no-forwarding",
              "no-forwarding"
            ],
            [
              "virtual-router",
              "virtual-router"
            ],
            [
              "virtual-switch",
              "virtual-switch"
            ],
            [
              "vpls",
              "vpls"
            ],
            [
              "vrf",
              "vrf"
            ]
          ]
        },
        {
          "key": "vrf_exports",
          "fieldId": "VRF_EXPORTS",
          "type": "text",
          "label": "vrf exports",
          "yamlType": "list",
          "description": "Export policy for VRF instance RIBs."
        },
        {
          "key": "vrf_imports",
          "fieldId": "VRF_IMPORTS",
          "type": "text",
          "label": "vrf imports",
          "yamlType": "list",
          "description": "Import policy for VRF instance RIBs."
        }
      ],
      "unmappedParams": [
        "config.bridge_domains (has suboptions)",
        "config.egress_protection (has suboptions)",
        "config.interfaces (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_routing_options",
      "blockType": "juniper_device_junos_routing_options",
      "label": "junos_routing_options",
      "tooltip": "Manage routing-options configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_routing_options_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system routing-options). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. Refer to examples for more details.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "router_id",
          "fieldId": "ROUTER_ID",
          "type": "text",
          "label": "router id",
          "description": "Specify Router identifier."
        }
      ],
      "unmappedParams": [
        "config.autonomous_system (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_rpc",
      "blockType": "juniper_device_junos_rpc",
      "label": "junos_rpc",
      "tooltip": "Runs an arbitrary RPC over NetConf on an Juniper JUNOS device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_rpc_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "args",
          "fieldId": "ARGS",
          "type": "text",
          "label": "args",
          "yamlType": "dict",
          "description": "The C(args) argument provides a set of arguments for the RPC call and are encoded in the request message.  This argument accepts a set of key=value arguments."
        },
        {
          "key": "attrs",
          "fieldId": "ATTRS",
          "type": "text",
          "label": "attrs",
          "yamlType": "dict",
          "description": "The C(attrs) arguments defines a list of attributes and their values to set for the RPC call. This accepts a dictionary of key-values."
        },
        {
          "key": "output",
          "fieldId": "OUTPUT",
          "type": "dropdown",
          "label": "output",
          "default": "xml",
          "description": "The C(output) argument specifies the desired output of the return data.  This argument accepts one of C(xml), C(text), or C(json).  For C(json), the JUNOS device must be running a version of software that supports native JSON output.",
          "options": [
            [
              "xml",
              "xml"
            ],
            [
              "json",
              "json"
            ],
            [
              "text",
              "text"
            ]
          ]
        },
        {
          "key": "rpc",
          "fieldId": "RPC",
          "type": "text",
          "label": "rpc",
          "description": "The C(rpc) argument specifies the RPC call to send to the remote devices to be executed.  The RPC Reply message is parsed and the contents are returned to the playbook.",
          "required": true
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_scp",
      "blockType": "juniper_device_junos_scp",
      "label": "junos_scp",
      "tooltip": "Transfer files from or to remote devices running Junos",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_scp_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": ".",
          "description": "The C(dest) argument specifies the path in which to receive the files."
        },
        {
          "key": "recursive",
          "fieldId": "RECURSIVE",
          "type": "boolean",
          "label": "recursive",
          "default": false,
          "description": "The C(recursive) argument enables recursive transfer of files and directories."
        },
        {
          "key": "remote_src",
          "fieldId": "REMOTE_SRC",
          "type": "boolean",
          "label": "remote src",
          "default": false,
          "description": "The C(remote_src) argument enables the download of files (I(scp get)) from the remote device. The default behavior is to upload files (I(scp put)) to the remote device."
        },
        {
          "key": "src",
          "fieldId": "SRC",
          "type": "text",
          "label": "src",
          "yamlType": "list",
          "description": "The C(src) argument takes a single path, or a list of paths to be transferred. The argument C(recursive) must be C(true) to transfer directories.",
          "required": true
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The C(ssh_config) argument is path to the SSH configuration file. This can be used to load SSH information from a configuration file. If this option is not given by default ~/.ssh/config is queried."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "description": "The C(ssh_private_key_file) argument is path to the SSH private key file. This can be used if you need to provide a private key rather than loading the key into the ssh-key-ring/environment"
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_security_policies",
      "blockType": "juniper_device_junos_security_policies",
      "label": "junos_security_policies",
      "tooltip": "Create and manage security policies on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_security_policies_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed) The value of this option should be the output received from the JunOS device by executing the command B(show configuration security policies) The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result For state I(rendered) active connection to remote host is not required The state I(replaced) will replace the running configuration with the provided configuration The state I(replaced) and state I(overridden) have the same behaviour The state I(gathered) will fetch the running configuration from device and transform it into structured data in the format as per the resource module argspec and the value is returned in the I(gathered) key within the result The state I(parsed) reads the configuration from C(running_config) option and transforms it into JSON format as per the resource module parameters and the value is returned in the I(parsed) key within the result The value of C(running_config) option should be the same format as the output of command I(show security policies detail) executed on device For state I(parsed) active connection to remote host is not required",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        }
      ],
      "unmappedParams": [
        "config.from_zones (has suboptions)",
        "config.global (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_security_policies_global",
      "blockType": "juniper_device_junos_security_policies_global",
      "label": "junos_security_policies_global",
      "tooltip": "Manage global security policy settings on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_security_policies_global_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the JunOS device by executing the command B(show security policies). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device. The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result. For state I(rendered) active connection to remote host is not required. behaviour for this module. The state I(replaced) will replace the running configuration with the provided configuration The state I(replaced) and state I(overridden) have the same behaviour The state I(gathered) will fetch the running configuration from device and transform it into structured data in the format as per the resource module argspec and the value is returned in the I(gathered) key within the result. The state I(parsed) reads the configuration from C(running_config) option and transforms it into JSON format as per the resource module parameters and the value is returned in the I(parsed) key within the result. The value of C(running_config) option should be the same format as the output of command I(show security policies detail) executed on device. For state I(parsed) active connection to remote host is not required.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "default_policy",
          "fieldId": "DEFAULT_POLICY",
          "type": "dropdown",
          "label": "default policy",
          "description": "Configure the default security policy that defines the actions the device takes on a packet that does not match any user-defined policy.",
          "options": [
            [
              "deny-all",
              "deny-all"
            ],
            [
              "permit-all",
              "permit-all"
            ]
          ]
        }
      ],
      "unmappedParams": [
        "config.policy_rematch (has suboptions)",
        "config.policy_stats (has suboptions)",
        "config.pre_id_default_policy_action (has suboptions)",
        "config.traceoptions (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_security_zones",
      "blockType": "juniper_device_junos_security_zones",
      "label": "junos_security_zones",
      "tooltip": "Manage security zones on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_security_zones_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the JunOS device by executing the command B(show security policies). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in The states I(rendered), I(gathered) and I(parsed) does not perform any change on the device. The state I(rendered) will transform the configuration in C(config) option to platform specific CLI commands which will be returned in the I(rendered) key within the result. For state I(rendered) active connection to remote host is not required. behaviour for this module. The state I(gathered) will fetch the running configuration from device and transform it into structured data in the format as per the resource module argspec and the value is returned in the I(gathered) key within the result. The state I(parsed) reads the configuration from C(running_config) option and transforms it into JSON format as per the resource module parameters and the value is returned in the I(parsed) key within the result. The value of C(running_config) option should be the same format as the output of command I(show security policies detail) executed on device. For state I(parsed) active connection to remote host is not required.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        }
      ],
      "unmappedParams": [
        "config.functional_zone_management (has suboptions)",
        "config.zones (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_snmp_server",
      "blockType": "juniper_device_junos_snmp_server",
      "label": "junos_snmp_server",
      "tooltip": "Manage SNMP server configuration on Junos devices.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_snmp_server_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "object"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show system snmp). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result.",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in. Refer to examples for more details.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "contact",
          "fieldId": "CONTACT",
          "type": "text",
          "label": "contact",
          "description": "Specify contact information for administrator."
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "System description."
        },
        {
          "key": "filter_duplicates",
          "fieldId": "FILTER_DUPLICATES",
          "type": "boolean",
          "label": "filter duplicates",
          "description": "Filter requests with duplicate source address/port and request ID."
        },
        {
          "key": "if_count_with_filter_interfaces",
          "fieldId": "IF_COUNT_WITH_FILTER_INTERFACES",
          "type": "boolean",
          "label": "if count with filter interfaces",
          "description": "Filter interfaces config for ifNumber and ipv6Interfaces."
        },
        {
          "key": "interfaces",
          "fieldId": "INTERFACES",
          "type": "text",
          "label": "interfaces",
          "yamlType": "list",
          "description": "Restrict SNMP requests to interfaces."
        },
        {
          "key": "location",
          "fieldId": "LOCATION",
          "type": "text",
          "label": "location",
          "description": "Specify physical location of system."
        },
        {
          "key": "logical_system_trap_filter",
          "fieldId": "LOGICAL_SYSTEM_TRAP_FILTER",
          "type": "boolean",
          "label": "logical system trap filter",
          "description": "Allow only logical-system specific traps."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "System name override."
        }
      ],
      "unmappedParams": [
        "config.arp (has suboptions)",
        "config.client_lists (has suboptions)",
        "config.communities (has suboptions)",
        "config.customization (has suboptions)",
        "config.engine_id (has suboptions)",
        "config.filter_interfaces (has suboptions)",
        "config.health_monitor (has suboptions)",
        "config.nonvolatile (has suboptions)",
        "config.proxies (has suboptions)",
        "config.rmon (has suboptions)",
        "config.routing_instance_access (has suboptions)",
        "config.snmp_v3 (has suboptions)",
        "config.subagent (has suboptions)",
        "config.traceoptions (has suboptions)",
        "config.trap_groups (has suboptions)",
        "config.trap_options (has suboptions)",
        "config.views (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_static_routes",
      "blockType": "juniper_device_junos_static_routes",
      "label": "junos_static_routes",
      "tooltip": "Static routes resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_static_routes_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show routing-options). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state the configuration should be left in",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "rendered",
              "rendered"
            ],
            [
              "parsed",
              "parsed"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "vrf",
          "fieldId": "VRF",
          "type": "text",
          "label": "vrf",
          "description": "Virtual Routing and Forwarding (VRF) name"
        }
      ],
      "unmappedParams": [
        "config.address_families (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_system",
      "blockType": "juniper_device_junos_system",
      "label": "junos_system",
      "tooltip": "Manage the system attributes on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_system_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "active",
          "fieldId": "ACTIVE",
          "type": "boolean",
          "label": "active",
          "default": true,
          "description": "Specifies whether or not the configuration is active or deactivated"
        },
        {
          "key": "domain_name",
          "fieldId": "DOMAIN_NAME",
          "type": "text",
          "label": "domain name",
          "description": "Configure the IP domain name on the remote device to the provided value. Value should be in the dotted name form and will be appended to the C(hostname) to create a fully-qualified domain name."
        },
        {
          "key": "domain_search",
          "fieldId": "DOMAIN_SEARCH",
          "type": "text",
          "label": "domain search",
          "yamlType": "list",
          "description": "Provides the list of domain suffixes to append to the hostname for the purpose of doing name resolution. This argument accepts a list of names and will be reconciled with the current active configuration on the running node."
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "Configure the device hostname parameter. This option takes an ASCII string value."
        },
        {
          "key": "name_servers",
          "fieldId": "NAME_SERVERS",
          "type": "text",
          "label": "name servers",
          "yamlType": "list",
          "description": "List of DNS name servers by IP address to use to perform name resolution lookups.  This argument accepts either a list of DNS servers See examples."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "State of the configuration values in the device's current active configuration.  When set to I(present), the values should be configured in the device active configuration and when set to I(absent) the values should not be in the device active configuration",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_user",
      "blockType": "juniper_device_junos_user",
      "label": "junos_user",
      "tooltip": "Manage local user accounts on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_user_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "active",
          "fieldId": "ACTIVE",
          "type": "boolean",
          "label": "active",
          "default": true,
          "description": "Specifies whether or not the configuration is active or deactivated"
        },
        {
          "key": "encrypted_password",
          "fieldId": "ENCRYPTED_PASSWORD",
          "type": "text",
          "label": "encrypted password",
          "description": "The C(encrypted_password) argument set already hashed password for the user account on the remote system."
        },
        {
          "key": "full_name",
          "fieldId": "FULL_NAME",
          "type": "text",
          "label": "full name",
          "description": "The C(full_name) argument provides the full name of the user account to be created on the remote device.  This argument accepts any text string value."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The C(name) argument defines the username of the user to be created on the system.  This argument must follow appropriate usernaming conventions for the target device running JUNOS.  This argument is mutually exclusive with the C(aggregate) argument."
        },
        {
          "key": "purge",
          "fieldId": "PURGE",
          "type": "boolean",
          "label": "purge",
          "default": false,
          "description": "The C(purge) argument instructs the module to consider the users definition absolute.  It will remove any previously configured users on the device with the exception of the current defined set of aggregate."
        },
        {
          "key": "role",
          "fieldId": "ROLE",
          "type": "dropdown",
          "label": "role",
          "description": "The C(role) argument defines the role of the user account on the remote system.  User accounts can have more than one role configured.",
          "options": [
            [
              "operator",
              "operator"
            ],
            [
              "read-only",
              "read-only"
            ],
            [
              "super-user",
              "super-user"
            ],
            [
              "unauthorized",
              "unauthorized"
            ]
          ]
        },
        {
          "key": "sshkey",
          "fieldId": "SSHKEY",
          "type": "text",
          "label": "sshkey",
          "description": "The C(sshkey) argument defines the public SSH key to be configured for the user account on the remote system.  This argument must be a valid SSH key"
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "The C(state) argument configures the state of the user definitions as it relates to the device operational configuration.  When set to I(present), the user should be configured in the device active configuration and when set to I(absent) the user should not be in the device active configuration",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        }
      ],
      "unmappedParams": [
        "aggregate (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.junos_vlans",
      "blockType": "juniper_device_junos_vlans",
      "label": "junos_vlans",
      "tooltip": "VLANs resource module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_vlans_module.html",
      "structure": {
        "type": "wrapped",
        "wrapper": "config",
        "wrapperType": "list-of-one"
      },
      "fields": [
        {
          "key": "running_config",
          "fieldId": "RUNNING_CONFIG",
          "type": "text",
          "label": "running config",
          "description": "This option is used only with state I(parsed). The value of this option should be the output received from the Junos device by executing the command B(show vlans). The state I(parsed) reads the configuration from C(running_config) option and transforms it into Ansible structured data as per the resource module's argspec and the value is then returned in the I(parsed) key within the result",
          "topLevel": true
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "merged",
          "description": "The state of the configuration after module completion.",
          "options": [
            [
              "merged",
              "merged"
            ],
            [
              "replaced",
              "replaced"
            ],
            [
              "overridden",
              "overridden"
            ],
            [
              "deleted",
              "deleted"
            ],
            [
              "gathered",
              "gathered"
            ],
            [
              "parsed",
              "parsed"
            ],
            [
              "rendered",
              "rendered"
            ]
          ],
          "topLevel": true
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "Text description of VLANs"
        },
        {
          "key": "l3_interface",
          "fieldId": "L3_INTERFACE",
          "type": "text",
          "label": "l3 interface",
          "description": "Name of logical layer 3 interface."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name of VLAN.",
          "required": true
        },
        {
          "key": "vlan_id",
          "fieldId": "VLAN_ID",
          "type": "number",
          "label": "vlan id",
          "description": "IEEE 802.1q VLAN identifier for VLAN (1..4094)."
        }
      ]
    },
    {
      "fqcn": "juniper.device.junos_vrf",
      "blockType": "juniper_device_junos_vrf",
      "label": "junos_vrf",
      "tooltip": "Manage the VRF definitions on Juniper JUNOS devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/junos_vrf_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "active",
          "fieldId": "ACTIVE",
          "type": "boolean",
          "label": "active",
          "default": true,
          "description": "Specifies whether or not the configuration is active or deactivated"
        },
        {
          "key": "description",
          "fieldId": "DESCRIPTION",
          "type": "text",
          "label": "description",
          "description": "Provides a short description of the VRF definition in the current active configuration.  The VRF definition value accepts alphanumeric characters used to provide additional information about the VRF."
        },
        {
          "key": "interfaces",
          "fieldId": "INTERFACES",
          "type": "text",
          "label": "interfaces",
          "yamlType": "list",
          "description": "Identifies the set of interfaces that should be configured in the VRF. Interfaces must be routed interfaces in order to be placed into a VRF."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the VRF definition to be managed on the remote IOS device.  The VRF definition name is an ASCII string name used to uniquely identify the VRF.  This argument is mutually exclusive with the C(aggregate) argument"
        },
        {
          "key": "rd",
          "fieldId": "RD",
          "type": "text",
          "label": "rd",
          "yamlType": "list",
          "description": "The router-distinguisher value uniquely identifies the VRF to routing processes on the remote IOS system.  The RD value takes the form of C(A:B) where C(A) and C(B) are both numeric values."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Configures the state of the VRF definition as it relates to the device operational configuration.  When set to I(present), the VRF should be configured in the device active configuration and when set to I(absent) the VRF should not be in the device active configuration",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "table_label",
          "fieldId": "TABLE_LABEL",
          "type": "boolean",
          "label": "table label",
          "default": true,
          "description": "Causes JUNOS to allocate a VPN label per VRF rather than per VPN FEC. This allows for forwarding of traffic to directly connected subnets, COS Egress filtering etc."
        },
        {
          "key": "target",
          "fieldId": "TARGET",
          "type": "text",
          "label": "target",
          "yamlType": "list",
          "description": "It configures VRF target community configuration. The target value takes the form of C(target:A:B) where C(A) and C(B) are both numeric values."
        }
      ],
      "unmappedParams": [
        "aggregate (has suboptions)"
      ]
    },
    {
      "fqcn": "juniper.device.ping",
      "blockType": "juniper_device_ping",
      "label": "ping",
      "tooltip": "Execute ping from a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/ping_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "acceptable_percent_loss",
          "fieldId": "ACCEPTABLE_PERCENT_LOSS",
          "type": "number",
          "label": "acceptable percent loss",
          "default": 0,
          "description": "Maximum percentage of packets that may be lost and still consider the task not to have failed.",
          "aliases": [
            "acceptable_packet_loss"
          ]
        },
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "count",
          "fieldId": "COUNT",
          "type": "number",
          "label": "count",
          "default": 5,
          "description": "Number of packets to send."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": "none",
          "description": "The IP address, or hostname if DNS is configured on the Junos device, used as the destination of the ping.",
          "required": true,
          "aliases": [
            "dest_ip",
            "dest_host",
            "destination",
            "destination_ip",
            "destination_host"
          ]
        },
        {
          "key": "do_not_fragment",
          "fieldId": "DO_NOT_FRAGMENT",
          "type": "boolean",
          "label": "do not fragment",
          "default": false,
          "description": "Set Do Not Fragment bit on ping packets."
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "interface",
          "fieldId": "INTERFACE",
          "type": "text",
          "label": "interface",
          "default": "none",
          "description": "The source interface from which the the ping is sent. If not specified, the default Junos algorithm for determining the source interface is used."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "rapid",
          "fieldId": "RAPID",
          "type": "boolean",
          "label": "rapid",
          "default": true,
          "description": "Send ping requests rapidly"
        },
        {
          "key": "routing_instance",
          "fieldId": "ROUTING_INSTANCE",
          "type": "text",
          "label": "routing instance",
          "default": "none",
          "description": "Name of the source routing instance from which the ping is originated. If not specified, the default routing instance is used."
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "description": "The size of the ICMP payload of the ping. Total size of the IP packet is I(size) + the 20 byte IP header + the 8 byte ICMP header. Therefore, I(size) of C(1472) generates an IP packet of size 1500."
        },
        {
          "key": "source",
          "fieldId": "SOURCE",
          "type": "text",
          "label": "source",
          "default": "none",
          "description": "The IP address, or hostname if DNS is configured on the Junos device, used as the source address of the ping. If not specified, the Junos default algorithm for determining the source address is used.",
          "aliases": [
            "source_ip",
            "source_host",
            "src",
            "src_ip",
            "src_host"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "ttl",
          "fieldId": "TTL",
          "type": "number",
          "label": "ttl",
          "description": "Maximum number of IP routers (hops) allowed between source and destination."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.pmtud",
      "blockType": "juniper_device_pmtud",
      "label": "pmtud",
      "tooltip": "Perform path MTU discovery from a Junos device to a destination",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/pmtud_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": "none",
          "description": "The IPv4 address, or hostname if DNS is configured on the Junos device, used as the destination of the PMTUD.",
          "required": true,
          "aliases": [
            "dest_ip",
            "dest_host",
            "destination",
            "destination_ip",
            "destination_host"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "interface",
          "fieldId": "INTERFACE",
          "type": "text",
          "label": "interface",
          "default": "none",
          "description": "The source interface from which the the PMTUD is performed. If not specified, the default Junos algorithm for determining the source interface is used."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "max_range",
          "fieldId": "MAX_RANGE",
          "type": "number",
          "label": "max range",
          "default": 512,
          "description": "The maximum range of MTU values, in bytes, which will be searched when performing path MTU discovery. This value must be C(0) or a power of 2 (2^n) between C(2) and C(65536). The minimum IPv4 MTU value attempted when performing path MTU discovery is I(min_test_size) = (I(max_size) - I(max_range) + 1)"
        },
        {
          "key": "max_size",
          "fieldId": "MAX_SIZE",
          "type": "number",
          "label": "max size",
          "default": 1500,
          "description": "The maximum IPv4 MTU, in bytes, to attempt when performing path MTU discovery. The value returned for I(inet_mtu) will be no more than this value even if the path actually supports a higher MTU. This value must be between 68 and 65496."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "routing_instance",
          "fieldId": "ROUTING_INSTANCE",
          "type": "text",
          "label": "routing instance",
          "default": "none",
          "description": "Name of the source routing instance from which the ping is originated. If not specified, the default routing instance is used."
        },
        {
          "key": "source",
          "fieldId": "SOURCE",
          "type": "text",
          "label": "source",
          "default": "none",
          "description": "The IPv4 address, or hostname if DNS is configured on the Junos device, used as the source address of the PMTUD. If not specified, the Junos default algorithm for determining the source address is used.",
          "aliases": [
            "source_ip",
            "source_host",
            "src",
            "src_ip",
            "src_host"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.rpc",
      "blockType": "juniper_device_rpc",
      "label": "rpc",
      "tooltip": "Execute one or more NETCONF RPCs on a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/rpc_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "attrs",
          "fieldId": "ATTRS",
          "type": "text",
          "label": "attrs",
          "default": "none",
          "description": "The attributes and values to the RPCs specified by the I(rpcs) option. The value of this option can either be a single dictionary of keywords and values, or a list of dictionaries containing keywords and values. There is a one-to-one correspondence between the elements in the I(kwargs) list and the RPCs in the I(rpcs) list. In other words, the two lists must always contain the same number of elements.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "default": "None",
          "description": "The path to a file, on the Ansible control machine, where the output of the RPC will be saved. The file must be writeable. If the file already exists, it is overwritten. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(dest) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(dest_dir) option in new playbooks. The I(dest) and I(dest_dir) options are mutually exclusive.",
          "aliases": [
            "destination"
          ]
        },
        {
          "key": "dest_dir",
          "fieldId": "DEST_DIR",
          "type": "text",
          "label": "dest dir",
          "default": "None",
          "description": "The path to a directory, on the Ansible control machine, where the output of the RPC will be saved. The output will be logged to a file named C({{ inventory_hostname }}_)I(rpc).I(format) in the I(dest_dir) directory. The destination file must be writeable. If the file already exists, it is overwritten. It is the users responsibility to ensure a unique I(dest_dir) value is provided for each execution of this module within a playbook. The I(dest_dir) and I(dest) options are mutually exclusive. The I(dest_dir) option is recommended for all new playbooks.",
          "aliases": [
            "destination_dir",
            "destdir"
          ]
        },
        {
          "key": "filter",
          "fieldId": "FILTER",
          "type": "text",
          "label": "filter",
          "default": "none",
          "description": "This argument only applies if the I(rpcs) option contains a single RPC with the value C(get-config). When used, this value specifies an XML filter used to restrict the portions of the configuration which are retrieved. See the PyEZ U(get_config method|http://junos-pyez.readthedocs.io/en/stable/jnpr.junos.html#jnpr.junos.rpcmeta._RpcMetaExec.get_config) for details on the value of this option.",
          "aliases": [
            "filter_xml"
          ]
        },
        {
          "key": "formats",
          "fieldId": "FORMATS",
          "type": "dropdown",
          "label": "formats",
          "default": "xml",
          "description": "The format of the reply for the RPCs specified by the I(rpcs) option. The specified format(s) must be supported by the target Junos device. The value of this option can either be a single format, or a list of formats. If a single format is specified, it applies to all RPCs specified by the I(rpcs) option. If a list of formats are specified, there must be one value in the list for each RPC specified by the I(rpcs) option.",
          "options": [
            [
              "text",
              "text"
            ],
            [
              "xml",
              "xml"
            ],
            [
              "json",
              "json"
            ]
          ],
          "aliases": [
            "format",
            "display",
            "output"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "ignore_warning",
          "fieldId": "IGNORE_WARNING",
          "type": "text",
          "label": "ignore warning",
          "default": "none",
          "description": "A boolean, string or list of strings. If the value is C(true), ignore all warnings regardless of the warning message. If the value is a string, it will ignore warning(s) if the message of each warning matches the string. If the value is a list of strings, ignore warning(s) if the message of each warning matches at least one of the strings in the list. The value of the I(ignore_warning) option is applied to the load and commit operations performed by this module."
        },
        {
          "key": "kwargs",
          "fieldId": "KWARGS",
          "type": "text",
          "label": "kwargs",
          "default": "none",
          "description": "The keyword arguments and values to the RPCs specified by the I(rpcs) option. The value of this option can either be a single dictionary of keywords and values, or a list of dictionaries containing keywords and values. There must be a one-to-one correspondence between the elements in the I(kwargs) list and the RPCs in the I(rpcs) list. In other words, the two lists must always contain the same number of elements. For RPC arguments which do not require a value, specify the value of True as shown in the :ref:`rpc-examples-label`. By default \"0\" and \"1\" will be converted to boolean values. In case it doesn't need to be transformed to boolean pass first kwargs as",
          "aliases": [
            "kwarg",
            "args",
            "arg"
          ]
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "return_output",
          "fieldId": "RETURN_OUTPUT",
          "type": "boolean",
          "label": "return output",
          "default": true,
          "description": "Indicates if the output of the RPC should be returned in the module's response. You might want to set this option to C(false), and set the I(dest_dir) option, if the RPC output is very large and you only need to save the output rather than using it's content in subsequent tasks/plays of your playbook."
        },
        {
          "key": "rpcs",
          "fieldId": "RPCS",
          "type": "text",
          "label": "rpcs",
          "yamlType": "list",
          "default": "none",
          "description": "A list of one or more NETCONF RPCs to execute on the Junos device.",
          "required": true,
          "aliases": [
            "rpc"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.software",
      "blockType": "juniper_device_software",
      "label": "software",
      "tooltip": "Install software on a Junos device",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/software_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "all_re",
          "fieldId": "ALL_RE",
          "type": "boolean",
          "label": "all re",
          "default": true,
          "description": "Whether or not to install the software on all Routing Engines of the target Junos device. If C(true), and the device has multiple Routing Engines, the software is installed on all Routing Engines. If C(false), the software is only installed on the current Routing Engine."
        },
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "checksum",
          "fieldId": "CHECKSUM",
          "type": "text",
          "label": "checksum",
          "default": "none",
          "description": "The pre-calculated checksum, using the I(checksum_algorithm) of the file specified by the I(local_package) option. Specifying this option is simply an optimization to avoid repeatedly computing the checksum of the I(local_package) file once for each target Junos host."
        },
        {
          "key": "checksum_algorithm",
          "fieldId": "CHECKSUM_ALGORITHM",
          "type": "text",
          "label": "checksum algorithm",
          "default": "md5",
          "description": "The algorithm to use when calculating the checksum of the local and remote software packages."
        },
        {
          "key": "checksum_timeout",
          "fieldId": "CHECKSUM_TIMEOUT",
          "type": "number",
          "label": "checksum timeout",
          "description": "The number of seconds to wait for the calculation of the checksum to complete on the target Junos device."
        },
        {
          "key": "cleanfs",
          "fieldId": "CLEANFS",
          "type": "boolean",
          "label": "cleanfs",
          "default": true,
          "description": "Whether or not to perform a C(request system storage cleanup) prior to copying or installing the software."
        },
        {
          "key": "cleanfs_timeout",
          "fieldId": "CLEANFS_TIMEOUT",
          "type": "number",
          "label": "cleanfs timeout",
          "description": "The number of seconds to wait for the C(request system storage cleanup) to complete on the target Junos device."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "force_host",
          "fieldId": "FORCE_HOST",
          "type": "boolean",
          "label": "force host",
          "default": false,
          "description": "Forces the upgrade of the Host Software package on QFX-series devices."
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "install_timeout",
          "fieldId": "INSTALL_TIMEOUT",
          "type": "number",
          "label": "install timeout",
          "description": "The number of seconds to wait for the software installation to complete on the target Junos device."
        },
        {
          "key": "issu",
          "fieldId": "ISSU",
          "type": "boolean",
          "label": "issu",
          "default": false,
          "description": "Indicates if a unified in-service software upgrade (ISSU) should be attempted. ISSU enables the upgrade between two different Junos OS releases with no control plane disruption and minimal data plane traffic disruption. In order for an ISSU to succeed, ISSU must be supported. This includes support for the current to desired Junos versions, the hardware of the target Junos device, and the current software configuration of the target Junos device. The I(issu) and I(nssu) options are mutually exclusive."
        },
        {
          "key": "kwargs",
          "fieldId": "KWARGS",
          "type": "text",
          "label": "kwargs",
          "yamlType": "dict",
          "default": "none",
          "description": "Additional keyword arguments and values which are passed to the C(<request-package-add>) RPC used to install the software package. The value of this option is a dictionary of keywords and values.",
          "aliases": [
            "kwarg",
            "args",
            "arg"
          ]
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "local_package",
          "fieldId": "LOCAL_PACKAGE",
          "type": "text",
          "label": "local package",
          "default": "none",
          "description": "The path, on the local Ansible control machine, of a Junos software package. This Junos software package will be installed on the target Junos device. If this option is specified, and a file with the same MD5 checksum doesn't already exist at the I(remote_package) location on the target Junos device, then the file is copied from the local Ansible control machine to the target Junos device. If this option is not specified, it is assumed that the software package already exists on the target Junos device. In this case, the I(remote_package) option must be specified.",
          "aliases": [
            "package"
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "member_id",
          "fieldId": "MEMBER_ID",
          "type": "text",
          "label": "member id",
          "yamlType": "list",
          "default": "none",
          "description": "install software on the specified members ids of VC."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "no_copy",
          "fieldId": "NO_COPY",
          "type": "boolean",
          "label": "no copy",
          "default": false,
          "description": "Indicates if the file containing the software package should be copied from the I(local_package) location on the local Ansible control machine to the I(remote_package) location on the target Junos device. If the value is C(true), or if the I(local_package) option is not specified, then the copy is skipped and the file must already exist at the I(remote_package) location on the target Junos device."
        },
        {
          "key": "nssu",
          "fieldId": "NSSU",
          "type": "boolean",
          "label": "nssu",
          "default": false,
          "description": "Indicates if a non-stop software upgrade (NSSU) should be attempted. NSSU enables the upgrade between two different Junos OS releases with minimal data plane traffic disruption. NSSU is specific to EX-series Virtual Chassis systems or EX-series stand-alone systems with redundant Routing Engines. In order for an NSSU to succeed, NSSU must be supported. This includes support for the current to desired Junos versions, the hardware of the target Junos device, and the current software configuration of the target Junos device. The I(nssu) and I(issu) options are mutually exclusive."
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "pkg_set",
          "fieldId": "PKG_SET",
          "type": "text",
          "label": "pkg set",
          "yamlType": "list",
          "description": "install software on the members in a mixed Virtual Chassis. Currently we are not doing target package check this option is provided."
        },
        {
          "key": "reboot",
          "fieldId": "REBOOT",
          "type": "boolean",
          "label": "reboot",
          "default": true,
          "description": "Indicates if the target Junos device should be rebooted after performing the software install."
        },
        {
          "key": "reboot_pause",
          "fieldId": "REBOOT_PAUSE",
          "type": "number",
          "label": "reboot pause",
          "default": 10,
          "description": "The amount of time, in seconds, to wait after the reboot is issued before the module returns. This gives time for the reboot to begin. The default value of 10 seconds is designed to ensure the device is no longer reachable (because the reboot has begun) when the next task begins. The value must be an integer greater than or equal to 0."
        },
        {
          "key": "remote_package",
          "fieldId": "REMOTE_PACKAGE",
          "type": "text",
          "label": "remote package",
          "default": "C(/var/tmp/) + filename portion of I(local_package)",
          "description": "This option may take one of two formats. The first format is a URL, from the perspective of the target Junos device, from which the device retrieves the software package to be installed. The acceptable formats for the URL value may be found U(here|https://www.juniper.net/documentation/en_US/junos/topics/concept/junos-software-formats-filenames-urls.html). When using the URL format, the I(local_package) and I(no_copy) options must not be specified. The second format is a file path, on the taget Junos device, to the software package. If the I(local_package) option is also specified, and the I(no_copy) option is C(false), the software package will be copied from I(local_package) to I(remote_package), if necessary. If the I(no_copy) option is C(true) or the I(local_package) option is not specified, then the file specified by this option must already exist on the target Junos device. If this option is not specified, it is assumed that the software package will be copied into the C(/var/tmp) directory on the target Junos device using the filename portion of the I(local_package) option. In this case, the I(local_package) option must be specified. Specifying the I(remote_package) option and not specifying the I(local_package) option is equivalent to specifying the I(local_package) option and the I(no_copy) option. In this case, you no longer have to explicitly specify the I(no_copy) option. If the I(remote_package) value is a directory (ends with /), then the filename portion of I(local_package) will be appended to the I(remote_package) value. If the I(remote_package) value is a file (does not end with /), then the filename portion of I(remote_package) must be the same as the filename portion of I(local_package)."
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        },
        {
          "key": "validate",
          "fieldId": "VALIDATE",
          "type": "boolean",
          "label": "validate",
          "default": false,
          "description": "Whether or not to have the target Junos device should validate the current configuration against the new software package."
        },
        {
          "key": "version",
          "fieldId": "VERSION",
          "type": "text",
          "label": "version",
          "default": "Attempt to extract the version from the file name specified by the I(local_package) or I(remote_package) option values IF the package appears to be a Junos software package. Otherwise, C(none).",
          "description": "The version of software contained in the file specified by the I(local_package) and/or I(remote_package) options. This value should match the Junos version which will be reported by the device once the new software is installed. If the device is already running a version of software which matches the I(version) option value, the software install is not necessary. In this case the module returns a I(changed) value of C(false) and an I(failed) value of C(false) and does not attempt to perform the software install.",
          "aliases": [
            "target_version",
            "new_version",
            "desired_version"
          ]
        },
        {
          "key": "vmhost",
          "fieldId": "VMHOST",
          "type": "boolean",
          "label": "vmhost",
          "default": false,
          "description": "Whether or not this is a vmhost software installation."
        }
      ]
    },
    {
      "fqcn": "juniper.device.srx_cluster",
      "blockType": "juniper_device_srx_cluster",
      "label": "srx_cluster",
      "tooltip": "Add or remove SRX chassis cluster configuration",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/srx_cluster_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "cluster_id",
          "fieldId": "CLUSTER_ID",
          "type": "number",
          "label": "cluster id",
          "description": "The cluster ID to configure. Required when I(enable) is C(true).",
          "aliases": [
            "cluster"
          ]
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "enable",
          "fieldId": "ENABLE",
          "type": "boolean",
          "label": "enable",
          "default": true,
          "description": "Enable or disable cluster mode. When C(true) cluster mode is enabled and I(cluster_id) and I(node_id) must also be specified. When C(false) cluster mode is disabled and the device returns to stand-alone mode.",
          "required": true,
          "aliases": [
            "cluster_enable"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "node_id",
          "fieldId": "NODE_ID",
          "type": "number",
          "label": "node id",
          "description": "The node ID to configure. (C(0) or C(1)) Required when I(enable) is C(true).",
          "aliases": [
            "node"
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.system",
      "blockType": "juniper_device_system",
      "label": "system",
      "tooltip": "Initiate operational actions on the Junos system",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/system_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "action",
          "fieldId": "ACTION",
          "type": "dropdown",
          "label": "action",
          "default": "none",
          "description": "The action performed by the module. The following actions are supported:\n B(shutdown) - Power off the Junos devices. The values C(off), C(power-off), and C(power_off) are aliases for this value. This is the equivalent of the C(request system power-off) CLI command. B(halt) - Stop the Junos OS running on the RE, but do not power off the system. Once the system is halted, it will reboot if a keystroke is entered on the console. This is the equivalent of the C(request system halt) CLI command. B(reboot) - Reboot the system. This is the equivalent of the C(request system reboot) CLI command. B(zeroize) - Restore the system (configuration, log files, etc.) to a factory default state. This is the equivalent of the C(request system zeroize) CLI command.",
          "options": [
            [
              "shutdown",
              "shutdown"
            ],
            [
              "halt",
              "halt"
            ],
            [
              "reboot",
              "reboot"
            ],
            [
              "zeroize",
              "zeroize"
            ],
            [
              "off",
              "off"
            ],
            [
              "power-off",
              "power-off"
            ],
            [
              "power_off",
              "power_off"
            ]
          ],
          "required": true
        },
        {
          "key": "all_re",
          "fieldId": "ALL_RE",
          "type": "boolean",
          "label": "all re",
          "default": true,
          "description": "If the system has multiple Routing Engines and this option is C(true), then the action is performed on all REs in the system. If the system does not have multiple Routing Engines, then this option has no effect. This option applies to all I(action) values. The I(all_re) option is mutually exclusive with the I(other_re) option."
        },
        {
          "key": "at",
          "fieldId": "AT",
          "type": "text",
          "label": "at",
          "default": "none",
          "description": "The time at which to shutdown, halt, or reboot the system. The value may be specified in one of the following ways:\n B(now) - The action takes effect immediately. B(+minutes) \u2014 The action takes effect in C(minutes) minutes from now. B(yymmddhhmm) \u2014 The action takes effect at C(yymmddhhmm) absolute time, specified as year, month, day, hour, and minute. B(hh:mm) \u2014 The action takes effect at C(hh:mm) absolute time on the current day, specified in 24-hour time. The I(at) option can not be used when the I(action) option has a value of C(zeroize). The I(at) option is mutually exclusive with the I(in_min) option."
        },
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "in_min",
          "fieldId": "IN_MIN",
          "type": "number",
          "label": "in min",
          "default": 0,
          "description": "Specify a delay, in minutes, before the shutdown, halt, or reboot. The I(in_min) option can not be used when the I(action) option has a value of C(zeroize). The I(in_min) option is mutually exclusive with the I(at) option."
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "media",
          "fieldId": "MEDIA",
          "type": "boolean",
          "label": "media",
          "default": false,
          "description": "Overwrite media when performing the zeroize operation. This option is only valid when the I(action) option has a value of C(zeroize)."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "other_re",
          "fieldId": "OTHER_RE",
          "type": "boolean",
          "label": "other re",
          "default": false,
          "description": "If the system has dual Routing Engines and this option is C(true), then the action is performed on the other REs in the system. If the system does not have dual Routing Engines, then this option has no effect. The I(other_re) option can not be used when the I(action) option has a value of C(zeroize). The I(other_re) option is mutually exclusive with the I(all_re) option."
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        },
        {
          "key": "vmhost",
          "fieldId": "VMHOST",
          "type": "boolean",
          "label": "vmhost",
          "default": false,
          "description": "Whether or not this is a vmhost reboot."
        }
      ]
    },
    {
      "fqcn": "juniper.device.table",
      "blockType": "juniper_device_table",
      "label": "table",
      "tooltip": "Retrieve data from a Junos device using a PyEZ table/view",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/table_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attempts",
          "fieldId": "ATTEMPTS",
          "type": "number",
          "label": "attempts",
          "default": 10,
          "description": "The number of times to try connecting and logging in to the Junos device. This option is only applicable when using C(mode = 'telnet') or C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "baud",
          "fieldId": "BAUD",
          "type": "number",
          "label": "baud",
          "default": 9600,
          "description": "The serial baud rate, in bits per second, used to connect to the Junos device. This option is only applicable when using C(mode = 'serial'). Mutually exclusive with the I(console) option."
        },
        {
          "key": "console",
          "fieldId": "CONSOLE",
          "type": "text",
          "label": "console",
          "default": "none",
          "description": "An alternate method of specifying a NETCONF over serial console connection to the Junos device using Telnet to a console server. The value of this option must be a string in the format C(--telnet <console_hostname>,<console_port_number>). This option is deprecated. It is present only for backwards compatibility. The string value of this option is exactly equivalent to specifying I(host) with a value of C(<console_hostname>), I(mode) with a value of C(telnet), and I(port) with a value of C(<console_port_number>). Mutually exclusive with the I(mode), I(port), I(baud), and I(attempts) options."
        },
        {
          "key": "cs_passwd",
          "fieldId": "CS_PASSWD",
          "type": "text",
          "label": "cs passwd",
          "description": "The password used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_password"
          ]
        },
        {
          "key": "cs_user",
          "fieldId": "CS_USER",
          "type": "text",
          "label": "cs user",
          "description": "The username used to authenticate with the console server over SSH. This option is only required if you want to connect to a device over console using SSH as transport. Mutually exclusive with the I(console) option.",
          "aliases": [
            "console_username"
          ]
        },
        {
          "key": "file",
          "fieldId": "FILE",
          "type": "text",
          "label": "file",
          "default": "none",
          "description": "Name of the YAML file, relative to the I(path) option, that contains the table/view definition. The file name must end with the C(.yml) or C(.yaml) extension.",
          "required": true
        },
        {
          "key": "huge_tree",
          "fieldId": "HUGE_TREE",
          "type": "boolean",
          "label": "huge tree",
          "default": false,
          "description": "Parse XML with very deep trees and long text content."
        },
        {
          "key": "kwargs",
          "fieldId": "KWARGS",
          "type": "text",
          "label": "kwargs",
          "yamlType": "dict",
          "default": "none",
          "description": "Optional keyword arguments and values to the table's get() method. The value of this option is a dictionary of keywords and values which are used to refine the data return from performing a get() on the table. The exact keywords and values which are supported are specific to the table's definition and the underlying RPC which the table invokes.",
          "aliases": [
            "kwarg",
            "args",
            "arg"
          ]
        },
        {
          "key": "level",
          "fieldId": "LEVEL",
          "type": "dropdown",
          "label": "level",
          "default": "WARNING",
          "description": "The level of information to be logged can be modified using this option 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged.",
          "options": [
            [
              "INFO",
              "INFO"
            ],
            [
              "DEBUG",
              "DEBUG"
            ]
          ]
        },
        {
          "key": "logdir",
          "fieldId": "LOGDIR",
          "type": "text",
          "label": "logdir",
          "default": "none",
          "description": "The path to a directory, on the Ansible control machine, where debugging information for the particular task is logged. If this option is specified, debugging information is logged to a file named C({{ inventory_hostname }}.log) in the directory specified by the I(logdir) option. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. The I(logfile) and I(logdir) options are mutually exclusive. The I(logdir) option is recommended for all new playbooks.",
          "aliases": [
            "log_dir"
          ]
        },
        {
          "key": "logfile",
          "fieldId": "LOGFILE",
          "type": "text",
          "label": "logfile",
          "default": "none",
          "description": "The path to a file, on the Ansible control machine, where debugging information for the particular task is logged. The log file must be writeable. If the file already exists, it is appended. It is the users responsibility to delete/rotate log files. The level of information logged in this file is controlled by Ansible's verbosity, debug options and level option in task 1) By default, messages at level C(WARNING) or higher are logged. 2) If the C(-v) or C(--verbose) command-line options to the C(ansible-playbook) command are specified, messages at level C(INFO) or higher are logged. 3) If the C(-vv) (or more verbose) command-line option to the C(ansible-playbook) command is specified, or the C(ANSIBLE_DEBUG) environment variable is set, then messages at level C(DEBUG) or higher are logged. 4) If C(level) is mentioned then messages at level C(level) or more are logged. When tasks are executed against more than one target host, one process is forked for each target host. (Up to the maximum specified by the forks configuration. See U(forks|http://docs.ansible.com/ansible/latest/intro_configuration.html#forks) for details.) This means that the value of this option must be unique per target host. This is usually accomplished by including C({{ inventory_hostname }}) in the I(logfile) value. It is the user's responsibility to ensure this value is unique per target host. For this reason, this option is deprecated. It is maintained for backwards compatibility. Use the I(logdir) option in new playbooks. The I(logfile) and I(logdir) options are mutually exclusive.",
          "aliases": [
            "log_file"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "none",
          "description": "The PyEZ mode used to establish a NETCONF connection to the Junos device. A value of C(none) uses the default NETCONF over SSH mode. Depending on the values of the I(host) and I(port) options, a value of C(telnet) results in either a direct NETCONF over Telnet connection to the Junos device, or a NETCONF over serial console connection to the Junos device using Telnet to a console server. A value of C(serial) results in a NETCONF over serial console connection to the Junos device. Mutually exclusive with the I(console) option.",
          "options": [
            [
              "none",
              "none"
            ],
            [
              "telnet",
              "telnet"
            ],
            [
              "serial",
              "serial"
            ]
          ]
        },
        {
          "key": "passwd",
          "fieldId": "PASSWD",
          "type": "text",
          "label": "passwd",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_PASSWORD) environment variable. (used by Ansible Tower) 2) The value specified using the C(-k) or C(--ask-pass) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (An empty password/passphrase)",
          "description": "The password, or ssh key's passphrase, used to authenticate with the Junos device. If this option is not specified, authentication is attempted using an empty password, or ssh key passphrase.",
          "aliases": [
            "password"
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "default": "C(op) directory in C(jnpr.junos.op)",
          "description": "The directory containing the YAML table/view definition file as specified by the I(file) option. The default value is the C(op) directory in C(jnpr.junos.op). This is the directory containing the table/view definitions which are included in the PyEZ distribution.",
          "aliases": [
            "directory",
            "dir"
          ]
        },
        {
          "key": "response_type",
          "fieldId": "RESPONSE_TYPE",
          "type": "dropdown",
          "label": "response type",
          "default": "list_of_dicts",
          "description": "Defines the format of data returned by the module. See RETURN. The value of the I(resource) key in the module's response is either a list of dictionaries C(list_of_dicts) or PyEZ's native return format C(juniper_items). Because Ansible module's may only return JSON data, PyEZ's native return format C(juniper_items) is translated into a list of lists.",
          "options": [
            [
              "list_of_dicts",
              "list_of_dicts"
            ],
            [
              "juniper_items",
              "juniper_items"
            ]
          ]
        },
        {
          "key": "ssh_config",
          "fieldId": "SSH_CONFIG",
          "type": "text",
          "label": "ssh config",
          "description": "The path to the SSH client configuration file. If this option is not specified, then the PyEZ Device instance by default queries file ~/.ssh/config."
        },
        {
          "key": "ssh_private_key_file",
          "fieldId": "SSH_PRIVATE_KEY_FILE",
          "type": "text",
          "label": "ssh private key file",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_SSH_KEYFILE) environment variable. (used by Ansible Tower) 2) The value specified using the C(--private-key) or C(--key-file) command line arguments to the C(ansible) or C(ansible-playbook) command. 3) none (the file specified in the user's SSH configuration, or the operating-system-specific default)",
          "description": "The path to the SSH private key file used to authenticate with the Junos device. If this option is not specified, and no default value is found using the algorithm below, then the SSH private key file specified in the user's SSH configuration, or the operating-system-specific default is used. This must be in the RSA PEM format, and not the newer OPENSSH format. To check if the private key is in the correct format, issue the command `head -n1 ~/.ssh/some_private_key` and ensure that it's RSA and not OPENSSH. To create a key in the RSA PEM format, issue the command `ssh-keygen -m PEM -t rsa -b 4096`. To convert an OPENSSH key to an RSA key, issue the command `ssh-keygen -p -m PEM -f ~/.ssh/some_private_key`",
          "aliases": [
            "ssh_keyfile"
          ]
        },
        {
          "key": "table",
          "fieldId": "TABLE",
          "type": "text",
          "label": "table",
          "default": "The name of the table defined in the I(file) option.",
          "description": "Name of the PyEZ table used to retrieve data. If not specified, defaults to the name of the table defined in the I(file) option. Any table names in I(file) which begin with C(_) are ignored. If more than one table is defined in I(file), the module fails with an error message. In this case, you must manually specify the name of the table by setting this option."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "default": "The first defined value from the following list 1) The C(ANSIBLE_NET_USERNAME) environment variable. (used by Ansible Tower) 2) The C(remote_user) as defined by Ansible. Ansible sets this value via several methods including a) C(-u) or C(--user) command line arguments to the C(ansible) or C(ansible-playbook) command. b) C(ANSIBLE_REMOTE_USER) environment variable. c) C(remote_user) configuration setting. See the Ansible documentation for the precedence used to set the C(remote_user) value. 3) The C(USER) environment variable.",
          "description": "The username used to authenticate with the Junos device. This option is required, but does not have to be specified explicitly by the user due to the algorithm for determining the default value.",
          "required": true,
          "aliases": [
            "username"
          ]
        }
      ]
    },
    {
      "fqcn": "juniper.device.tbrdevice_facts",
      "blockType": "juniper_device_tbrdevice_facts",
      "label": "tbrdevice_facts",
      "tooltip": "Collect facts from remote devices running Juniper Junos",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/juniper/device/tbrdevice_facts_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "available_network_resources",
          "fieldId": "AVAILABLE_NETWORK_RESOURCES",
          "type": "boolean",
          "label": "available network resources",
          "default": false,
          "description": "When 'True' a list of network resources for which resource modules are available will be provided."
        },
        {
          "key": "config_format",
          "fieldId": "CONFIG_FORMAT",
          "type": "dropdown",
          "label": "config format",
          "default": "text",
          "description": "The I(config_format) argument specifies the format of the configuration when serializing output from the device. This argument is applicable only when C(config) value is present in I(gather_subset). The I(config_format) should be supported by the junos version running on device. This value is not applicable while fetching old style facts that is when C(ofacts) value is present in value if I(gather_subset) value. This option is valid only for C(gather_subset) values.",
          "options": [
            [
              "xml",
              "xml"
            ],
            [
              "text",
              "text"
            ],
            [
              "set",
              "set"
            ],
            [
              "json",
              "json"
            ]
          ]
        },
        {
          "key": "gather_network_resources",
          "fieldId": "GATHER_NETWORK_RESOURCES",
          "type": "text",
          "label": "gather network resources",
          "yamlType": "list",
          "description": "When supplied, this argument will restrict the facts collected to a given subset. Possible values for this argument include all and the resources like interfaces, vlans etc. Can specify a list of values to include a larger subset. Values can also be used with an initial C(!) to specify that a specific subset should not be collected. Valid subsets are 'all', 'interfaces', 'lacp', 'lacp_interfaces', 'lag_interfaces', 'l2_interfaces', 'l3_interfaces', 'lldp_global', 'lldp_interfaces', 'vlans'."
        },
        {
          "key": "gather_subset",
          "fieldId": "GATHER_SUBSET",
          "type": "text",
          "label": "gather subset",
          "yamlType": "list",
          "description": "When supplied, this argument will restrict the facts collected to a given subset.  Possible values for this argument include C(all), C(hardware), C(config), C(interfaces) and C(min). Can specify a list of values to include a larger subset.  Values can also be used with an initial C(!) to specify that a specific subset should not be collected. To maintain backward compatibility old style facts can be retrieved by explicitly adding C(ofacts)  to value, this requires junos-eznc to be installed as a prerequisite. Valid value of gather_subset are default, hardware, config, interfaces, ofacts. If C(ofacts) is present in the list it fetches the old style facts (fact keys without 'ansible_' prefix) and it requires junos-eznc library to be installed."
        }
      ]
    }
  ]
}
