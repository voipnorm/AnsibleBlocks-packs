{
  "schemaVersion": 1,
  "packVersion": "1.0.0",
  "id": "community-crypto",
  "name": "Community Crypto",
  "collection": "community.crypto",
  "collectionVersion": "3.2.2",
  "minAppVersion": "1.0.0",
  "testedWithAppVersion": "0.1.0",
  "publishedAt": "2026-07-09T05:26:14Z",
  "qualityLevel": "generated",
  "source": "generated",
  "categoryColor": "#2B5797",
  "blockStyleName": "community_crypto_blocks",
  "categoryStyleName": "community_crypto_category",
  "modules": [
    {
      "fqcn": "community.crypto.acme_account",
      "blockType": "community_crypto_acme_account",
      "label": "acme_account",
      "tooltip": "Create, modify or delete ACME accounts",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_account_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "allow_creation",
          "fieldId": "ALLOW_CREATION",
          "type": "boolean",
          "label": "allow creation",
          "default": true,
          "description": "Whether account creation is allowed (when state is V(present))."
        },
        {
          "key": "contact",
          "fieldId": "CONTACT",
          "type": "text",
          "label": "contact",
          "yamlType": "list",
          "description": "A list of contact URLs. Email addresses must be prefixed with C(mailto:). See U(https://tools.ietf.org/html/rfc8555#section-7.3) for what is allowed. Must be specified when state is V(present). Will be ignored if state is V(absent) or V(changed_key)."
        },
        {
          "key": "new_account_key_content",
          "fieldId": "NEW_ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "new account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key to change to. Same restrictions apply as to O(account_key_content). Mutually exclusive with O(new_account_key_src). Required if O(new_account_key_src) is not used and O(state) is V(changed_key)."
        },
        {
          "key": "new_account_key_passphrase",
          "fieldId": "NEW_ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "new account key passphrase",
          "description": "Passphrase to use to decode the new account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "new_account_key_src",
          "fieldId": "NEW_ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "new account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key to change to. Same restrictions apply as to O(account_key_src). Mutually exclusive with O(new_account_key_content). Required if O(new_account_key_content) is not used and O(state) is V(changed_key)."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "description": "The state of the account, to be identified by its account key. If the state is V(absent), the account will either not exist or be deactivated. If the state is V(changed_key), the account must exist. The account key will be changed; no other information will be touched.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ],
            [
              "changed_key",
              "changed_key"
            ]
          ],
          "required": true
        },
        {
          "key": "terms_agreed",
          "fieldId": "TERMS_AGREED",
          "type": "boolean",
          "label": "terms agreed",
          "default": false,
          "description": "Boolean indicating whether you agree to the terms of service document. ACME servers can require this to be V(true)."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ],
      "unmappedParams": [
        "external_account_binding (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.acme_account_info",
      "blockType": "community_crypto_acme_account_info",
      "label": "acme_account_info",
      "tooltip": "Retrieves information on ACME accounts",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_account_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "retrieve_orders",
          "fieldId": "RETRIEVE_ORDERS",
          "type": "dropdown",
          "label": "retrieve orders",
          "default": "ignore",
          "description": "Whether to retrieve the list of order URLs or order objects, if provided by the ACME server. A value of V(ignore) will not fetch the list of orders. If the value is not V(ignore) and the ACME server supports orders, the RV(order_uris) return value is always populated. The RV(orders) return value is only returned if this option is set to V(object_list). Currently, Let's Encrypt does not return orders, so the RV(orders) result will always be empty.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "url_list",
              "url_list"
            ],
            [
              "object_list",
              "object_list"
            ]
          ]
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_ari_info",
      "blockType": "community_crypto_acme_ari_info",
      "label": "acme_ari_info",
      "tooltip": "Retrieves ACME Renewal Information (ARI) for a certificate",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_ari_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "certificate_content",
          "fieldId": "CERTIFICATE_CONTENT",
          "type": "text",
          "label": "certificate content",
          "description": "The content of the X.509 certificate to request information for. Exactly one of O(certificate_path) and O(certificate_content) must be provided."
        },
        {
          "key": "certificate_path",
          "fieldId": "CERTIFICATE_PATH",
          "type": "text",
          "label": "certificate path",
          "description": "A path to the X.509 certificate to request information for. Exactly one of O(certificate_path) and O(certificate_content) must be provided."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate",
      "blockType": "community_crypto_acme_certificate",
      "label": "acme_certificate",
      "tooltip": "Create SSL/TLS certificates with the ACME protocol",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_email",
          "fieldId": "ACCOUNT_EMAIL",
          "type": "text",
          "label": "account email",
          "description": "The email address associated with this account. It will be used for certificate expiration warnings. Note that when O(modify_account) is not set to V(false) and you also used the M(community.crypto.acme_account) module to specify more than one contact for your account, this module will update your account and restrict it to the (at most one) contact email address specified here."
        },
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "agreement",
          "fieldId": "AGREEMENT",
          "type": "text",
          "label": "agreement",
          "description": "URI to a terms of service document you agree to when using the ACME v1 service at O(acme_directory). Default is latest gathered from O(acme_directory) URL. This option has no longer any effect. It is deprecated and will be removed from community.crypto 4.0.0."
        },
        {
          "key": "chain_dest",
          "fieldId": "CHAIN_DEST",
          "type": "text",
          "label": "chain dest",
          "description": "If specified, the intermediate certificate will be written to this file.",
          "aliases": [
            "chain"
          ]
        },
        {
          "key": "challenge",
          "fieldId": "CHALLENGE",
          "type": "dropdown",
          "label": "challenge",
          "default": "http-01",
          "description": "The challenge to be performed. If set to V(no challenge), no challenge will be used. This is necessary for some private CAs which use External Account Binding and other means of validating certificate assurance. For example, an account could be allowed to issue certificates for C(foo.example.com) without any further validation for a certain period of time. Support for V(dns-account-01) and V(dns-persist-01) has been added in community.crypto 3.2.0.",
          "options": [
            [
              "http-01",
              "http-01"
            ],
            [
              "dns-01",
              "dns-01"
            ],
            [
              "dns-account-01",
              "dns-account-01"
            ],
            [
              "dns-persist-01",
              "dns-persist-01"
            ],
            [
              "tls-alpn-01",
              "tls-alpn-01"
            ],
            [
              "no challenge",
              "no challenge"
            ]
          ]
        },
        {
          "key": "csr",
          "fieldId": "CSR",
          "type": "text",
          "label": "csr",
          "description": "File containing the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified.",
          "aliases": [
            "src"
          ]
        },
        {
          "key": "csr_content",
          "fieldId": "CSR_CONTENT",
          "type": "text",
          "label": "csr content",
          "description": "Content of the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr_pipe). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified."
        },
        {
          "key": "data",
          "fieldId": "DATA",
          "type": "text",
          "label": "data",
          "yamlType": "dict",
          "description": "The data to validate ongoing challenges. This must be specified for the second run of the module only. The value that must be used here will be provided by a previous use of this module. See the examples for more details. Note that for ACME v2, only the C(order_uri) entry of O(data) will be used. For ACME v1, O(data) must be non-empty to indicate the second stage is active; all needed data will be taken from the CSR. I(Note): the O(data) option was marked as C(no_log) up to Ansible 2.5. From Ansible 2.6 on, it is no longer marked this way as it causes error messages to be come unusable, and O(data) does not contain any information which can be used without having access to the account key or which are not public anyway."
        },
        {
          "key": "deactivate_authzs",
          "fieldId": "DEACTIVATE_AUTHZS",
          "type": "boolean",
          "label": "deactivate authzs",
          "default": false,
          "description": "Deactivate authentication objects (authz) after issuing a certificate, or when issuing the certificate failed. Authentication objects are bound to an account key and remain valid for a certain amount of time, and can be used to issue certificates without having to re-authenticate the domain. This can be a security concern."
        },
        {
          "key": "dest",
          "fieldId": "DEST",
          "type": "text",
          "label": "dest",
          "description": "The destination file for the certificate. Required if O(fullchain_dest) is not specified.",
          "aliases": [
            "cert"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Enforces the execution of the challenge and validation, even if an existing certificate is still valid for more than O(remaining_days). This is especially helpful when having an updated CSR, for example with additional domains for which a new certificate is desired."
        },
        {
          "key": "fullchain_dest",
          "fieldId": "FULLCHAIN_DEST",
          "type": "text",
          "label": "fullchain dest",
          "description": "The destination file for the full chain (that is, a certificate followed by chain of intermediate certificates). Required if O(dest) is not specified.",
          "aliases": [
            "fullchain"
          ]
        },
        {
          "key": "include_renewal_cert_id",
          "fieldId": "INCLUDE_RENEWAL_CERT_ID",
          "type": "text",
          "label": "include renewal cert id",
          "default": "never",
          "description": "Determines whether to request renewal of an existing certificate according to L(Section 5 of RFC 9773, https://www.rfc-editor.org/rfc/rfc9773.html#section-5). This is only used when the certificate specified in O(dest) or O(fullchain_dest) already exists. Generally you should use V(when_ari_supported) if you know that the ACME service supports a compatible draft (or final version, once it is out) of the ARI extension. V(always) should never be necessary. If you are not sure, or if you receive strange errors on invalid C(replaces) values in order objects, use V(never), which also happens to be the default. ACME servers might refuse to create new orders with C(replaces) for certificates that already have an existing order. This can happen if this module is used to create an order, and then the playbook/role fails in case the challenges cannot be set up. If the playbook/role does not record the order data to continue with the existing order, but tries to create a new one on the next run, creating the new order might fail. If O(order_creation_error_strategy=fail) this will make the module fail. O(order_creation_error_strategy=auto) and O(order_creation_error_strategy=retry_without_replaces_cert_id) will avoid this by leaving away C(replaces) on retries. If O(order_creation_error_strategy=fail), for the above reason, this option should only be set to a value different from V(never) if the role/playbook using it keeps track of order data accross restarts, or if it takes care to deactivate orders whose processing is aborted. Orders can be deactivated with the M(community.crypto.acme_certificate_deactivate_authz) module."
        },
        {
          "key": "modify_account",
          "fieldId": "MODIFY_ACCOUNT",
          "type": "boolean",
          "label": "modify account",
          "description": "Boolean indicating whether the module should create the account if necessary, and update its contact data. Set to V(false) if you want to use the M(community.crypto.acme_account) module to manage your account instead, and to avoid accidental creation of a new account using an old key if you changed the account key with M(community.crypto.acme_account). If set to V(false), O(terms_agreed) and O(account_email) are ignored. The current default V(true) is B(deprecated) and will change to V(false) in community.crypto 4.0.0."
        },
        {
          "key": "order_creation_error_strategy",
          "fieldId": "ORDER_CREATION_ERROR_STRATEGY",
          "type": "text",
          "label": "order creation error strategy",
          "default": "auto",
          "description": "Selects the error handling strategy for ACME protocol errors if creating a new ACME order fails."
        },
        {
          "key": "order_creation_max_retries",
          "fieldId": "ORDER_CREATION_MAX_RETRIES",
          "type": "number",
          "label": "order creation max retries",
          "default": 3,
          "description": "Depending on the strategy selected in O(order_creation_error_strategy), will retry creating new orders for at most the specified amount of times."
        },
        {
          "key": "profile",
          "fieldId": "PROFILE",
          "type": "text",
          "label": "profile",
          "description": "Chose a specific profile for certificate selection. The available profiles depend on the CA. See L(a blog post by Let's Encrypt, https://letsencrypt.org/2025/01/09/acme-profiles/) and L(draft-aaron-acme-profiles-01, https://datatracker.ietf.org/doc/draft-aaron-acme-profiles/) for more information."
        },
        {
          "key": "remaining_days",
          "fieldId": "REMAINING_DAYS",
          "type": "number",
          "label": "remaining days",
          "default": 10,
          "description": "The number of days the certificate must have left being valid. If RV(cert_days) < O(remaining_days), then it will be renewed. If the certificate is not renewed, module return values will not include RV(challenge_data). To make sure that the certificate is renewed in any case, you can use the O(force) option."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "retrieve_all_alternates",
          "fieldId": "RETRIEVE_ALL_ALTERNATES",
          "type": "boolean",
          "label": "retrieve all alternates",
          "default": false,
          "description": "When set to V(true), will retrieve all alternate trust chains offered by the ACME CA. These will not be written to disk, but will be returned together with the main chain as RV(all_chains). See the documentation for the RV(all_chains) return value for details."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "terms_agreed",
          "fieldId": "TERMS_AGREED",
          "type": "boolean",
          "label": "terms agreed",
          "default": false,
          "description": "Boolean indicating whether you agree to the terms of service document. ACME servers can require this to be true."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ],
      "unmappedParams": [
        "select_chain (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_deactivate_authz",
      "blockType": "community_crypto_acme_certificate_deactivate_authz",
      "label": "acme_certificate_deactivate_authz",
      "tooltip": "Deactivate all authz for an ACME v2 order",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_deactivate_authz_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "order_uri",
          "fieldId": "ORDER_URI",
          "type": "text",
          "label": "order uri",
          "description": "The ACME v2 order to deactivate. Can be obtained from RV(community.crypto.acme_certificate#module:order_uri).",
          "required": true
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_order_create",
      "blockType": "community_crypto_acme_certificate_order_create",
      "label": "acme_certificate_order_create",
      "tooltip": "Create an ACME v2 order",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_order_create_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "csr",
          "fieldId": "CSR",
          "type": "text",
          "label": "csr",
          "description": "File containing the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified."
        },
        {
          "key": "csr_content",
          "fieldId": "CSR_CONTENT",
          "type": "text",
          "label": "csr content",
          "description": "Content of the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr_pipe). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified."
        },
        {
          "key": "deactivate_authzs",
          "fieldId": "DEACTIVATE_AUTHZS",
          "type": "boolean",
          "label": "deactivate authzs",
          "default": true,
          "description": "Deactivate authentication objects (authz) when issuing the certificate failed. Authentication objects are bound to an account key and remain valid for a certain amount of time, and can be used to issue certificates without having to re-authenticate the domain. This can be a security concern."
        },
        {
          "key": "order_creation_error_strategy",
          "fieldId": "ORDER_CREATION_ERROR_STRATEGY",
          "type": "text",
          "label": "order creation error strategy",
          "default": "auto",
          "description": "Selects the error handling strategy for ACME protocol errors if creating a new ACME order fails."
        },
        {
          "key": "order_creation_max_retries",
          "fieldId": "ORDER_CREATION_MAX_RETRIES",
          "type": "number",
          "label": "order creation max retries",
          "default": 3,
          "description": "Depending on the strategy selected in O(order_creation_error_strategy), will retry creating new orders for at most the specified amount of times."
        },
        {
          "key": "profile",
          "fieldId": "PROFILE",
          "type": "text",
          "label": "profile",
          "description": "Chose a specific profile for certificate selection. The available profiles depend on the CA. See L(a blog post by Let's Encrypt, https://letsencrypt.org/2025/01/09/acme-profiles/) and L(draft-aaron-acme-profiles-01, https://datatracker.ietf.org/doc/draft-aaron-acme-profiles/) for more information."
        },
        {
          "key": "replaces_cert_id",
          "fieldId": "REPLACES_CERT_ID",
          "type": "text",
          "label": "replaces cert id",
          "description": "If provided, will request the order to replace the certificate identified by this certificate ID according to L(Section 5 of RFC 9773, https://www.rfc-editor.org/rfc/rfc9773.html#section-5). This certificate ID must be computed as specified in L(Section 4.1 of RFC 9773, https://www.rfc-editor.org/rfc/rfc9773.html#section-4.1). It is returned as return value RV(community.crypto.acme_certificate_renewal_info#module:cert_id) of the M(community.crypto.acme_certificate_renewal_info) module. ACME servers might refuse to create new orders that indicate to replace a certificate for which an active replacement order already exists. This can happen if this module is used to create an order, and then the playbook/role fails in case the challenges cannot be set up. If the playbook/role does not record the order data to continue with the existing order, but tries to create a new one on the next run, creating the new order might fail. If O(order_creation_error_strategy=fail) this will make the module fail. O(order_creation_error_strategy=auto) and O(order_creation_error_strategy=retry_without_replaces_cert_id) will avoid this by leaving away C(replaces) on retries. If O(order_creation_error_strategy=fail), for the above reason, this option should only be used if the role/playbook using it keeps track of order data accross restarts, or if it takes care to deactivate orders whose processing is aborted. Orders can be deactivated with the M(community.crypto.acme_certificate_deactivate_authz) module."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_order_finalize",
      "blockType": "community_crypto_acme_certificate_order_finalize",
      "label": "acme_certificate_order_finalize",
      "tooltip": "Finalize an ACME v2 order",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_order_finalize_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "cert_dest",
          "fieldId": "CERT_DEST",
          "type": "text",
          "label": "cert dest",
          "description": "The destination file for the certificate."
        },
        {
          "key": "chain_dest",
          "fieldId": "CHAIN_DEST",
          "type": "text",
          "label": "chain dest",
          "description": "If specified, the intermediate certificate will be written to this file."
        },
        {
          "key": "csr",
          "fieldId": "CSR",
          "type": "text",
          "label": "csr",
          "description": "File containing the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified."
        },
        {
          "key": "csr_content",
          "fieldId": "CSR_CONTENT",
          "type": "text",
          "label": "csr content",
          "description": "Content of the CSR for the new certificate. Can be created with M(community.crypto.openssl_csr_pipe). The CSR may contain multiple Subject Alternate Names, but each one will lead to an individual challenge that must be fulfilled for the CSR to be signed. B(Note): the private key used to create the CSR B(must not) be the account key. This is a bad idea from a security point of view, and the CA should not accept the CSR. The ACME server should return an error in this case. Precisely one of O(csr) or O(csr_content) must be specified."
        },
        {
          "key": "deactivate_authzs",
          "fieldId": "DEACTIVATE_AUTHZS",
          "type": "dropdown",
          "label": "deactivate authzs",
          "default": "always",
          "description": "Deactivate authentication objects (authz) after issuing a certificate, or when issuing the certificate failed. V(never) never deactivates them. V(always) always deactivates them in cases of errors or when the certificate was issued. V(on_error) only deactivates them in case of errors. V(on_success) only deactivates them in case the certificate was successfully issued. Authentication objects are bound to an account key and remain valid for a certain amount of time, and can be used to issue certificates without having to re-authenticate the domain. This can be a security concern.",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "on_error",
              "on_error"
            ],
            [
              "on_success",
              "on_success"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "fullchain_dest",
          "fieldId": "FULLCHAIN_DEST",
          "type": "text",
          "label": "fullchain dest",
          "description": "The destination file for the full chain (that is, a certificate followed by chain of intermediate certificates)."
        },
        {
          "key": "order_uri",
          "fieldId": "ORDER_URI",
          "type": "text",
          "label": "order uri",
          "description": "The order URI provided by RV(community.crypto.acme_certificate_order_create#module:order_uri).",
          "required": true
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "retrieve_all_alternates",
          "fieldId": "RETRIEVE_ALL_ALTERNATES",
          "type": "boolean",
          "label": "retrieve all alternates",
          "default": false,
          "description": "When set to V(true), will retrieve all alternate trust chains offered by the ACME CA. These will not be written to disk, but will be returned together with the main chain as RV(all_chains). See the documentation for the RV(all_chains) return value for details."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ],
      "unmappedParams": [
        "select_chain (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_order_info",
      "blockType": "community_crypto_acme_certificate_order_info",
      "label": "acme_certificate_order_info",
      "tooltip": "Obtain information for an ACME v2 order",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_order_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "order_uri",
          "fieldId": "ORDER_URI",
          "type": "text",
          "label": "order uri",
          "description": "The order URI provided by RV(community.crypto.acme_certificate_order_create#module:order_uri).",
          "required": true
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_order_validate",
      "blockType": "community_crypto_acme_certificate_order_validate",
      "label": "acme_certificate_order_validate",
      "tooltip": "Validate authorizations of an ACME v2 order",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_order_validate_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "challenge",
          "fieldId": "CHALLENGE",
          "type": "dropdown",
          "label": "challenge",
          "description": "The challenge to be performed for every pending authorization. Must be provided if there is at least one pending authorization. In case of authorization reuse, or in case of CAs which use External Account Binding and other means of validating certificate assurance, it might not be necessary to provide this option. Support for V(dns-account-01) and V(dns-persist-01) has been added in community.crypto 3.2.0.",
          "options": [
            [
              "http-01",
              "http-01"
            ],
            [
              "dns-01",
              "dns-01"
            ],
            [
              "dns-account-01",
              "dns-account-01"
            ],
            [
              "dns-persist-01",
              "dns-persist-01"
            ],
            [
              "tls-alpn-01",
              "tls-alpn-01"
            ]
          ]
        },
        {
          "key": "deactivate_authzs",
          "fieldId": "DEACTIVATE_AUTHZS",
          "type": "boolean",
          "label": "deactivate authzs",
          "default": true,
          "description": "Deactivate authentication objects (authz) in case an error happens. Authentication objects are bound to an account key and remain valid for a certain amount of time, and can be used to issue certificates without having to re-authenticate the domain. This can be a security concern."
        },
        {
          "key": "order_uri",
          "fieldId": "ORDER_URI",
          "type": "text",
          "label": "order uri",
          "description": "The order URI provided by RV(community.crypto.acme_certificate_order_create#module:order_uri).",
          "required": true
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_renewal_info",
      "blockType": "community_crypto_acme_certificate_renewal_info",
      "label": "acme_certificate_renewal_info",
      "tooltip": "Determine whether a certificate should be renewed or not",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_renewal_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "ari_algorithm",
          "fieldId": "ARI_ALGORITHM",
          "type": "dropdown",
          "label": "ari algorithm",
          "default": "standard",
          "description": "If ARI information is used, selects which algorithm is used to determine whether to renew now. V(standard) selects the L(algorithm provided in the ARI specification, https://www.rfc-editor.org/rfc/rfc9773.html#section-4.2). V(start) returns RV(should_renew=true) once the start of the renewal interval has been reached.",
          "options": [
            [
              "standard",
              "standard"
            ],
            [
              "start",
              "start"
            ]
          ]
        },
        {
          "key": "certificate_content",
          "fieldId": "CERTIFICATE_CONTENT",
          "type": "text",
          "label": "certificate content",
          "description": "The content of the X.509 certificate to determine renewal of. O(certificate_path) and O(certificate_content) are mutually exclusive."
        },
        {
          "key": "certificate_path",
          "fieldId": "CERTIFICATE_PATH",
          "type": "text",
          "label": "certificate path",
          "description": "A path to the X.509 certificate to determine renewal of. In case the certificate does not exist, the module will always return RV(should_renew=true). O(certificate_path) and O(certificate_content) are mutually exclusive."
        },
        {
          "key": "now",
          "fieldId": "NOW",
          "type": "text",
          "label": "now",
          "description": "Use this timestamp instead of the current timestamp to determine whether a certificate should be renewed. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h))."
        },
        {
          "key": "remaining_days",
          "fieldId": "REMAINING_DAYS",
          "type": "number",
          "label": "remaining days",
          "description": "The number of days the certificate must have left being valid. For example, if O(remaining_days=20), this check causes RV(should_renew=true) if the certificate is valid for less than 20 days."
        },
        {
          "key": "remaining_percentage",
          "fieldId": "REMAINING_PERCENTAGE",
          "type": "number",
          "label": "remaining percentage",
          "description": "The percentage of the certificate's validity period that should be left. For example, if O(remaining_percentage=0.1), and the certificate's validity period is 90 days, this check causes RV(should_renew=true) if the certificate is valid for less than 9 days. Must be a value between 0 and 1."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "treat_parsing_error_as_non_existing",
          "fieldId": "TREAT_PARSING_ERROR_AS_NON_EXISTING",
          "type": "boolean",
          "label": "treat parsing error as non existing",
          "default": false,
          "description": "Determines the behavior when the certificate file exists or its contents are provided, but the certificate cannot be parsed. If V(true), will exit successfully with RV(exists=true), RV(parsable=false), and RV(should_renew=true). If V(false), the module will fail. If the file exists, but cannot be loaded due to I/O errors or permission errors, the module always fails."
        },
        {
          "key": "use_ari",
          "fieldId": "USE_ARI",
          "type": "boolean",
          "label": "use ari",
          "default": true,
          "description": "Whether to use ARI information, if available. Set this to V(false) if the ACME server implements ARI in a way that is incompatible with this module."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_certificate_revoke",
      "blockType": "community_crypto_acme_certificate_revoke",
      "label": "acme_certificate_revoke",
      "tooltip": "Revoke certificates with the ACME protocol",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_certificate_revoke_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. Note that exactly one of O(account_key_src), O(account_key_content), O(private_key_src), or O(private_key_content) must be specified. I(Warning): the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. RSA keys can be created with C(openssl rsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "certificate",
          "fieldId": "CERTIFICATE",
          "type": "text",
          "label": "certificate",
          "description": "Path to the certificate to revoke.",
          "required": true
        },
        {
          "key": "private_key_content",
          "fieldId": "PRIVATE_KEY_CONTENT",
          "type": "text",
          "label": "private key content",
          "description": "Content of the certificate's private key. Note that exactly one of O(account_key_src), O(account_key_content), O(private_key_src), or O(private_key_content) must be specified. I(Warning): the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "private_key_passphrase",
          "fieldId": "PRIVATE_KEY_PASSPHRASE",
          "type": "text",
          "label": "private key passphrase",
          "description": "Passphrase to use to decode the certificate's private key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "private_key_src",
          "fieldId": "PRIVATE_KEY_SRC",
          "type": "text",
          "label": "private key src",
          "description": "Path to the certificate's private key. Note that exactly one of O(account_key_src), O(account_key_content), O(private_key_src), or O(private_key_content) must be specified."
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "revoke_reason",
          "fieldId": "REVOKE_REASON",
          "type": "number",
          "label": "revoke reason",
          "description": "One of the revocation reasonCodes defined in L(Section 5.3.1 of RFC5280,https://tools.ietf.org/html/rfc5280#section-5.3.1). Possible values are V(0) (unspecified), V(1) (keyCompromise), V(2) (cACompromise), V(3) (affiliationChanged), V(4) (superseded), V(5) (cessationOfOperation), V(6) (certificateHold), V(8) (removeFromCRL), V(9) (privilegeWithdrawn), V(10) (aACompromise)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_challenge_cert_helper",
      "blockType": "community_crypto_acme_challenge_cert_helper",
      "label": "acme_challenge_cert_helper",
      "tooltip": "Prepare certificates required for ACME challenges such as C(tls-alpn-01)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_challenge_cert_helper_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "challenge",
          "fieldId": "CHALLENGE",
          "type": "dropdown",
          "label": "challenge",
          "description": "The challenge type.",
          "options": [
            [
              "tls-alpn-01",
              "tls-alpn-01"
            ]
          ],
          "required": true
        },
        {
          "key": "challenge_data",
          "fieldId": "CHALLENGE_DATA",
          "type": "text",
          "label": "challenge data",
          "yamlType": "dict",
          "description": "The RV(community.crypto.acme_certificate#module:challenge_data) entry provided by M(community.crypto.acme_certificate) for the challenge.",
          "required": true
        },
        {
          "key": "private_key_content",
          "fieldId": "PRIVATE_KEY_CONTENT",
          "type": "text",
          "label": "private key content",
          "description": "Content of the private key to use for this challenge certificate. Mutually exclusive with O(private_key_src)."
        },
        {
          "key": "private_key_passphrase",
          "fieldId": "PRIVATE_KEY_PASSPHRASE",
          "type": "text",
          "label": "private key passphrase",
          "description": "Passphrase to use to decode the private key."
        },
        {
          "key": "private_key_src",
          "fieldId": "PRIVATE_KEY_SRC",
          "type": "text",
          "label": "private key src",
          "description": "Path to a file containing the private key file to use for this challenge certificate. Mutually exclusive with O(private_key_content)."
        }
      ]
    },
    {
      "fqcn": "community.crypto.acme_inspect",
      "blockType": "community_crypto_acme_inspect",
      "label": "acme_inspect",
      "tooltip": "Send direct requests to an ACME server",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/acme_inspect_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "account_key_content",
          "fieldId": "ACCOUNT_KEY_CONTENT",
          "type": "text",
          "label": "account key content",
          "description": "Content of the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Mutually exclusive with O(account_key_src). Required if O(account_key_src) is not used. B(Warning:) the content will be written into a temporary file, which will be deleted by Ansible when the module completes. Since this is an important private key \u2014 it can be used to change the account key, or to revoke your certificates without knowing their private keys \u2014, this might not be acceptable. In case C(cryptography) is used, the content is not written into a temporary file. It can still happen that it is written to disk by Ansible in the process of moving the module with its argument to the node where it is executed."
        },
        {
          "key": "account_key_passphrase",
          "fieldId": "ACCOUNT_KEY_PASSPHRASE",
          "type": "text",
          "label": "account key passphrase",
          "description": "Passphrase to use to decode the account key. B(Note:) this is not supported by the C(openssl) backend, only by the C(cryptography) backend."
        },
        {
          "key": "account_key_src",
          "fieldId": "ACCOUNT_KEY_SRC",
          "type": "text",
          "label": "account key src",
          "description": "Path to a file containing the ACME account RSA or Elliptic Curve key. For Elliptic Curve keys only the following curves are supported: V(secp256r1), V(secp384r1), and V(secp521r1). Private keys can be created with the M(community.crypto.openssl_privatekey) or M(community.crypto.openssl_privatekey_pipe) modules. If the requisite (cryptography) is not available, keys can also be created directly with the C(openssl) command line tool: RSA keys can be created with C(openssl genrsa ...). Elliptic curve keys can be created with C(openssl ecparam -genkey ...). Any other tool creating private keys in PEM format can be used as well. Mutually exclusive with O(account_key_content). Required if O(account_key_content) is not used.",
          "aliases": [
            "account_key"
          ]
        },
        {
          "key": "account_uri",
          "fieldId": "ACCOUNT_URI",
          "type": "text",
          "label": "account uri",
          "description": "If specified, assumes that the account URI is as given. If the account key does not match this account, or an account with this URI does not exist, the module fails."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "description": "The ACME directory to use. This is the entry point URL to access the ACME CA server API. For safety reasons the default is set to the Let's Encrypt staging server (for the ACME v1 protocol). This will create technically correct, but untrusted certificates. For Let's Encrypt, all staging endpoints can be found here: U(https://letsencrypt.org/docs/staging-environment/). For B(Let's Encrypt), the production directory URL for ACME v2 is U(https://acme-v02.api.letsencrypt.org/directory). For B(ZeroSSL), the production directory URL for ACME v2 is U(https://acme.zerossl.com/v2/DV90). For B(Sectigo), the production directory URL for ACME v2 is U(https://acme-qa.secure.trust-provider.com/v2/DV). For B(HARICA), the production directory URL for ACME v2 is U(https://acme.harica.gr/XXX/directory) with XXX being specific to your account. The notes for this module contain a list of ACME services this module has been tested against.",
          "required": true
        },
        {
          "key": "acme_version",
          "fieldId": "ACME_VERSION",
          "type": "dropdown",
          "label": "acme version",
          "default": 2,
          "description": "The ACME version of the endpoint. Must be V(2) for standardized ACME v2 endpoints. The value V(1) is no longer supported since community.crypto 3.0.0.",
          "options": [
            [
              "2",
              "2"
            ]
          ]
        },
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "An encoded JSON object which will be sent as the content if O(method) is V(post). Required when O(method) is V(post), and not allowed otherwise."
        },
        {
          "key": "fail_on_acme_error",
          "fieldId": "FAIL_ON_ACME_ERROR",
          "type": "boolean",
          "label": "fail on acme error",
          "default": true,
          "description": "If O(method) is V(post) or V(get), make the module fail in case an ACME error is returned."
        },
        {
          "key": "method",
          "fieldId": "METHOD",
          "type": "dropdown",
          "label": "method",
          "default": "get",
          "description": "The method to use to access the given URL on the ACME server. The value V(post) executes an authenticated POST request. The content must be specified in the O(content) option. The value V(get) executes an authenticated POST-as-GET request for ACME v2, and a regular GET request for ACME v1. The value V(directory-only) only retrieves the directory, without doing a request.",
          "options": [
            [
              "get",
              "get"
            ],
            [
              "post",
              "post"
            ],
            [
              "directory-only",
              "directory-only"
            ]
          ]
        },
        {
          "key": "request_timeout",
          "fieldId": "REQUEST_TIMEOUT",
          "type": "number",
          "label": "request timeout",
          "default": 10,
          "description": "The time Ansible should wait for a response from the ACME API. This timeout is applied to all HTTP(S) requests (HEAD, GET, POST)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the C(openssl) binary. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "The URL to send the request to. Must be specified if O(method) is not V(directory-only)."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": true,
          "description": "Whether calls to the ACME directory will validate TLS certificates. B(Warning:) Should B(only ever) be set to V(false) for testing purposes, for example when testing against a local Pebble server."
        }
      ]
    },
    {
      "fqcn": "community.crypto.certificate_complete_chain",
      "blockType": "community_crypto_certificate_complete_chain",
      "label": "certificate_complete_chain",
      "tooltip": "Complete certificate chain given a set of untrusted and root certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/certificate_complete_chain_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "input_chain",
          "fieldId": "INPUT_CHAIN",
          "type": "text",
          "label": "input chain",
          "description": "A concatenated set of certificates in PEM format forming a chain. The module will try to complete this chain.",
          "required": true
        },
        {
          "key": "intermediate_certificates",
          "fieldId": "INTERMEDIATE_CERTIFICATES",
          "type": "text",
          "label": "intermediate certificates",
          "yamlType": "list",
          "description": "A list of filenames or directories. A filename is assumed to point to a file containing one or more certificates in PEM format. All certificates in this file will be added to the set of root certificates. If a directory name is given, all files in the directory and its subdirectories will be scanned and tried to be parsed as concatenated certificates in PEM format. Symbolic links will be followed."
        },
        {
          "key": "root_certificates",
          "fieldId": "ROOT_CERTIFICATES",
          "type": "text",
          "label": "root certificates",
          "yamlType": "list",
          "description": "A list of filenames or directories. A filename is assumed to point to a file containing one or more certificates in PEM format. All certificates in this file will be added to the set of root certificates. If a directory name is given, all files in the directory and its subdirectories will be scanned and tried to be parsed as concatenated certificates in PEM format. Symbolic links will be followed.",
          "required": true
        }
      ]
    },
    {
      "fqcn": "community.crypto.get_certificate",
      "blockType": "community_crypto_get_certificate",
      "label": "get_certificate",
      "tooltip": "Get a certificate from a host:port",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/get_certificate_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "asn1_base64",
          "fieldId": "ASN1_BASE64",
          "type": "boolean",
          "label": "asn1 base64",
          "default": true,
          "description": "Whether to encode the ASN.1 values in the RV(extensions) return value with Base64 or not. The documentation claimed for a long time that the values are Base64 encoded, but they never were. For compatibility this option is set to V(false). The default value was changed from V(false) to V(true) incommunity.crypto 3.0.0."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "A PEM file containing one or more root certificates; if present, the cert will be validated against these root certs. Note that this only validates the certificate is signed by the chain; not that the cert is valid for the host presenting it."
        },
        {
          "key": "ciphers",
          "fieldId": "CIPHERS",
          "type": "text",
          "label": "ciphers",
          "yamlType": "list",
          "description": "SSL/TLS Ciphers to use for the request. When a list is provided, all ciphers are joined in order with V(:). See the L(OpenSSL Cipher List Format,https://www.openssl.org/docs/manmaster/man1/openssl-ciphers.html#CIPHER-LIST-FORMAT) for more details. The available ciphers is dependent on the Python and OpenSSL/LibreSSL versions."
        },
        {
          "key": "get_certificate_chain",
          "fieldId": "GET_CERTIFICATE_CHAIN",
          "type": "boolean",
          "label": "get certificate chain",
          "default": false,
          "description": "If set to V(true), will obtain the certificate chain next to the certificate itself. The chain as returned by the server can be found in RV(unverified_chain), and the chain that passed validation in RV(verified_chain). B(Note) that this needs B(Python 3.10 or newer). Also note that only Python 3.13 or newer officially supports this. The module uses internal APIs of Python 3.10, 3.11, and 3.12 to achieve the same. It can be that future versions of Python 3.10, 3.11, or 3.12 break this."
        },
        {
          "key": "proxy_host",
          "fieldId": "PROXY_HOST",
          "type": "text",
          "label": "proxy host",
          "description": "Proxy host used when get a certificate."
        },
        {
          "key": "proxy_port",
          "fieldId": "PROXY_PORT",
          "type": "number",
          "label": "proxy port",
          "default": 8080,
          "description": "Proxy port used when get a certificate."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "server_name",
          "fieldId": "SERVER_NAME",
          "type": "text",
          "label": "server name",
          "description": "Server name used for SNI (L(Server Name Indication,https://en.wikipedia.org/wiki/Server_Name_Indication)) when hostname is an IP or is different from server name."
        },
        {
          "key": "starttls",
          "fieldId": "STARTTLS",
          "type": "dropdown",
          "label": "starttls",
          "description": "Requests a secure connection for protocols which require clients to initiate encryption. Only available for V(mysql) currently.",
          "options": [
            [
              "mysql",
              "mysql"
            ]
          ]
        },
        {
          "key": "tls_ctx_options",
          "fieldId": "TLS_CTX_OPTIONS",
          "type": "text",
          "label": "tls ctx options",
          "yamlType": "list",
          "description": "TLS context options (TLS/SSL OP flags) to use for the request. See the L(List of SSL OP Flags,https://wiki.openssl.org/index.php/List_of_SSL_OP_Flags) for more details. The available TLS context options is dependent on the Python and OpenSSL/LibreSSL versions."
        }
      ]
    },
    {
      "fqcn": "community.crypto.luks_device",
      "blockType": "community_crypto_luks_device",
      "label": "luks_device",
      "tooltip": "Manage encrypted (LUKS) devices",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/luks_device_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "allow_discards",
          "fieldId": "ALLOW_DISCARDS",
          "type": "boolean",
          "label": "allow discards",
          "default": false,
          "description": "Allow discards (also known as TRIM) requests for device. Will only be used when opening containers."
        },
        {
          "key": "cipher",
          "fieldId": "CIPHER",
          "type": "text",
          "label": "cipher",
          "description": "This option allows the user to define the cipher specification string for the LUKS container. Will only be used on container creation. For pre-2.6.10 kernels, use V(aes-plain) as they do not understand the new cipher spec strings. To use ESSIV, use V(aes-cbc-essiv:sha256)."
        },
        {
          "key": "device",
          "fieldId": "DEVICE",
          "type": "text",
          "label": "device",
          "description": "Device to work with (for example V(/dev/sda1)). Needed in most cases. Can be omitted only when O(state=closed) together with O(name) is provided."
        },
        {
          "key": "force_remove_last_key",
          "fieldId": "FORCE_REMOVE_LAST_KEY",
          "type": "boolean",
          "label": "force remove last key",
          "default": false,
          "description": "If set to V(true), allows removing the last key from a container. BEWARE that when the last key has been removed from a container, the container can no longer be opened!"
        },
        {
          "key": "hash",
          "fieldId": "HASH",
          "type": "text",
          "label": "hash",
          "description": "This option allows the user to specify the hash function used in LUKS key setup scheme and volume key digest. Will only be used on container creation."
        },
        {
          "key": "keyfile",
          "fieldId": "KEYFILE",
          "type": "text",
          "label": "keyfile",
          "description": "Used to unlock the container. Either a O(keyfile) or a O(passphrase) is needed for most of the operations. Parameter value is the path to the keyfile with the passphrase. BEWARE that working with keyfiles in plaintext is dangerous. Make sure that they are protected."
        },
        {
          "key": "keysize",
          "fieldId": "KEYSIZE",
          "type": "number",
          "label": "keysize",
          "description": "Sets the key size only if LUKS container does not exist."
        },
        {
          "key": "keyslot",
          "fieldId": "KEYSLOT",
          "type": "number",
          "label": "keyslot",
          "description": "Adds the O(keyfile) or O(passphrase) to a specific keyslot when creating a new container on O(device). Parameter value is the number of the keyslot. Defines the keyslot whose priority will be changed by O(keyslot_priority) B(Note) that a device of O(type=luks1) supports the keyslot numbers V(0)-V(7) and a device of O(type=luks2) supports the keyslot numbers V(0)-V(31). In order to use the keyslots V(8)-V(31) when creating a new container, setting O(type) to V(luks2) is required."
        },
        {
          "key": "keyslot_priority",
          "fieldId": "KEYSLOT_PRIORITY",
          "type": "dropdown",
          "label": "keyslot priority",
          "description": "Sets the keyslot priority for the keyslot specified by O(keyslot). B(Note) that keyslot priority is only supported for LUKS2 containers.",
          "options": [
            [
              "prefer",
              "prefer"
            ],
            [
              "normal",
              "normal"
            ],
            [
              "ignore",
              "ignore"
            ]
          ]
        },
        {
          "key": "label",
          "fieldId": "LABEL",
          "type": "text",
          "label": "label",
          "description": "This option allow the user to create a LUKS2 format container with label support, respectively to identify the container by label on later usages. Will only be used on container creation, or when O(device) is not specified. This cannot be specified if O(type) is set to V(luks1)."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Sets container name when O(state=opened). Can be used instead of O(device) when closing the existing container (that is, when O(state=closed))."
        },
        {
          "key": "new_keyfile",
          "fieldId": "NEW_KEYFILE",
          "type": "text",
          "label": "new keyfile",
          "description": "Adds additional key to given container on O(device). Needs O(keyfile) or O(passphrase) option for authorization. LUKS container supports up to 8 keyslots. Parameter value is the path to the keyfile with the passphrase. NOTE that adding additional keys is idempotent only since community.crypto 1.4.0. For older versions, a new keyslot will be used even if another keyslot already exists for this keyfile. BEWARE that working with keyfiles in plaintext is dangerous. Make sure that they are protected."
        },
        {
          "key": "new_keyslot",
          "fieldId": "NEW_KEYSLOT",
          "type": "number",
          "label": "new keyslot",
          "description": "Adds the additional O(new_keyfile) or O(new_passphrase) to a specific keyslot on the given O(device). Parameter value is the number of the keyslot. B(Note) that a device of O(type=luks1) supports the keyslot numbers V(0)-V(7) and a device of O(type=luks2) supports the keyslot numbers V(0)-V(31)."
        },
        {
          "key": "new_passphrase",
          "fieldId": "NEW_PASSPHRASE",
          "type": "text",
          "label": "new passphrase",
          "description": "Adds additional passphrase to given container on O(device). Needs O(keyfile) or O(passphrase) option for authorization. LUKS container supports up to 8 keyslots. Parameter value is a string with the new passphrase. NOTE that adding additional passphrase is idempotent only since community.crypto 1.4.0. For older versions, a new keyslot will be used even if another keyslot already exists for this passphrase. B(Note) that the passphrase must be UTF-8 encoded text. If you want to use arbitrary binary data, or text using another encoding, use the O(passphrase_encoding) option and provide the passphrase Base64 encoded."
        },
        {
          "key": "new_tpm2",
          "fieldId": "NEW_TPM2",
          "type": "text",
          "label": "new tpm2",
          "description": "Adds a TPM2 security chip to given container on O(device). Expects a device node path referring to the TPM2 chip (e.g. V(/dev/tpmrm0)). Alternatively the special value V(auto) may be specified, in order to automatically determine the device node of a currently discovered TPM2 device (of which there must be exactly one). Requires O(new_tpm2_pcrs). B(Note) that O(new_keyslot) does not affect the keyslot for TPM2 enrollment. B(Note) that only LUKS2 containers are supported. B(Note) that systemd-cryptsetup (v248 or newer) is required."
        },
        {
          "key": "new_tpm2_pcrs",
          "fieldId": "NEW_TPM2_PCRS",
          "type": "text",
          "label": "new tpm2 pcrs",
          "description": "TPM2 PCRs (Platform Configuration Registers) to bind to. See systemd-cryptenroll documentation for details (C(--tpm2-pcrs) argument)."
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "Used to unlock the container. Either a O(passphrase) or a O(keyfile) is needed for most of the operations. Parameter value is a string with the passphrase. B(Note) that the passphrase must be UTF-8 encoded text. If you want to use arbitrary binary data, or text using another encoding, use the O(passphrase_encoding) option and provide the passphrase Base64 encoded."
        },
        {
          "key": "passphrase_encoding",
          "fieldId": "PASSPHRASE_ENCODING",
          "type": "text",
          "label": "passphrase encoding",
          "default": "text",
          "description": "Determine how passphrases are provided to parameters such as O(passphrase), O(new_passphrase), and O(remove_passphrase)."
        },
        {
          "key": "perf_no_read_workqueue",
          "fieldId": "PERF_NO_READ_WORKQUEUE",
          "type": "boolean",
          "label": "perf no read workqueue",
          "default": false,
          "description": "Allows the user to bypass dm-crypt internal workqueue and process read requests synchronously. Will only be used when opening containers."
        },
        {
          "key": "perf_no_write_workqueue",
          "fieldId": "PERF_NO_WRITE_WORKQUEUE",
          "type": "boolean",
          "label": "perf no write workqueue",
          "default": false,
          "description": "Allows the user to bypass dm-crypt internal workqueue and process write requests synchronously. Will only be used when opening containers."
        },
        {
          "key": "perf_same_cpu_crypt",
          "fieldId": "PERF_SAME_CPU_CRYPT",
          "type": "boolean",
          "label": "perf same cpu crypt",
          "default": false,
          "description": "Allows the user to perform encryption using the same CPU that IO was submitted on. The default is to use an unbound workqueue so that encryption work is automatically balanced between available CPUs. Will only be used when opening containers."
        },
        {
          "key": "perf_submit_from_crypt_cpus",
          "fieldId": "PERF_SUBMIT_FROM_CRYPT_CPUS",
          "type": "boolean",
          "label": "perf submit from crypt cpus",
          "default": false,
          "description": "Allows the user to disable offloading writes to a separate thread after encryption. There are some situations where offloading block write IO operations from the encryption threads to a single thread degrades performance significantly. The default is to offload block write IO operations to the same thread. Will only be used when opening containers."
        },
        {
          "key": "persistent",
          "fieldId": "PERSISTENT",
          "type": "boolean",
          "label": "persistent",
          "default": false,
          "description": "Allows the user to store options into container's metadata persistently and automatically use them next time. Only O(perf_same_cpu_crypt), O(perf_submit_from_crypt_cpus), O(perf_no_read_workqueue), O(perf_no_write_workqueue), and O(allow_discards) can be stored persistently. Will only work with LUKS2 containers. Will only be used when opening containers."
        },
        {
          "key": "remove_keyfile",
          "fieldId": "REMOVE_KEYFILE",
          "type": "text",
          "label": "remove keyfile",
          "description": "Removes given key from the container on O(device). Does not remove the keyfile from filesystem. Parameter value is the path to the keyfile with the passphrase. NOTE that removing keys is idempotent only since community.crypto 1.4.0. For older versions, trying to remove a key which no longer exists results in an error. NOTE that to remove the last key from a LUKS container, the O(force_remove_last_key) option must be set to V(true). BEWARE that working with keyfiles in plaintext is dangerous. Make sure that they are protected."
        },
        {
          "key": "remove_keyslot",
          "fieldId": "REMOVE_KEYSLOT",
          "type": "number",
          "label": "remove keyslot",
          "description": "Removes the key in the given slot on O(device). Needs O(keyfile) or O(passphrase) for authorization. B(Note) that a device of O(type=luks1) supports the keyslot numbers V(0)-V(7) and a device of O(type=luks2) supports the keyslot numbers V(0)-V(31). B(Note) that the given O(keyfile) or O(passphrase) must not be in the slot to be removed."
        },
        {
          "key": "remove_passphrase",
          "fieldId": "REMOVE_PASSPHRASE",
          "type": "text",
          "label": "remove passphrase",
          "description": "Removes given passphrase from the container on O(device). Parameter value is a string with the passphrase to remove. NOTE that removing passphrases is idempotent only since community.crypto 1.4.0. For older versions, trying to remove a passphrase which no longer exists results in an error. NOTE that to remove the last keyslot from a LUKS container, the O(force_remove_last_key) option must be set to V(true). B(Note) that the passphrase must be UTF-8 encoded text. If you want to use arbitrary binary data, or text using another encoding, use the O(passphrase_encoding) option and provide the passphrase Base64 encoded."
        },
        {
          "key": "remove_tpm2",
          "fieldId": "REMOVE_TPM2",
          "type": "boolean",
          "label": "remove tpm2",
          "default": false,
          "description": "Removes B(all) key slots on O(device) that are unlocked by a TPM2 device. Needs O(keyfile), O(passphrase), or O(tpm2_device) for authorization. B(Note) that systemd-cryptsetup (v248 or newer) is required. B(Note) that you should avoid using O(tpm2_device) to authorize removal of all TPM2 slots to ensure that you can still access the container afterwards."
        },
        {
          "key": "sector_size",
          "fieldId": "SECTOR_SIZE",
          "type": "number",
          "label": "sector size",
          "description": "This option allows the user to specify the sector size (in bytes) used for LUKS2 containers. Will only be used on container creation."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Desired state of the LUKS container. Based on its value creates, destroys, opens or closes the LUKS container on a given device. V(present) will create LUKS container unless already present. Requires O(device) and either O(keyfile) or O(passphrase) options to be provided. V(absent) will remove existing LUKS container if it exists. Requires O(device) or O(name) to be specified. V(opened) will unlock the LUKS container. Requires O(device) and one of O(keyfile), O(passphrase), O(tpm2_device) to be specified. If the container does not exist it will be created first, however O(tpm2_device) can not be used for creation. Use the O(name) option to set the name of the opened container. Otherwise the name will be generated automatically and returned as a part of the result. V(closed) will lock the LUKS container. However if the container does not exist it will be created. Requires O(device) and either O(keyfile) or O(passphrase) options to be provided. If container does already exist O(device) or O(name) will suffice.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ],
            [
              "opened",
              "opened"
            ],
            [
              "closed",
              "closed"
            ]
          ]
        },
        {
          "key": "tpm2_device",
          "fieldId": "TPM2_DEVICE",
          "type": "text",
          "label": "tpm2 device",
          "description": "Used to unlock the container, but can not be used for container creation. A device node path referring to a TPM2 chip (for example V(/dev/tpmrm0)). Alternatively the special value V(auto) may be specified, in order to automatically determine the device node of a currently discovered TPM2 device (of which there must be exactly one). B(Note) that only LUKS2 containers are supported B(Note) that systemd-cryptsetup (v256 or newer) is required."
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "description": "This option allow the user explicit define the format of LUKS container that wants to work with. Options are V(luks1) or V(luks2).",
          "options": [
            [
              "luks1",
              "luks1"
            ],
            [
              "luks2",
              "luks2"
            ]
          ]
        },
        {
          "key": "uuid",
          "fieldId": "UUID",
          "type": "text",
          "label": "uuid",
          "description": "With this option user can identify the LUKS container by UUID. Will only be used when O(device) and O(label) are not specified."
        }
      ],
      "unmappedParams": [
        "pbkdf (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.openssh_cert",
      "blockType": "community_crypto_openssh_cert",
      "label": "openssh_cert",
      "tooltip": "Generate OpenSSH host or user certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssh_cert_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the certificate be regenerated even if it already exists and is valid. Equivalent to O(regenerate=always)."
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "identifier",
          "fieldId": "IDENTIFIER",
          "type": "text",
          "label": "identifier",
          "description": "Specify the key identity when signing a public key. The identifier that is logged by the server when the certificate is used for authentication."
        },
        {
          "key": "ignore_timestamps",
          "fieldId": "IGNORE_TIMESTAMPS",
          "type": "boolean",
          "label": "ignore timestamps",
          "default": false,
          "description": "Whether the O(valid_from) and O(valid_to) timestamps should be ignored for idempotency checks. However, the values will still be applied to a new certificate if it meets any other necessary conditions for generation/regeneration."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "options",
          "fieldId": "OPTIONS",
          "type": "text",
          "label": "options",
          "yamlType": "list",
          "description": "Specify certificate options when signing a key. The option that are valid for user certificates are: V(clear): Clear all enabled permissions. This is useful for clearing the default set of permissions so permissions may be added individually. V(force-command=command): Forces the execution of command instead of any shell or command specified by the user when the certificate is used for authentication. V(no-agent-forwarding): Disable ssh-agent forwarding (permitted by default). V(no-port-forwarding): Disable port forwarding (permitted by default). V(no-pty): Disable PTY allocation (permitted by default). V(no-user-rc): Disable execution of C(~/.ssh/rc) by sshd (permitted by default). V(no-x11-forwarding): Disable X11 forwarding (permitted by default). V(permit-agent-forwarding): Allows ssh-agent forwarding. V(permit-port-forwarding): Allows port forwarding. V(permit-pty): Allows PTY allocation. V(permit-user-rc): Allows execution of C(~/.ssh/rc) by sshd. V(permit-x11-forwarding): Allows X11 forwarding. V(source-address=address_list): Restrict the source addresses from which the certificate is considered valid. The C(address_list) is a comma-separated list of one or more address/netmask pairs in CIDR format. At present, no options are valid for host keys."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Path of the file containing the certificate.",
          "required": true
        },
        {
          "key": "pkcs11_provider",
          "fieldId": "PKCS11_PROVIDER",
          "type": "text",
          "label": "pkcs11 provider",
          "description": "To use a signing key that resides on a PKCS#11 token, set this to the name (or full path) of the shared library to use with the token. Usually C(libpkcs11.so). If this is set, O(signing_key) needs to point to a file containing the public key of the CA."
        },
        {
          "key": "principals",
          "fieldId": "PRINCIPALS",
          "type": "text",
          "label": "principals",
          "yamlType": "list",
          "description": "Certificates may be limited to be valid for a set of principal (user/host) names. By default, generated certificates are valid for all users or hosts."
        },
        {
          "key": "public_key",
          "fieldId": "PUBLIC_KEY",
          "type": "text",
          "label": "public key",
          "description": "The path to the public key that will be signed with the signing key in order to generate the certificate. Required if O(state) is V(present)."
        },
        {
          "key": "regenerate",
          "fieldId": "REGENERATE",
          "type": "dropdown",
          "label": "regenerate",
          "default": "partial_idempotence",
          "description": "When V(never) the task will fail if a certificate already exists at O(path) and is unreadable otherwise a new certificate will only be generated if there is no existing certificate. When V(fail) the task will fail if a certificate already exists at O(path) and does not match the module's options. When V(partial_idempotence) an existing certificate will be regenerated based on O(serial_number), O(signature_algorithm), O(type), O(valid_from), O(valid_to), O(valid_at), and O(principals). O(valid_from) and O(valid_to) can be excluded by O(ignore_timestamps=true). When V(full_idempotence) O(identifier), O(options), O(public_key), and O(signing_key) are also considered when compared against an existing certificate. V(always) is equivalent to O(force=true).",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "fail",
              "fail"
            ],
            [
              "partial_idempotence",
              "partial_idempotence"
            ],
            [
              "full_idempotence",
              "full_idempotence"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serial_number",
          "fieldId": "SERIAL_NUMBER",
          "type": "number",
          "label": "serial number",
          "description": "Specify the certificate serial number. The serial number is logged by the server when the certificate is used for authentication. The certificate serial number may be used in a KeyRevocationList. The serial number may be omitted for checks, but must be specified again for a new certificate. Note: The default value set by ssh-keygen is 0. This option accepts an B(integer). If you want to provide serial numbers as colon-separated hex strings, such as C(11:22:33), you need to convert them to an integer with P(community.crypto.parse_serial#filter)."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "signature_algorithm",
          "fieldId": "SIGNATURE_ALGORITHM",
          "type": "dropdown",
          "label": "signature algorithm",
          "description": "As of OpenSSH 8.2 the SHA-1 signature algorithm for RSA keys has been disabled and C(ssh) will refuse host certificates signed with the SHA-1 algorithm. OpenSSH 8.1 made V(rsa-sha2-512) the default algorithm when acting as a CA and signing certificates with a RSA key. However, for OpenSSH versions less than 8.1 the SHA-2 signature algorithms, V(rsa-sha2-256) or V(rsa-sha2-512), must be specified using this option if compatibility with newer C(ssh) clients is required. Conversely if hosts using OpenSSH version 8.2 or greater must remain compatible with C(ssh) clients using OpenSSH less than 7.2, then V(ssh-rsa) can be used when generating host certificates (a corresponding change to the sshd_config to add V(ssh-rsa) to the C(CASignatureAlgorithms) keyword is also required). Using any value for this option with a non-RSA O(signing_key) will cause this module to fail. Note: OpenSSH versions prior to 7.2 do not support SHA-2 signature algorithms for RSA keys and OpenSSH versions prior to 7.3 do not support SHA-2 signature algorithms for certificates. See U(https://www.openssh.com/txt/release-8.2) for more information.",
          "options": [
            [
              "ssh-rsa",
              "ssh-rsa"
            ],
            [
              "rsa-sha2-256",
              "rsa-sha2-256"
            ],
            [
              "rsa-sha2-512",
              "rsa-sha2-512"
            ]
          ]
        },
        {
          "key": "signing_key",
          "fieldId": "SIGNING_KEY",
          "type": "text",
          "label": "signing key",
          "description": "The path to the private openssh key that is used for signing the public key in order to generate the certificate. If the private key is on a PKCS#11 token (O(pkcs11_provider)), set this to the path to the public key instead. Required if O(state) is V(present)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the host or user certificate should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "description": "Whether the module should generate a host or a user certificate. Required if O(state) is V(present).",
          "options": [
            [
              "host",
              "host"
            ],
            [
              "user",
              "user"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        },
        {
          "key": "use_agent",
          "fieldId": "USE_AGENT",
          "type": "boolean",
          "label": "use agent",
          "default": false,
          "description": "Should the ssh-keygen use a CA key residing in a ssh-agent."
        },
        {
          "key": "valid_at",
          "fieldId": "VALID_AT",
          "type": "text",
          "label": "valid at",
          "description": "Check if the certificate is valid at a certain point in time. If it is not the certificate will be regenerated. Time will always be interpreted as UTC. Mainly to be used with relative timespec for O(valid_from) and / or O(valid_to). Note that if using relative time this module is NOT idempotent."
        },
        {
          "key": "valid_from",
          "fieldId": "VALID_FROM",
          "type": "text",
          "label": "valid from",
          "description": "The point in time the certificate is valid from. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid formats are: C([+-]timespec | YYYY-MM-DD | YYYY-MM-DDTHH:MM:SS | YYYY-MM-DD HH:MM:SS | always) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). Note that if using relative time this module is NOT idempotent. The value V(always) is only supported for OpenSSH 7.7 and greater, however, the value V(1970-01-01T00:00:01) can be used with earlier versions as an equivalent expression. To ignore this value during comparison with an existing certificate set O(ignore_timestamps=true). Required if O(state) is V(present)."
        },
        {
          "key": "valid_to",
          "fieldId": "VALID_TO",
          "type": "text",
          "label": "valid to",
          "description": "The point in time the certificate is valid to. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid formats are: C([+-]timespec | YYYY-MM-DD | YYYY-MM-DDTHH:MM:SS | YYYY-MM-DD HH:MM:SS | forever) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). Note that if using relative time this module is NOT idempotent. To ignore this value during comparison with an existing certificate set O(ignore_timestamps=true). Required if O(state) is V(present)."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssh_keypair",
      "blockType": "community_crypto_openssh_keypair",
      "label": "openssh_keypair",
      "tooltip": "Generate OpenSSH private and public keys",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssh_keypair_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backend",
          "fieldId": "BACKEND",
          "type": "dropdown",
          "label": "backend",
          "default": "auto",
          "description": "Selects between the V(cryptography) library or the OpenSSH binary V(opensshbin). V(auto) will default to V(opensshbin) unless the OpenSSH binary is not installed or when using O(passphrase).",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "opensshbin",
              "opensshbin"
            ]
          ]
        },
        {
          "key": "comment",
          "fieldId": "COMMENT",
          "type": "text",
          "label": "comment",
          "description": "Provides a new comment to the public key."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the key be regenerated even if it already exists."
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "Passphrase used to decrypt an existing private key or encrypt a newly generated private key. Passphrases are not supported for O(type=rsa1). Can only be used when O(backend=cryptography), or when O(backend=auto) and a required C(cryptography) version is installed. Note that depending on the cryptography version, the C(bcrypt) package is also needed."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Name of the files containing the public and private key. The file containing the public key will have the extension C(.pub).",
          "required": true
        },
        {
          "key": "private_key_format",
          "fieldId": "PRIVATE_KEY_FORMAT",
          "type": "dropdown",
          "label": "private key format",
          "default": "auto",
          "description": "Used when O(backend=cryptography) to select a format for the private key at the provided O(path). When set to V(auto) this module will match the key format of the installed OpenSSH version. For OpenSSH < 7.8 private keys will be in PKCS1 format except ed25519 keys which will be in OpenSSH format. For OpenSSH >= 7.8 all private key types will be in the OpenSSH format. Using this option when O(regenerate=partial_idempotence) or O(regenerate=full_idempotence) will cause a new keypair to be generated if the private key's format does not match the value of O(private_key_format). This module will not however convert existing private keys between formats.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "pkcs1",
              "pkcs1"
            ],
            [
              "pkcs8",
              "pkcs8"
            ],
            [
              "ssh",
              "ssh"
            ]
          ]
        },
        {
          "key": "regenerate",
          "fieldId": "REGENERATE",
          "type": "dropdown",
          "label": "regenerate",
          "default": "partial_idempotence",
          "description": "Allows to configure in which situations the module is allowed to regenerate private keys. The module will always generate a new key if the destination file does not exist. By default, the key will be regenerated when it does not match the module's options, except when the key cannot be read or the passphrase does not match. Please note that this B(changed) for Ansible 2.10. For Ansible 2.9, the behavior was as if V(full_idempotence) is specified. If set to V(never), the module will fail if the key cannot be read or the passphrase is not matching, and will never regenerate an existing key. If set to V(fail), the module will fail if the key does not correspond to the module's options. If set to V(partial_idempotence), the key will be regenerated if it does not conform to the module's options. The key is B(not) regenerated if it cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. If set to V(full_idempotence), the key will be regenerated if it does not conform to the module's options. This is also the case if the key cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. Make sure you have a B(backup) when using this option! If set to V(always), the module will always regenerate the key. This is equivalent to setting O(force) to V(true). Note that adjusting the comment and the permissions can be changed without regeneration. Therefore, even for V(never), the task can result in changed.",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "fail",
              "fail"
            ],
            [
              "partial_idempotence",
              "partial_idempotence"
            ],
            [
              "full_idempotence",
              "full_idempotence"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "description": "Specifies the number of bits in the private key to create. For RSA keys, the minimum size is 1024 bits and the default is 4096 bits. Generally, 2048 bits is considered sufficient. DSA keys must be exactly 1024 bits as specified by FIPS 186-2. For ECDSA keys, size determines the key length by selecting from one of three elliptic curve sizes: 256, 384 or 521 bits. Attempting to use bit lengths other than these three values for ECDSA keys will cause this module to fail. Ed25519 keys have a fixed length and the size will be ignored."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the private and public keys should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "default": "rsa",
          "description": "The algorithm used to generate the SSH private key. V(rsa1) is for protocol version 1. V(rsa1) is deprecated and may not be supported by every version of ssh-keygen.",
          "options": [
            [
              "rsa",
              "rsa"
            ],
            [
              "dsa",
              "dsa"
            ],
            [
              "rsa1",
              "rsa1"
            ],
            [
              "ecdsa",
              "ecdsa"
            ],
            [
              "ed25519",
              "ed25519"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_csr",
      "blockType": "community_crypto_openssl_csr",
      "label": "openssl_csr",
      "tooltip": "Generate OpenSSL Certificate Signing Request (CSR)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_csr_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "authority_cert_issuer",
          "fieldId": "AUTHORITY_CERT_ISSUER",
          "type": "text",
          "label": "authority cert issuer",
          "yamlType": "list",
          "description": "Names that will be present in the authority cert issuer field of the certificate signing request. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA). Example: V(DNS:ca.example.org). If specified, O(authority_cert_serial_number) must also be specified. Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified."
        },
        {
          "key": "authority_cert_serial_number",
          "fieldId": "AUTHORITY_CERT_SERIAL_NUMBER",
          "type": "number",
          "label": "authority cert serial number",
          "description": "The authority cert serial number. If specified, O(authority_cert_issuer) must also be specified. Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified. This option accepts an B(integer). If you want to provide serial numbers as colon-separated hex strings, such as C(11:22:33), you need to convert them to an integer with P(community.crypto.parse_serial#filter)."
        },
        {
          "key": "authority_key_identifier",
          "fieldId": "AUTHORITY_KEY_IDENTIFIER",
          "type": "text",
          "label": "authority key identifier",
          "description": "The authority key identifier as a hex string, where two bytes are separated by colons. Example: V(00:11:22:33:44:55:66:77:88:99:aa:bb:cc:dd:ee:ff:00:11:22:33). Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified."
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original CSR back if you overwrote it with a new one by accident."
        },
        {
          "key": "basic_constraints",
          "fieldId": "BASIC_CONSTRAINTS",
          "type": "text",
          "label": "basic constraints",
          "yamlType": "list",
          "description": "Indicates basic constraints, such as if the certificate is a CA.",
          "aliases": [
            "basicConstraints"
          ]
        },
        {
          "key": "basic_constraints_critical",
          "fieldId": "BASIC_CONSTRAINTS_CRITICAL",
          "type": "boolean",
          "label": "basic constraints critical",
          "default": false,
          "description": "Should the basicConstraints extension be considered as critical.",
          "aliases": [
            "basicConstraints_critical"
          ]
        },
        {
          "key": "common_name",
          "fieldId": "COMMON_NAME",
          "type": "text",
          "label": "common name",
          "description": "The commonName field of the certificate signing request subject.",
          "aliases": [
            "CN",
            "commonName"
          ]
        },
        {
          "key": "country_name",
          "fieldId": "COUNTRY_NAME",
          "type": "text",
          "label": "country name",
          "description": "The countryName field of the certificate signing request subject.",
          "aliases": [
            "C",
            "countryName"
          ]
        },
        {
          "key": "create_subject_key_identifier",
          "fieldId": "CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "boolean",
          "label": "create subject key identifier",
          "default": false,
          "description": "Create the Subject Key Identifier from the public key. Please note that commercial CAs can ignore the value, respectively use a value of their own choice instead. Specifying this option is mostly useful for self-signed certificates or for own CAs."
        },
        {
          "key": "digest",
          "fieldId": "DIGEST",
          "type": "text",
          "label": "digest",
          "default": "sha256",
          "description": "The digest used when signing the certificate signing request with the private key."
        },
        {
          "key": "email_address",
          "fieldId": "EMAIL_ADDRESS",
          "type": "text",
          "label": "email address",
          "description": "The emailAddress field of the certificate signing request subject.",
          "aliases": [
            "E",
            "emailAddress"
          ]
        },
        {
          "key": "extended_key_usage",
          "fieldId": "EXTENDED_KEY_USAGE",
          "type": "text",
          "label": "extended key usage",
          "yamlType": "list",
          "description": "Additional restrictions (for example client authentication, server authentication) on the allowed purposes for which the public key may be used.",
          "aliases": [
            "extKeyUsage",
            "extendedKeyUsage"
          ]
        },
        {
          "key": "extended_key_usage_critical",
          "fieldId": "EXTENDED_KEY_USAGE_CRITICAL",
          "type": "boolean",
          "label": "extended key usage critical",
          "default": false,
          "description": "Should the extkeyUsage extension be considered as critical.",
          "aliases": [
            "extKeyUsage_critical",
            "extendedKeyUsage_critical"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the certificate signing request be forced regenerated by this ansible module."
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "key_usage",
          "fieldId": "KEY_USAGE",
          "type": "text",
          "label": "key usage",
          "yamlType": "list",
          "description": "This defines the purpose (for example encipherment, signature, certificate signing) of the key contained in the certificate.",
          "aliases": [
            "keyUsage"
          ]
        },
        {
          "key": "key_usage_critical",
          "fieldId": "KEY_USAGE_CRITICAL",
          "type": "boolean",
          "label": "key usage critical",
          "default": false,
          "description": "Should the keyUsage extension be considered as critical.",
          "aliases": [
            "keyUsage_critical"
          ]
        },
        {
          "key": "locality_name",
          "fieldId": "LOCALITY_NAME",
          "type": "text",
          "label": "locality name",
          "description": "The localityName field of the certificate signing request subject.",
          "aliases": [
            "L",
            "localityName"
          ]
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "name_constraints_critical",
          "fieldId": "NAME_CONSTRAINTS_CRITICAL",
          "type": "boolean",
          "label": "name constraints critical",
          "default": false,
          "description": "Should the Name Constraints extension be considered as critical."
        },
        {
          "key": "name_constraints_excluded",
          "fieldId": "NAME_CONSTRAINTS_EXCLUDED",
          "type": "text",
          "label": "name constraints excluded",
          "yamlType": "list",
          "description": "For CA certificates, this specifies a list of identifiers which describe subtrees of names that this CA is B(not) allowed to issue certificates for. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA)."
        },
        {
          "key": "name_constraints_permitted",
          "fieldId": "NAME_CONSTRAINTS_PERMITTED",
          "type": "text",
          "label": "name constraints permitted",
          "yamlType": "list",
          "description": "For CA certificates, this specifies a list of identifiers which describe subtrees of names that this CA is allowed to issue certificates for. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA)."
        },
        {
          "key": "ocsp_must_staple",
          "fieldId": "OCSP_MUST_STAPLE",
          "type": "boolean",
          "label": "ocsp must staple",
          "default": false,
          "description": "Indicates that the certificate should contain the OCSP Must Staple extension (U(https://tools.ietf.org/html/rfc7633)).",
          "aliases": [
            "ocspMustStaple"
          ]
        },
        {
          "key": "ocsp_must_staple_critical",
          "fieldId": "OCSP_MUST_STAPLE_CRITICAL",
          "type": "boolean",
          "label": "ocsp must staple critical",
          "default": false,
          "description": "Should the OCSP Must Staple extension be considered as critical. Note that according to the RFC, this extension should not be marked as critical, as old clients not knowing about OCSP Must Staple are required to reject such certificates (see U(https://tools.ietf.org/html/rfc7633#section-4)).",
          "aliases": [
            "ocspMustStaple_critical"
          ]
        },
        {
          "key": "organization_name",
          "fieldId": "ORGANIZATION_NAME",
          "type": "text",
          "label": "organization name",
          "description": "The organizationName field of the certificate signing request subject.",
          "aliases": [
            "O",
            "organizationName"
          ]
        },
        {
          "key": "organizational_unit_name",
          "fieldId": "ORGANIZATIONAL_UNIT_NAME",
          "type": "text",
          "label": "organizational unit name",
          "description": "The organizationalUnitName field of the certificate signing request subject.",
          "aliases": [
            "OU",
            "organizationalUnitName"
          ]
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The name of the file into which the generated OpenSSL certificate signing request will be written.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "The content of the private key to use when signing the certificate signing request. Either O(privatekey_path) or O(privatekey_content) must be specified if O(state) is V(present), but not both."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the private key. This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "The path to the private key to use when signing the certificate signing request. Either O(privatekey_path) or O(privatekey_content) must be specified if O(state) is V(present), but not both."
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) CSR's content as RV(csr)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the certificate signing request should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "state_or_province_name",
          "fieldId": "STATE_OR_PROVINCE_NAME",
          "type": "text",
          "label": "state or province name",
          "description": "The stateOrProvinceName field of the certificate signing request subject.",
          "aliases": [
            "ST",
            "stateOrProvinceName"
          ]
        },
        {
          "key": "subject",
          "fieldId": "SUBJECT",
          "type": "text",
          "label": "subject",
          "yamlType": "dict",
          "description": "Key/value pairs that will be present in the subject name field of the certificate signing request. If you need to specify more than one value with the same key, use a list as value. If the order of the components is important, use O(subject_ordered). Mutually exclusive with O(subject_ordered)."
        },
        {
          "key": "subject_alt_name",
          "fieldId": "SUBJECT_ALT_NAME",
          "type": "text",
          "label": "subject alt name",
          "yamlType": "list",
          "description": "Subject Alternative Name (SAN) extension to attach to the certificate signing request. Values must be prefixed by their options. (These are C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA). Note that if no SAN is specified, but a common name, the common name will be added as a SAN except if O(use_common_name_for_san) is set to V(false). More at U(https://tools.ietf.org/html/rfc5280#section-4.2.1.6).",
          "aliases": [
            "subjectAltName"
          ]
        },
        {
          "key": "subject_alt_name_critical",
          "fieldId": "SUBJECT_ALT_NAME_CRITICAL",
          "type": "boolean",
          "label": "subject alt name critical",
          "default": false,
          "description": "Should the subjectAltName extension be considered as critical.",
          "aliases": [
            "subjectAltName_critical"
          ]
        },
        {
          "key": "subject_key_identifier",
          "fieldId": "SUBJECT_KEY_IDENTIFIER",
          "type": "text",
          "label": "subject key identifier",
          "description": "The subject key identifier as a hex string, where two bytes are separated by colons. Example: V(00:11:22:33:44:55:66:77:88:99:aa:bb:cc:dd:ee:ff:00:11:22:33). Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. Note that this option can only be used if O(create_subject_key_identifier) is V(false)."
        },
        {
          "key": "subject_ordered",
          "fieldId": "SUBJECT_ORDERED",
          "type": "text",
          "label": "subject ordered",
          "yamlType": "list",
          "description": "A list of dictionaries, where every dictionary must contain one key/value pair. This key/value pair will be present in the subject name field of the certificate signing request. If you want to specify more than one value with the same key in a row, you can use a list as value. Mutually exclusive with O(subject), and any other subject field option, such as O(country_name), O(state_or_province_name), O(locality_name), O(organization_name), O(organizational_unit_name), O(common_name), or O(email_address)."
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        },
        {
          "key": "use_common_name_for_san",
          "fieldId": "USE_COMMON_NAME_FOR_SAN",
          "type": "boolean",
          "label": "use common name for san",
          "default": true,
          "description": "If set to V(true), the module will fill the common name in for O(subject_alt_name) with C(DNS:) prefix if no SAN is specified.",
          "aliases": [
            "useCommonNameForSAN"
          ]
        },
        {
          "key": "version",
          "fieldId": "VERSION",
          "type": "dropdown",
          "label": "version",
          "default": 1,
          "description": "The version of the certificate signing request. The only allowed value according to L(RFC 2986,https://tools.ietf.org/html/rfc2986#section-4.1) is 1. This option no longer accepts unsupported values since community.crypto 2.0.0.",
          "options": [
            [
              "1",
              "1"
            ]
          ]
        }
      ],
      "unmappedParams": [
        "crl_distribution_points (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.openssl_csr_info",
      "blockType": "community_crypto_openssl_csr_info",
      "label": "openssl_csr_info",
      "tooltip": "Provide information of OpenSSL Certificate Signing Requests (CSR)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_csr_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "Content of the CSR file. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "name_encoding",
          "fieldId": "NAME_ENCODING",
          "type": "dropdown",
          "label": "name encoding",
          "default": "ignore",
          "description": "How to encode names (DNS names, URIs, email addresses) in return values. V(ignore) will use the encoding returned by the backend. V(idna) will convert all labels of domain names to IDNA encoding. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 encoding fails. V(unicode) will convert all labels of domain names to Unicode. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 decoding fails. B(Note) that V(idna) and V(unicode) require the L(idna Python library,https://pypi.org/project/idna/) to be installed.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "idna",
              "idna"
            ],
            [
              "unicode",
              "unicode"
            ]
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the CSR file is loaded from. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_csr_pipe",
      "blockType": "community_crypto_openssl_csr_pipe",
      "label": "openssl_csr_pipe",
      "tooltip": "Generate OpenSSL Certificate Signing Request (CSR)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_csr_pipe_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "authority_cert_issuer",
          "fieldId": "AUTHORITY_CERT_ISSUER",
          "type": "text",
          "label": "authority cert issuer",
          "yamlType": "list",
          "description": "Names that will be present in the authority cert issuer field of the certificate signing request. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA). Example: V(DNS:ca.example.org). If specified, O(authority_cert_serial_number) must also be specified. Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified."
        },
        {
          "key": "authority_cert_serial_number",
          "fieldId": "AUTHORITY_CERT_SERIAL_NUMBER",
          "type": "number",
          "label": "authority cert serial number",
          "description": "The authority cert serial number. If specified, O(authority_cert_issuer) must also be specified. Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified. This option accepts an B(integer). If you want to provide serial numbers as colon-separated hex strings, such as C(11:22:33), you need to convert them to an integer with P(community.crypto.parse_serial#filter)."
        },
        {
          "key": "authority_key_identifier",
          "fieldId": "AUTHORITY_KEY_IDENTIFIER",
          "type": "text",
          "label": "authority key identifier",
          "description": "The authority key identifier as a hex string, where two bytes are separated by colons. Example: V(00:11:22:33:44:55:66:77:88:99:aa:bb:cc:dd:ee:ff:00:11:22:33). Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. The C(AuthorityKeyIdentifier) extension will only be added if at least one of O(authority_key_identifier), O(authority_cert_issuer) and O(authority_cert_serial_number) is specified."
        },
        {
          "key": "basic_constraints",
          "fieldId": "BASIC_CONSTRAINTS",
          "type": "text",
          "label": "basic constraints",
          "yamlType": "list",
          "description": "Indicates basic constraints, such as if the certificate is a CA.",
          "aliases": [
            "basicConstraints"
          ]
        },
        {
          "key": "basic_constraints_critical",
          "fieldId": "BASIC_CONSTRAINTS_CRITICAL",
          "type": "boolean",
          "label": "basic constraints critical",
          "default": false,
          "description": "Should the basicConstraints extension be considered as critical.",
          "aliases": [
            "basicConstraints_critical"
          ]
        },
        {
          "key": "common_name",
          "fieldId": "COMMON_NAME",
          "type": "text",
          "label": "common name",
          "description": "The commonName field of the certificate signing request subject.",
          "aliases": [
            "CN",
            "commonName"
          ]
        },
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "The existing CSR."
        },
        {
          "key": "country_name",
          "fieldId": "COUNTRY_NAME",
          "type": "text",
          "label": "country name",
          "description": "The countryName field of the certificate signing request subject.",
          "aliases": [
            "C",
            "countryName"
          ]
        },
        {
          "key": "create_subject_key_identifier",
          "fieldId": "CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "boolean",
          "label": "create subject key identifier",
          "default": false,
          "description": "Create the Subject Key Identifier from the public key. Please note that commercial CAs can ignore the value, respectively use a value of their own choice instead. Specifying this option is mostly useful for self-signed certificates or for own CAs."
        },
        {
          "key": "digest",
          "fieldId": "DIGEST",
          "type": "text",
          "label": "digest",
          "default": "sha256",
          "description": "The digest used when signing the certificate signing request with the private key."
        },
        {
          "key": "email_address",
          "fieldId": "EMAIL_ADDRESS",
          "type": "text",
          "label": "email address",
          "description": "The emailAddress field of the certificate signing request subject.",
          "aliases": [
            "E",
            "emailAddress"
          ]
        },
        {
          "key": "extended_key_usage",
          "fieldId": "EXTENDED_KEY_USAGE",
          "type": "text",
          "label": "extended key usage",
          "yamlType": "list",
          "description": "Additional restrictions (for example client authentication, server authentication) on the allowed purposes for which the public key may be used.",
          "aliases": [
            "extKeyUsage",
            "extendedKeyUsage"
          ]
        },
        {
          "key": "extended_key_usage_critical",
          "fieldId": "EXTENDED_KEY_USAGE_CRITICAL",
          "type": "boolean",
          "label": "extended key usage critical",
          "default": false,
          "description": "Should the extkeyUsage extension be considered as critical.",
          "aliases": [
            "extKeyUsage_critical",
            "extendedKeyUsage_critical"
          ]
        },
        {
          "key": "key_usage",
          "fieldId": "KEY_USAGE",
          "type": "text",
          "label": "key usage",
          "yamlType": "list",
          "description": "This defines the purpose (for example encipherment, signature, certificate signing) of the key contained in the certificate.",
          "aliases": [
            "keyUsage"
          ]
        },
        {
          "key": "key_usage_critical",
          "fieldId": "KEY_USAGE_CRITICAL",
          "type": "boolean",
          "label": "key usage critical",
          "default": false,
          "description": "Should the keyUsage extension be considered as critical.",
          "aliases": [
            "keyUsage_critical"
          ]
        },
        {
          "key": "locality_name",
          "fieldId": "LOCALITY_NAME",
          "type": "text",
          "label": "locality name",
          "description": "The localityName field of the certificate signing request subject.",
          "aliases": [
            "L",
            "localityName"
          ]
        },
        {
          "key": "name_constraints_critical",
          "fieldId": "NAME_CONSTRAINTS_CRITICAL",
          "type": "boolean",
          "label": "name constraints critical",
          "default": false,
          "description": "Should the Name Constraints extension be considered as critical."
        },
        {
          "key": "name_constraints_excluded",
          "fieldId": "NAME_CONSTRAINTS_EXCLUDED",
          "type": "text",
          "label": "name constraints excluded",
          "yamlType": "list",
          "description": "For CA certificates, this specifies a list of identifiers which describe subtrees of names that this CA is B(not) allowed to issue certificates for. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA)."
        },
        {
          "key": "name_constraints_permitted",
          "fieldId": "NAME_CONSTRAINTS_PERMITTED",
          "type": "text",
          "label": "name constraints permitted",
          "yamlType": "list",
          "description": "For CA certificates, this specifies a list of identifiers which describe subtrees of names that this CA is allowed to issue certificates for. Values must be prefixed by their options. (That is, C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA)."
        },
        {
          "key": "ocsp_must_staple",
          "fieldId": "OCSP_MUST_STAPLE",
          "type": "boolean",
          "label": "ocsp must staple",
          "default": false,
          "description": "Indicates that the certificate should contain the OCSP Must Staple extension (U(https://tools.ietf.org/html/rfc7633)).",
          "aliases": [
            "ocspMustStaple"
          ]
        },
        {
          "key": "ocsp_must_staple_critical",
          "fieldId": "OCSP_MUST_STAPLE_CRITICAL",
          "type": "boolean",
          "label": "ocsp must staple critical",
          "default": false,
          "description": "Should the OCSP Must Staple extension be considered as critical. Note that according to the RFC, this extension should not be marked as critical, as old clients not knowing about OCSP Must Staple are required to reject such certificates (see U(https://tools.ietf.org/html/rfc7633#section-4)).",
          "aliases": [
            "ocspMustStaple_critical"
          ]
        },
        {
          "key": "organization_name",
          "fieldId": "ORGANIZATION_NAME",
          "type": "text",
          "label": "organization name",
          "description": "The organizationName field of the certificate signing request subject.",
          "aliases": [
            "O",
            "organizationName"
          ]
        },
        {
          "key": "organizational_unit_name",
          "fieldId": "ORGANIZATIONAL_UNIT_NAME",
          "type": "text",
          "label": "organizational unit name",
          "description": "The organizationalUnitName field of the certificate signing request subject.",
          "aliases": [
            "OU",
            "organizationalUnitName"
          ]
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "The content of the private key to use when signing the certificate signing request. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the private key. This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "The path to the private key to use when signing the certificate signing request. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "state_or_province_name",
          "fieldId": "STATE_OR_PROVINCE_NAME",
          "type": "text",
          "label": "state or province name",
          "description": "The stateOrProvinceName field of the certificate signing request subject.",
          "aliases": [
            "ST",
            "stateOrProvinceName"
          ]
        },
        {
          "key": "subject",
          "fieldId": "SUBJECT",
          "type": "text",
          "label": "subject",
          "yamlType": "dict",
          "description": "Key/value pairs that will be present in the subject name field of the certificate signing request. If you need to specify more than one value with the same key, use a list as value. If the order of the components is important, use O(subject_ordered). Mutually exclusive with O(subject_ordered)."
        },
        {
          "key": "subject_alt_name",
          "fieldId": "SUBJECT_ALT_NAME",
          "type": "text",
          "label": "subject alt name",
          "yamlType": "list",
          "description": "Subject Alternative Name (SAN) extension to attach to the certificate signing request. Values must be prefixed by their options. (These are C(email), C(URI), C(DNS), C(RID), C(IP), C(dirName), C(otherName), and the ones specific to your CA). Note that if no SAN is specified, but a common name, the common name will be added as a SAN except if O(use_common_name_for_san) is set to V(false). More at U(https://tools.ietf.org/html/rfc5280#section-4.2.1.6).",
          "aliases": [
            "subjectAltName"
          ]
        },
        {
          "key": "subject_alt_name_critical",
          "fieldId": "SUBJECT_ALT_NAME_CRITICAL",
          "type": "boolean",
          "label": "subject alt name critical",
          "default": false,
          "description": "Should the subjectAltName extension be considered as critical.",
          "aliases": [
            "subjectAltName_critical"
          ]
        },
        {
          "key": "subject_key_identifier",
          "fieldId": "SUBJECT_KEY_IDENTIFIER",
          "type": "text",
          "label": "subject key identifier",
          "description": "The subject key identifier as a hex string, where two bytes are separated by colons. Example: V(00:11:22:33:44:55:66:77:88:99:aa:bb:cc:dd:ee:ff:00:11:22:33). Please note that commercial CAs ignore this value, respectively use a value of their own choice. Specifying this option is mostly useful for self-signed certificates or for own CAs. Note that this option can only be used if O(create_subject_key_identifier) is V(false)."
        },
        {
          "key": "subject_ordered",
          "fieldId": "SUBJECT_ORDERED",
          "type": "text",
          "label": "subject ordered",
          "yamlType": "list",
          "description": "A list of dictionaries, where every dictionary must contain one key/value pair. This key/value pair will be present in the subject name field of the certificate signing request. If you want to specify more than one value with the same key in a row, you can use a list as value. Mutually exclusive with O(subject), and any other subject field option, such as O(country_name), O(state_or_province_name), O(locality_name), O(organization_name), O(organizational_unit_name), O(common_name), or O(email_address)."
        },
        {
          "key": "use_common_name_for_san",
          "fieldId": "USE_COMMON_NAME_FOR_SAN",
          "type": "boolean",
          "label": "use common name for san",
          "default": true,
          "description": "If set to V(true), the module will fill the common name in for O(subject_alt_name) with C(DNS:) prefix if no SAN is specified.",
          "aliases": [
            "useCommonNameForSAN"
          ]
        },
        {
          "key": "version",
          "fieldId": "VERSION",
          "type": "dropdown",
          "label": "version",
          "default": 1,
          "description": "The version of the certificate signing request. The only allowed value according to L(RFC 2986,https://tools.ietf.org/html/rfc2986#section-4.1) is 1. This option no longer accepts unsupported values since community.crypto 2.0.0.",
          "options": [
            [
              "1",
              "1"
            ]
          ]
        }
      ],
      "unmappedParams": [
        "crl_distribution_points (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.openssl_dhparam",
      "blockType": "community_crypto_openssl_dhparam",
      "label": "openssl_dhparam",
      "tooltip": "Generate OpenSSL Diffie-Hellman Parameters",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_dhparam_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original DH params back if you overwrote them with new ones by accident."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the parameters be regenerated even it it already exists."
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Name of the file in which the generated parameters will be saved.",
          "required": true
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) DH parameter's content as RV(dhparams)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available, and falls back to C(openssl). If set to V(openssl), will try to use the OpenSSL C(openssl) executable. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ],
            [
              "openssl",
              "openssl"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "default": 4096,
          "description": "Size (in bits) of the generated DH-params."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the parameters should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_pkcs12",
      "blockType": "community_crypto_openssl_pkcs12",
      "label": "openssl_pkcs12",
      "tooltip": "Generate OpenSSL PKCS#12 archive",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_pkcs12_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "action",
          "fieldId": "ACTION",
          "type": "dropdown",
          "label": "action",
          "default": "export",
          "description": "V(export) or V(parse) a PKCS#12.",
          "options": [
            [
              "export",
              "export"
            ],
            [
              "parse",
              "parse"
            ]
          ]
        },
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original output file back if you overwrote it with a new one by accident."
        },
        {
          "key": "certificate_content",
          "fieldId": "CERTIFICATE_CONTENT",
          "type": "text",
          "label": "certificate content",
          "description": "Content of the certificate file in PEM format. Mutually exclusive with O(certificate_path)."
        },
        {
          "key": "certificate_path",
          "fieldId": "CERTIFICATE_PATH",
          "type": "text",
          "label": "certificate path",
          "description": "The path to read certificates and private keys from. Must be in PEM format. Mutually exclusive with O(certificate_content)."
        },
        {
          "key": "encryption_level",
          "fieldId": "ENCRYPTION_LEVEL",
          "type": "dropdown",
          "label": "encryption level",
          "default": "auto",
          "description": "Determines the encryption level used. V(auto) uses the default of the selected backend. For C(cryptography), this is what the cryptography library's specific version considers the best available encryption. V(compatibility2022) uses compatibility settings for older software in 2022. This is only supported by the C(cryptography) backend if cryptography >= 38.0.0 is available. B(Note) that this option is B(not used for idempotency).",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "compatibility2022",
              "compatibility2022"
            ]
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the file be regenerated even if it already exists."
        },
        {
          "key": "friendly_name",
          "fieldId": "FRIENDLY_NAME",
          "type": "text",
          "label": "friendly name",
          "description": "Specifies the friendly name for the certificate and private key.",
          "aliases": [
            "name"
          ]
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "iter_size",
          "fieldId": "ITER_SIZE",
          "type": "number",
          "label": "iter size",
          "description": "Number of times to repeat the encryption step. This is B(not considered during idempotency checks). This is only used when O(encryption_level=compatibility2022). When using it, the default is V(50000)."
        },
        {
          "key": "maciter_size",
          "fieldId": "MACITER_SIZE",
          "type": "number",
          "label": "maciter size",
          "description": "Number of times to repeat the MAC step. This is B(not considered during idempotency checks). This value is B(not used). It is deprecated and will be removed from community.crypto 4.0.0."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "other_certificates",
          "fieldId": "OTHER_CERTIFICATES",
          "type": "text",
          "label": "other certificates",
          "yamlType": "list",
          "description": "List of other certificates to include. Pre Ansible 2.8 this parameter was called O(ca_certificates). Assumes there is one PEM-encoded certificate per file. If a file contains multiple PEM certificates, set O(other_certificates_parse_all) to V(true). Mutually exclusive with O(other_certificates_content).",
          "aliases": [
            "ca_certificates"
          ]
        },
        {
          "key": "other_certificates_content",
          "fieldId": "OTHER_CERTIFICATES_CONTENT",
          "type": "text",
          "label": "other certificates content",
          "yamlType": "list",
          "description": "List of other certificates to include. Assumes there is one PEM-encoded certificate per item. If an item contains multiple PEM certificates, set O(other_certificates_parse_all) Mutually exclusive with O(other_certificates)."
        },
        {
          "key": "other_certificates_parse_all",
          "fieldId": "OTHER_CERTIFICATES_PARSE_ALL",
          "type": "boolean",
          "label": "other certificates parse all",
          "default": false,
          "description": "If set to V(true), assumes that the files mentioned in O(other_certificates)/O(other_certificates_content) can contain more than one certificate per file/item (or even none per file/item)."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "The PKCS#12 password. B(Note:) PKCS12 encryption is typically not secure and should not be used as a security mechanism. If you need to store or send a PKCS12 file safely, you should additionally encrypt it with something else. (L(Source, https://cryptography.io/en/latest/hazmat/primitives/asymmetric/serialization/#cryptography.hazmat.primitives.serialization.pkcs12.serialize_key_and_certificates))."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Filename to write the PKCS#12 file to.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "Content of the private key file. Mutually exclusive with O(privatekey_path)."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "Passphrase source to decrypt any input private keys with."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "File to read private key from. Mutually exclusive with O(privatekey_content)."
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) PKCS#12's content as RV(pkcs12)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. The value V(pyopenssl) has been removed for community.crypto 3.0.0. Note that with community.crypto 3.0.0, all remaining values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "src",
          "fieldId": "SRC",
          "type": "text",
          "label": "src",
          "description": "PKCS#12 file path to parse."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the file should exist or not. All parameters except O(path) are ignored when state is V(absent).",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_privatekey",
      "blockType": "community_crypto_openssl_privatekey",
      "label": "openssl_privatekey",
      "tooltip": "Generate OpenSSL private keys",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_privatekey_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original private key back if you overwrote it with a new one by accident."
        },
        {
          "key": "cipher",
          "fieldId": "CIPHER",
          "type": "text",
          "label": "cipher",
          "default": "auto",
          "description": "The cipher to encrypt the private key. This is only used when O(passphrase) is provided. Must be V(auto)."
        },
        {
          "key": "curve",
          "fieldId": "CURVE",
          "type": "dropdown",
          "label": "curve",
          "description": "Note that not all curves are supported by all versions of C(cryptography). For maximal interoperability, V(secp384r1) or V(secp256r1) should be used. We use the curve names as defined in the L(IANA registry for TLS,https://www.iana.org/assignments/tls-parameters/tls-parameters.xhtml#tls-parameters-8). Please note that all curves except V(secp224r1), V(secp256k1), V(secp256r1), V(secp384r1), and V(secp521r1) are discouraged for new private keys.",
          "options": [
            [
              "secp224r1",
              "secp224r1"
            ],
            [
              "secp256k1",
              "secp256k1"
            ],
            [
              "secp256r1",
              "secp256r1"
            ],
            [
              "secp384r1",
              "secp384r1"
            ],
            [
              "secp521r1",
              "secp521r1"
            ],
            [
              "secp192r1",
              "secp192r1"
            ],
            [
              "brainpoolP256r1",
              "brainpoolP256r1"
            ],
            [
              "brainpoolP384r1",
              "brainpoolP384r1"
            ],
            [
              "brainpoolP512r1",
              "brainpoolP512r1"
            ],
            [
              "sect163k1",
              "sect163k1"
            ],
            [
              "sect163r2",
              "sect163r2"
            ],
            [
              "sect233k1",
              "sect233k1"
            ],
            [
              "sect233r1",
              "sect233r1"
            ],
            [
              "sect283k1",
              "sect283k1"
            ],
            [
              "sect283r1",
              "sect283r1"
            ],
            [
              "sect409k1",
              "sect409k1"
            ],
            [
              "sect409r1",
              "sect409r1"
            ],
            [
              "sect571k1",
              "sect571k1"
            ],
            [
              "sect571r1",
              "sect571r1"
            ]
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the key be regenerated even if it already exists."
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "auto_ignore",
          "description": "Determines which format the private key is written in. By default, PKCS1 (traditional OpenSSL format) is used for all keys which support it. Please note that not every key can be exported in any format. The value V(auto) selects a format based on the key format. The value V(auto_ignore) does the same, but for existing private key files, it will not force a regenerate when its format is not the automatically selected one for generation. Note that if the format for an existing private key mismatches, the key is B(regenerated) by default. To change this behavior, use the O(format_mismatch) option.",
          "options": [
            [
              "pkcs1",
              "pkcs1"
            ],
            [
              "pkcs8",
              "pkcs8"
            ],
            [
              "raw",
              "raw"
            ],
            [
              "auto",
              "auto"
            ],
            [
              "auto_ignore",
              "auto_ignore"
            ]
          ]
        },
        {
          "key": "format_mismatch",
          "fieldId": "FORMAT_MISMATCH",
          "type": "dropdown",
          "label": "format mismatch",
          "default": "regenerate",
          "description": "Determines behavior of the module if the format of a private key does not match the expected format, but all other parameters are as expected. If set to V(regenerate) (default), generates a new private key. If set to V(convert), the key will be converted to the new format instead.",
          "options": [
            [
              "regenerate",
              "regenerate"
            ],
            [
              "convert",
              "convert"
            ]
          ]
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "The passphrase for the private key."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Name of the file in which the generated TLS/SSL private key will be written. It will have V(0600) mode if O(mode) is not explicitly set.",
          "required": true
        },
        {
          "key": "regenerate",
          "fieldId": "REGENERATE",
          "type": "dropdown",
          "label": "regenerate",
          "default": "full_idempotence",
          "description": "Allows to configure in which situations the module is allowed to regenerate private keys. The module will always generate a new key if the destination file does not exist. By default, the key will be regenerated when it does not match the module's options, except when the key cannot be read or the passphrase does not match. Please note that this B(changed) for Ansible 2.10. For Ansible 2.9, the behavior was as if V(full_idempotence) is specified. If set to V(never), the module will fail if the key cannot be read or the passphrase is not matching, and will never regenerate an existing key. If set to V(fail), the module will fail if the key does not correspond to the module's options. If set to V(partial_idempotence), the key will be regenerated if it does not conform to the module's options. The key is B(not) regenerated if it cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. If set to V(full_idempotence), the key will be regenerated if it does not conform to the module's options. This is also the case if the key cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. Make sure you have a B(backup) when using this option! If set to V(always), the module will always regenerate the key. This is equivalent to setting O(force) to V(true). Note that if O(format_mismatch) is set to V(convert) and everything matches except the format, the key will always be converted, except if O(regenerate) is set to V(always).",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "fail",
              "fail"
            ],
            [
              "partial_idempotence",
              "partial_idempotence"
            ],
            [
              "full_idempotence",
              "full_idempotence"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) private key's content as RV(privatekey). Note that especially if the private key is not encrypted, you have to make sure that the returned value is treated appropriately and not accidentally written to logs, and so on! Use with care! Use Ansible's C(no_log) task option to avoid the output being shown. See also U(https://docs.ansible.com/ansible/latest/reference_appendices/faq.html#how-do-i-keep-secret-data-in-my-playbook)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "default": 4096,
          "description": "Size (in bits) of the TLS/SSL key to generate."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the private key should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "default": "RSA",
          "description": "The algorithm used to generate the TLS/SSL private key.",
          "options": [
            [
              "DSA",
              "DSA"
            ],
            [
              "ECC",
              "ECC"
            ],
            [
              "Ed25519",
              "Ed25519"
            ],
            [
              "Ed448",
              "Ed448"
            ],
            [
              "RSA",
              "RSA"
            ],
            [
              "X25519",
              "X25519"
            ],
            [
              "X448",
              "X448"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_privatekey_convert",
      "blockType": "community_crypto_openssl_privatekey_convert",
      "label": "openssl_privatekey_convert",
      "tooltip": "Convert OpenSSL private keys",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_privatekey_convert_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original private key back if you overwrote it with a new one by accident."
        },
        {
          "key": "dest_passphrase",
          "fieldId": "DEST_PASSPHRASE",
          "type": "text",
          "label": "dest passphrase",
          "description": "The passphrase for the private key to store."
        },
        {
          "key": "dest_path",
          "fieldId": "DEST_PATH",
          "type": "text",
          "label": "dest path",
          "description": "Name of the file in which the generated TLS/SSL private key will be written. It will have V(0600) mode if O(mode) is not explicitly set.",
          "required": true
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "description": "Determines which format the destination private key should be written in. Please note that not every key can be exported in any format, and that not every format supports encryption.",
          "options": [
            [
              "pkcs1",
              "pkcs1"
            ],
            [
              "pkcs8",
              "pkcs8"
            ],
            [
              "raw",
              "raw"
            ]
          ],
          "required": true
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "src_content",
          "fieldId": "SRC_CONTENT",
          "type": "text",
          "label": "src content",
          "description": "The content of the file containing the OpenSSL private key to convert. Exactly one of O(src_path) or O(src_content) must be specified."
        },
        {
          "key": "src_passphrase",
          "fieldId": "SRC_PASSPHRASE",
          "type": "text",
          "label": "src passphrase",
          "description": "The passphrase for the private key to load."
        },
        {
          "key": "src_path",
          "fieldId": "SRC_PATH",
          "type": "text",
          "label": "src path",
          "description": "Name of the file containing the OpenSSL private key to convert. Exactly one of O(src_path) or O(src_content) must be specified."
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_privatekey_info",
      "blockType": "community_crypto_openssl_privatekey_info",
      "label": "openssl_privatekey_info",
      "tooltip": "Provide information for OpenSSL private keys",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_privatekey_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "check_consistency",
          "fieldId": "CHECK_CONSISTENCY",
          "type": "boolean",
          "label": "check consistency",
          "default": false,
          "description": "Whether to check consistency of the private key. In community.crypto < 2.0.0, consistency was always checked. Since community.crypto 2.0.0, the consistency check has been disabled by default to avoid private key material to be transported around and computed with, and only do so when requested explicitly. This can potentially prevent L(side-channel attacks,https://en.wikipedia.org/wiki/Side-channel_attack). Note that consistency checks only work for certain key types, and might depend on the version of the cryptography library. For example, with cryptography 42.0.0 and newer consistency of RSA keys can no longer be checked."
        },
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "Content of the private key file. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "The passphrase for the private key."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the private key file is loaded from."
        },
        {
          "key": "return_private_key_data",
          "fieldId": "RETURN_PRIVATE_KEY_DATA",
          "type": "boolean",
          "label": "return private key data",
          "default": false,
          "description": "Whether to return private key data. Only set this to V(true) when you want private information about this key to leave the remote machine. B(WARNING:) you have to make sure that private key data is not accidentally logged!"
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_privatekey_pipe",
      "blockType": "community_crypto_openssl_privatekey_pipe",
      "label": "openssl_privatekey_pipe",
      "tooltip": "Generate OpenSSL private keys without disk access",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_privatekey_pipe_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "cipher",
          "fieldId": "CIPHER",
          "type": "text",
          "label": "cipher",
          "default": "auto",
          "description": "The cipher to encrypt the private key. This is only used when O(passphrase) is provided. Must be V(auto)."
        },
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "The current private key data. Needed for idempotency. If not provided, the module will always return a change, and all idempotence-related options are ignored."
        },
        {
          "key": "content_base64",
          "fieldId": "CONTENT_BASE64",
          "type": "boolean",
          "label": "content base64",
          "default": false,
          "description": "Set to V(true) if the content is base64 encoded."
        },
        {
          "key": "curve",
          "fieldId": "CURVE",
          "type": "dropdown",
          "label": "curve",
          "description": "Note that not all curves are supported by all versions of C(cryptography). For maximal interoperability, V(secp384r1) or V(secp256r1) should be used. We use the curve names as defined in the L(IANA registry for TLS,https://www.iana.org/assignments/tls-parameters/tls-parameters.xhtml#tls-parameters-8). Please note that all curves except V(secp224r1), V(secp256k1), V(secp256r1), V(secp384r1), and V(secp521r1) are discouraged for new private keys.",
          "options": [
            [
              "secp224r1",
              "secp224r1"
            ],
            [
              "secp256k1",
              "secp256k1"
            ],
            [
              "secp256r1",
              "secp256r1"
            ],
            [
              "secp384r1",
              "secp384r1"
            ],
            [
              "secp521r1",
              "secp521r1"
            ],
            [
              "secp192r1",
              "secp192r1"
            ],
            [
              "brainpoolP256r1",
              "brainpoolP256r1"
            ],
            [
              "brainpoolP384r1",
              "brainpoolP384r1"
            ],
            [
              "brainpoolP512r1",
              "brainpoolP512r1"
            ],
            [
              "sect163k1",
              "sect163k1"
            ],
            [
              "sect163r2",
              "sect163r2"
            ],
            [
              "sect233k1",
              "sect233k1"
            ],
            [
              "sect233r1",
              "sect233r1"
            ],
            [
              "sect283k1",
              "sect283k1"
            ],
            [
              "sect283r1",
              "sect283r1"
            ],
            [
              "sect409k1",
              "sect409k1"
            ],
            [
              "sect409r1",
              "sect409r1"
            ],
            [
              "sect571k1",
              "sect571k1"
            ],
            [
              "sect571r1",
              "sect571r1"
            ]
          ]
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "auto_ignore",
          "description": "Determines which format the private key is written in. By default, PKCS1 (traditional OpenSSL format) is used for all keys which support it. Please note that not every key can be exported in any format. The value V(auto) selects a format based on the key format. The value V(auto_ignore) does the same, but for existing private key files, it will not force a regenerate when its format is not the automatically selected one for generation. Note that if the format for an existing private key mismatches, the key is B(regenerated) by default. To change this behavior, use the O(format_mismatch) option.",
          "options": [
            [
              "pkcs1",
              "pkcs1"
            ],
            [
              "pkcs8",
              "pkcs8"
            ],
            [
              "raw",
              "raw"
            ],
            [
              "auto",
              "auto"
            ],
            [
              "auto_ignore",
              "auto_ignore"
            ]
          ]
        },
        {
          "key": "format_mismatch",
          "fieldId": "FORMAT_MISMATCH",
          "type": "dropdown",
          "label": "format mismatch",
          "default": "regenerate",
          "description": "Determines behavior of the module if the format of a private key does not match the expected format, but all other parameters are as expected. If set to V(regenerate) (default), generates a new private key. If set to V(convert), the key will be converted to the new format instead.",
          "options": [
            [
              "regenerate",
              "regenerate"
            ],
            [
              "convert",
              "convert"
            ]
          ]
        },
        {
          "key": "passphrase",
          "fieldId": "PASSPHRASE",
          "type": "text",
          "label": "passphrase",
          "description": "The passphrase for the private key."
        },
        {
          "key": "regenerate",
          "fieldId": "REGENERATE",
          "type": "dropdown",
          "label": "regenerate",
          "default": "full_idempotence",
          "description": "Allows to configure in which situations the module is allowed to regenerate private keys. The module will always generate a new key if the destination file does not exist. By default, the key will be regenerated when it does not match the module's options, except when the key cannot be read or the passphrase does not match. Please note that this B(changed) for Ansible 2.10. For Ansible 2.9, the behavior was as if V(full_idempotence) is specified. If set to V(never), the module will fail if the key cannot be read or the passphrase is not matching, and will never regenerate an existing key. If set to V(fail), the module will fail if the key does not correspond to the module's options. If set to V(partial_idempotence), the key will be regenerated if it does not conform to the module's options. The key is B(not) regenerated if it cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. If set to V(full_idempotence), the key will be regenerated if it does not conform to the module's options. This is also the case if the key cannot be read (broken file), the key is protected by an unknown passphrase, or when they key is not protected by a passphrase, but a passphrase is specified. Make sure you have a B(backup) when using this option! If set to V(always), the module will always regenerate the key. Note that if O(format_mismatch) is set to V(convert) and everything matches except the format, the key will always be converted, except if O(regenerate) is set to V(always).",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "fail",
              "fail"
            ],
            [
              "partial_idempotence",
              "partial_idempotence"
            ],
            [
              "full_idempotence",
              "full_idempotence"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "return_current_key",
          "fieldId": "RETURN_CURRENT_KEY",
          "type": "boolean",
          "label": "return current key",
          "default": false,
          "description": "Set to V(true) to return the current private key when the module did not generate a new one. Note that in case of check mode, when this option is not set to V(true), the module always returns the current key (if it was provided) and Ansible will replace it by C(VALUE_SPECIFIED_IN_NO_LOG_PARAMETER)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "size",
          "fieldId": "SIZE",
          "type": "number",
          "label": "size",
          "default": 4096,
          "description": "Size (in bits) of the TLS/SSL key to generate."
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "default": "RSA",
          "description": "The algorithm used to generate the TLS/SSL private key.",
          "options": [
            [
              "DSA",
              "DSA"
            ],
            [
              "ECC",
              "ECC"
            ],
            [
              "Ed25519",
              "Ed25519"
            ],
            [
              "Ed448",
              "Ed448"
            ],
            [
              "RSA",
              "RSA"
            ],
            [
              "X25519",
              "X25519"
            ],
            [
              "X448",
              "X448"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_publickey",
      "blockType": "community_crypto_openssl_publickey",
      "label": "openssl_publickey",
      "tooltip": "Generate an OpenSSL public key from its private key",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_publickey_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original public key back if you overwrote it with a different one by accident."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the key be regenerated even it it already exists."
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "PEM",
          "description": "The format of the public key.",
          "options": [
            [
              "OpenSSH",
              "OpenSSH"
            ],
            [
              "PEM",
              "PEM"
            ]
          ]
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Name of the file in which the generated TLS/SSL public key will be written.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "The content of the TLS/SSL private key from which to generate the public key. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both. If O(state) is V(present), one of them is required."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the private key."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "Path to the TLS/SSL private key from which to generate the public key. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both. If O(state) is V(present), one of them is required."
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) public key's content as RV(publickey)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the public key should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_publickey_info",
      "blockType": "community_crypto_openssl_publickey_info",
      "label": "openssl_publickey_info",
      "tooltip": "Provide information for OpenSSL public keys",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_publickey_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "Content of the public key file. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the public key file is loaded from."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_signature",
      "blockType": "community_crypto_openssl_signature",
      "label": "openssl_signature",
      "tooltip": "Sign data with openssl",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_signature_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The file to sign. This file will only be read and not modified.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "The content of the private key to use when signing the certificate signing request. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the private key. This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "The path to the private key to use when signing. Either O(privatekey_path) or O(privatekey_content) must be specified, but not both."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.openssl_signature_info",
      "blockType": "community_crypto_openssl_signature_info",
      "label": "openssl_signature_info",
      "tooltip": "Verify signatures with openssl",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/openssl_signature_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "certificate_content",
          "fieldId": "CERTIFICATE_CONTENT",
          "type": "text",
          "label": "certificate content",
          "description": "The content of the certificate used to verify the signature. Either O(certificate_path) or O(certificate_content) must be specified, but not both."
        },
        {
          "key": "certificate_path",
          "fieldId": "CERTIFICATE_PATH",
          "type": "text",
          "label": "certificate path",
          "description": "The path to the certificate used to verify the signature. Either O(certificate_path) or O(certificate_content) must be specified, but not both."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The signed file to verify. This file will only be read and not modified.",
          "required": true
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "signature",
          "fieldId": "SIGNATURE",
          "type": "text",
          "label": "signature",
          "description": "Base64 encoded signature.",
          "required": true
        }
      ]
    },
    {
      "fqcn": "community.crypto.x509_certificate",
      "blockType": "community_crypto_x509_certificate",
      "label": "x509_certificate",
      "tooltip": "Generate and/or check OpenSSL certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_certificate_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "acme_accountkey_path",
          "fieldId": "ACME_ACCOUNTKEY_PATH",
          "type": "text",
          "label": "acme accountkey path",
          "description": "The path to the accountkey for the V(acme) provider. This is only used by the V(acme) provider."
        },
        {
          "key": "acme_chain",
          "fieldId": "ACME_CHAIN",
          "type": "boolean",
          "label": "acme chain",
          "default": false,
          "description": "Include the intermediate certificate to the generated certificate This is only used by the V(acme) provider. Note that this is only available for older versions of C(acme-tiny). New versions include the chain automatically, and setting O(acme_chain) to V(true) results in an error."
        },
        {
          "key": "acme_challenge_path",
          "fieldId": "ACME_CHALLENGE_PATH",
          "type": "text",
          "label": "acme challenge path",
          "description": "The path to the ACME challenge directory that is served on U(http://<HOST>:80/.well-known/acme-challenge/) This is only used by the V(acme) provider."
        },
        {
          "key": "acme_directory",
          "fieldId": "ACME_DIRECTORY",
          "type": "text",
          "label": "acme directory",
          "default": "https://acme-v02.api.letsencrypt.org/directory",
          "description": "The ACME directory to use. You can use any directory that supports the ACME protocol, such as Let's Encrypt. Let's Encrypt recommends using their staging server while developing jobs. U(https://letsencrypt.org/docs/staging-environment/)."
        },
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original certificate back if you overwrote it with a new one by accident."
        },
        {
          "key": "csr_content",
          "fieldId": "CSR_CONTENT",
          "type": "text",
          "label": "csr content",
          "description": "Content of the Certificate Signing Request (CSR) used to generate this certificate. This is mutually exclusive with O(csr_path)."
        },
        {
          "key": "csr_path",
          "fieldId": "CSR_PATH",
          "type": "text",
          "label": "csr path",
          "description": "Path to the Certificate Signing Request (CSR) used to generate this certificate. This is mutually exclusive with O(csr_content)."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Generate the certificate, even if it already exists."
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "ignore_timestamps",
          "fieldId": "IGNORE_TIMESTAMPS",
          "type": "boolean",
          "label": "ignore timestamps",
          "default": true,
          "description": "Whether the \"not before\" and \"not after\" timestamps should be ignored for idempotency checks. It is better to keep the default value V(true) when using relative timestamps (like V(+0s) for now)."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "ownca_content",
          "fieldId": "OWNCA_CONTENT",
          "type": "text",
          "label": "ownca content",
          "description": "Content of the CA (Certificate Authority) certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_path)."
        },
        {
          "key": "ownca_create_authority_key_identifier",
          "fieldId": "OWNCA_CREATE_AUTHORITY_KEY_IDENTIFIER",
          "type": "boolean",
          "label": "ownca create authority key identifier",
          "default": true,
          "description": "Create a Authority Key Identifier from the CA's certificate. If the CSR provided a authority key identifier, it is ignored. The Authority Key Identifier is generated from the CA certificate's Subject Key Identifier, if available. If it is not available, the CA certificate's public key will be used. This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_create_subject_key_identifier",
          "fieldId": "OWNCA_CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "dropdown",
          "label": "ownca create subject key identifier",
          "default": "create_if_not_provided",
          "description": "Whether to create the Subject Key Identifier (SKI) from the public key. A value of V(create_if_not_provided) (default) only creates a SKI when the CSR does not provide one. A value of V(always_create) always creates a SKI. If the CSR provides one, that one is ignored. A value of V(never_create) never creates a SKI. If the CSR provides one, that one is used. This is only used by the V(ownca) provider.",
          "options": [
            [
              "create_if_not_provided",
              "create_if_not_provided"
            ],
            [
              "always_create",
              "always_create"
            ],
            [
              "never_create",
              "never_create"
            ]
          ]
        },
        {
          "key": "ownca_digest",
          "fieldId": "OWNCA_DIGEST",
          "type": "text",
          "label": "ownca digest",
          "default": "sha256",
          "description": "The digest algorithm to be used for the V(ownca) certificate. This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_not_after",
          "fieldId": "OWNCA_NOT_AFTER",
          "type": "text",
          "label": "ownca not after",
          "default": "+3650d",
          "description": "The point in time at which the certificate stops being valid. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will stop being valid 10 years from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(ownca) provider. On macOS 10.15 and onwards, TLS server certificates must have a validity period of 825 days or fewer. Please see U(https://support.apple.com/en-us/HT210176) for more details."
        },
        {
          "key": "ownca_not_before",
          "fieldId": "OWNCA_NOT_BEFORE",
          "type": "text",
          "label": "ownca not before",
          "default": "+0s",
          "description": "The point in time the certificate is valid from. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will start being valid from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_path",
          "fieldId": "OWNCA_PATH",
          "type": "text",
          "label": "ownca path",
          "description": "Remote absolute path of the CA (Certificate Authority) certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_content)."
        },
        {
          "key": "ownca_privatekey_content",
          "fieldId": "OWNCA_PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "ownca privatekey content",
          "description": "Content of the CA (Certificate Authority) private key to use when signing the certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_privatekey_path)."
        },
        {
          "key": "ownca_privatekey_passphrase",
          "fieldId": "OWNCA_PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "ownca privatekey passphrase",
          "description": "The passphrase for the O(ownca_privatekey_path) resp. O(ownca_privatekey_content). This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_privatekey_path",
          "fieldId": "OWNCA_PRIVATEKEY_PATH",
          "type": "text",
          "label": "ownca privatekey path",
          "description": "Path to the CA (Certificate Authority) private key to use when signing the certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_privatekey_content)."
        },
        {
          "key": "ownca_version",
          "fieldId": "OWNCA_VERSION",
          "type": "dropdown",
          "label": "ownca version",
          "default": 3,
          "description": "The version of the V(ownca) certificate. Nowadays it should almost always be V(3). This is only used by the V(ownca) provider.",
          "options": [
            [
              "3",
              "3"
            ]
          ]
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the generated certificate file should be created or is already located.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "Content of the private key to use when signing the certificate. This is mutually exclusive with O(privatekey_path)."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the O(privatekey_path) resp. O(privatekey_content). This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "Path to the private key to use when signing the certificate. This is mutually exclusive with O(privatekey_content)."
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) certificate's content as RV(certificate)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "selfsigned_create_subject_key_identifier",
          "fieldId": "SELFSIGNED_CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "dropdown",
          "label": "selfsigned create subject key identifier",
          "default": "create_if_not_provided",
          "description": "Whether to create the Subject Key Identifier (SKI) from the public key. A value of V(create_if_not_provided) (default) only creates a SKI when the CSR does not provide one. A value of V(always_create) always creates a SKI. If the CSR provides one, that one is ignored. A value of V(never_create) never creates a SKI. If the CSR provides one, that one is used. This is only used by the V(selfsigned) provider.",
          "options": [
            [
              "create_if_not_provided",
              "create_if_not_provided"
            ],
            [
              "always_create",
              "always_create"
            ],
            [
              "never_create",
              "never_create"
            ]
          ]
        },
        {
          "key": "selfsigned_digest",
          "fieldId": "SELFSIGNED_DIGEST",
          "type": "text",
          "label": "selfsigned digest",
          "default": "sha256",
          "description": "Digest algorithm to be used when self-signing the certificate. This is only used by the V(selfsigned) provider."
        },
        {
          "key": "selfsigned_not_after",
          "fieldId": "SELFSIGNED_NOT_AFTER",
          "type": "text",
          "label": "selfsigned not after",
          "default": "+3650d",
          "description": "The point in time at which the certificate stops being valid. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will stop being valid 10 years from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(selfsigned) provider. On macOS 10.15 and onwards, TLS server certificates must have a validity period of 825 days or fewer. Please see U(https://support.apple.com/en-us/HT210176) for more details.",
          "aliases": [
            "selfsigned_notAfter"
          ]
        },
        {
          "key": "selfsigned_not_before",
          "fieldId": "SELFSIGNED_NOT_BEFORE",
          "type": "text",
          "label": "selfsigned not before",
          "default": "+0s",
          "description": "The point in time the certificate is valid from. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will start being valid from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(selfsigned) provider.",
          "aliases": [
            "selfsigned_notBefore"
          ]
        },
        {
          "key": "selfsigned_version",
          "fieldId": "SELFSIGNED_VERSION",
          "type": "dropdown",
          "label": "selfsigned version",
          "default": 3,
          "description": "Version of the V(selfsigned) certificate. Nowadays it should almost always be V(3). This is only used by the V(selfsigned) provider.",
          "options": [
            [
              "3",
              "3"
            ]
          ]
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the certificate should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ]
    },
    {
      "fqcn": "community.crypto.x509_certificate_convert",
      "blockType": "community_crypto_x509_certificate_convert",
      "label": "x509_certificate_convert",
      "tooltip": "Convert X.509 certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_certificate_convert_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original X.509 certificate back if you overwrote it with a new one by accident."
        },
        {
          "key": "dest_path",
          "fieldId": "DEST_PATH",
          "type": "text",
          "label": "dest path",
          "description": "Name of the file in which the generated TLS/SSL X.509 certificate will be written.",
          "required": true
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "description": "Determines which format the destination X.509 certificate should be written in. Please note that not every key can be exported in any format, and that not every format supports encryption.",
          "options": [
            [
              "pem",
              "pem"
            ],
            [
              "der",
              "der"
            ]
          ],
          "required": true
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "src_content",
          "fieldId": "SRC_CONTENT",
          "type": "text",
          "label": "src content",
          "description": "The content of the file containing the X.509 certificate to convert. This must be text. If you are not sure that the input file is PEM, you must Base64 encode the value and set O(src_content_base64=true). You can use the P(ansible.builtin.b64encode#filter) filter plugin for this. Exactly one of O(src_path) or O(src_content) must be specified."
        },
        {
          "key": "src_content_base64",
          "fieldId": "SRC_CONTENT_BASE64",
          "type": "boolean",
          "label": "src content base64",
          "default": false,
          "description": "If set to V(true) when O(src_content) is provided, the module assumes that the value of O(src_content) is Base64 encoded."
        },
        {
          "key": "src_path",
          "fieldId": "SRC_PATH",
          "type": "text",
          "label": "src path",
          "description": "Name of the file containing the X.509 certificate to convert. Exactly one of O(src_path) or O(src_content) must be specified."
        },
        {
          "key": "strict",
          "fieldId": "STRICT",
          "type": "boolean",
          "label": "strict",
          "default": false,
          "description": "If the input is a PEM file, ensure that it contains a single PEM object, that the header and footer match, and are of type C(CERTIFICATE) or C(X509 CERTIFICATE). See also the O(verify_cert_parsable) option, which checks whether the certificate is parsable."
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        },
        {
          "key": "verify_cert_parsable",
          "fieldId": "VERIFY_CERT_PARSABLE",
          "type": "boolean",
          "label": "verify cert parsable",
          "default": false,
          "description": "If set to V(true), ensures that the certificate can be parsed. To ensure that a PEM file does not contain multiple certificates, use the O(strict) option."
        }
      ]
    },
    {
      "fqcn": "community.crypto.x509_certificate_info",
      "blockType": "community_crypto_x509_certificate_info",
      "label": "x509_certificate_info",
      "tooltip": "Provide information of OpenSSL X.509 certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_certificate_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "Content of the X.509 certificate in PEM format. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "name_encoding",
          "fieldId": "NAME_ENCODING",
          "type": "dropdown",
          "label": "name encoding",
          "default": "ignore",
          "description": "How to encode names (DNS names, URIs, email addresses) in return values. V(ignore) will use the encoding returned by the backend. V(idna) will convert all labels of domain names to IDNA encoding. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 encoding fails. V(unicode) will convert all labels of domain names to Unicode. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 decoding fails. B(Note) that V(idna) and V(unicode) require the L(idna Python library,https://pypi.org/project/idna/) to be installed.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "idna",
              "idna"
            ],
            [
              "unicode",
              "unicode"
            ]
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the certificate file is loaded from. Either O(path) or O(content) must be specified, but not both. PEM and DER formats are supported."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "valid_at",
          "fieldId": "VALID_AT",
          "type": "text",
          "label": "valid at",
          "yamlType": "dict",
          "description": "A dict of names mapping to time specifications. Every time specified here will be checked whether the certificate is valid at this point. See the RV(valid_at) return value for information on the result. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)), and ASN.1 TIME (in other words, pattern C(YYYYMMDDHHMMSSZ)). Note that all timestamps will be treated as being in UTC."
        }
      ]
    },
    {
      "fqcn": "community.crypto.x509_certificate_pipe",
      "blockType": "community_crypto_x509_certificate_pipe",
      "label": "x509_certificate_pipe",
      "tooltip": "Generate and/or check OpenSSL certificates",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_certificate_pipe_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "The existing certificate."
        },
        {
          "key": "csr_content",
          "fieldId": "CSR_CONTENT",
          "type": "text",
          "label": "csr content",
          "description": "Content of the Certificate Signing Request (CSR) used to generate this certificate. This is mutually exclusive with O(csr_path)."
        },
        {
          "key": "csr_path",
          "fieldId": "CSR_PATH",
          "type": "text",
          "label": "csr path",
          "description": "Path to the Certificate Signing Request (CSR) used to generate this certificate. This is mutually exclusive with O(csr_content)."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Generate the certificate, even if it already exists."
        },
        {
          "key": "ignore_timestamps",
          "fieldId": "IGNORE_TIMESTAMPS",
          "type": "boolean",
          "label": "ignore timestamps",
          "default": true,
          "description": "Whether the \"not before\" and \"not after\" timestamps should be ignored for idempotency checks. It is better to keep the default value V(true) when using relative timestamps (like V(+0s) for now)."
        },
        {
          "key": "ownca_content",
          "fieldId": "OWNCA_CONTENT",
          "type": "text",
          "label": "ownca content",
          "description": "Content of the CA (Certificate Authority) certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_path)."
        },
        {
          "key": "ownca_create_authority_key_identifier",
          "fieldId": "OWNCA_CREATE_AUTHORITY_KEY_IDENTIFIER",
          "type": "boolean",
          "label": "ownca create authority key identifier",
          "default": true,
          "description": "Create a Authority Key Identifier from the CA's certificate. If the CSR provided a authority key identifier, it is ignored. The Authority Key Identifier is generated from the CA certificate's Subject Key Identifier, if available. If it is not available, the CA certificate's public key will be used. This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_create_subject_key_identifier",
          "fieldId": "OWNCA_CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "dropdown",
          "label": "ownca create subject key identifier",
          "default": "create_if_not_provided",
          "description": "Whether to create the Subject Key Identifier (SKI) from the public key. A value of V(create_if_not_provided) (default) only creates a SKI when the CSR does not provide one. A value of V(always_create) always creates a SKI. If the CSR provides one, that one is ignored. A value of V(never_create) never creates a SKI. If the CSR provides one, that one is used. This is only used by the V(ownca) provider.",
          "options": [
            [
              "create_if_not_provided",
              "create_if_not_provided"
            ],
            [
              "always_create",
              "always_create"
            ],
            [
              "never_create",
              "never_create"
            ]
          ]
        },
        {
          "key": "ownca_digest",
          "fieldId": "OWNCA_DIGEST",
          "type": "text",
          "label": "ownca digest",
          "default": "sha256",
          "description": "The digest algorithm to be used for the V(ownca) certificate. This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_not_after",
          "fieldId": "OWNCA_NOT_AFTER",
          "type": "text",
          "label": "ownca not after",
          "default": "+3650d",
          "description": "The point in time at which the certificate stops being valid. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will stop being valid 10 years from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(ownca) provider. On macOS 10.15 and onwards, TLS server certificates must have a validity period of 825 days or fewer. Please see U(https://support.apple.com/en-us/HT210176) for more details."
        },
        {
          "key": "ownca_not_before",
          "fieldId": "OWNCA_NOT_BEFORE",
          "type": "text",
          "label": "ownca not before",
          "default": "+0s",
          "description": "The point in time the certificate is valid from. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will start being valid from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_path",
          "fieldId": "OWNCA_PATH",
          "type": "text",
          "label": "ownca path",
          "description": "Remote absolute path of the CA (Certificate Authority) certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_content)."
        },
        {
          "key": "ownca_privatekey_content",
          "fieldId": "OWNCA_PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "ownca privatekey content",
          "description": "Content of the CA (Certificate Authority) private key to use when signing the certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_privatekey_path)."
        },
        {
          "key": "ownca_privatekey_passphrase",
          "fieldId": "OWNCA_PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "ownca privatekey passphrase",
          "description": "The passphrase for the O(ownca_privatekey_path) resp. O(ownca_privatekey_content). This is only used by the V(ownca) provider."
        },
        {
          "key": "ownca_privatekey_path",
          "fieldId": "OWNCA_PRIVATEKEY_PATH",
          "type": "text",
          "label": "ownca privatekey path",
          "description": "Path to the CA (Certificate Authority) private key to use when signing the certificate. This is only used by the V(ownca) provider. This is mutually exclusive with O(ownca_privatekey_content)."
        },
        {
          "key": "ownca_version",
          "fieldId": "OWNCA_VERSION",
          "type": "dropdown",
          "label": "ownca version",
          "default": 3,
          "description": "The version of the V(ownca) certificate. Nowadays it should almost always be V(3). This is only used by the V(ownca) provider.",
          "options": [
            [
              "3",
              "3"
            ]
          ]
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "Content of the private key to use when signing the certificate. This is mutually exclusive with O(privatekey_path)."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the O(privatekey_path) resp. O(privatekey_content). This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "Path to the private key to use when signing the certificate. This is mutually exclusive with O(privatekey_content)."
        },
        {
          "key": "select_crypto_backend",
          "fieldId": "SELECT_CRYPTO_BACKEND",
          "type": "dropdown",
          "label": "select crypto backend",
          "default": "auto",
          "description": "Determines which crypto backend to use. The default choice is V(auto), which tries to use C(cryptography) if available. If set to V(cryptography), will try to use the L(cryptography,https://cryptography.io/) library. Note that with community.crypto 3.0.0, all values behave the same. This option will be deprecated in a later version. We recommend to not set it explicitly.",
          "options": [
            [
              "auto",
              "auto"
            ],
            [
              "cryptography",
              "cryptography"
            ]
          ]
        },
        {
          "key": "selfsigned_create_subject_key_identifier",
          "fieldId": "SELFSIGNED_CREATE_SUBJECT_KEY_IDENTIFIER",
          "type": "dropdown",
          "label": "selfsigned create subject key identifier",
          "default": "create_if_not_provided",
          "description": "Whether to create the Subject Key Identifier (SKI) from the public key. A value of V(create_if_not_provided) (default) only creates a SKI when the CSR does not provide one. A value of V(always_create) always creates a SKI. If the CSR provides one, that one is ignored. A value of V(never_create) never creates a SKI. If the CSR provides one, that one is used. This is only used by the V(selfsigned) provider.",
          "options": [
            [
              "create_if_not_provided",
              "create_if_not_provided"
            ],
            [
              "always_create",
              "always_create"
            ],
            [
              "never_create",
              "never_create"
            ]
          ]
        },
        {
          "key": "selfsigned_digest",
          "fieldId": "SELFSIGNED_DIGEST",
          "type": "text",
          "label": "selfsigned digest",
          "default": "sha256",
          "description": "Digest algorithm to be used when self-signing the certificate. This is only used by the V(selfsigned) provider."
        },
        {
          "key": "selfsigned_not_after",
          "fieldId": "SELFSIGNED_NOT_AFTER",
          "type": "text",
          "label": "selfsigned not after",
          "default": "+3650d",
          "description": "The point in time at which the certificate stops being valid. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will stop being valid 10 years from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(selfsigned) provider. On macOS 10.15 and onwards, TLS server certificates must have a validity period of 825 days or fewer. Please see U(https://support.apple.com/en-us/HT210176) for more details.",
          "aliases": [
            "selfsigned_notAfter"
          ]
        },
        {
          "key": "selfsigned_not_before",
          "fieldId": "SELFSIGNED_NOT_BEFORE",
          "type": "text",
          "label": "selfsigned not before",
          "default": "+0s",
          "description": "The point in time the certificate is valid from. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). If this value is not specified, the certificate will start being valid from now. Note that this value is B(not used to determine whether an existing certificate should be regenerated). This can be changed by setting the O(ignore_timestamps) option to V(false). Please note that you should avoid relative timestamps when setting O(ignore_timestamps=false). This is only used by the V(selfsigned) provider.",
          "aliases": [
            "selfsigned_notBefore"
          ]
        },
        {
          "key": "selfsigned_version",
          "fieldId": "SELFSIGNED_VERSION",
          "type": "dropdown",
          "label": "selfsigned version",
          "default": 3,
          "description": "Version of the V(selfsigned) certificate. Nowadays it should almost always be V(3). This is only used by the V(selfsigned) provider.",
          "options": [
            [
              "3",
              "3"
            ]
          ]
        }
      ]
    },
    {
      "fqcn": "community.crypto.x509_crl",
      "blockType": "community_crypto_x509_crl",
      "label": "x509_crl",
      "tooltip": "Generate Certificate Revocation Lists (CRLs)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_crl_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "attributes",
          "fieldId": "ATTRIBUTES",
          "type": "text",
          "label": "attributes",
          "description": "The attributes the resulting filesystem object should have. To get supported flags look at the man page for I(chattr) on the target system. This string should contain the attributes in the same order as the one displayed by I(lsattr). The C(=) operator is assumed as default, otherwise C(+) or C(-) operators need to be included in the string.",
          "aliases": [
            "attr"
          ]
        },
        {
          "key": "backup",
          "fieldId": "BACKUP",
          "type": "boolean",
          "label": "backup",
          "default": false,
          "description": "Create a backup file including a timestamp so you can get the original CRL back if you overwrote it with a new one by accident."
        },
        {
          "key": "crl_mode",
          "fieldId": "CRL_MODE",
          "type": "dropdown",
          "label": "crl mode",
          "default": "generate",
          "description": "Defines how to process entries of existing CRLs. If set to V(generate), makes sure that the CRL has the exact set of revoked certificates as specified in O(revoked_certificates). If set to V(update), makes sure that the CRL contains the revoked certificates from O(revoked_certificates), but can also contain other revoked certificates. If the CRL file already exists, all entries from the existing CRL will also be included in the new CRL. When using V(update), you might be interested in setting O(ignore_timestamps) to V(true). The default value is V(generate). This parameter was called O(mode) before community.crypto 2.13.0. It has been renamed to avoid a collision with the common O(mode) parameter for setting the CRL file's access mode.",
          "options": [
            [
              "generate",
              "generate"
            ],
            [
              "update",
              "update"
            ]
          ]
        },
        {
          "key": "digest",
          "fieldId": "DIGEST",
          "type": "text",
          "label": "digest",
          "default": "sha256",
          "description": "Digest algorithm to be used when signing the CRL."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Should the CRL be forced to be regenerated."
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "pem",
          "description": "Whether the CRL file should be in PEM or DER format. If an existing CRL file does match everything but O(format), it will be converted to the correct format instead of regenerated.",
          "options": [
            [
              "pem",
              "pem"
            ],
            [
              "der",
              "der"
            ]
          ]
        },
        {
          "key": "group",
          "fieldId": "GROUP",
          "type": "text",
          "label": "group",
          "description": "Name of the group that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current group of the current user unless you are root, in which case it can preserve the previous ownership."
        },
        {
          "key": "ignore_timestamps",
          "fieldId": "IGNORE_TIMESTAMPS",
          "type": "boolean",
          "label": "ignore timestamps",
          "default": false,
          "description": "Whether the timestamps O(last_update), O(next_update) and O(revoked_certificates[].revocation_date) should be ignored for idempotency checks. The timestamp O(revoked_certificates[].invalidity_date) will never be ignored. Use this in combination with relative timestamps for these values to get idempotency."
        },
        {
          "key": "issuer",
          "fieldId": "ISSUER",
          "type": "text",
          "label": "issuer",
          "yamlType": "dict",
          "description": "Key/value pairs that will be present in the issuer name field of the CRL. If you need to specify more than one value with the same key, use a list as value. If the order of the components is important, use O(issuer_ordered). One of O(issuer) and O(issuer_ordered) is required if O(state) is V(present). Mutually exclusive with O(issuer_ordered)."
        },
        {
          "key": "issuer_ordered",
          "fieldId": "ISSUER_ORDERED",
          "type": "text",
          "label": "issuer ordered",
          "yamlType": "list",
          "description": "A list of dictionaries, where every dictionary must contain one key/value pair. This key/value pair will be present in the issuer name field of the CRL. If you want to specify more than one value with the same key in a row, you can use a list as value. One of O(issuer) and O(issuer_ordered) is required if O(state) is V(present). Mutually exclusive with O(issuer)."
        },
        {
          "key": "last_update",
          "fieldId": "LAST_UPDATE",
          "type": "text",
          "label": "last update",
          "default": "+0s",
          "description": "The point in time from which this CRL can be trusted. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). Note that if using relative time this module is NOT idempotent, except when O(ignore_timestamps) is set to V(true)."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The permissions the resulting filesystem object should have. For those used to I(/usr/bin/chmod) remember that modes are actually octal numbers. You must give Ansible enough information to parse them correctly. For consistent results, quote octal numbers (for example, C('644') or C('1777')) so Ansible receives a string and can do its own conversion from string into number. Adding a leading zero (for example, C(0755)) works sometimes, but can fail in loops and some other circumstances. Giving Ansible a number without following either of these rules will end up with a decimal number which will have unexpected results. As of Ansible 1.8, the mode may be specified as a symbolic mode (for example, C(u+rwx) or C(u=rw,g=r,o=r)). If C(mode) is not specified and the destination filesystem object B(does not) exist, the default C(umask) on the system will be used when setting the mode for the newly created filesystem object. If C(mode) is not specified and the destination filesystem object B(does) exist, the mode of the existing filesystem object will be used. Specifying C(mode) is the best way to ensure filesystem objects are created with the correct permissions. See CVE-2020-1736 for further details."
        },
        {
          "key": "name_encoding",
          "fieldId": "NAME_ENCODING",
          "type": "dropdown",
          "label": "name encoding",
          "default": "ignore",
          "description": "How to encode names (DNS names, URIs, email addresses) in return values. V(ignore) will use the encoding returned by the backend. V(idna) will convert all labels of domain names to IDNA encoding. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 encoding fails. V(unicode) will convert all labels of domain names to Unicode. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 decoding fails. B(Note) that V(idna) and V(unicode) require the L(idna Python library,https://pypi.org/project/idna/) to be installed.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "idna",
              "idna"
            ],
            [
              "unicode",
              "unicode"
            ]
          ]
        },
        {
          "key": "next_update",
          "fieldId": "NEXT_UPDATE",
          "type": "text",
          "label": "next update",
          "description": "The absolute latest point in time by which this O(issuer) is expected to have issued another CRL. Many clients will treat a CRL as expired once O(next_update) occurs. Time can be specified either as relative time or as absolute timestamp. Time will always be interpreted as UTC. Valid format is C([+-]timespec | ASN.1 TIME) where timespec can be an integer + C([w | d | h | m | s]) (for example V(+32w1d2h)). Note that if using relative time this module is NOT idempotent, except when O(ignore_timestamps) is set to V(true). Required if O(state) is V(present)."
        },
        {
          "key": "owner",
          "fieldId": "OWNER",
          "type": "text",
          "label": "owner",
          "description": "Name of the user that should own the filesystem object, as would be fed to I(chown). When left unspecified, it uses the current user unless you are root, in which case it can preserve the previous ownership. Specifying a numeric username will be assumed to be a user ID and not a username. Avoid numeric usernames to avoid this confusion."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the generated CRL file should be created or is already located.",
          "required": true
        },
        {
          "key": "privatekey_content",
          "fieldId": "PRIVATEKEY_CONTENT",
          "type": "text",
          "label": "privatekey content",
          "description": "The content of the CA's private key to use when signing the CRL. Either O(privatekey_path) or O(privatekey_content) must be specified if O(state) is V(present), but not both."
        },
        {
          "key": "privatekey_passphrase",
          "fieldId": "PRIVATEKEY_PASSPHRASE",
          "type": "text",
          "label": "privatekey passphrase",
          "description": "The passphrase for the O(privatekey_path). This is required if the private key is password protected."
        },
        {
          "key": "privatekey_path",
          "fieldId": "PRIVATEKEY_PATH",
          "type": "text",
          "label": "privatekey path",
          "description": "Path to the CA's private key to use when signing the CRL. Either O(privatekey_path) or O(privatekey_content) must be specified if O(state) is V(present), but not both."
        },
        {
          "key": "return_content",
          "fieldId": "RETURN_CONTENT",
          "type": "boolean",
          "label": "return content",
          "default": false,
          "description": "If set to V(true), will return the (current or generated) CRL's content as RV(crl)."
        },
        {
          "key": "selevel",
          "fieldId": "SELEVEL",
          "type": "text",
          "label": "selevel",
          "description": "The level part of the SELinux filesystem object context. This is the MLS/MCS attribute, sometimes known as the C(range). When set to C(_default), it will use the C(level) portion of the policy if available."
        },
        {
          "key": "serial_numbers",
          "fieldId": "SERIAL_NUMBERS",
          "type": "dropdown",
          "label": "serial numbers",
          "default": "integer",
          "description": "This option determines which values will be accepted for O(revoked_certificates[].serial_number). If set to V(integer) (default), serial numbers are assumed to be integers, for example V(66223). (This example value is equivalent to the hex octet string V(01:02:AF)). If set to V(hex-octets), serial numbers are assumed to be colon-separated hex octet strings, for example V(01:02:AF). (This example value is equivalent to the integer V(66223)).",
          "options": [
            [
              "integer",
              "integer"
            ],
            [
              "hex-octets",
              "hex-octets"
            ]
          ]
        },
        {
          "key": "serole",
          "fieldId": "SEROLE",
          "type": "text",
          "label": "serole",
          "description": "The role part of the SELinux filesystem object context. When set to C(_default), it will use the C(role) portion of the policy if available."
        },
        {
          "key": "setype",
          "fieldId": "SETYPE",
          "type": "text",
          "label": "setype",
          "description": "The type part of the SELinux filesystem object context. When set to C(_default), it will use the C(type) portion of the policy if available."
        },
        {
          "key": "seuser",
          "fieldId": "SEUSER",
          "type": "text",
          "label": "seuser",
          "description": "The user part of the SELinux filesystem object context. By default it uses the C(system) policy, where applicable. When set to C(_default), it will use the C(user) portion of the policy if available."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Whether the CRL file should exist or not, taking action if the state is different from what is stated.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "unsafe_writes",
          "fieldId": "UNSAFE_WRITES",
          "type": "boolean",
          "label": "unsafe writes",
          "default": false,
          "description": "Influence when to use atomic operation to prevent data corruption or inconsistent reads from the target filesystem object. By default this module uses atomic operations to prevent data corruption or inconsistent reads from the target filesystem objects, but sometimes systems are configured or just broken in ways that prevent this. One example is docker mounted filesystem objects, which cannot be updated atomically from inside the container and can only be written in an unsafe manner. This option allows Ansible to fall back to unsafe methods of updating filesystem objects when atomic operations fail (however, it doesn't force Ansible to perform unsafe writes). IMPORTANT! Unsafe writes are subject to race conditions and can lead to data corruption."
        }
      ],
      "unmappedParams": [
        "revoked_certificates (has suboptions)"
      ]
    },
    {
      "fqcn": "community.crypto.x509_crl_info",
      "blockType": "community_crypto_x509_crl_info",
      "label": "x509_crl_info",
      "tooltip": "Retrieve information on Certificate Revocation Lists (CRLs)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/crypto/x509_crl_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "Content of the X.509 CRL in PEM format, or Base64-encoded X.509 CRL. Either O(path) or O(content) must be specified, but not both."
        },
        {
          "key": "list_revoked_certificates",
          "fieldId": "LIST_REVOKED_CERTIFICATES",
          "type": "boolean",
          "label": "list revoked certificates",
          "default": true,
          "description": "If set to V(false), the list of revoked certificates is not included in the result. This is useful when retrieving information on large CRL files. Enumerating all revoked certificates can take some time, including serializing the result as JSON, sending it to the Ansible controller, and decoding it again."
        },
        {
          "key": "name_encoding",
          "fieldId": "NAME_ENCODING",
          "type": "dropdown",
          "label": "name encoding",
          "default": "ignore",
          "description": "How to encode names (DNS names, URIs, email addresses) in return values. V(ignore) will use the encoding returned by the backend. V(idna) will convert all labels of domain names to IDNA encoding. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 encoding fails. V(unicode) will convert all labels of domain names to Unicode. IDNA2008 will be preferred, and IDNA2003 will be used if IDNA2008 decoding fails. B(Note) that V(idna) and V(unicode) require the L(idna Python library,https://pypi.org/project/idna/) to be installed.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "idna",
              "idna"
            ],
            [
              "unicode",
              "unicode"
            ]
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Remote absolute path where the generated CRL file should be created or is already located. Either O(path) or O(content) must be specified, but not both."
        }
      ]
    }
  ]
}
