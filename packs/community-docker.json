{
  "schemaVersion": 1,
  "packVersion": "1.0.0",
  "id": "community-docker",
  "name": "Community Docker",
  "collection": "community.docker",
  "collectionVersion": "5.2.1",
  "minAppVersion": "1.0.0",
  "testedWithAppVersion": "0.1.0",
  "publishedAt": "2026-07-09T05:24:23Z",
  "qualityLevel": "generated",
  "source": "generated",
  "categoryColor": "#2496ED",
  "blockStyleName": "community_docker_blocks",
  "categoryStyleName": "community_docker_category",
  "modules": [
    {
      "fqcn": "community.docker.docker_compose_v2",
      "blockType": "community_docker_compose_v2",
      "label": "docker_compose_v2",
      "tooltip": "Manage multi-container Docker applications with Docker Compose CLI plugin",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_compose_v2_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "assume_yes",
          "fieldId": "ASSUME_YES",
          "type": "boolean",
          "label": "assume yes",
          "default": false,
          "description": "When O(assume_yes=true), pass C(-y)/C(--yes) to assume \"yes\" as answer to all prompts and run non-interactively. Right now a prompt is asked whenever a non-matching volume should be re-created. O(assume_yes=false) results in the question being answered by \"no\", which will simply re-use the existing volume. This option is only available on Docker Compose 2.32.0 or newer."
        },
        {
          "key": "build",
          "fieldId": "BUILD",
          "type": "dropdown",
          "label": "build",
          "default": "policy",
          "description": "Whether to build images before starting containers. This is used when C(docker compose up) is run. V(always) always builds before starting containers. This is equivalent to the C(--build) option of C(docker compose up). V(never) never builds before starting containers. This is equivalent to the C(--no-build) option of C(docker compose up). V(policy) uses the policy as defined in the Compose file.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "never",
              "never"
            ],
            [
              "policy",
              "policy"
            ]
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "check_files_existing",
          "fieldId": "CHECK_FILES_EXISTING",
          "type": "boolean",
          "label": "check files existing",
          "default": true,
          "description": "If set to V(false), the module will not check whether one of the files C(compose.yaml), C(compose.yml), C(docker-compose.yaml), or C(docker-compose.yml) exists in O(project_src) if O(files) is not provided. This can be useful if environment files with C(COMPOSE_FILE) are used to configure a different filename. The module currently does not check for C(COMPOSE_FILE) in environment files or the current environment."
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "definition",
          "fieldId": "DEFINITION",
          "type": "text",
          "label": "definition",
          "yamlType": "dict",
          "description": "Compose file describing one or more services, networks and volumes. Mutually exclusive with O(project_src) and O(files). One of O(project_src) and O(definition) must be provided. If provided, PyYAML must be available to this module, and O(project_name) must be specified. Note that a temporary directory will be created and deleted afterwards when using this option."
        },
        {
          "key": "dependencies",
          "fieldId": "DEPENDENCIES",
          "type": "boolean",
          "label": "dependencies",
          "default": true,
          "description": "When O(state) is V(present) or V(restarted), specify whether or not to include linked services."
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "env_files",
          "fieldId": "ENV_FILES",
          "type": "text",
          "label": "env files",
          "yamlType": "list",
          "description": "By default environment files are loaded from a C(.env) file located directly under the O(project_src) directory. O(env_files) can be used to specify the path of one or multiple custom environment files instead. The path is relative to the O(project_src) directory."
        },
        {
          "key": "files",
          "fieldId": "FILES",
          "type": "text",
          "label": "files",
          "yamlType": "list",
          "description": "List of Compose file names relative to O(project_src) to be used instead of the main Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). Files are loaded and merged in the order given. Mutually exclusive with O(definition)."
        },
        {
          "key": "ignore_build_events",
          "fieldId": "IGNORE_BUILD_EVENTS",
          "type": "boolean",
          "label": "ignore build events",
          "default": true,
          "description": "Ignores image building events for change detection. If O(state=present) and O(ignore_build_events=true) and O(build=always), a rebuild that does not trigger a container restart no longer results in RV(ignore:changed=true). Note that Docker Compose 2.31.0 is the first Compose 2.x version to emit build events. For older versions, the behavior is always as if O(ignore_build_events=true)."
        },
        {
          "key": "profiles",
          "fieldId": "PROFILES",
          "type": "text",
          "label": "profiles",
          "yamlType": "list",
          "description": "List of profiles to enable when starting services. Equivalent to C(docker compose --profile)."
        },
        {
          "key": "project_name",
          "fieldId": "PROJECT_NAME",
          "type": "text",
          "label": "project name",
          "description": "Provide a project name. If not provided, the project name is taken from the basename of O(project_src). Required when O(definition) is provided."
        },
        {
          "key": "project_src",
          "fieldId": "PROJECT_SRC",
          "type": "text",
          "label": "project src",
          "description": "Path to a directory containing a Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). If O(files) is provided, will look for these files in this directory instead. Mutually exclusive with O(definition). One of O(project_src) and O(definition) must be provided."
        },
        {
          "key": "pull",
          "fieldId": "PULL",
          "type": "dropdown",
          "label": "pull",
          "default": "policy",
          "description": "Whether to pull images before running. This is used when C(docker compose up) is run. V(always) ensures that the images are always pulled, even when already present on the Docker daemon. V(missing) only pulls them when they are not present on the Docker daemon. V(never) never pulls images. If they are not present, the module will fail when trying to create the containers that need them. V(policy) use the Compose file's C(pull_policy) defined for the service to figure out what to do.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "missing",
              "missing"
            ],
            [
              "never",
              "never"
            ],
            [
              "policy",
              "policy"
            ]
          ]
        },
        {
          "key": "recreate",
          "fieldId": "RECREATE",
          "type": "dropdown",
          "label": "recreate",
          "default": "auto",
          "description": "By default containers will be recreated when their configuration differs from the service definition. Setting to V(never) ignores configuration differences and leaves existing containers unchanged. Setting to V(always) forces recreation of all existing containers.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "never",
              "never"
            ],
            [
              "auto",
              "auto"
            ]
          ]
        },
        {
          "key": "remove_images",
          "fieldId": "REMOVE_IMAGES",
          "type": "dropdown",
          "label": "remove images",
          "description": "Use with O(state=absent) to remove all images or only local images.",
          "options": [
            [
              "all",
              "all"
            ],
            [
              "local",
              "local"
            ]
          ]
        },
        {
          "key": "remove_orphans",
          "fieldId": "REMOVE_ORPHANS",
          "type": "boolean",
          "label": "remove orphans",
          "default": false,
          "description": "Remove containers for services not defined in the Compose file."
        },
        {
          "key": "remove_volumes",
          "fieldId": "REMOVE_VOLUMES",
          "type": "boolean",
          "label": "remove volumes",
          "default": false,
          "description": "Use with O(state=absent) to remove data volumes."
        },
        {
          "key": "renew_anon_volumes",
          "fieldId": "RENEW_ANON_VOLUMES",
          "type": "boolean",
          "label": "renew anon volumes",
          "default": false,
          "description": "Whether to recreate instead of reuse anonymous volumes from previous containers. V(true) is equivalent to the C(--renew-anon-volumes) option of C(docker compose up)."
        },
        {
          "key": "scale",
          "fieldId": "SCALE",
          "type": "text",
          "label": "scale",
          "yamlType": "dict",
          "description": "Define how to scale services when running C(docker compose up). Provide a dictionary of key/value pairs where the key is the name of the service and the value is an integer count for the number of containers."
        },
        {
          "key": "services",
          "fieldId": "SERVICES",
          "type": "text",
          "label": "services",
          "yamlType": "list",
          "description": "Specifies a subset of services to be targeted."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Desired state of the project. V(present) is equivalent to running C(docker compose up). V(stopped) is equivalent to running C(docker compose stop). V(absent) is equivalent to running C(docker compose down). V(restarted) is equivalent to running C(docker compose restart).",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "stopped",
              "stopped"
            ],
            [
              "restarted",
              "restarted"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "wait",
          "fieldId": "WAIT",
          "type": "boolean",
          "label": "wait",
          "default": false,
          "description": "When running C(docker compose up), pass C(--wait) to wait for services to be running/healthy. A timeout can be set with the O(wait_timeout) option."
        },
        {
          "key": "wait_timeout",
          "fieldId": "WAIT_TIMEOUT",
          "type": "number",
          "label": "wait timeout",
          "description": "When O(wait=true), wait at most this amount of seconds."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_compose_v2_exec",
      "blockType": "community_docker_compose_v2_exec",
      "label": "docker_compose_v2_exec",
      "tooltip": "Run command in a container of a Compose service",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_compose_v2_exec_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "argv",
          "fieldId": "ARGV",
          "type": "text",
          "label": "argv",
          "yamlType": "list",
          "description": "The command to execute. Since this is a list of arguments, no quoting is needed. Exactly one of O(argv) or O(command) must be specified."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "chdir",
          "fieldId": "CHDIR",
          "type": "text",
          "label": "chdir",
          "description": "The directory to run the command in."
        },
        {
          "key": "check_files_existing",
          "fieldId": "CHECK_FILES_EXISTING",
          "type": "boolean",
          "label": "check files existing",
          "default": true,
          "description": "If set to V(false), the module will not check whether one of the files C(compose.yaml), C(compose.yml), C(docker-compose.yaml), or C(docker-compose.yml) exists in O(project_src) if O(files) is not provided. This can be useful if environment files with C(COMPOSE_FILE) are used to configure a different filename. The module currently does not check for C(COMPOSE_FILE) in environment files or the current environment."
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "command",
          "fieldId": "COMMAND",
          "type": "text",
          "label": "command",
          "description": "The command to execute. Exactly one of O(argv) or O(command) must be specified."
        },
        {
          "key": "definition",
          "fieldId": "DEFINITION",
          "type": "text",
          "label": "definition",
          "yamlType": "dict",
          "description": "Compose file describing one or more services, networks and volumes. Mutually exclusive with O(project_src) and O(files). One of O(project_src) and O(definition) must be provided. If provided, PyYAML must be available to this module, and O(project_name) must be specified. Note that a temporary directory will be created and deleted afterwards when using this option."
        },
        {
          "key": "detach",
          "fieldId": "DETACH",
          "type": "boolean",
          "label": "detach",
          "default": false,
          "description": "Whether to run the command synchronously (O(detach=false), default) or asynchronously (O(detach=true)). If set to V(true), O(stdin) cannot be provided, and the return values RV(stdout), RV(stderr), and RV(rc) are not returned."
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "env",
          "fieldId": "ENV",
          "type": "text",
          "label": "env",
          "yamlType": "dict",
          "description": "Dictionary of environment variables with their respective values to be passed to the command ran inside the container. Values which might be parsed as numbers, booleans or other types by the YAML parser must be quoted (for example V(\"true\")) in order to avoid data loss. Please note that if you are passing values in with Jinja2 templates, like V(\"{{ value }}\"), you need to add V(| string) to prevent Ansible to convert strings such as V(\"true\") back to booleans. The correct way is to use V(\"{{ value | string }}\")."
        },
        {
          "key": "env_files",
          "fieldId": "ENV_FILES",
          "type": "text",
          "label": "env files",
          "yamlType": "list",
          "description": "By default environment files are loaded from a C(.env) file located directly under the O(project_src) directory. O(env_files) can be used to specify the path of one or multiple custom environment files instead. The path is relative to the O(project_src) directory."
        },
        {
          "key": "files",
          "fieldId": "FILES",
          "type": "text",
          "label": "files",
          "yamlType": "list",
          "description": "List of Compose file names relative to O(project_src) to be used instead of the main Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). Files are loaded and merged in the order given. Mutually exclusive with O(definition)."
        },
        {
          "key": "index",
          "fieldId": "INDEX",
          "type": "number",
          "label": "index",
          "description": "The index of the container to run the command in if the service has multiple replicas."
        },
        {
          "key": "privileged",
          "fieldId": "PRIVILEGED",
          "type": "boolean",
          "label": "privileged",
          "default": false,
          "description": "Whether to give extended privileges to the process."
        },
        {
          "key": "profiles",
          "fieldId": "PROFILES",
          "type": "text",
          "label": "profiles",
          "yamlType": "list",
          "description": "List of profiles to enable when starting services. Equivalent to C(docker compose --profile)."
        },
        {
          "key": "project_name",
          "fieldId": "PROJECT_NAME",
          "type": "text",
          "label": "project name",
          "description": "Provide a project name. If not provided, the project name is taken from the basename of O(project_src). Required when O(definition) is provided."
        },
        {
          "key": "project_src",
          "fieldId": "PROJECT_SRC",
          "type": "text",
          "label": "project src",
          "description": "Path to a directory containing a Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). If O(files) is provided, will look for these files in this directory instead. Mutually exclusive with O(definition). One of O(project_src) and O(definition) must be provided."
        },
        {
          "key": "service",
          "fieldId": "SERVICE",
          "type": "text",
          "label": "service",
          "description": "The service to run the command in.",
          "required": true
        },
        {
          "key": "stdin",
          "fieldId": "STDIN",
          "type": "text",
          "label": "stdin",
          "description": "Set the stdin of the command directly to the specified value. Can only be used if O(detach=false)."
        },
        {
          "key": "stdin_add_newline",
          "fieldId": "STDIN_ADD_NEWLINE",
          "type": "boolean",
          "label": "stdin add newline",
          "default": true,
          "description": "If set to V(true), appends a newline to O(stdin)."
        },
        {
          "key": "strip_empty_ends",
          "fieldId": "STRIP_EMPTY_ENDS",
          "type": "boolean",
          "label": "strip empty ends",
          "default": true,
          "description": "Strip empty lines from the end of stdout/stderr in result."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tty",
          "fieldId": "TTY",
          "type": "boolean",
          "label": "tty",
          "default": true,
          "description": "Whether to allocate a TTY."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "description": "If specified, the user to execute this command with."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_compose_v2_pull",
      "blockType": "community_docker_compose_v2_pull",
      "label": "docker_compose_v2_pull",
      "tooltip": "Pull a Docker compose project",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_compose_v2_pull_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "check_files_existing",
          "fieldId": "CHECK_FILES_EXISTING",
          "type": "boolean",
          "label": "check files existing",
          "default": true,
          "description": "If set to V(false), the module will not check whether one of the files C(compose.yaml), C(compose.yml), C(docker-compose.yaml), or C(docker-compose.yml) exists in O(project_src) if O(files) is not provided. This can be useful if environment files with C(COMPOSE_FILE) are used to configure a different filename. The module currently does not check for C(COMPOSE_FILE) in environment files or the current environment."
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "definition",
          "fieldId": "DEFINITION",
          "type": "text",
          "label": "definition",
          "yamlType": "dict",
          "description": "Compose file describing one or more services, networks and volumes. Mutually exclusive with O(project_src) and O(files). One of O(project_src) and O(definition) must be provided. If provided, PyYAML must be available to this module, and O(project_name) must be specified. Note that a temporary directory will be created and deleted afterwards when using this option."
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "env_files",
          "fieldId": "ENV_FILES",
          "type": "text",
          "label": "env files",
          "yamlType": "list",
          "description": "By default environment files are loaded from a C(.env) file located directly under the O(project_src) directory. O(env_files) can be used to specify the path of one or multiple custom environment files instead. The path is relative to the O(project_src) directory."
        },
        {
          "key": "files",
          "fieldId": "FILES",
          "type": "text",
          "label": "files",
          "yamlType": "list",
          "description": "List of Compose file names relative to O(project_src) to be used instead of the main Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). Files are loaded and merged in the order given. Mutually exclusive with O(definition)."
        },
        {
          "key": "ignore_buildable",
          "fieldId": "IGNORE_BUILDABLE",
          "type": "boolean",
          "label": "ignore buildable",
          "default": false,
          "description": "If set to V(true), will not pull images that can be built."
        },
        {
          "key": "ignore_pull_failures",
          "fieldId": "IGNORE_PULL_FAILURES",
          "type": "boolean",
          "label": "ignore pull failures",
          "default": false,
          "description": "If set to V(true), will pull what it can and ignores images with pull failures."
        },
        {
          "key": "include_deps",
          "fieldId": "INCLUDE_DEPS",
          "type": "boolean",
          "label": "include deps",
          "default": false,
          "description": "If set to V(true), also pull services that are declared as dependencies. This only makes sense if O(services) is used."
        },
        {
          "key": "policy",
          "fieldId": "POLICY",
          "type": "dropdown",
          "label": "policy",
          "default": "always",
          "description": "Whether to pull images before running. This is used when C(docker compose up) is ran. V(always) ensures that the images are always pulled, even when already present on the Docker daemon. V(missing) only pulls them when they are not present on the Docker daemon. This is only supported since Docker Compose 2.22.0.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "missing",
              "missing"
            ]
          ]
        },
        {
          "key": "profiles",
          "fieldId": "PROFILES",
          "type": "text",
          "label": "profiles",
          "yamlType": "list",
          "description": "List of profiles to enable when starting services. Equivalent to C(docker compose --profile)."
        },
        {
          "key": "project_name",
          "fieldId": "PROJECT_NAME",
          "type": "text",
          "label": "project name",
          "description": "Provide a project name. If not provided, the project name is taken from the basename of O(project_src). Required when O(definition) is provided."
        },
        {
          "key": "project_src",
          "fieldId": "PROJECT_SRC",
          "type": "text",
          "label": "project src",
          "description": "Path to a directory containing a Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). If O(files) is provided, will look for these files in this directory instead. Mutually exclusive with O(definition). One of O(project_src) and O(definition) must be provided."
        },
        {
          "key": "services",
          "fieldId": "SERVICES",
          "type": "text",
          "label": "services",
          "yamlType": "list",
          "description": "Specifies a subset of services to be targeted."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_compose_v2_run",
      "blockType": "community_docker_compose_v2_run",
      "label": "docker_compose_v2_run",
      "tooltip": "Run command in a new container of a Compose service",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_compose_v2_run_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "argv",
          "fieldId": "ARGV",
          "type": "text",
          "label": "argv",
          "yamlType": "list",
          "description": "The command to execute. Since this is a list of arguments, no quoting is needed. O(argv) or O(command) are mutually exclusive."
        },
        {
          "key": "build",
          "fieldId": "BUILD",
          "type": "boolean",
          "label": "build",
          "default": false,
          "description": "Build image before starting container. Note that building can insert information into RV(stdout) or RV(stderr)."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cap_add",
          "fieldId": "CAP_ADD",
          "type": "text",
          "label": "cap add",
          "yamlType": "list",
          "description": "Linux capabilities to add to the container."
        },
        {
          "key": "cap_drop",
          "fieldId": "CAP_DROP",
          "type": "text",
          "label": "cap drop",
          "yamlType": "list",
          "description": "Linux capabilities to drop from the container."
        },
        {
          "key": "chdir",
          "fieldId": "CHDIR",
          "type": "text",
          "label": "chdir",
          "description": "The directory to run the command in."
        },
        {
          "key": "check_files_existing",
          "fieldId": "CHECK_FILES_EXISTING",
          "type": "boolean",
          "label": "check files existing",
          "default": true,
          "description": "If set to V(false), the module will not check whether one of the files C(compose.yaml), C(compose.yml), C(docker-compose.yaml), or C(docker-compose.yml) exists in O(project_src) if O(files) is not provided. This can be useful if environment files with C(COMPOSE_FILE) are used to configure a different filename. The module currently does not check for C(COMPOSE_FILE) in environment files or the current environment."
        },
        {
          "key": "cleanup",
          "fieldId": "CLEANUP",
          "type": "boolean",
          "label": "cleanup",
          "default": false,
          "description": "Automatically remove th econtainer when it exits. Corresponds to the C(--rm) option of C(docker compose run)."
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "command",
          "fieldId": "COMMAND",
          "type": "text",
          "label": "command",
          "description": "The command to execute. O(argv) or O(command) are mutually exclusive."
        },
        {
          "key": "definition",
          "fieldId": "DEFINITION",
          "type": "text",
          "label": "definition",
          "yamlType": "dict",
          "description": "Compose file describing one or more services, networks and volumes. Mutually exclusive with O(project_src) and O(files). One of O(project_src) and O(definition) must be provided. If provided, PyYAML must be available to this module, and O(project_name) must be specified. Note that a temporary directory will be created and deleted afterwards when using this option."
        },
        {
          "key": "detach",
          "fieldId": "DETACH",
          "type": "boolean",
          "label": "detach",
          "default": false,
          "description": "Whether to run the command synchronously (O(detach=false), default) or asynchronously (O(detach=true)). If set to V(true), O(stdin) cannot be provided, and the return values RV(stdout), RV(stderr), and RV(rc) are not returned. Instead, the return value RV(container_id) is provided."
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "entrypoint",
          "fieldId": "ENTRYPOINT",
          "type": "text",
          "label": "entrypoint",
          "description": "Override the entrypoint of the container image."
        },
        {
          "key": "env",
          "fieldId": "ENV",
          "type": "text",
          "label": "env",
          "yamlType": "dict",
          "description": "Dictionary of environment variables with their respective values to be passed to the command ran inside the container. Values which might be parsed as numbers, booleans or other types by the YAML parser must be quoted (for example V(\"true\")) in order to avoid data loss. Please note that if you are passing values in with Jinja2 templates, like V(\"{{ value }}\"), you need to add V(| string) to prevent Ansible to convert strings such as V(\"true\") back to booleans. The correct way is to use V(\"{{ value | string }}\")."
        },
        {
          "key": "env_files",
          "fieldId": "ENV_FILES",
          "type": "text",
          "label": "env files",
          "yamlType": "list",
          "description": "By default environment files are loaded from a C(.env) file located directly under the O(project_src) directory. O(env_files) can be used to specify the path of one or multiple custom environment files instead. The path is relative to the O(project_src) directory."
        },
        {
          "key": "files",
          "fieldId": "FILES",
          "type": "text",
          "label": "files",
          "yamlType": "list",
          "description": "List of Compose file names relative to O(project_src) to be used instead of the main Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). Files are loaded and merged in the order given. Mutually exclusive with O(definition)."
        },
        {
          "key": "interactive",
          "fieldId": "INTERACTIVE",
          "type": "boolean",
          "label": "interactive",
          "default": true,
          "description": "Whether to keep STDIN open even if not attached."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "list",
          "description": "Add or override labels to the container."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Assign a name to the container."
        },
        {
          "key": "no_deps",
          "fieldId": "NO_DEPS",
          "type": "boolean",
          "label": "no deps",
          "default": false,
          "description": "Do not start linked services."
        },
        {
          "key": "profiles",
          "fieldId": "PROFILES",
          "type": "text",
          "label": "profiles",
          "yamlType": "list",
          "description": "List of profiles to enable when starting services. Equivalent to C(docker compose --profile)."
        },
        {
          "key": "project_name",
          "fieldId": "PROJECT_NAME",
          "type": "text",
          "label": "project name",
          "description": "Provide a project name. If not provided, the project name is taken from the basename of O(project_src). Required when O(definition) is provided."
        },
        {
          "key": "project_src",
          "fieldId": "PROJECT_SRC",
          "type": "text",
          "label": "project src",
          "description": "Path to a directory containing a Compose file (C(compose.yml), C(compose.yaml), C(docker-compose.yml), or C(docker-compose.yaml)). If O(files) is provided, will look for these files in this directory instead. Mutually exclusive with O(definition). One of O(project_src) and O(definition) must be provided."
        },
        {
          "key": "publish",
          "fieldId": "PUBLISH",
          "type": "text",
          "label": "publish",
          "yamlType": "list",
          "description": "Publish a container's port(s) to the host."
        },
        {
          "key": "quiet_pull",
          "fieldId": "QUIET_PULL",
          "type": "boolean",
          "label": "quiet pull",
          "default": false,
          "description": "Pull without printing progress information. Note that pulling can insert information into RV(stdout) or RV(stderr)."
        },
        {
          "key": "remove_orphans",
          "fieldId": "REMOVE_ORPHANS",
          "type": "boolean",
          "label": "remove orphans",
          "default": false,
          "description": "Remove containers for services not defined in the Compose file."
        },
        {
          "key": "service",
          "fieldId": "SERVICE",
          "type": "text",
          "label": "service",
          "description": "The service to run the command in.",
          "required": true
        },
        {
          "key": "service_ports",
          "fieldId": "SERVICE_PORTS",
          "type": "boolean",
          "label": "service ports",
          "default": false,
          "description": "Run command with all service's ports enabled and mapped to the host."
        },
        {
          "key": "stdin",
          "fieldId": "STDIN",
          "type": "text",
          "label": "stdin",
          "description": "Set the stdin of the command directly to the specified value. Can only be used if O(detach=false)."
        },
        {
          "key": "stdin_add_newline",
          "fieldId": "STDIN_ADD_NEWLINE",
          "type": "boolean",
          "label": "stdin add newline",
          "default": true,
          "description": "If set to V(true), appends a newline to O(stdin)."
        },
        {
          "key": "strip_empty_ends",
          "fieldId": "STRIP_EMPTY_ENDS",
          "type": "boolean",
          "label": "strip empty ends",
          "default": true,
          "description": "Strip empty lines from the end of stdout/stderr in result."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tty",
          "fieldId": "TTY",
          "type": "boolean",
          "label": "tty",
          "default": true,
          "description": "Whether to allocate a TTY."
        },
        {
          "key": "use_aliases",
          "fieldId": "USE_ALIASES",
          "type": "boolean",
          "label": "use aliases",
          "default": false,
          "description": "Use the service's network C(useAliases) in the network(s) the container connects to."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "description": "If specified, the user to execute this command with."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "volumes",
          "fieldId": "VOLUMES",
          "type": "text",
          "label": "volumes",
          "yamlType": "list",
          "description": "Bind mount one or more volumes."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_config",
      "blockType": "community_docker_config",
      "label": "docker_config",
      "tooltip": "Manage docker configs",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_config_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "data",
          "fieldId": "DATA",
          "type": "text",
          "label": "data",
          "description": "The value of the config. Mutually exclusive with O(data_src). One of O(data) and O(data_src) is required if O(state=present)."
        },
        {
          "key": "data_is_b64",
          "fieldId": "DATA_IS_B64",
          "type": "boolean",
          "label": "data is b64",
          "default": false,
          "description": "If set to V(true), the data is assumed to be Base64 encoded and will be decoded before being used. To use binary O(data), it is better to keep it Base64 encoded and let it be decoded by this option."
        },
        {
          "key": "data_src",
          "fieldId": "DATA_SRC",
          "type": "text",
          "label": "data src",
          "description": "The file on the target from which to read the config. Mutually exclusive with O(data). One of O(data) and O(data_src) is required if O(state=present)."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Use with O(state=present) to always remove and recreate an existing config. If V(true), an existing config will be replaced, even if it has not been changed."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "A map of key:value meta data, where both the C(key) and C(value) are expected to be a string. If new meta data is provided, or existing meta data is modified, the config will be updated by removing it and creating it again."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the config.",
          "required": true
        },
        {
          "key": "rolling_versions",
          "fieldId": "ROLLING_VERSIONS",
          "type": "boolean",
          "label": "rolling versions",
          "default": false,
          "description": "If set to V(true), configs are created with an increasing version number appended to their name. Adds a label containing the version number to the managed configs with the name C(ansible_version)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Set to V(present), if the config should exist, and V(absent), if it should not.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "template_driver",
          "fieldId": "TEMPLATE_DRIVER",
          "type": "dropdown",
          "label": "template driver",
          "description": "Set to V(golang) to use a Go template in O(data) or a Go template file in O(data_src).",
          "options": [
            [
              "golang",
              "golang"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "versions_to_keep",
          "fieldId": "VERSIONS_TO_KEEP",
          "type": "number",
          "label": "versions to keep",
          "default": 5,
          "description": "When using O(rolling_versions), the number of old versions of the config to keep. Extraneous old configs are deleted after the new one is created. Set to V(-1) to keep everything or V(0) or V(1) to keep only the current one."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_container",
      "blockType": "community_docker_container",
      "label": "docker_container",
      "tooltip": "manage Docker containers",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_container_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "auto_remove",
          "fieldId": "AUTO_REMOVE",
          "type": "boolean",
          "label": "auto remove",
          "description": "Enable auto-removal of the container on daemon side when the container's process exits. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "blkio_weight",
          "fieldId": "BLKIO_WEIGHT",
          "type": "number",
          "label": "blkio weight",
          "description": "Block IO (relative weight), between 10 and 1000."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cap_drop",
          "fieldId": "CAP_DROP",
          "type": "text",
          "label": "cap drop",
          "yamlType": "list",
          "description": "List of capabilities to drop from the container."
        },
        {
          "key": "capabilities",
          "fieldId": "CAPABILITIES",
          "type": "text",
          "label": "capabilities",
          "yamlType": "list",
          "description": "List of capabilities to add to the container. This is equivalent to C(docker run --cap-add), or the docker-compose option C(cap_add)."
        },
        {
          "key": "cgroup_parent",
          "fieldId": "CGROUP_PARENT",
          "type": "text",
          "label": "cgroup parent",
          "description": "Specify the parent cgroup for the container."
        },
        {
          "key": "cgroupns_mode",
          "fieldId": "CGROUPNS_MODE",
          "type": "dropdown",
          "label": "cgroupns mode",
          "description": "Specify the cgroup namespace mode for the container. The Docker CLI calls this simply C(cgroupns).",
          "options": [
            [
              "host",
              "host"
            ],
            [
              "private",
              "private"
            ]
          ]
        },
        {
          "key": "cleanup",
          "fieldId": "CLEANUP",
          "type": "boolean",
          "label": "cleanup",
          "default": false,
          "description": "Use with O(detach=false) to remove the container after successful execution."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "command",
          "fieldId": "COMMAND",
          "type": "text",
          "label": "command",
          "multiline": true,
          "description": "Command to execute when the container starts. A command may be either a string or a list. Prior to version 2.4, strings were split on commas. See O(command_handling) for differences in how strings and lists are handled."
        },
        {
          "key": "command_handling",
          "fieldId": "COMMAND_HANDLING",
          "type": "dropdown",
          "label": "command handling",
          "default": "correct",
          "description": "The default behavior for O(command) (when provided as a list) and O(entrypoint) is to convert them to strings without considering shell quoting rules. (For comparing idempotency, the resulting string is split considering shell quoting rules). Also, setting O(command) to an empty list of string, and setting O(entrypoint) to an empty list will be handled as if these options are not specified. This is different from idempotency handling for other container-config related options. When this is set to V(compatibility), which was the default until community.docker 3.0.0, the current behavior will be kept. When this is set to V(correct), these options are kept as lists, and an empty value or empty list will be handled correctly for idempotency checks. This has been the default since community.docker 3.0.0.",
          "options": [
            [
              "compatibility",
              "compatibility"
            ],
            [
              "correct",
              "correct"
            ]
          ]
        },
        {
          "key": "comparisons",
          "fieldId": "COMPARISONS",
          "type": "text",
          "label": "comparisons",
          "yamlType": "dict",
          "description": "Allows to specify how properties of existing containers are compared with module options to decide whether the container should be recreated / updated or not. Only options which correspond to the state of a container as handled by the Docker daemon can be specified, as well as O(networks). Must be a dictionary specifying for an option one of the keys V(strict), V(ignore) and V(allow_more_present). If V(strict) is specified, values are tested for equality, and changes always result in updating or restarting. If V(ignore) is specified, changes are ignored. V(allow_more_present) is allowed only for lists, sets and dicts. If it is specified for lists or sets, the container will only be updated or restarted if the module option contains a value which is not present in the container's options. If the option is specified for a dict, the container will only be updated or restarted if the module option contains a key which is not present in the container's option, or if the value of a key present differs. The wildcard option C(*) can be used to set one of the default values V(strict) or V(ignore) to I(all) comparisons which are not explicitly set to other values. See the examples for details."
        },
        {
          "key": "container_default_behavior",
          "fieldId": "CONTAINER_DEFAULT_BEHAVIOR",
          "type": "dropdown",
          "label": "container default behavior",
          "default": "no_defaults",
          "description": "In older versions of this module, various module options used to have default values. This caused problems with containers which use different values for these options. The default value is now V(no_defaults). To restore the old behavior, set it to V(compatibility), which will ensure that the default values are used when the values are not explicitly specified by the user. This affects the O(auto_remove), O(detach), O(init), O(interactive), O(memory), O(paused), O(privileged), O(read_only), and O(tty) options.",
          "options": [
            [
              "compatibility",
              "compatibility"
            ],
            [
              "no_defaults",
              "no_defaults"
            ]
          ]
        },
        {
          "key": "cpu_period",
          "fieldId": "CPU_PERIOD",
          "type": "number",
          "label": "cpu period",
          "description": "Limit CPU CFS (Completely Fair Scheduler) period. See O(cpus) for an easier to use alternative."
        },
        {
          "key": "cpu_quota",
          "fieldId": "CPU_QUOTA",
          "type": "number",
          "label": "cpu quota",
          "description": "Limit CPU CFS (Completely Fair Scheduler) quota. See O(cpus) for an easier to use alternative."
        },
        {
          "key": "cpu_shares",
          "fieldId": "CPU_SHARES",
          "type": "number",
          "label": "cpu shares",
          "description": "CPU shares (relative weight)."
        },
        {
          "key": "cpus",
          "fieldId": "CPUS",
          "type": "number",
          "label": "cpus",
          "description": "Specify how much of the available CPU resources a container can use. A value of V(1.5) means that at most one and a half CPU (core) will be used."
        },
        {
          "key": "cpuset_cpus",
          "fieldId": "CPUSET_CPUS",
          "type": "text",
          "label": "cpuset cpus",
          "description": "CPUs in which to allow execution. For example V(1,3) or V(1-3)."
        },
        {
          "key": "cpuset_mems",
          "fieldId": "CPUSET_MEMS",
          "type": "text",
          "label": "cpuset mems",
          "description": "Memory nodes (MEMs) in which to allow execution V(0-3) or V(0,1)."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "default_host_ip",
          "fieldId": "DEFAULT_HOST_IP",
          "type": "text",
          "label": "default host ip",
          "description": "Define the default host IP to use. Must be an empty string, an IPv4 address, or an IPv6 address. With Docker 20.10.2 or newer, this should be set to an empty string (V(\"\")) to avoid the port bindings without an explicit IP address to only bind to IPv4. See U(https://github.com/ansible-collections/community.docker/issues/70) for details. By default, the module will try to auto-detect this value from the C(bridge) network's C(com.docker.network.bridge.host_binding_ipv4) option. If it cannot auto-detect it, it will fall back to V(0.0.0.0)."
        },
        {
          "key": "detach",
          "fieldId": "DETACH",
          "type": "boolean",
          "label": "detach",
          "description": "Enable detached mode to leave the container running in background. If disabled, the task will reflect the status of the container run (failed if the command failed). If O(container_default_behavior=compatibility), this option has a default of V(true)."
        },
        {
          "key": "device_cgroup_rules",
          "fieldId": "DEVICE_CGROUP_RULES",
          "type": "text",
          "label": "device cgroup rules",
          "yamlType": "list",
          "description": "List of cgroup rules to apply to the container."
        },
        {
          "key": "devices",
          "fieldId": "DEVICES",
          "type": "text",
          "label": "devices",
          "yamlType": "list",
          "description": "List of host device bindings to add to the container. Each binding is a mapping expressed in the format C(<path_on_host>:<path_in_container>:<cgroup_permissions>)."
        },
        {
          "key": "dns_opts",
          "fieldId": "DNS_OPTS",
          "type": "text",
          "label": "dns opts",
          "yamlType": "list",
          "description": "List of DNS options."
        },
        {
          "key": "dns_search_domains",
          "fieldId": "DNS_SEARCH_DOMAINS",
          "type": "text",
          "label": "dns search domains",
          "yamlType": "list",
          "description": "List of custom DNS search domains."
        },
        {
          "key": "dns_servers",
          "fieldId": "DNS_SERVERS",
          "type": "text",
          "label": "dns servers",
          "yamlType": "list",
          "description": "List of custom DNS servers."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "domainname",
          "fieldId": "DOMAINNAME",
          "type": "text",
          "label": "domainname",
          "description": "Container domainname."
        },
        {
          "key": "entrypoint",
          "fieldId": "ENTRYPOINT",
          "type": "text",
          "label": "entrypoint",
          "yamlType": "list",
          "description": "Command that overwrites the default C(ENTRYPOINT) of the image. See O(command_handling) for differences in how strings and lists are handled."
        },
        {
          "key": "env",
          "fieldId": "ENV",
          "type": "text",
          "label": "env",
          "yamlType": "dict",
          "description": "Dictionary of key,value pairs. Values which might be parsed as numbers, booleans or other types by the YAML parser must be quoted (for example V(\"true\")) in order to avoid data loss. Please note that if you are passing values in with Jinja2 templates, like V(\"{{ value }}\"), you need to add V(| string) to prevent Ansible to convert strings such as V(\"true\") back to booleans. The correct way is to use V(\"{{ value | string }}\")."
        },
        {
          "key": "env_file",
          "fieldId": "ENV_FILE",
          "type": "text",
          "label": "env file",
          "description": "Path to a file, present on the target, containing environment variables C(FOO=BAR). If variable also present in O(env), then the O(env) value will override."
        },
        {
          "key": "etc_hosts",
          "fieldId": "ETC_HOSTS",
          "type": "text",
          "label": "etc hosts",
          "yamlType": "dict",
          "description": "Dict of host-to-IP mappings, where each host name is a key in the dictionary. Each host name will be added to the container's C(/etc/hosts) file. Instead of an IP address, the special value V(host-gateway) can also be used, which resolves to the host's gateway IP and allows containers to connect to services running on the host."
        },
        {
          "key": "exposed_ports",
          "fieldId": "EXPOSED_PORTS",
          "type": "text",
          "label": "exposed ports",
          "yamlType": "list",
          "description": "List of additional container ports which informs Docker that the container listens on the specified network ports at runtime. If the port is already exposed using C(EXPOSE) in a Dockerfile, it does not need to be exposed again.",
          "aliases": [
            "exposed",
            "expose"
          ]
        },
        {
          "key": "force_kill",
          "fieldId": "FORCE_KILL",
          "type": "boolean",
          "label": "force kill",
          "default": false,
          "description": "Use the kill command when stopping a running container.",
          "aliases": [
            "forcekill"
          ]
        },
        {
          "key": "groups",
          "fieldId": "GROUPS",
          "type": "text",
          "label": "groups",
          "yamlType": "list",
          "description": "List of additional group names and/or IDs that the container process will run as."
        },
        {
          "key": "healthy_wait_timeout",
          "fieldId": "HEALTHY_WAIT_TIMEOUT",
          "type": "number",
          "label": "healthy wait timeout",
          "default": 300,
          "description": "When waiting for the container to become healthy if O(state=healthy), this option controls how long the module waits until the container state becomes healthy. The timeout is specified in seconds. The default, V(300), is 5 minutes. Set this to 0 or a negative value to wait indefinitely. Note that depending on the container this can result in the module not terminating."
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "The container's hostname."
        },
        {
          "key": "image",
          "fieldId": "IMAGE",
          "type": "text",
          "label": "image",
          "description": "Repository path and tag used to create the container. If an image is not found or pull is true, the image will be pulled from the registry. If no tag is included, V(latest) will be used. Can also be an image ID. If this is the case, the image is assumed to be available locally. The O(pull) option is ignored for this case."
        },
        {
          "key": "image_comparison",
          "fieldId": "IMAGE_COMPARISON",
          "type": "dropdown",
          "label": "image comparison",
          "default": "desired-image",
          "description": "Determines which image to use for idempotency checks that depend on image parameters. The default, V(desired-image), will use the image that is provided to the module with the O(image) parameter. V(current-image) will use the image that the container is currently using, if the container exists. It falls back to the image that is provided in case the container does not yet exist. This affects the O(env), O(env_file), O(exposed_ports), O(labels), and O(volumes) options.",
          "options": [
            [
              "desired-image",
              "desired-image"
            ],
            [
              "current-image",
              "current-image"
            ]
          ]
        },
        {
          "key": "image_label_mismatch",
          "fieldId": "IMAGE_LABEL_MISMATCH",
          "type": "dropdown",
          "label": "image label mismatch",
          "default": "ignore",
          "description": "How to handle labels inherited from the image that are not set explicitly. When V(ignore), labels that are present in the image but not specified in O(labels) will be ignored. This is useful to avoid having to specify the image labels in O(labels) while keeping labels O(comparisons) V(strict). When V(fail), if there are labels present in the image which are not set from O(labels), the module will fail. This prevents introducing unexpected labels from the base image. B(Warning:) This option is ignored unless C(labels: strict) or C(*: strict) is specified in the O(comparisons) option.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "fail",
              "fail"
            ]
          ]
        },
        {
          "key": "image_name_mismatch",
          "fieldId": "IMAGE_NAME_MISMATCH",
          "type": "dropdown",
          "label": "image name mismatch",
          "default": "recreate",
          "description": "Determines what the module does if the image matches, but the image name in the container's configuration does not match the image name provided to the module. This is ignored if C(image: ignore) is set in O(comparisons). If set to V(recreate) (default) the container will be recreated. If set to V(ignore) the container will not be recreated because of this. It might still get recreated for other reasons. This has been the default behavior of the module for a long time, but might not be what users expect. The default changed from V(ignore) to V(recreate) in community.docker 4.0.0.",
          "options": [
            [
              "recreate",
              "recreate"
            ],
            [
              "ignore",
              "ignore"
            ]
          ]
        },
        {
          "key": "init",
          "fieldId": "INIT",
          "type": "boolean",
          "label": "init",
          "description": "Run an init inside the container that forwards signals and reaps processes. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "interactive",
          "fieldId": "INTERACTIVE",
          "type": "boolean",
          "label": "interactive",
          "description": "Keep stdin open after a container is launched, even if not attached. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "ipc_mode",
          "fieldId": "IPC_MODE",
          "type": "text",
          "label": "ipc mode",
          "description": "Set the IPC mode for the container. Can be one of V(container:<name|id>) to reuse another container's IPC namespace or V(host) to use the host's IPC namespace within the container."
        },
        {
          "key": "keep_volumes",
          "fieldId": "KEEP_VOLUMES",
          "type": "boolean",
          "label": "keep volumes",
          "default": true,
          "description": "Retain anonymous volumes associated with a removed container."
        },
        {
          "key": "kernel_memory",
          "fieldId": "KERNEL_MEMORY",
          "type": "text",
          "label": "kernel memory",
          "description": "Kernel memory limit in format C(<number>[<unit>]). Number is a positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Minimum is V(4M). Omitting the unit defaults to bytes."
        },
        {
          "key": "kill_signal",
          "fieldId": "KILL_SIGNAL",
          "type": "text",
          "label": "kill signal",
          "description": "Override default signal used to kill a running container."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "Dictionary of key value pairs."
        },
        {
          "key": "links",
          "fieldId": "LINKS",
          "type": "text",
          "label": "links",
          "yamlType": "list",
          "description": "List of name aliases for linked containers in the format C(container_name:alias). Setting this will force container to be restarted."
        },
        {
          "key": "log_driver",
          "fieldId": "LOG_DRIVER",
          "type": "text",
          "label": "log driver",
          "description": "Specify the logging driver. Docker uses V(json-file) by default. See L(the Docker logging configuration documentation,https://docs.docker.com/config/containers/logging/configure/) for possible choices."
        },
        {
          "key": "log_options",
          "fieldId": "LOG_OPTIONS",
          "type": "text",
          "label": "log options",
          "yamlType": "dict",
          "description": "Dictionary of options specific to the chosen O(log_driver). See U(https://docs.docker.com/engine/admin/logging/overview/) for details. O(log_driver) needs to be specified for O(log_options) to take effect, even if using the default V(json-file) driver.",
          "aliases": [
            "log_opt"
          ]
        },
        {
          "key": "mac_address",
          "fieldId": "MAC_ADDRESS",
          "type": "text",
          "label": "mac address",
          "description": "Container MAC address (for example, V(92:d0:c6:0a:29:33)). Note that the global container-wide MAC address is deprecated and no longer used since Docker API version 1.44. Use O(networks[].mac_address) instead."
        },
        {
          "key": "memory",
          "fieldId": "MEMORY",
          "type": "text",
          "label": "memory",
          "description": "Memory limit in format C(<number>[<unit>]). Number is a positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes. If O(container_default_behavior=compatibility), this option has a default of V(\"0\")."
        },
        {
          "key": "memory_reservation",
          "fieldId": "MEMORY_RESERVATION",
          "type": "text",
          "label": "memory reservation",
          "description": "Memory soft limit in format C(<number>[<unit>]). Number is a positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes."
        },
        {
          "key": "memory_swap",
          "fieldId": "MEMORY_SWAP",
          "type": "text",
          "label": "memory swap",
          "description": "Total memory limit (memory + swap) in format C(<number>[<unit>]), or the special values V(unlimited) or V(-1) for unlimited swap usage. Number is a positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes."
        },
        {
          "key": "memory_swappiness",
          "fieldId": "MEMORY_SWAPPINESS",
          "type": "number",
          "label": "memory swappiness",
          "description": "Tune a container's memory swappiness behavior. Accepts an integer between 0 and 100. If not set, the value will be remain the same if container exists and will be inherited from the host machine if it is (re-)created."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Assign a name to a new container or match an existing container. When identifying an existing container name may be a name or a long or short container ID.",
          "required": true
        },
        {
          "key": "network_mode",
          "fieldId": "NETWORK_MODE",
          "type": "text",
          "label": "network mode",
          "description": "Connect the container to a network. Choices are V(bridge), V(host), V(none), C(container:<name|id>), C(<network_name>) or V(default). Since community.docker 2.0.0, if O(networks_cli_compatible=true) and O(networks) contains at least one network, the default value for O(network_mode) is the name of the first network in the O(networks) list. You can prevent this by explicitly specifying a value for O(network_mode), like the default value V(default) which will be used by Docker if O(network_mode) is not specified."
        },
        {
          "key": "networks_cli_compatible",
          "fieldId": "NETWORKS_CLI_COMPATIBLE",
          "type": "boolean",
          "label": "networks cli compatible",
          "default": true,
          "description": "If O(networks_cli_compatible=true) (default), this module will behave as C(docker run --network) and will B(not) add the default network if O(networks) is specified. If O(networks) is not specified, the default network will be attached. When O(networks_cli_compatible=false) and networks are provided to the module with the O(networks) option, the module behaves differently than C(docker run --network): C(docker run --network other) will create a container with network C(other) attached, but the default network not attached. This module with O(networks) set to C({name: other}) will create a container with both C(default) and C(other) attached. If C(networks: strict) or C(*: strict) is set in O(comparisons), the C(default) network will be removed afterwards."
        },
        {
          "key": "oom_killer",
          "fieldId": "OOM_KILLER",
          "type": "boolean",
          "label": "oom killer",
          "description": "Whether or not to disable OOM Killer for the container."
        },
        {
          "key": "oom_score_adj",
          "fieldId": "OOM_SCORE_ADJ",
          "type": "number",
          "label": "oom score adj",
          "description": "An integer value containing the score given to the container in order to tune OOM killer preferences."
        },
        {
          "key": "output_logs",
          "fieldId": "OUTPUT_LOGS",
          "type": "boolean",
          "label": "output logs",
          "default": false,
          "description": "If set to true, output of the container command will be printed. Only effective when O(log_driver) is set to V(json-file), V(journald), or V(local)."
        },
        {
          "key": "paused",
          "fieldId": "PAUSED",
          "type": "boolean",
          "label": "paused",
          "description": "Use with the started state to pause running processes inside the container. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "pid_mode",
          "fieldId": "PID_MODE",
          "type": "text",
          "label": "pid mode",
          "description": "Set the PID namespace mode for the container."
        },
        {
          "key": "pids_limit",
          "fieldId": "PIDS_LIMIT",
          "type": "number",
          "label": "pids limit",
          "description": "Set PIDs limit for the container. It accepts an integer value. Set V(-1) for unlimited PIDs."
        },
        {
          "key": "platform",
          "fieldId": "PLATFORM",
          "type": "text",
          "label": "platform",
          "description": "Platform for the container in the format C(os[/arch[/variant]]). Note that since community.docker 3.5.0, the module uses both the image's metadata and the Docker daemon's information to normalize platform strings similarly to how Docker itself is doing this. If you notice idempotency problems, please verify whether this is still a problem with the latest release of community.docker, and if it is, L(create an issue in the community.docker GitHub repository, https://github.com/ansible-collections/community.docker/issues/new?assignees=&labels=&projects=&template=bug_report.md). For older community.docker versions, you can use the O(comparisons) option with C(platform: ignore) to prevent accidental recreation of the container due to this."
        },
        {
          "key": "privileged",
          "fieldId": "PRIVILEGED",
          "type": "boolean",
          "label": "privileged",
          "description": "Give extended privileges to the container. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "publish_all_ports",
          "fieldId": "PUBLISH_ALL_PORTS",
          "type": "boolean",
          "label": "publish all ports",
          "description": "Publish all ports to the host. Any specified port bindings from O(published_ports) will remain intact when V(true)."
        },
        {
          "key": "published_ports",
          "fieldId": "PUBLISHED_PORTS",
          "type": "text",
          "label": "published ports",
          "yamlType": "list",
          "description": "List of ports to publish from the container to the host. Use docker CLI syntax: V(8000), V(9000:8000), or V(0.0.0.0:9000:8000), where 8000 is a container port, 9000 is a host port, and 0.0.0.0 is a host interface. Port ranges can be used for source and destination ports. If two ranges with different lengths are specified, the shorter range will be used. Since community.general 0.2.0, if the source port range has length 1, the port will not be assigned to the first port of the destination range, but to a free port in that range. This is the same behavior as for C(docker) command line utility. Bind addresses must be either IPv4 or IPv6 addresses. Hostnames are B(not) allowed. This is different from the C(docker) command line utility. Use the P(community.general.dig#lookup) lookup to resolve hostnames. If O(networks) parameter is provided, will inspect each network to see if there exists a bridge network with optional parameter C(com.docker.network.bridge.host_binding_ipv4). If such a network is found, then published ports where no host IP address is specified will be bound to the host IP pointed to by C(com.docker.network.bridge.host_binding_ipv4). Note that the first bridge network with a C(com.docker.network.bridge.host_binding_ipv4) value encountered in the list of O(networks) is the one that will be used. The value V(all) was allowed in earlier versions of this module. Support for it was removed in community.docker 3.0.0. Use the O(publish_all_ports) option instead.",
          "aliases": [
            "ports"
          ]
        },
        {
          "key": "pull",
          "fieldId": "PULL",
          "type": "dropdown",
          "label": "pull",
          "default": "missing",
          "description": "If set to V(never), will never try to pull an image. Will fail if the image is not available on the Docker daemon. If set to V(missing) or V(false), only pull the image if it is not available on the Docker daemon. This is the default behavior. If set to V(always) or V(true), always try to pull the latest version of the image. B(Note:) images are only pulled when specified by name. If the image is specified as a image ID (hash), it cannot be pulled, and this option is ignored. B(Note:) the values V(never), V(missing), and V(always) are only available since community.docker 3.8.0. Earlier versions only support V(true) and V(false).",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "missing",
              "missing"
            ],
            [
              "always",
              "always"
            ],
            [
              "True",
              "True"
            ],
            [
              "False",
              "False"
            ]
          ]
        },
        {
          "key": "pull_check_mode_behavior",
          "fieldId": "PULL_CHECK_MODE_BEHAVIOR",
          "type": "dropdown",
          "label": "pull check mode behavior",
          "default": "image_not_present",
          "description": "Allows to adjust the behavior when O(pull=always) or O(pull=true) in check mode. Since the Docker daemon does not expose any functionality to test whether a pull will result in a changed image, the module by default acts like O(pull=always) only results in a change when the image is not present. If set to V(image_not_present) (default), only report changes in check mode when the image is not present. If set to V(always), always report changes in check mode.",
          "options": [
            [
              "image_not_present",
              "image_not_present"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "read_only",
          "fieldId": "READ_ONLY",
          "type": "boolean",
          "label": "read only",
          "description": "Mount the container's root file system as read-only. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "recreate",
          "fieldId": "RECREATE",
          "type": "boolean",
          "label": "recreate",
          "default": false,
          "description": "Use with present and started states to force the re-creation of an existing container."
        },
        {
          "key": "removal_wait_timeout",
          "fieldId": "REMOVAL_WAIT_TIMEOUT",
          "type": "number",
          "label": "removal wait timeout",
          "description": "When removing an existing container, the docker daemon API call exists after the container is scheduled for removal. Removal usually is very fast, but it can happen that during high I/O load, removal can take longer. By default, the module will wait until the container has been removed before trying to (re-)create it, however long this takes. By setting this option, the module will wait at most this many seconds for the container to be removed. If the container is still in the removal phase after this many seconds, the module will fail."
        },
        {
          "key": "restart",
          "fieldId": "RESTART",
          "type": "boolean",
          "label": "restart",
          "default": false,
          "description": "Use with started state to force a matching container to be stopped and restarted."
        },
        {
          "key": "restart_policy",
          "fieldId": "RESTART_POLICY",
          "type": "dropdown",
          "label": "restart policy",
          "description": "Container restart policy. Place quotes around V(no) option.",
          "options": [
            [
              "no",
              "no"
            ],
            [
              "on-failure",
              "on-failure"
            ],
            [
              "always",
              "always"
            ],
            [
              "unless-stopped",
              "unless-stopped"
            ]
          ]
        },
        {
          "key": "restart_retries",
          "fieldId": "RESTART_RETRIES",
          "type": "number",
          "label": "restart retries",
          "description": "Use with restart policy to control maximum number of restart attempts."
        },
        {
          "key": "runtime",
          "fieldId": "RUNTIME",
          "type": "text",
          "label": "runtime",
          "description": "Runtime to use for the container."
        },
        {
          "key": "security_opts",
          "fieldId": "SECURITY_OPTS",
          "type": "text",
          "label": "security opts",
          "yamlType": "list",
          "description": "List of security options in the form of C(\"label:user:User\")."
        },
        {
          "key": "shm_size",
          "fieldId": "SHM_SIZE",
          "type": "text",
          "label": "shm size",
          "description": "Size of C(/dev/shm) in format C(<number>[<unit>]). Number is positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes. If you omit the size entirely, Docker daemon uses V(64M)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "started",
          "description": "V(absent) - A container matching the specified name will be stopped and removed. Use O(force_kill) to kill the container rather than stopping it. Use O(keep_volumes) to retain anonymous volumes associated with the removed container. V(present) - Asserts the existence of a container matching the name and any provided configuration parameters. If no container matches the name, a container will be created. If a container matches the name but the provided configuration does not match, the container will be updated, if it can be. If it cannot be updated, it will be removed and re-created with the requested config. V(started) - Asserts that the container is first V(present), and then if the container is not running moves it to a running state. Use O(restart) to force a matching container to be stopped and restarted. V(healthy) - Asserts that the container is V(present) and V(started), and is actually healthy as well. This means that the conditions defined in O(healthcheck) respectively in the image's C(HEALTHCHECK) (L(Docker reference for HEALTHCHECK, https://docs.docker.com/reference/dockerfile/#healthcheck)) are satisfied. The time waited can be controlled with O(healthy_wait_timeout). This state has been added in community.docker 3.11.0. V(stopped) - Asserts that the container is first V(present), and then if the container is running moves it to a stopped state. To control what will be taken into account when comparing configuration, see the O(comparisons) option. To avoid that the image version will be taken into account, you can also use the V(image: ignore) in the O(comparisons) option. Use the O(recreate) option to always force re-creation of a matching container, even if it is running. If the container should be killed instead of stopped in case it needs to be stopped for recreation, or because O(state) is V(stopped), please use the O(force_kill) option. Use O(keep_volumes) to retain anonymous volumes associated with a removed container. Use O(keep_volumes) to retain anonymous volumes associated with a removed container.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ],
            [
              "healthy",
              "healthy"
            ],
            [
              "stopped",
              "stopped"
            ],
            [
              "started",
              "started"
            ]
          ]
        },
        {
          "key": "stop_signal",
          "fieldId": "STOP_SIGNAL",
          "type": "text",
          "label": "stop signal",
          "description": "Override default signal used to stop the container."
        },
        {
          "key": "stop_timeout",
          "fieldId": "STOP_TIMEOUT",
          "type": "number",
          "label": "stop timeout",
          "description": "Number of seconds to wait for the container to stop before sending C(SIGKILL). When the container is created by this module, its C(StopTimeout) configuration will be set to this value. When the container is stopped, will be used as a timeout for stopping the container. In case the container has a custom C(StopTimeout) configuration, the behavior depends on the version of the docker daemon. New versions of the docker daemon will always use the container's configured C(StopTimeout) value if it has been configured."
        },
        {
          "key": "storage_opts",
          "fieldId": "STORAGE_OPTS",
          "type": "text",
          "label": "storage opts",
          "yamlType": "dict",
          "description": "Storage driver options for this container as a key-value mapping."
        },
        {
          "key": "sysctls",
          "fieldId": "SYSCTLS",
          "type": "text",
          "label": "sysctls",
          "yamlType": "dict",
          "description": "Dictionary of key,value pairs."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "tmpfs",
          "fieldId": "TMPFS",
          "type": "text",
          "label": "tmpfs",
          "yamlType": "list",
          "description": "Mount a tmpfs directory."
        },
        {
          "key": "tty",
          "fieldId": "TTY",
          "type": "boolean",
          "label": "tty",
          "description": "Allocate a pseudo-TTY. If O(container_default_behavior=compatibility), this option has a default of V(false)."
        },
        {
          "key": "ulimits",
          "fieldId": "ULIMITS",
          "type": "text",
          "label": "ulimits",
          "yamlType": "list",
          "description": "List of ulimit options. A ulimit is specified as V(nofile:262144:262144)."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "description": "Sets the username or UID used and optionally the groupname or GID for the specified command. Can be of the forms C(user), C(user:group), C(uid), C(uid:gid), C(user:gid) or C(uid:group)."
        },
        {
          "key": "userns_mode",
          "fieldId": "USERNS_MODE",
          "type": "text",
          "label": "userns mode",
          "description": "Set the user namespace mode for the container. Currently, the only valid value are V(host) and the empty string (V(\"\"))."
        },
        {
          "key": "uts",
          "fieldId": "UTS",
          "type": "text",
          "label": "uts",
          "description": "Set the UTS namespace mode for the container."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "volume_driver",
          "fieldId": "VOLUME_DRIVER",
          "type": "text",
          "label": "volume driver",
          "description": "The container volume driver."
        },
        {
          "key": "volumes",
          "fieldId": "VOLUMES",
          "type": "text",
          "label": "volumes",
          "yamlType": "list",
          "description": "List of volumes to mount within the container. Use docker CLI-style syntax: C(/host:/container[:mode]). Mount modes can be a comma-separated list of various modes such as V(ro), V(rw), V(consistent), V(delegated), V(cached), V(rprivate), V(private), V(rshared), V(shared), V(rslave), V(slave), and V(nocopy). Note that the docker daemon might not support all modes and combinations of such modes. SELinux hosts can additionally use V(z) or V(Z) to use a shared or private label for the volume. Note that Ansible 2.7 and earlier only supported one mode, which had to be one of V(ro), V(rw), V(z), and V(Z)."
        },
        {
          "key": "volumes_from",
          "fieldId": "VOLUMES_FROM",
          "type": "text",
          "label": "volumes from",
          "yamlType": "list",
          "description": "List of container names or IDs to get volumes from."
        },
        {
          "key": "working_dir",
          "fieldId": "WORKING_DIR",
          "type": "text",
          "label": "working dir",
          "description": "Path to the working directory."
        }
      ],
      "unmappedParams": [
        "device_read_bps (has suboptions)",
        "device_read_iops (has suboptions)",
        "device_requests (has suboptions)",
        "device_write_bps (has suboptions)",
        "device_write_iops (has suboptions)",
        "healthcheck (has suboptions)",
        "mounts (has suboptions)",
        "networks (has suboptions)"
      ]
    },
    {
      "fqcn": "community.docker.docker_container_copy_into",
      "blockType": "community_docker_container_copy_into",
      "label": "docker_container_copy_into",
      "tooltip": "Copy a file into a Docker container",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_container_copy_into_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "container",
          "fieldId": "CONTAINER",
          "type": "text",
          "label": "container",
          "description": "The name of the container to copy files to.",
          "required": true
        },
        {
          "key": "container_path",
          "fieldId": "CONTAINER_PATH",
          "type": "text",
          "label": "container path",
          "description": "Path to a file inside the Docker container. Must be an absolute path.",
          "required": true
        },
        {
          "key": "content",
          "fieldId": "CONTENT",
          "type": "text",
          "label": "content",
          "description": "The file's content. If you plan to provide binary data, provide it pre-encoded to base64, and set O(content_is_b64=true). Mutually exclusive with O(path). One of O(content) and O(path) is required."
        },
        {
          "key": "content_is_b64",
          "fieldId": "CONTENT_IS_B64",
          "type": "boolean",
          "label": "content is b64",
          "default": false,
          "description": "If set to V(true), the content in O(content) is assumed to be Base64 encoded and will be decoded before being used. To use binary O(content), it is better to keep it Base64 encoded and let it be decoded by this option. Otherwise you risk the data to be interpreted as UTF-8 and corrupted."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "follow",
          "fieldId": "FOLLOW",
          "type": "boolean",
          "label": "follow",
          "default": false,
          "description": "This flag indicates that filesystem links in the Docker container, if they exist, should be followed."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "description": "If set to V(true), force writing the file (without performing any idempotency checks). If set to V(false), only write the file if it does not exist on the target. If a filesystem object exists at the destination, the module will not do any change. If this option is not specified, the module will be idempotent. To verify idempotency, it will try to get information on the filesystem object in the container, and if everything seems to match will download the file from the container to compare it to the file to upload."
        },
        {
          "key": "group_id",
          "fieldId": "GROUP_ID",
          "type": "number",
          "label": "group id",
          "description": "The group ID to use when writing the file to disk. If provided, O(owner_id) must also be provided. If not provided, the module will try to determine the user and group ID for the current user in the container. This will only work if C(/bin/sh) is present in the container and the C(id) binary or shell builtin is available. Also the container must be running."
        },
        {
          "key": "local_follow",
          "fieldId": "LOCAL_FOLLOW",
          "type": "boolean",
          "label": "local follow",
          "default": true,
          "description": "This flag indicates that filesystem links in the source tree (where the module is executed), if they exist, should be followed."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "text",
          "label": "mode",
          "multiline": true,
          "description": "The file mode to use when writing the file to disk. Will use the file's mode from the source system if this option is not provided. This option is parsed depending on how O(mode_parse) is set."
        },
        {
          "key": "mode_parse",
          "fieldId": "MODE_PARSE",
          "type": "text",
          "label": "mode parse",
          "default": "legacy",
          "description": "Determines how to parse the O(mode) parameter."
        },
        {
          "key": "owner_id",
          "fieldId": "OWNER_ID",
          "type": "number",
          "label": "owner id",
          "description": "The owner ID to use when writing the file to disk. If provided, O(group_id) must also be provided. If not provided, the module will try to determine the user and group ID for the current user in the container. This will only work if C(/bin/sh) is present in the container and the C(id) binary or shell builtin is available. Also the container must be running."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Path to a file on the managed node. Mutually exclusive with O(content). One of O(content) and O(path) is required."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_container_exec",
      "blockType": "community_docker_container_exec",
      "label": "docker_container_exec",
      "tooltip": "Execute command in a docker container",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_container_exec_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "argv",
          "fieldId": "ARGV",
          "type": "text",
          "label": "argv",
          "yamlType": "list",
          "description": "The command to execute. Since this is a list of arguments, no quoting is needed. Exactly one of O(argv) or O(command) must be specified."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "chdir",
          "fieldId": "CHDIR",
          "type": "text",
          "label": "chdir",
          "description": "The directory to run the command in."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "command",
          "fieldId": "COMMAND",
          "type": "text",
          "label": "command",
          "description": "The command to execute. Exactly one of O(argv) or O(command) must be specified."
        },
        {
          "key": "container",
          "fieldId": "CONTAINER",
          "type": "text",
          "label": "container",
          "description": "The name of the container to execute the command in.",
          "required": true
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "detach",
          "fieldId": "DETACH",
          "type": "boolean",
          "label": "detach",
          "default": false,
          "description": "Whether to run the command synchronously (O(detach=false), default) or asynchronously (O(detach=true)). If set to V(true), O(stdin) cannot be provided, and the return values RV(stdout), RV(stderr), and RV(rc) are not returned."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "env",
          "fieldId": "ENV",
          "type": "text",
          "label": "env",
          "yamlType": "dict",
          "description": "Dictionary of environment variables with their respective values to be passed to the command ran inside the container. Values which might be parsed as numbers, booleans or other types by the YAML parser must be quoted (for example V(\"true\")) in order to avoid data loss. Please note that if you are passing values in with Jinja2 templates, like V(\"{{ value }}\"), you need to add V(| string) to prevent Ansible to convert strings such as V(\"true\") back to booleans. The correct way is to use V(\"{{ value | string }}\")."
        },
        {
          "key": "stdin",
          "fieldId": "STDIN",
          "type": "text",
          "label": "stdin",
          "description": "Set the stdin of the command directly to the specified value. Can only be used if O(detach=false)."
        },
        {
          "key": "stdin_add_newline",
          "fieldId": "STDIN_ADD_NEWLINE",
          "type": "boolean",
          "label": "stdin add newline",
          "default": true,
          "description": "If set to V(true), appends a newline to O(stdin)."
        },
        {
          "key": "strip_empty_ends",
          "fieldId": "STRIP_EMPTY_ENDS",
          "type": "boolean",
          "label": "strip empty ends",
          "default": true,
          "description": "Strip empty lines from the end of stdout/stderr in result."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "tty",
          "fieldId": "TTY",
          "type": "boolean",
          "label": "tty",
          "default": false,
          "description": "Whether to allocate a TTY."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "description": "If specified, the user to execute this command with."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_container_info",
      "blockType": "community_docker_container_info",
      "label": "docker_container_info",
      "tooltip": "Retrieves facts about docker container",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_container_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the container to inspect. When identifying an existing container name may be a name or a long or short container ID.",
          "required": true
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_context_info",
      "blockType": "community_docker_context_info",
      "label": "docker_context_info",
      "tooltip": "Retrieve information on Docker contexts for the current user",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_context_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "Override for the default context's name. This is preferably used for context selection when O(only_current=true), and it is used to compute the return values RV(contexts[].current) and RV(current_context_name)."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "A specific Docker CLI context to query. The module will fail if this context does not exist. If you simply want to query whether a context exists, do not specify this parameter and use Jinja2 to search the resulting list for a context of the given name instead. Mutually exclusive with O(only_current)."
        },
        {
          "key": "only_current",
          "fieldId": "ONLY_CURRENT",
          "type": "boolean",
          "label": "only current",
          "default": false,
          "description": "If set to V(true), RV(contexts) will just contain the current context and none else. If set to V(false) (default), RV(contexts) will list all contexts, unless O(name) is specified. Mutually exclusive to O(name)."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_host_info",
      "blockType": "community_docker_host_info",
      "label": "docker_host_info",
      "tooltip": "Retrieves facts about docker host and lists of objects of the services",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_host_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "containers",
          "fieldId": "CONTAINERS",
          "type": "boolean",
          "label": "containers",
          "default": false,
          "description": "Whether to list containers."
        },
        {
          "key": "containers_all",
          "fieldId": "CONTAINERS_ALL",
          "type": "boolean",
          "label": "containers all",
          "default": false,
          "description": "By default, only running containers are returned. This corresponds to the C(--all) option to C(docker container list)."
        },
        {
          "key": "containers_filters",
          "fieldId": "CONTAINERS_FILTERS",
          "type": "text",
          "label": "containers filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting containers to list. For example, C(until: 24h). C(label) is a special case of filter which can be a string C(<key>) matching when a label is present, a string C(<key>=<value>) matching when a label has a particular value, or a list of strings C(<key>)/C(<key>=<value). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/container_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "disk_usage",
          "fieldId": "DISK_USAGE",
          "type": "boolean",
          "label": "disk usage",
          "default": false,
          "description": "Summary information on used disk space by all Docker layers. The output is a sum of images, volumes, containers and build cache."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "images",
          "fieldId": "IMAGES",
          "type": "boolean",
          "label": "images",
          "default": false,
          "description": "Whether to list images."
        },
        {
          "key": "images_filters",
          "fieldId": "IMAGES_FILTERS",
          "type": "text",
          "label": "images filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting images to list. For example, C(dangling: true). C(label) is a special case of filter which can be a string C(<key>) matching when a label is present, a string C(<key>=<value>) matching when a label has a particular value, or a list of strings C(<key>)/C(<key>=<value). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/image_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "networks",
          "fieldId": "NETWORKS",
          "type": "boolean",
          "label": "networks",
          "default": false,
          "description": "Whether to list networks."
        },
        {
          "key": "networks_filters",
          "fieldId": "NETWORKS_FILTERS",
          "type": "text",
          "label": "networks filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting networks to list. C(label) is a special case of filter which can be a string C(<key>) matching when a label is present, a string C(<key>=<value>) matching when a label has a particular value, or a list of strings C(<key>)/C(<key>=<value). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/network_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "verbose_output",
          "fieldId": "VERBOSE_OUTPUT",
          "type": "boolean",
          "label": "verbose output",
          "default": false,
          "description": "When set to V(true) and O(networks), O(volumes), O(images), O(containers), or O(disk_usage) is set to V(true) then output will contain verbose information about objects matching the full output of API method. For details see the documentation of your version of Docker API at U(https://docs.docker.com/engine/api/). The verbose output in this module contains only subset of information returned by this module for each type of the objects."
        },
        {
          "key": "volumes",
          "fieldId": "VOLUMES",
          "type": "boolean",
          "label": "volumes",
          "default": false,
          "description": "Whether to list volumes."
        },
        {
          "key": "volumes_filters",
          "fieldId": "VOLUMES_FILTERS",
          "type": "text",
          "label": "volumes filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting volumes to list. C(label) is a special case of filter which can be a string C(<key>) matching when a label is present, a string C(<key>=<value>) matching when a label has a particular value, or a list of strings C(<key>)/C(<key>=<value). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/volume_prune/#filtering) for more information on possible filters."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image",
      "blockType": "community_docker_image",
      "label": "docker_image",
      "tooltip": "Manage docker images",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "archive_path",
          "fieldId": "ARCHIVE_PATH",
          "type": "text",
          "label": "archive path",
          "description": "Use with O(state=present) to archive an image to a C(.tar) file."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "force_absent",
          "fieldId": "FORCE_ABSENT",
          "type": "boolean",
          "label": "force absent",
          "default": false,
          "description": "Use with O(state=absent) to un-tag and remove all images matching the specified name."
        },
        {
          "key": "force_source",
          "fieldId": "FORCE_SOURCE",
          "type": "boolean",
          "label": "force source",
          "default": false,
          "description": "Use with O(state=present) to build, load or pull an image (depending on the value of the O(source) option) when the image already exists."
        },
        {
          "key": "force_tag",
          "fieldId": "FORCE_TAG",
          "type": "boolean",
          "label": "force tag",
          "default": false,
          "description": "Use with O(state=present) to force tagging an image."
        },
        {
          "key": "load_path",
          "fieldId": "LOAD_PATH",
          "type": "text",
          "label": "load path",
          "description": "Use with O(state=present) to load an image from a C(.tar) file. Set O(source=load) if you want to load the image."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format will be one of: C(name), C(repository/name), C(registry_server:port/name). When pushing or pulling an image the name can optionally include the tag by appending C(:tag_name). Note that image IDs (hashes) are only supported for O(state=absent), for O(state=present) with O(source=load), and for O(state=present) with O(source=local).",
          "required": true
        },
        {
          "key": "push",
          "fieldId": "PUSH",
          "type": "boolean",
          "label": "push",
          "default": false,
          "description": "Push the image to the registry. Specify the registry as part of the O(name) or O(repository) parameter."
        },
        {
          "key": "repository",
          "fieldId": "REPOSITORY",
          "type": "text",
          "label": "repository",
          "description": "Use with O(state=present) to tag the image. Expects format C(repository:tag). If no tag is provided, will use the value of the O(tag) parameter or V(latest). If O(push=true), O(repository) must either include a registry, or will be assumed to belong to the default registry (Docker Hub)."
        },
        {
          "key": "source",
          "fieldId": "SOURCE",
          "type": "dropdown",
          "label": "source",
          "description": "Determines where the module will try to retrieve the image from. Use V(build) to build the image from a C(Dockerfile). O(build.path) must be specified when this value is used. Use V(load) to load the image from a C(.tar) file. O(load_path) must be specified when this value is used. Use V(pull) to pull the image from a registry. Use V(local) to make sure that the image is already available on the local docker daemon. This means that the module does not try to build, pull or load the image.",
          "options": [
            [
              "build",
              "build"
            ],
            [
              "load",
              "load"
            ],
            [
              "pull",
              "pull"
            ],
            [
              "local",
              "local"
            ]
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Make assertions about the state of an image. When V(absent) an image will be removed. Use the force option to un-tag and remove all images matching the provided name. When V(present) check if an image exists using the provided name and tag. If the image is not found or the force option is used, the image will either be pulled, built or loaded, depending on the O(source) option.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Used to select an image when pulling. Will be added to the image when pushing, tagging or building. Defaults to V(latest). If O(name) parameter format is C(name:tag), then tag value from O(name) will take precedence."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ],
      "unmappedParams": [
        "build.container_limits (deep nesting)",
        "build (has suboptions)",
        "pull (has suboptions)"
      ]
    },
    {
      "fqcn": "community.docker.docker_image_build",
      "blockType": "community_docker_image_build",
      "label": "docker_image_build",
      "tooltip": "Build Docker images using Docker buildx",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_build_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "args",
          "fieldId": "ARGS",
          "type": "text",
          "label": "args",
          "yamlType": "dict",
          "description": "Provide a dictionary of C(key:value) build arguments that map to Dockerfile ARG directive. Docker expects the value to be a string. For convenience any non-string values will be converted to strings."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cache_from",
          "fieldId": "CACHE_FROM",
          "type": "text",
          "label": "cache from",
          "yamlType": "list",
          "description": "List of image names to consider as cache source."
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "dockerfile",
          "fieldId": "DOCKERFILE",
          "type": "text",
          "label": "dockerfile",
          "description": "Provide an alternate name for the Dockerfile to use when building an image. This can also include a relative path (relative to O(path))."
        },
        {
          "key": "etc_hosts",
          "fieldId": "ETC_HOSTS",
          "type": "text",
          "label": "etc hosts",
          "yamlType": "dict",
          "description": "Extra hosts to add to C(/etc/hosts) in building containers, as a mapping of hostname to IP address. Instead of an IP address, the special value V(host-gateway) can also be used, which resolves to the host's gateway IP and allows building containers to connect to services running on the host."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "Dictionary of key value pairs."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format will be one of: C(name), C(repository/name), C(registry_server:port/name). When pushing or pulling an image the name can optionally include the tag by appending C(:tag_name). Note that image IDs (hashes) and names with digest cannot be used.",
          "required": true
        },
        {
          "key": "network",
          "fieldId": "NETWORK",
          "type": "text",
          "label": "network",
          "description": "The network to use for C(RUN) build instructions."
        },
        {
          "key": "nocache",
          "fieldId": "NOCACHE",
          "type": "boolean",
          "label": "nocache",
          "default": false,
          "description": "Do not use cache when building an image."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The path for the build environment.",
          "required": true
        },
        {
          "key": "platform",
          "fieldId": "PLATFORM",
          "type": "text",
          "label": "platform",
          "yamlType": "list",
          "description": "Platforms in the format C(os[/arch[/variant]]). Since community.docker 3.10.0 this can be a list of platforms, instead of just a single platform."
        },
        {
          "key": "pull",
          "fieldId": "PULL",
          "type": "boolean",
          "label": "pull",
          "default": false,
          "description": "When building an image downloads any updates to the FROM image in Dockerfile."
        },
        {
          "key": "rebuild",
          "fieldId": "REBUILD",
          "type": "dropdown",
          "label": "rebuild",
          "default": "never",
          "description": "Defines the behavior of the module if the image to build (as specified in O(name) and O(tag)) already exists.",
          "options": [
            [
              "never",
              "never"
            ],
            [
              "always",
              "always"
            ]
          ]
        },
        {
          "key": "shm_size",
          "fieldId": "SHM_SIZE",
          "type": "text",
          "label": "shm size",
          "description": "Size of C(/dev/shm) in format C(<number>[<unit>]). Number is positive integer. Unit can be V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes. If you omit the size entirely, Docker daemon uses V(64M)."
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Tag for the image name O(name) that is to be tagged. If O(name)'s format is C(name:tag), then the tag value from O(name) will take precedence."
        },
        {
          "key": "target",
          "fieldId": "TARGET",
          "type": "text",
          "label": "target",
          "description": "When building an image specifies an intermediate build stage by name as a final stage for the resulting image."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ],
      "unmappedParams": [
        "outputs (has suboptions)",
        "secrets (has suboptions)"
      ]
    },
    {
      "fqcn": "community.docker.docker_image_export",
      "blockType": "community_docker_image_export",
      "label": "docker_image_export",
      "tooltip": "Export (archive) Docker images",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_export_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Export the image even if the C(.tar) file already exists and seems to contain the right image."
        },
        {
          "key": "names",
          "fieldId": "NAMES",
          "type": "text",
          "label": "names",
          "yamlType": "list",
          "description": "One or more image names. Name format will be one of: C(name), C(repository/name), C(registry_server:port/name). When pushing or pulling an image the name can optionally include the tag by appending C(:tag_name). Note that image IDs (hashes) can also be used.",
          "required": true,
          "aliases": [
            "name"
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The C(.tar) file the image should be exported to."
        },
        {
          "key": "platform",
          "fieldId": "PLATFORM",
          "type": "text",
          "label": "platform",
          "description": "Ask for this specific platform when exporting. For example, C(linux/amd64), C(linux/arm64). Requires Docker API 1.48 or newer."
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Tag for the image name O(name) that is to be tagged. If O(name)'s format is C(name:tag), then the tag value from O(name) will take precedence."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_info",
      "blockType": "community_docker_image_info",
      "label": "docker_image_info",
      "tooltip": "Inspect docker images",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "yamlType": "list",
          "description": "An image name or a list of image names. Name format will be C(name[:tag]) or C(repository/name[:tag]), where C(tag) is optional. If a tag is not provided, V(latest) will be used. Instead of image names, also image IDs can be used. If no name is provided, a list of all images will be returned."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_load",
      "blockType": "community_docker_image_load",
      "label": "docker_image_load",
      "tooltip": "Load docker image(s) from archives",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_load_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "The path to the C(.tar) archive to load Docker image(s) from.",
          "required": true
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_pull",
      "blockType": "community_docker_image_pull",
      "label": "docker_image_pull",
      "tooltip": "Pull Docker images from registries",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_pull_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format must be one of V(name), V(repository/name), or V(registry_server:port/name). The name can optionally include the tag by appending V(:tag_name), or it can contain a digest by appending V(@hash:digest).",
          "required": true
        },
        {
          "key": "platform",
          "fieldId": "PLATFORM",
          "type": "text",
          "label": "platform",
          "description": "Ask for this specific platform when pulling."
        },
        {
          "key": "pull",
          "fieldId": "PULL",
          "type": "dropdown",
          "label": "pull",
          "default": "always",
          "description": "Determines when to pull an image. If V(always), will always pull the image. If V(not_present), will only pull the image if no image of the name exists on the current Docker daemon, or if O(platform) does not match.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "not_present",
              "not_present"
            ]
          ]
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Used to select an image when pulling. Defaults to V(latest). If O(name) parameter format is C(name:tag) or C(image@hash:digest), then O(tag) will be ignored."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_push",
      "blockType": "community_docker_image_push",
      "label": "docker_image_push",
      "tooltip": "Push Docker images to registries",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_push_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format must be one of V(name), V(repository/name), or V(registry_server:port/name). The name can optionally include the tag by appending V(:tag_name), or it can contain a digest by appending V(@hash:digest).",
          "required": true
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Select which image to push. Defaults to V(latest). If O(name) parameter format is C(name:tag) or C(image@hash:digest), then O(tag) will be ignored."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_remove",
      "blockType": "community_docker_image_remove",
      "label": "docker_image_remove",
      "tooltip": "Remove Docker images",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_remove_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Un-tag and remove all images matching the specified name."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format will be one of: C(name), C(repository/name), C(registry_server:port/name). When pushing or pulling an image the name can optionally include the tag by appending C(:tag_name). Note that image IDs (hashes) can also be used.",
          "required": true
        },
        {
          "key": "prune",
          "fieldId": "PRUNE",
          "type": "boolean",
          "label": "prune",
          "default": true,
          "description": "Delete untagged parent images."
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Tag for the image name O(name) that is to be tagged. If O(name)'s format is C(name:tag), then the tag value from O(name) will take precedence."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_image_tag",
      "blockType": "community_docker_image_tag",
      "label": "docker_image_tag",
      "tooltip": "Tag Docker images with new names and/or tags",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_image_tag_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "existing_images",
          "fieldId": "EXISTING_IMAGES",
          "type": "dropdown",
          "label": "existing images",
          "default": "overwrite",
          "description": "Defines the behavior if the image to be tagged already exists and is another image than the one identified by O(name) and O(tag). If set to V(keep), the tagged image is kept. If set to V(overwrite), the tagged image is overwritten by the specified one.",
          "options": [
            [
              "keep",
              "keep"
            ],
            [
              "overwrite",
              "overwrite"
            ]
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Image name. Name format will be one of: C(name), C(repository/name), C(registry_server:port/name). When pushing or pulling an image the name can optionally include the tag by appending C(:tag_name). Note that image IDs (hashes) can also be used.",
          "required": true
        },
        {
          "key": "repository",
          "fieldId": "REPOSITORY",
          "type": "text",
          "label": "repository",
          "yamlType": "list",
          "description": "List of new image names to tag the image as. Expects format C(repository:tag). If no tag is provided, will use the value of the O(tag) parameter if present, or V(latest).",
          "required": true
        },
        {
          "key": "tag",
          "fieldId": "TAG",
          "type": "text",
          "label": "tag",
          "default": "latest",
          "description": "Tag for the image name O(name) that is to be tagged. If O(name)'s format is C(name:tag), then the tag value from O(name) will take precedence."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_login",
      "blockType": "community_docker_login",
      "label": "docker_login",
      "tooltip": "Log into a Docker registry",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_login_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "config_path",
          "fieldId": "CONFIG_PATH",
          "type": "text",
          "label": "config path",
          "default": "~/.docker/config.json",
          "description": "Custom path to the Docker CLI configuration file.",
          "aliases": [
            "dockercfg_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "reauthorize",
          "fieldId": "REAUTHORIZE",
          "type": "boolean",
          "label": "reauthorize",
          "default": false,
          "description": "Refresh existing authentication found in the configuration file.",
          "aliases": [
            "reauth"
          ]
        },
        {
          "key": "registry_url",
          "fieldId": "REGISTRY_URL",
          "type": "text",
          "label": "registry url",
          "default": "https://index.docker.io/v1/",
          "description": "The registry URL.",
          "aliases": [
            "registry",
            "url"
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "This controls the current state of the user. V(present) will login in a user, V(absent) will log them out. To logout you only need the registry server, which defaults to DockerHub. Before 2.1 you could ONLY log in. Docker does not support 'logout' with a custom config file.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_network",
      "blockType": "community_docker_network",
      "label": "docker_network",
      "tooltip": "Manage Docker networks",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_network_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "appends",
          "fieldId": "APPENDS",
          "type": "boolean",
          "label": "appends",
          "default": false,
          "description": "By default the connected list is canonical, meaning containers not on the list are removed from the network. Use O(appends) to leave existing containers connected.",
          "aliases": [
            "incremental"
          ]
        },
        {
          "key": "attachable",
          "fieldId": "ATTACHABLE",
          "type": "boolean",
          "label": "attachable",
          "description": "If enabled, and the network is in the global scope, non-service containers on worker nodes will be able to connect to the network."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "config_from",
          "fieldId": "CONFIG_FROM",
          "type": "text",
          "label": "config from",
          "description": "Specifies the config only network to use the config from."
        },
        {
          "key": "config_only",
          "fieldId": "CONFIG_ONLY",
          "type": "boolean",
          "label": "config only",
          "description": "Sets that this is a config only network."
        },
        {
          "key": "connected",
          "fieldId": "CONNECTED",
          "type": "text",
          "label": "connected",
          "yamlType": "list",
          "description": "List of container names or container IDs to connect to a network. Please note that the module only makes sure that these containers are connected to the network, but does not care about connection options. If you rely on specific IP addresses and so on, use the M(community.docker.docker_container) module to ensure your containers are correctly connected to this network.",
          "aliases": [
            "containers"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "driver",
          "fieldId": "DRIVER",
          "type": "text",
          "label": "driver",
          "default": "bridge",
          "description": "Specify the type of network. Docker provides bridge and overlay drivers, but 3rd party drivers can also be used."
        },
        {
          "key": "driver_options",
          "fieldId": "DRIVER_OPTIONS",
          "type": "text",
          "label": "driver options",
          "yamlType": "dict",
          "description": "Dictionary of network settings. Consult docker docs for valid options and values."
        },
        {
          "key": "enable_ipv4",
          "fieldId": "ENABLE_IPV4",
          "type": "boolean",
          "label": "enable ipv4",
          "description": "Enable IPv4 networking. This is enabled by default, but can be explicitly disabled. Requires Docker API 1.47 or newer."
        },
        {
          "key": "enable_ipv6",
          "fieldId": "ENABLE_IPV6",
          "type": "boolean",
          "label": "enable ipv6",
          "description": "Enable IPv6 networking."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "With state V(present) will disconnect all containers for existing networks, delete the network and re-create the network. This option is required if you have changed the IPAM or driver options and want an existing network to be updated to use the new options."
        },
        {
          "key": "ingress",
          "fieldId": "INGRESS",
          "type": "boolean",
          "label": "ingress",
          "description": "Enable Swarm routing-mesh."
        },
        {
          "key": "internal",
          "fieldId": "INTERNAL",
          "type": "boolean",
          "label": "internal",
          "description": "Restrict external access to the network."
        },
        {
          "key": "ipam_driver",
          "fieldId": "IPAM_DRIVER",
          "type": "text",
          "label": "ipam driver",
          "description": "Specify an IPAM driver."
        },
        {
          "key": "ipam_driver_options",
          "fieldId": "IPAM_DRIVER_OPTIONS",
          "type": "text",
          "label": "ipam driver options",
          "yamlType": "dict",
          "description": "Dictionary of IPAM driver options."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "Dictionary of labels."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name of the network to operate on.",
          "required": true,
          "aliases": [
            "network_name"
          ]
        },
        {
          "key": "scope",
          "fieldId": "SCOPE",
          "type": "dropdown",
          "label": "scope",
          "description": "Specify the network's scope.",
          "options": [
            [
              "local",
              "local"
            ],
            [
              "global",
              "global"
            ],
            [
              "swarm",
              "swarm"
            ]
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "V(absent) deletes the network. If a network has connected containers, these will be detached from the network. V(present) creates the network, if it does not already exist with the specified parameters, and connects the list of containers provided by the O(connected) parameter. Containers not on the list will be disconnected. An empty list will leave no containers connected to the network. Use the O(appends) option to leave existing containers connected. Use the O(force) options to force re-creation of the network.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ],
      "unmappedParams": [
        "ipam_config (has suboptions)"
      ]
    },
    {
      "fqcn": "community.docker.docker_network_info",
      "blockType": "community_docker_network_info",
      "label": "docker_network_info",
      "tooltip": "Retrieves facts about docker network",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_network_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the network to inspect. When identifying an existing network name may be a name or a long or short network ID.",
          "required": true
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_node",
      "blockType": "community_docker_node",
      "label": "docker_node",
      "tooltip": "Manage Docker Swarm node",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_node_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "availability",
          "fieldId": "AVAILABILITY",
          "type": "dropdown",
          "label": "availability",
          "description": "Node availability to assign. If not provided then node availability remains unchanged.",
          "options": [
            [
              "active",
              "active"
            ],
            [
              "pause",
              "pause"
            ],
            [
              "drain",
              "drain"
            ]
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "The hostname or ID of node as registered in Swarm. If more than one node is registered using the same hostname the ID must be used, otherwise module will fail.",
          "required": true
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "User-defined key/value metadata that will be assigned as node attribute. Label operations in this module apply to the docker swarm node specified by O(hostname). Use M(community.docker.docker_swarm) module to add/modify/remove swarm cluster labels. The actual state of labels assigned to the node when module completes its work depends on O(labels_state) and O(labels_to_remove) parameters values. See description below."
        },
        {
          "key": "labels_state",
          "fieldId": "LABELS_STATE",
          "type": "dropdown",
          "label": "labels state",
          "default": "merge",
          "description": "It defines the operation on the labels assigned to node and labels specified in O(labels) option. Set to V(merge) to combine labels provided in O(labels) with those already assigned to the node. If no labels are assigned then it will add listed labels. For labels that are already assigned to the node, it will update their values. The labels not specified in O(labels) will remain unchanged. If O(labels) is empty then no changes will be made. Set to V(replace) to replace all assigned labels with provided ones. If O(labels) is empty then all labels assigned to the node will be removed.",
          "options": [
            [
              "merge",
              "merge"
            ],
            [
              "replace",
              "replace"
            ]
          ]
        },
        {
          "key": "labels_to_remove",
          "fieldId": "LABELS_TO_REMOVE",
          "type": "text",
          "label": "labels to remove",
          "yamlType": "list",
          "description": "List of labels that will be removed from the node configuration. The list has to contain only label names, not their values. If the label provided on the list is not assigned to the node, the entry is ignored. If the label is both on the O(labels_to_remove) and O(labels), then value provided in O(labels) remains assigned to the node. If O(labels_state=replace) and O(labels) is not provided or empty then all labels assigned to node are removed and O(labels_to_remove) is ignored."
        },
        {
          "key": "role",
          "fieldId": "ROLE",
          "type": "dropdown",
          "label": "role",
          "description": "Node role to assign. If not provided then node role remains unchanged.",
          "options": [
            [
              "manager",
              "manager"
            ],
            [
              "worker",
              "worker"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_node_info",
      "blockType": "community_docker_node_info",
      "label": "docker_node_info",
      "tooltip": "Retrieves facts about docker swarm node from Swarm Manager",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_node_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "yamlType": "list",
          "description": "The name of the node to inspect. The list of nodes names to inspect. If empty then return information of all nodes in Swarm cluster. When identifying the node use either the hostname of the node (as registered in Swarm) or node ID. If O(self=true) then this parameter is ignored."
        },
        {
          "key": "self",
          "fieldId": "SELF",
          "type": "boolean",
          "label": "self",
          "default": false,
          "description": "If V(true), queries the node (that is, the docker daemon) the module communicates with. If V(true) then O(name) is ignored. If V(false) then query depends on O(name) presence and value."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_plugin",
      "blockType": "community_docker_plugin",
      "label": "docker_plugin",
      "tooltip": "Manage Docker plugins",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_plugin_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "alias",
          "fieldId": "ALIAS",
          "type": "text",
          "label": "alias",
          "description": "Local name for plugin."
        },
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "enable_timeout",
          "fieldId": "ENABLE_TIMEOUT",
          "type": "number",
          "label": "enable timeout",
          "default": 0,
          "description": "Timeout in seconds."
        },
        {
          "key": "force_remove",
          "fieldId": "FORCE_REMOVE",
          "type": "boolean",
          "label": "force remove",
          "default": false,
          "description": "Remove even if the plugin is enabled."
        },
        {
          "key": "plugin_name",
          "fieldId": "PLUGIN_NAME",
          "type": "text",
          "label": "plugin name",
          "description": "Name of the plugin to operate on.",
          "required": true
        },
        {
          "key": "plugin_options",
          "fieldId": "PLUGIN_OPTIONS",
          "type": "text",
          "label": "plugin options",
          "yamlType": "dict",
          "description": "Dictionary of plugin settings."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "V(absent) remove the plugin. V(present) install the plugin, if it does not already exist. V(enable) enable the plugin. V(disable) disable the plugin.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ],
            [
              "enable",
              "enable"
            ],
            [
              "disable",
              "disable"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_prune",
      "blockType": "community_docker_prune",
      "label": "docker_prune",
      "tooltip": "Allows to prune various docker objects",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_prune_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "builder_cache",
          "fieldId": "BUILDER_CACHE",
          "type": "boolean",
          "label": "builder cache",
          "default": false,
          "description": "Whether to prune the builder cache."
        },
        {
          "key": "builder_cache_all",
          "fieldId": "BUILDER_CACHE_ALL",
          "type": "boolean",
          "label": "builder cache all",
          "default": false,
          "description": "Whether to remove all types of build cache."
        },
        {
          "key": "builder_cache_filters",
          "fieldId": "BUILDER_CACHE_FILTERS",
          "type": "text",
          "label": "builder cache filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting images to delete. For example, C(until: 10m). See L(the API documentation,https://docs.docker.com/engine/api/v1.44/#tag/Image/operation/BuildPrune) for more information on possible filters."
        },
        {
          "key": "builder_cache_keep_storage",
          "fieldId": "BUILDER_CACHE_KEEP_STORAGE",
          "type": "text",
          "label": "builder cache keep storage",
          "description": "Amount of disk space to keep for cache in format C(<number>[<unit>]).\". Number is a positive integer. Unit can be one of V(B) (byte), V(K) (kibibyte, 1024B), V(M) (mebibyte), V(G) (gibibyte), V(T) (tebibyte), or V(P) (pebibyte). Omitting the unit defaults to bytes."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "containers",
          "fieldId": "CONTAINERS",
          "type": "boolean",
          "label": "containers",
          "default": false,
          "description": "Whether to prune containers."
        },
        {
          "key": "containers_filters",
          "fieldId": "CONTAINERS_FILTERS",
          "type": "text",
          "label": "containers filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting containers to delete. For example, C(until: 24h). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/container_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "images",
          "fieldId": "IMAGES",
          "type": "boolean",
          "label": "images",
          "default": false,
          "description": "Whether to prune images."
        },
        {
          "key": "images_filters",
          "fieldId": "IMAGES_FILTERS",
          "type": "text",
          "label": "images filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting images to delete. For example, C(dangling: true). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/image_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "networks",
          "fieldId": "NETWORKS",
          "type": "boolean",
          "label": "networks",
          "default": false,
          "description": "Whether to prune networks."
        },
        {
          "key": "networks_filters",
          "fieldId": "NETWORKS_FILTERS",
          "type": "text",
          "label": "networks filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting networks to delete. See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/network_prune/#filtering) for more information on possible filters."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "volumes",
          "fieldId": "VOLUMES",
          "type": "boolean",
          "label": "volumes",
          "default": false,
          "description": "Whether to prune volumes."
        },
        {
          "key": "volumes_filters",
          "fieldId": "VOLUMES_FILTERS",
          "type": "text",
          "label": "volumes filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting volumes to delete. See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/volume_prune/#filtering) for more information on possible filters."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_secret",
      "blockType": "community_docker_secret",
      "label": "docker_secret",
      "tooltip": "Manage docker secrets",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_secret_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "data",
          "fieldId": "DATA",
          "type": "text",
          "label": "data",
          "description": "The value of the secret. Mutually exclusive with O(data_src). One of O(data) and O(data_src) is required if O(state=present)."
        },
        {
          "key": "data_is_b64",
          "fieldId": "DATA_IS_B64",
          "type": "boolean",
          "label": "data is b64",
          "default": false,
          "description": "If set to V(true), the data is assumed to be Base64 encoded and will be decoded before being used. To use binary O(data), it is better to keep it Base64 encoded and let it be decoded by this option."
        },
        {
          "key": "data_src",
          "fieldId": "DATA_SRC",
          "type": "text",
          "label": "data src",
          "description": "The file on the target from which to read the secret. Mutually exclusive with O(data). One of O(data) and O(data_src) is required if O(state=present)."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Use with O(state=present) to always remove and recreate an existing secret. If V(true), an existing secret will be replaced, even if it has not changed."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "A map of key:value meta data, where both key and value are expected to be strings. If new meta data is provided, or existing meta data is modified, the secret will be updated by removing it and creating it again."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the secret.",
          "required": true
        },
        {
          "key": "rolling_versions",
          "fieldId": "ROLLING_VERSIONS",
          "type": "boolean",
          "label": "rolling versions",
          "default": false,
          "description": "If set to V(true), secrets are created with an increasing version number appended to their name. Adds a label containing the version number to the managed secrets with the name C(ansible_version)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Set to V(present), if the secret should exist, and V(absent), if it should not.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "versions_to_keep",
          "fieldId": "VERSIONS_TO_KEEP",
          "type": "number",
          "label": "versions to keep",
          "default": 5,
          "description": "When using O(rolling_versions), the number of old versions of the secret to keep. Extraneous old secrets are deleted after the new one is created. Set to V(-1) to keep everything or to V(0) or V(1) to keep only the current one."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_stack",
      "blockType": "community_docker_stack",
      "label": "docker_stack",
      "tooltip": "docker stack module",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_stack_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "absent_retries",
          "fieldId": "ABSENT_RETRIES",
          "type": "number",
          "label": "absent retries",
          "default": 0,
          "description": "If larger than V(0) and O(state=absent) the module will retry up to O(absent_retries) times to delete the stack until all the resources have been effectively deleted. If the last try still reports the stack as not completely removed the module will fail."
        },
        {
          "key": "absent_retries_interval",
          "fieldId": "ABSENT_RETRIES_INTERVAL",
          "type": "number",
          "label": "absent retries interval",
          "default": 1,
          "description": "Interval in seconds between consecutive O(absent_retries)."
        },
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "compose",
          "fieldId": "COMPOSE",
          "type": "text",
          "label": "compose",
          "yamlType": "list",
          "description": "List of compose definitions. Any element may be a string referring to the path of the compose file on the target host or the YAML contents of a compose file nested as dictionary."
        },
        {
          "key": "detach",
          "fieldId": "DETACH",
          "type": "boolean",
          "label": "detach",
          "default": true,
          "description": "If V(false), the C(--detach=false) option is added to the C(docker stack deploy) command, allowing Docker to wait for tasks to converge before exiting. If V(true) (default), Docker exits immediately instead of waiting for tasks to converge."
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Stack name.",
          "required": true
        },
        {
          "key": "prune",
          "fieldId": "PRUNE",
          "type": "boolean",
          "label": "prune",
          "default": false,
          "description": "If true will add the C(--prune) option to the C(docker stack deploy) command. This will have docker remove the services not present in the current stack definition."
        },
        {
          "key": "resolve_image",
          "fieldId": "RESOLVE_IMAGE",
          "type": "dropdown",
          "label": "resolve image",
          "description": "If set will add the C(--resolve-image) option to the C(docker stack deploy) command. This will have docker query the registry to resolve image digest and supported platforms. If not set, docker use \"always\" by default.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "changed",
              "changed"
            ],
            [
              "never",
              "never"
            ]
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Service state.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "with_registry_auth",
          "fieldId": "WITH_REGISTRY_AUTH",
          "type": "boolean",
          "label": "with registry auth",
          "default": false,
          "description": "If true will add the C(--with-registry-auth) option to the C(docker stack deploy) command. This will have docker send registry authentication details to Swarm agents."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_stack_info",
      "blockType": "community_docker_stack_info",
      "label": "docker_stack_info",
      "tooltip": "Return information on all docker stacks",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_stack_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_stack_task_info",
      "blockType": "community_docker_stack_task_info",
      "label": "docker_stack_task_info",
      "tooltip": "Return information of the tasks on a docker stack",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_stack_task_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cli_context",
          "fieldId": "CLI_CONTEXT",
          "type": "text",
          "label": "cli context",
          "description": "The Docker CLI context to use. Mutually exclusive with O(docker_host)."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "docker_cli",
          "fieldId": "DOCKER_CLI",
          "type": "text",
          "label": "docker cli",
          "description": "Path to the Docker CLI. If not provided, will search for Docker CLI on the E(PATH)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used. Mutually exclusive with O(cli_context). If neither O(docker_host) nor O(cli_context) are provided, the value V(unix:///var/run/docker.sock) is used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Stack name.",
          "required": true
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_swarm",
      "blockType": "community_docker_swarm",
      "label": "docker_swarm",
      "tooltip": "Manage Swarm cluster",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_swarm_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "advertise_addr",
          "fieldId": "ADVERTISE_ADDR",
          "type": "text",
          "label": "advertise addr",
          "description": "Externally reachable address advertised to other nodes. This can either be an address/port combination in the form V(192.168.1.1:4567), or an interface followed by a port number, like V(eth0:4567). If the port number is omitted, the port number from the listen address is used. If O(advertise_addr) is not specified, it will be automatically detected when possible. Only used when swarm is initialised or joined. Because of this it is not considered for idempotency checking."
        },
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "autolock_managers",
          "fieldId": "AUTOLOCK_MANAGERS",
          "type": "boolean",
          "label": "autolock managers",
          "description": "If set, generate a key and use it to lock data stored on the managers. Docker default value is V(false). M(community.docker.docker_swarm_info) can be used to retrieve the unlock key."
        },
        {
          "key": "ca_force_rotate",
          "fieldId": "CA_FORCE_ROTATE",
          "type": "number",
          "label": "ca force rotate",
          "description": "An integer whose purpose is to force swarm to generate a new signing CA certificate and key, if none have been specified. Docker default value is V(0). Requires API version >= 1.30."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "data_path_addr",
          "fieldId": "DATA_PATH_ADDR",
          "type": "text",
          "label": "data path addr",
          "description": "Address or interface to use for data path traffic. This can either be an address in the form V(192.168.1.1), or an interface, like V(eth0). Only used when swarm is initialised or joined. Because of this it is not considered for idempotency checking. Requires API version >= 1.30."
        },
        {
          "key": "data_path_port",
          "fieldId": "DATA_PATH_PORT",
          "type": "number",
          "label": "data path port",
          "description": "Port to use for data path traffic. This needs to be a port number like V(9789). Only used when swarm is initialised. Because of this it is not considered for idempotency checking. Requires API version >= 1.40."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "default_addr_pool",
          "fieldId": "DEFAULT_ADDR_POOL",
          "type": "text",
          "label": "default addr pool",
          "yamlType": "list",
          "description": "Default address pool in CIDR format. Only used when swarm is initialised. Because of this it is not considered for idempotency checking. Requires API version >= 1.39."
        },
        {
          "key": "dispatcher_heartbeat_period",
          "fieldId": "DISPATCHER_HEARTBEAT_PERIOD",
          "type": "number",
          "label": "dispatcher heartbeat period",
          "description": "The delay (in nanoseconds) for an agent to send a heartbeat to the dispatcher. Docker default value is 5 seconds, which corresponds to a value of V(5000000000)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "election_tick",
          "fieldId": "ELECTION_TICK",
          "type": "number",
          "label": "election tick",
          "description": "Amount of ticks (in seconds) needed without a leader to trigger a new election. Docker default value is V(10) seconds."
        },
        {
          "key": "force",
          "fieldId": "FORCE",
          "type": "boolean",
          "label": "force",
          "default": false,
          "description": "Use with state V(present) to force creating a new Swarm, even if already part of one. Use with state V(absent) to Leave the swarm even if this node is a manager."
        },
        {
          "key": "heartbeat_tick",
          "fieldId": "HEARTBEAT_TICK",
          "type": "number",
          "label": "heartbeat tick",
          "description": "Amount of ticks (in seconds) between each heartbeat. Docker default value is V(1) seconds."
        },
        {
          "key": "join_token",
          "fieldId": "JOIN_TOKEN",
          "type": "text",
          "label": "join token",
          "description": "Swarm token used to join a swarm cluster. Used with O(state=join). If this value is specified, the corresponding value in the return values will be censored by Ansible. This is a side-effect of this value not being logged."
        },
        {
          "key": "keep_old_snapshots",
          "fieldId": "KEEP_OLD_SNAPSHOTS",
          "type": "number",
          "label": "keep old snapshots",
          "description": "Number of snapshots to keep beyond the current snapshot. Docker default value is V(0)."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "User-defined key/value metadata. Label operations in this module apply to the docker swarm cluster. Use M(community.docker.docker_node) module to add/modify/remove swarm node labels. Requires API version >= 1.32."
        },
        {
          "key": "listen_addr",
          "fieldId": "LISTEN_ADDR",
          "type": "text",
          "label": "listen addr",
          "default": "0.0.0.0:2377",
          "description": "Listen address used for inter-manager communication. This can either be an address/port combination in the form V(192.168.1.1:4567), or an interface followed by a port number, like V(eth0:4567). If the port number is omitted, the default swarm listening port is used. Only used when swarm is initialised or joined. Because of this it is not considered for idempotency checking."
        },
        {
          "key": "log_entries_for_slow_followers",
          "fieldId": "LOG_ENTRIES_FOR_SLOW_FOLLOWERS",
          "type": "number",
          "label": "log entries for slow followers",
          "description": "Number of log entries to keep around to sync up slow followers after a snapshot is created."
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the swarm."
        },
        {
          "key": "node_cert_expiry",
          "fieldId": "NODE_CERT_EXPIRY",
          "type": "number",
          "label": "node cert expiry",
          "description": "Automatic expiry for nodes certificates, given in nanoseconds. Docker default value is 90 days, which corresponds to a value of V(7776000000000000)."
        },
        {
          "key": "node_id",
          "fieldId": "NODE_ID",
          "type": "text",
          "label": "node id",
          "description": "Swarm id of the node to remove. Used with O(state=remove)."
        },
        {
          "key": "remote_addrs",
          "fieldId": "REMOTE_ADDRS",
          "type": "text",
          "label": "remote addrs",
          "yamlType": "list",
          "description": "Remote address of one or more manager nodes of an existing Swarm to connect to. Used with O(state=join)."
        },
        {
          "key": "rotate_manager_token",
          "fieldId": "ROTATE_MANAGER_TOKEN",
          "type": "boolean",
          "label": "rotate manager token",
          "default": false,
          "description": "Rotate the manager join token."
        },
        {
          "key": "rotate_worker_token",
          "fieldId": "ROTATE_WORKER_TOKEN",
          "type": "boolean",
          "label": "rotate worker token",
          "default": false,
          "description": "Rotate the worker join token."
        },
        {
          "key": "signing_ca_cert",
          "fieldId": "SIGNING_CA_CERT",
          "type": "text",
          "label": "signing ca cert",
          "description": "The desired signing CA certificate for all swarm node TLS leaf certificates, in PEM format. This must not be a path to a certificate, but the contents of the certificate. Requires API version >= 1.30."
        },
        {
          "key": "signing_ca_key",
          "fieldId": "SIGNING_CA_KEY",
          "type": "text",
          "label": "signing ca key",
          "description": "The desired signing CA key for all swarm node TLS leaf certificates, in PEM format. This must not be a path to a key, but the contents of the key. Requires API version >= 1.30."
        },
        {
          "key": "snapshot_interval",
          "fieldId": "SNAPSHOT_INTERVAL",
          "type": "number",
          "label": "snapshot interval",
          "description": "Number of logs entries between snapshot. Docker default value is V(10000)."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "Set to V(present), to create/update a new cluster. Set to V(join), to join an existing cluster. Set to V(absent), to leave an existing cluster. Set to V(remove), to remove an absent node from the cluster. Note that removing requires Docker SDK for Python >= 2.4.0. M(community.docker.docker_node) can be used to demote a manager before removal.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "join",
              "join"
            ],
            [
              "absent",
              "absent"
            ],
            [
              "remove",
              "remove"
            ]
          ]
        },
        {
          "key": "subnet_size",
          "fieldId": "SUBNET_SIZE",
          "type": "number",
          "label": "subnet size",
          "description": "Default address pool subnet mask length. Only used when swarm is initialised. Because of this it is not considered for idempotency checking. Requires API version >= 1.39."
        },
        {
          "key": "task_history_retention_limit",
          "fieldId": "TASK_HISTORY_RETENTION_LIMIT",
          "type": "number",
          "label": "task history retention limit",
          "description": "Maximum number of tasks history stored. Docker default value is V(5)."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_swarm_info",
      "blockType": "community_docker_swarm_info",
      "label": "docker_swarm_info",
      "tooltip": "Retrieves facts about Docker Swarm cluster",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_swarm_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "nodes",
          "fieldId": "NODES",
          "type": "boolean",
          "label": "nodes",
          "default": false,
          "description": "Whether to list swarm nodes."
        },
        {
          "key": "nodes_filters",
          "fieldId": "NODES_FILTERS",
          "type": "text",
          "label": "nodes filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting nodes to list. For example, C(name: mynode). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/node_ls/#filtering) for more information on possible filters."
        },
        {
          "key": "services",
          "fieldId": "SERVICES",
          "type": "boolean",
          "label": "services",
          "default": false,
          "description": "Whether to list swarm services."
        },
        {
          "key": "services_filters",
          "fieldId": "SERVICES_FILTERS",
          "type": "text",
          "label": "services filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting services to list. For example, C(name: myservice). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/service_ls/#filtering) for more information on possible filters."
        },
        {
          "key": "tasks",
          "fieldId": "TASKS",
          "type": "boolean",
          "label": "tasks",
          "default": false,
          "description": "Whether to list containers."
        },
        {
          "key": "tasks_filters",
          "fieldId": "TASKS_FILTERS",
          "type": "text",
          "label": "tasks filters",
          "yamlType": "dict",
          "description": "A dictionary of filter values used for selecting tasks to list. For example, C(node: mynode-1). See L(the docker documentation,https://docs.docker.com/engine/reference/commandline/service_ps/#filtering) for more information on possible filters."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "unlock_key",
          "fieldId": "UNLOCK_KEY",
          "type": "boolean",
          "label": "unlock key",
          "default": false,
          "description": "Whether to retrieve the swarm unlock key."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "verbose_output",
          "fieldId": "VERBOSE_OUTPUT",
          "type": "boolean",
          "label": "verbose output",
          "default": false,
          "description": "When set to V(true) and O(nodes), O(services), or O(tasks) is set to V(true), then the module output will contain verbose information about objects matching the full output of API method. For details see the documentation of your version of Docker API at U(https://docs.docker.com/engine/api/). The verbose output in this module contains only subset of information returned by this info module for each type of the objects."
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_swarm_service",
      "blockType": "community_docker_swarm_service",
      "label": "docker_swarm_service",
      "tooltip": "docker swarm service",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_swarm_service_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "args",
          "fieldId": "ARGS",
          "type": "text",
          "label": "args",
          "yamlType": "list",
          "description": "List arguments to be passed to the container. Corresponds to the C(ARG) parameter of C(docker service create)."
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "cap_add",
          "fieldId": "CAP_ADD",
          "type": "text",
          "label": "cap add",
          "yamlType": "list",
          "description": "List of capabilities to add to the container. Requires API version >= 1.41."
        },
        {
          "key": "cap_drop",
          "fieldId": "CAP_DROP",
          "type": "text",
          "label": "cap drop",
          "yamlType": "list",
          "description": "List of capabilities to drop from the container. Requires API version >= 1.41."
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "command",
          "fieldId": "COMMAND",
          "type": "text",
          "label": "command",
          "multiline": true,
          "description": "Command to execute when the container starts. A command may be either a string or a list or a list of strings. Corresponds to the C(COMMAND) parameter of C(docker service create)."
        },
        {
          "key": "container_labels",
          "fieldId": "CONTAINER_LABELS",
          "type": "text",
          "label": "container labels",
          "yamlType": "dict",
          "description": "Dictionary of key value pairs. Corresponds to the C(--container-label) option of C(docker service create)."
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "dns",
          "fieldId": "DNS",
          "type": "text",
          "label": "dns",
          "yamlType": "list",
          "description": "List of custom DNS servers. Corresponds to the C(--dns) option of C(docker service create)."
        },
        {
          "key": "dns_options",
          "fieldId": "DNS_OPTIONS",
          "type": "text",
          "label": "dns options",
          "yamlType": "list",
          "description": "List of custom DNS options. Corresponds to the C(--dns-option) option of C(docker service create)."
        },
        {
          "key": "dns_search",
          "fieldId": "DNS_SEARCH",
          "type": "text",
          "label": "dns search",
          "yamlType": "list",
          "description": "List of custom DNS search domains. Corresponds to the C(--dns-search) option of C(docker service create)."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "endpoint_mode",
          "fieldId": "ENDPOINT_MODE",
          "type": "dropdown",
          "label": "endpoint mode",
          "description": "Service endpoint mode. Corresponds to the C(--endpoint-mode) option of C(docker service create).",
          "options": [
            [
              "vip",
              "vip"
            ],
            [
              "dnsrr",
              "dnsrr"
            ]
          ]
        },
        {
          "key": "env",
          "fieldId": "ENV",
          "type": "text",
          "label": "env",
          "multiline": true,
          "description": "List or dictionary of the service environment variables. If passed a list each items need to be in the format of C(KEY=VALUE). If passed a dictionary values which might be parsed as numbers, booleans or other types by the YAML parser must be quoted (for example V(\"true\")) in order to avoid data loss. Corresponds to the C(--env) option of C(docker service create)."
        },
        {
          "key": "env_files",
          "fieldId": "ENV_FILES",
          "type": "text",
          "label": "env files",
          "yamlType": "list",
          "description": "List of paths to files, present on the target, containing environment variables C(FOO=BAR). The order of the list is significant in determining the value assigned to a variable that shows up more than once. If variable also present in O(env), then O(env) value will override."
        },
        {
          "key": "force_update",
          "fieldId": "FORCE_UPDATE",
          "type": "boolean",
          "label": "force update",
          "default": false,
          "description": "Force update even if no changes require it. Corresponds to the C(--force) option of C(docker service update)."
        },
        {
          "key": "groups",
          "fieldId": "GROUPS",
          "type": "text",
          "label": "groups",
          "yamlType": "list",
          "description": "List of additional group names and/or IDs that the container process will run as. Corresponds to the C(--group) option of C(docker service update)."
        },
        {
          "key": "hostname",
          "fieldId": "HOSTNAME",
          "type": "text",
          "label": "hostname",
          "description": "Container hostname. Corresponds to the C(--hostname) option of C(docker service create)."
        },
        {
          "key": "hosts",
          "fieldId": "HOSTS",
          "type": "text",
          "label": "hosts",
          "yamlType": "dict",
          "description": "Dict of host-to-IP mappings, where each host name is a key in the dictionary. Each host name will be added to the container's /etc/hosts file. Corresponds to the C(--host) option of C(docker service create)."
        },
        {
          "key": "image",
          "fieldId": "IMAGE",
          "type": "text",
          "label": "image",
          "description": "Service image path and tag. Corresponds to the C(IMAGE) parameter of C(docker service create)."
        },
        {
          "key": "init",
          "fieldId": "INIT",
          "type": "boolean",
          "label": "init",
          "description": "Use an init inside each service container to forward signals and reap processes. Corresponds to the C(--init) option of C(docker service create). Requires API version >= 1.37."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "Dictionary of key value pairs. Corresponds to the C(--label) option of C(docker service create)."
        },
        {
          "key": "mode",
          "fieldId": "MODE",
          "type": "dropdown",
          "label": "mode",
          "default": "replicated",
          "description": "Service replication mode. Service will be removed and recreated when changed. Corresponds to the C(--mode) option of C(docker service create). The value V(replicated-job) was added in community.docker 4.7.0, and requires API version >= 1.41 and Docker SDK for Python >= 6.0.0.",
          "options": [
            [
              "replicated",
              "replicated"
            ],
            [
              "global",
              "global"
            ],
            [
              "replicated-job",
              "replicated-job"
            ]
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Service name. Corresponds to the C(--name) option of C(docker service create).",
          "required": true
        },
        {
          "key": "networks",
          "fieldId": "NETWORKS",
          "type": "text",
          "label": "networks",
          "yamlType": "list",
          "description": "List of the service networks names or dictionaries. When passed dictionaries valid sub-options are C(name), which is required, and C(aliases) and C(options). Prior to API version 1.29, updating and removing networks is not supported. If changes are made the service will then be removed and recreated. Corresponds to the C(--network) option of C(docker service create)."
        },
        {
          "key": "read_only",
          "fieldId": "READ_ONLY",
          "type": "boolean",
          "label": "read only",
          "description": "Mount the containers root filesystem as read only. Corresponds to the C(--read-only) option of C(docker service create)."
        },
        {
          "key": "replicas",
          "fieldId": "REPLICAS",
          "type": "number",
          "label": "replicas",
          "default": -1,
          "description": "Number of containers instantiated in the service. Valid only if O(mode=replicated) or O(mode=replicated-job). If set to V(-1), and service is not present, service replicas will be set to V(1). If set to V(-1), and service is present, service replicas will be unchanged. Corresponds to the C(--replicas) option of C(docker service create)."
        },
        {
          "key": "resolve_image",
          "fieldId": "RESOLVE_IMAGE",
          "type": "boolean",
          "label": "resolve image",
          "default": false,
          "description": "If the current image digest should be resolved from registry and updated if changed. Requires API version >= 1.30."
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "V(absent) - A service matching the specified name will be removed and have its tasks stopped. V(present) - Asserts the existence of a service matching the name and provided configuration parameters. Unspecified configuration parameters will be set to docker defaults.",
          "options": [
            [
              "present",
              "present"
            ],
            [
              "absent",
              "absent"
            ]
          ]
        },
        {
          "key": "stop_grace_period",
          "fieldId": "STOP_GRACE_PERIOD",
          "type": "text",
          "label": "stop grace period",
          "description": "Time to wait before force killing a container. Accepts a duration as a string in a format that look like: V(5h34m56s), V(1m30s), and so on. The supported units are V(us), V(ms), V(s), V(m) and V(h). Corresponds to the C(--stop-grace-period) option of C(docker service create)."
        },
        {
          "key": "stop_signal",
          "fieldId": "STOP_SIGNAL",
          "type": "text",
          "label": "stop signal",
          "description": "Override default signal used to stop the container. Corresponds to the C(--stop-signal) option of C(docker service create)."
        },
        {
          "key": "sysctls",
          "fieldId": "SYSCTLS",
          "type": "text",
          "label": "sysctls",
          "yamlType": "dict",
          "description": "Dictionary of key, value pairs."
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "tty",
          "fieldId": "TTY",
          "type": "boolean",
          "label": "tty",
          "description": "Allocate a pseudo-TTY. Corresponds to the C(--tty) option of C(docker service create)."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "user",
          "fieldId": "USER",
          "type": "text",
          "label": "user",
          "description": "Sets the username or UID used for the specified command. Before Ansible 2.8, the default value for this option was V(root). The default has been removed so that the user defined in the image is used if no user is specified here. Corresponds to the C(--user) option of C(docker service create)."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "working_dir",
          "fieldId": "WORKING_DIR",
          "type": "text",
          "label": "working dir",
          "description": "Path to the working directory. Corresponds to the C(--workdir) option of C(docker service create)."
        }
      ],
      "unmappedParams": [
        "mounts.driver_config (deep nesting)",
        "configs (has suboptions)",
        "healthcheck (has suboptions)",
        "limits (has suboptions)",
        "logging (has suboptions)",
        "mounts (has suboptions)",
        "placement (has suboptions)",
        "publish (has suboptions)",
        "reservations (has suboptions)",
        "restart_config (has suboptions)",
        "rollback_config (has suboptions)",
        "secrets (has suboptions)",
        "update_config (has suboptions)"
      ]
    },
    {
      "fqcn": "community.docker.docker_swarm_service_info",
      "blockType": "community_docker_swarm_service_info",
      "label": "docker_swarm_service_info",
      "tooltip": "Retrieves information about docker services from a Swarm Manager",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_swarm_service_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by Docker SDK for Python and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode."
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "The name of the service to inspect.",
          "required": true
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0. B(Note:) this option is no longer supported for Docker SDK for Python 7.0.0+. Specifying it with Docker SDK for Python 7.0.0 or newer will lead to an error."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko. Requires Docker SDK for Python 4.4.0 or newer."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_volume",
      "blockType": "community_docker_volume",
      "label": "docker_volume",
      "tooltip": "Manage Docker volumes",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_volume_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "driver",
          "fieldId": "DRIVER",
          "type": "text",
          "label": "driver",
          "default": "local",
          "description": "Specify the type of volume. Docker provides the V(local) driver, but 3rd party drivers can also be used."
        },
        {
          "key": "driver_options",
          "fieldId": "DRIVER_OPTIONS",
          "type": "text",
          "label": "driver options",
          "yamlType": "dict",
          "description": "Dictionary of volume settings. Consult the Docker documentation for valid options and values: U(https://docs.docker.com/engine/reference/commandline/volume_create/#driver-specific-options)."
        },
        {
          "key": "labels",
          "fieldId": "LABELS",
          "type": "text",
          "label": "labels",
          "yamlType": "dict",
          "description": "Dictionary of label key/values to set for the volume."
        },
        {
          "key": "recreate",
          "fieldId": "RECREATE",
          "type": "dropdown",
          "label": "recreate",
          "default": "never",
          "description": "Controls when a volume will be recreated when O(state=present). Please note that recreating an existing volume will cause B(any data in the existing volume to be lost!) The volume will be deleted and a new volume with the same name will be created. The value V(always) forces the volume to be always recreated. The value V(never) makes sure the volume will not be recreated. The value V(options-changed) makes sure the volume will be recreated if the volume already exist and the driver, driver options or labels differ.",
          "options": [
            [
              "always",
              "always"
            ],
            [
              "never",
              "never"
            ],
            [
              "options-changed",
              "options-changed"
            ]
          ]
        },
        {
          "key": "state",
          "fieldId": "STATE",
          "type": "dropdown",
          "label": "state",
          "default": "present",
          "description": "V(absent) deletes the volume. V(present) creates the volume, if it does not already exist.",
          "options": [
            [
              "absent",
              "absent"
            ],
            [
              "present",
              "present"
            ]
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        },
        {
          "key": "volume_name",
          "fieldId": "VOLUME_NAME",
          "type": "text",
          "label": "volume name",
          "description": "Name of the volume to operate on.",
          "required": true,
          "aliases": [
            "name"
          ]
        }
      ]
    },
    {
      "fqcn": "community.docker.docker_volume_info",
      "blockType": "community_docker_volume_info",
      "label": "docker_volume_info",
      "tooltip": "Retrieve facts about Docker volumes",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/docker/docker_volume_info_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "api_version",
          "fieldId": "API_VERSION",
          "type": "text",
          "label": "api version",
          "default": "auto",
          "description": "The version of the Docker API running on the Docker Host. Defaults to the latest version of the API supported by this collection and the docker daemon. If the value is not specified in the task, the value of environment variable E(DOCKER_API_VERSION) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_api_version"
          ]
        },
        {
          "key": "ca_path",
          "fieldId": "CA_PATH",
          "type": "text",
          "label": "ca path",
          "description": "Use a CA certificate when performing server verification by providing the path to a CA certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(ca.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used. This option was called O(ca_cert) and got renamed to O(ca_path) in community.docker 3.6.0. The old name has been added as an alias and can still be used.",
          "aliases": [
            "ca_cert",
            "tls_ca_cert",
            "cacert_path"
          ]
        },
        {
          "key": "client_cert",
          "fieldId": "CLIENT_CERT",
          "type": "text",
          "label": "client cert",
          "description": "Path to the client's TLS certificate file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(cert.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_cert",
            "cert_path"
          ]
        },
        {
          "key": "client_key",
          "fieldId": "CLIENT_KEY",
          "type": "text",
          "label": "client key",
          "description": "Path to the client's TLS key file. If the value is not specified in the task and the environment variable E(DOCKER_CERT_PATH) is set, the file C(key.pem) from the directory specified in the environment variable E(DOCKER_CERT_PATH) will be used.",
          "aliases": [
            "tls_client_key",
            "key_path"
          ]
        },
        {
          "key": "debug",
          "fieldId": "DEBUG",
          "type": "boolean",
          "label": "debug",
          "default": false,
          "description": "Debug mode"
        },
        {
          "key": "docker_host",
          "fieldId": "DOCKER_HOST",
          "type": "text",
          "label": "docker host",
          "default": "unix:///var/run/docker.sock",
          "description": "The URL or Unix socket path used to connect to the Docker API. To connect to a remote host, provide the TCP connection string. For example, V(tcp://192.0.2.23:2376). If TLS is used to encrypt the connection, the module will automatically replace C(tcp) in the connection URL with C(https). If the value is not specified in the task, the value of environment variable E(DOCKER_HOST) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "docker_url"
          ]
        },
        {
          "key": "name",
          "fieldId": "NAME",
          "type": "text",
          "label": "name",
          "description": "Name of the volume to inspect.",
          "required": true,
          "aliases": [
            "volume_name"
          ]
        },
        {
          "key": "tls",
          "fieldId": "TLS",
          "type": "boolean",
          "label": "tls",
          "default": false,
          "description": "Secure the connection to the API by using TLS without verifying the authenticity of the Docker host server. Note that if O(validate_certs) is set to V(true) as well, it will take precedence. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS) will be used instead. If the environment variable is not set, the default value will be used."
        },
        {
          "key": "tls_hostname",
          "fieldId": "TLS_HOSTNAME",
          "type": "text",
          "label": "tls hostname",
          "description": "When verifying the authenticity of the Docker Host server, provide the expected name of the server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_HOSTNAME) will be used instead. If the environment variable is not set, the default value will be used. Note that this option had a default value V(localhost) in older versions. It was removed in community.docker 3.0.0."
        },
        {
          "key": "use_ssh_client",
          "fieldId": "USE_SSH_CLIENT",
          "type": "boolean",
          "label": "use ssh client",
          "default": false,
          "description": "For SSH transports, use the C(ssh) CLI tool instead of paramiko."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "default": false,
          "description": "Secure the connection to the API by using TLS and verifying the authenticity of the Docker host server. If the value is not specified in the task, the value of environment variable E(DOCKER_TLS_VERIFY) will be used instead. If the environment variable is not set, the default value will be used.",
          "aliases": [
            "tls_verify"
          ]
        }
      ]
    }
  ]
}
