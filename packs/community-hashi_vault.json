{
  "schemaVersion": 1,
  "packVersion": "1.0.0",
  "id": "community-hashi_vault",
  "name": "Community Hashi_Vault",
  "collection": "community.hashi_vault",
  "collectionVersion": "7.1.0",
  "minAppVersion": "1.0.0",
  "testedWithAppVersion": "0.1.0",
  "publishedAt": "2026-07-09T05:28:01Z",
  "qualityLevel": "generated",
  "source": "generated",
  "categoryColor": "#000000",
  "blockStyleName": "community_hashi_vault_blocks",
  "categoryStyleName": "community_hashi_vault_category",
  "modules": [
    {
      "fqcn": "community.hashi_vault.vault_database_connection_configure",
      "blockType": "community_hashi_vault_vault_database_connection_configure",
      "label": "vault_database_connection_configure",
      "tooltip": "Configures the database engine",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_connection_configure_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "allowed_roles",
          "fieldId": "ALLOWED_ROLES",
          "type": "text",
          "label": "allowed roles",
          "yamlType": "list",
          "description": "Allowed roles",
          "required": true
        },
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "Name of the database connection.",
          "required": true
        },
        {
          "key": "connection_password",
          "fieldId": "CONNECTION_PASSWORD",
          "type": "text",
          "label": "connection password",
          "description": "Password to connect to the database",
          "required": true
        },
        {
          "key": "connection_url",
          "fieldId": "CONNECTION_URL",
          "type": "text",
          "label": "connection url",
          "description": "Connection URL to the database",
          "required": true
        },
        {
          "key": "connection_username",
          "fieldId": "CONNECTION_USERNAME",
          "type": "text",
          "label": "connection username",
          "description": "Username to connect to the database",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "plugin_name",
          "fieldId": "PLUGIN_NAME",
          "type": "text",
          "label": "plugin name",
          "description": "Plugin name used to connect to the database",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_connection_delete",
      "blockType": "community_hashi_vault_vault_database_connection_delete",
      "label": "vault_database_connection_delete",
      "tooltip": "Delete a Database Connection",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_connection_delete_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name to be deleted.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_connection_read",
      "blockType": "community_hashi_vault_vault_database_connection_read",
      "label": "vault_database_connection_read",
      "tooltip": "Returns the configuration settings for a O(connection_name)",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_connection_read_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name to be read.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_connection_reset",
      "blockType": "community_hashi_vault_vault_database_connection_reset",
      "label": "vault_database_connection_reset",
      "tooltip": "Closes a O(connection_name) and its underlying plugin and restarts it with the configuration stored",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_connection_reset_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name to be resetted.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_connections_list",
      "blockType": "community_hashi_vault_vault_database_connections_list",
      "label": "vault_database_connections_list",
      "tooltip": "Returns a list of available connections",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_connections_list_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_role_create",
      "blockType": "community_hashi_vault_vault_database_role_create",
      "label": "vault_database_role_create",
      "tooltip": "Creates or updates a (dynamic) role definition",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_role_create_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name under which the role should be created.",
          "required": true
        },
        {
          "key": "creation_statements",
          "fieldId": "CREATION_STATEMENTS",
          "type": "text",
          "label": "creation statements",
          "yamlType": "list",
          "description": "Specifies the database statements executed to create and configure a user.",
          "required": true
        },
        {
          "key": "default_ttl",
          "fieldId": "DEFAULT_TTL",
          "type": "number",
          "label": "default ttl",
          "default": 3600,
          "description": "Default TTL for the role."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "max_ttl",
          "fieldId": "MAX_TTL",
          "type": "number",
          "label": "max ttl",
          "default": 86400,
          "description": "Max TTL for the role."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "renew_statements",
          "fieldId": "RENEW_STATEMENTS",
          "type": "text",
          "label": "renew statements",
          "yamlType": "list",
          "description": "Specifies the database statements to be executed to renew a user"
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "revocation_statements",
          "fieldId": "REVOCATION_STATEMENTS",
          "type": "text",
          "label": "revocation statements",
          "yamlType": "list",
          "description": "Specifies the database statements to be executed to revoke a user."
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The name of the role that should be created.",
          "required": true
        },
        {
          "key": "rollback_statements",
          "fieldId": "ROLLBACK_STATEMENTS",
          "type": "text",
          "label": "rollback statements",
          "yamlType": "list",
          "description": "Specifies the database statements to be executed to rollback a create operation in the event of an error."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_role_delete",
      "blockType": "community_hashi_vault_vault_database_role_delete",
      "label": "vault_database_role_delete",
      "tooltip": "Delete a role definition",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_role_delete_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The name of the role to rotate credentials for.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_role_read",
      "blockType": "community_hashi_vault_vault_database_role_read",
      "label": "vault_database_role_read",
      "tooltip": "Queries a dynamic role definition",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_role_read_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The role name to be read from Hashicorp Vault.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_roles_list",
      "blockType": "community_hashi_vault_vault_database_roles_list",
      "label": "vault_database_roles_list",
      "tooltip": "Returns a list of available (dynamic) roles",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_roles_list_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_rotate_root_credentials",
      "blockType": "community_hashi_vault_vault_database_rotate_root_credentials",
      "label": "vault_database_rotate_root_credentials",
      "tooltip": "Rotates the root credentials stored for the database connection. This user must have permissions to update its own password.",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_rotate_root_credentials_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name where the root credential rotation should be triggered.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_static_role_create",
      "blockType": "community_hashi_vault_vault_database_static_role_create",
      "label": "vault_database_static_role_create",
      "tooltip": "Create or update a static role",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_static_role_create_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "connection_name",
          "fieldId": "CONNECTION_NAME",
          "type": "text",
          "label": "connection name",
          "description": "The connection name under which the role should be created.",
          "required": true
        },
        {
          "key": "db_username",
          "fieldId": "DB_USERNAME",
          "type": "text",
          "label": "db username",
          "description": "The database username - Note that the user must exist in the target database!",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The name of the role that should be created.",
          "required": true
        },
        {
          "key": "rotation_period",
          "fieldId": "ROTATION_PERIOD",
          "type": "number",
          "label": "rotation period",
          "default": 86400,
          "description": "Password rotation period in seconds (defaults to 24hs)"
        },
        {
          "key": "rotation_statements",
          "fieldId": "ROTATION_STATEMENTS",
          "type": "text",
          "label": "rotation statements",
          "yamlType": "list",
          "description": "SQL statements to rotate the password for the given O(db_username)",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_static_role_get_credentials",
      "blockType": "community_hashi_vault_vault_database_static_role_get_credentials",
      "label": "vault_database_static_role_get_credentials",
      "tooltip": "Returns the current credentials based on the named static role",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_static_role_get_credentials_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The role name from which the credentials should be retrieved.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_static_role_read",
      "blockType": "community_hashi_vault_vault_database_static_role_read",
      "label": "vault_database_static_role_read",
      "tooltip": "Queries a static role definition",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_static_role_read_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The role name to be read from Hashicorp Vault.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_static_role_rotate_credentials",
      "blockType": "community_hashi_vault_vault_database_static_role_rotate_credentials",
      "label": "vault_database_static_role_rotate_credentials",
      "tooltip": "Trigger the credential rotation for a static role",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_static_role_rotate_credentials_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The name of the role to rotate credentials for.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_database_static_roles_list",
      "blockType": "community_hashi_vault_vault_database_static_roles_list",
      "label": "vault_database_static_roles_list",
      "tooltip": "Returns a list of available static roles",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_database_static_roles_list_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_kv1_get",
      "blockType": "community_hashi_vault_vault_kv1_get",
      "label": "vault_kv1_get",
      "tooltip": "Get a secret from HashiCorp Vault's KV version 1 secret store",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_kv1_get_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "default": "kv",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault KV path to be read. This is relative to the I(engine_mount_point), so the mount path should not be included.",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_kv2_delete",
      "blockType": "community_hashi_vault_vault_kv2_delete",
      "label": "vault_kv2_delete",
      "tooltip": "Delete one or more versions of a secret from HashiCorp Vault's KV version 2 secret store",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_kv2_delete_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "default": "secret",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault KV path to be deleted. This is relative to the I(engine_mount_point), so the mount path should not be included. For kv2, do not include C(/data/) or C(/metadata/).",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        },
        {
          "key": "versions",
          "fieldId": "VERSIONS",
          "type": "text",
          "label": "versions",
          "yamlType": "list",
          "description": "One or more versions of the secret to delete. When omitted, the latest version of the secret is deleted."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_kv2_get",
      "blockType": "community_hashi_vault_vault_kv2_get",
      "label": "vault_kv2_get",
      "tooltip": "Get a secret from HashiCorp Vault's KV version 2 secret store",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_kv2_get_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "default": "secret",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault KV path to be read. This is relative to the I(engine_mount_point), so the mount path should not be included. For kv2, do not include C(/data/) or C(/metadata/).",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        },
        {
          "key": "version",
          "fieldId": "VERSION",
          "type": "number",
          "label": "version",
          "description": "Specifies the version to return. If not set the latest version is returned."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_kv2_write",
      "blockType": "community_hashi_vault_vault_kv2_write",
      "label": "vault_kv2_write",
      "tooltip": "Perform a write operation against a KVv2 secret in HashiCorp Vault",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_kv2_write_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cas",
          "fieldId": "CAS",
          "type": "number",
          "label": "cas",
          "description": "Perform a check-and-set operation."
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "data",
          "fieldId": "DATA",
          "type": "text",
          "label": "data",
          "yamlType": "dict",
          "description": "KVv2 secret data to write.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "default": "secret",
          "description": "The path where the secret backend is mounted."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault KVv2 path to be written to. This is relative to the I(engine_mount_point), so the mount path should not be included.",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "read_before_write",
          "fieldId": "READ_BEFORE_WRITE",
          "type": "boolean",
          "label": "read before write",
          "default": false,
          "description": "Read the secret first and write only when I(data) differs from the read data. Requires C(read) permission on the secret if C(true). If C(false), this module will always write to I(path) when not in check mode."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_list",
      "blockType": "community_hashi_vault_vault_list",
      "label": "vault_list",
      "tooltip": "Perform a list operation against HashiCorp Vault",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_list_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault path to be listed.",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_login",
      "blockType": "community_hashi_vault_vault_login",
      "label": "vault_login",
      "tooltip": "Perform a login operation against HashiCorp Vault",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_login_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": true,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_pki_generate_certificate",
      "blockType": "community_hashi_vault_vault_pki_generate_certificate",
      "label": "vault_pki_generate_certificate",
      "tooltip": "Generates a new set of credentials (private key and certificate) using HashiCorp Vault PKI",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_pki_generate_certificate_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "alt_names",
          "fieldId": "ALT_NAMES",
          "type": "text",
          "label": "alt names",
          "yamlType": "list",
          "description": "Specifies requested Subject Alternative Names. These can be host names or email addresses; they will be parsed into their respective fields. If any requested names do not match role policy, the entire request will be denied."
        },
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "common_name",
          "fieldId": "COMMON_NAME",
          "type": "text",
          "label": "common name",
          "description": "Specifies the requested CN for the certificate. If the CN is allowed by role policy, it will be issued.",
          "required": true
        },
        {
          "key": "engine_mount_point",
          "fieldId": "ENGINE_MOUNT_POINT",
          "type": "text",
          "label": "engine mount point",
          "description": "Specify the mount point used by the PKI engine. Defaults to the default used by C(hvac)."
        },
        {
          "key": "exclude_cn_from_sans",
          "fieldId": "EXCLUDE_CN_FROM_SANS",
          "type": "boolean",
          "label": "exclude cn from sans",
          "default": false,
          "description": "If true, the given I(common_name) will not be included in DNS or Email Subject Alternate Names (as appropriate). Useful if the CN is not a hostname or email address, but is instead some human-readable identifier."
        },
        {
          "key": "format",
          "fieldId": "FORMAT",
          "type": "dropdown",
          "label": "format",
          "default": "pem",
          "description": "Specifies the format for returned data. Can be C(pem), C(der), or C(pem_bundle). If C(der), the output is base64 encoded. If C(pem_bundle), the C(certificate) field will contain the private key and certificate, concatenated. If the issuing CA is not a Vault-derived self-signed root, this will be included as well.",
          "options": [
            [
              "pem",
              "pem"
            ],
            [
              "der",
              "der"
            ],
            [
              "pem_bundle",
              "pem_bundle"
            ]
          ]
        },
        {
          "key": "ip_sans",
          "fieldId": "IP_SANS",
          "type": "text",
          "label": "ip sans",
          "yamlType": "list",
          "description": "Specifies requested IP Subject Alternative Names. Only valid if the role allows IP SANs (which is the default)."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "other_sans",
          "fieldId": "OTHER_SANS",
          "type": "text",
          "label": "other sans",
          "yamlType": "list",
          "description": "Specifies custom OID/UTF8-string SANs. These must match values specified on the role in C(allowed_other_sans). The format is the same as OpenSSL: C(<oid>;<type>:<value>) where the only current valid type is C(UTF8)."
        },
        {
          "key": "private_key_format",
          "fieldId": "PRIVATE_KEY_FORMAT",
          "type": "dropdown",
          "label": "private key format",
          "default": "der",
          "description": "Specifies the format for marshaling the private key. Defaults to C(der) which will return either base64-encoded DER or PEM-encoded DER, depending on the value of I(format). The other option is C(pkcs8) which will return the key marshalled as PEM-encoded PKCS8.",
          "options": [
            [
              "der",
              "der"
            ],
            [
              "pkcs8",
              "pkcs8"
            ]
          ]
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "Specifies the name of the role to create the certificate against.",
          "required": true
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "ttl",
          "fieldId": "TTL",
          "type": "text",
          "label": "ttl",
          "description": "Specifies requested Time To Live. Cannot be greater than the role's C(max_ttl) value. If not provided, the role's C(ttl) value will be used. Note that the role values default to system values if not explicitly set."
        },
        {
          "key": "uri_sans",
          "fieldId": "URI_SANS",
          "type": "text",
          "label": "uri sans",
          "yamlType": "list",
          "description": "Specifies the requested URI Subject Alternative Names."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_read",
      "blockType": "community_hashi_vault_vault_read",
      "label": "vault_read",
      "tooltip": "Perform a read operation against HashiCorp Vault",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_read_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault path to be read.",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_token_create",
      "blockType": "community_hashi_vault_vault_token_create",
      "label": "vault_token_create",
      "tooltip": "Create a HashiCorp Vault token",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_token_create_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "display_name",
          "fieldId": "DISPLAY_NAME",
          "type": "text",
          "label": "display name",
          "description": "The display name of the token."
        },
        {
          "key": "entity_alias",
          "fieldId": "ENTITY_ALIAS",
          "type": "text",
          "label": "entity alias",
          "description": "Name of the entity alias to associate with during token creation. Only works in combination with I(role_name) option and used entity alias must be listed in C(allowed_entity_aliases). If this has been specified, the entity will not be inherited from the parent."
        },
        {
          "key": "explicit_max_ttl",
          "fieldId": "EXPLICIT_MAX_TTL",
          "type": "text",
          "label": "explicit max ttl",
          "description": "If set, the token will have an explicit max TTL set upon it. This maximum token TTL cannot be changed later, and unlike with normal tokens, updates to the system/mount max TTL value will have no effect at renewal time. The token will never be able to be renewed or used past the value set at issue time."
        },
        {
          "key": "id",
          "fieldId": "ID",
          "type": "text",
          "label": "id",
          "description": "The ID of the client token. Can only be specified by a root token. The ID provided may not contain a C(.) character. Otherwise, the token ID is a randomly generated value."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "meta",
          "fieldId": "META",
          "type": "text",
          "label": "meta",
          "yamlType": "dict",
          "description": "A dict of string to string valued metadata. This is passed through to the audit devices."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "no_default_policy",
          "fieldId": "NO_DEFAULT_POLICY",
          "type": "boolean",
          "label": "no default policy",
          "description": "If C(true) the default policy will not be contained in this token's policy set. If the token will be used with this collection, set I(token_validate=false)."
        },
        {
          "key": "no_parent",
          "fieldId": "NO_PARENT",
          "type": "boolean",
          "label": "no parent",
          "description": "This option only has effect if used by a C(root) or C(sudo) caller and only when I(orphan=false). When C(true), the token created will not have a parent."
        },
        {
          "key": "num_uses",
          "fieldId": "NUM_USES",
          "type": "number",
          "label": "num uses",
          "description": "The maximum uses for the given token. This can be used to create a one-time-token or limited use token. The value of C(0) has no limit to the number of uses."
        },
        {
          "key": "orphan",
          "fieldId": "ORPHAN",
          "type": "boolean",
          "label": "orphan",
          "default": false,
          "description": "When C(true), uses the C(/create-orphan) API endpoint, which requires C(sudo) (but not C(root)) to create an orphan. With C(hvac>=1.0.0), requires collection version C(>=3.3.0)."
        },
        {
          "key": "period",
          "fieldId": "PERIOD",
          "type": "text",
          "label": "period",
          "description": "If specified, the token will be periodic. It will have no maximum TTL (unless an I(explicit_max_ttl) is also set) but every renewal will use the given period. Requires a root token or one with the C(sudo) capability."
        },
        {
          "key": "policies",
          "fieldId": "POLICIES",
          "type": "text",
          "label": "policies",
          "yamlType": "list",
          "description": "A list of policies for the token. This must be a subset of the policies belonging to the token making the request, unless root. If not specified, defaults to all the policies of the calling token."
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "renewable",
          "fieldId": "RENEWABLE",
          "type": "boolean",
          "label": "renewable",
          "description": "Set to C(false) to disable the ability of the token to be renewed past its initial TTL. Setting the value to C(true) will allow the token to be renewable up to the system/mount maximum TTL."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "role_name",
          "fieldId": "ROLE_NAME",
          "type": "text",
          "label": "role name",
          "description": "The name of the token role. If used, the token will be created against the specified role name which may override options set during this call."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "ttl",
          "fieldId": "TTL",
          "type": "text",
          "label": "ttl",
          "description": "The TTL period of the token, provided as C(1h) for example, where hour is the largest suffix. If not provided, the token is valid for the default lease TTL, or indefinitely if the root policy is used."
        },
        {
          "key": "type",
          "fieldId": "TYPE",
          "type": "dropdown",
          "label": "type",
          "description": "The token type. The default is determined by the role configuration specified by I(role_name).",
          "options": [
            [
              "batch",
              "batch"
            ],
            [
              "service",
              "service"
            ]
          ]
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        },
        {
          "key": "wrap_ttl",
          "fieldId": "WRAP_TTL",
          "type": "text",
          "label": "wrap ttl",
          "description": "Specifies response wrapping token creation with duration. For example C(15s), C(20m), C(25h)."
        }
      ]
    },
    {
      "fqcn": "community.hashi_vault.vault_write",
      "blockType": "community_hashi_vault_vault_write",
      "label": "vault_write",
      "tooltip": "Perform a write operation against HashiCorp Vault",
      "helpUrl": "https://docs.ansible.com/ansible/latest/collections/community/hashi_vault/vault_write_module.html",
      "structure": {
        "type": "flat"
      },
      "fields": [
        {
          "key": "auth_method",
          "fieldId": "AUTH_METHOD",
          "type": "dropdown",
          "label": "auth method",
          "default": "token",
          "description": "Authentication method to be used. C(none) auth method was added in collection version C(1.2.0). C(cert) auth method was added in collection version C(1.4.0). C(aws_iam_login) was renamed C(aws_iam) in collection version C(2.1.0) and was removed in C(3.0.0). C(azure) auth method was added in collection version C(3.2.0). C(gcp) auth method was added in collection version C(7.1.0).",
          "options": [
            [
              "token",
              "token"
            ],
            [
              "userpass",
              "userpass"
            ],
            [
              "ldap",
              "ldap"
            ],
            [
              "approle",
              "approle"
            ],
            [
              "aws_iam",
              "aws_iam"
            ],
            [
              "azure",
              "azure"
            ],
            [
              "jwt",
              "jwt"
            ],
            [
              "cert",
              "cert"
            ],
            [
              "gcp",
              "gcp"
            ],
            [
              "none",
              "none"
            ]
          ]
        },
        {
          "key": "aws_access_key",
          "fieldId": "AWS_ACCESS_KEY",
          "type": "text",
          "label": "aws access key",
          "description": "The AWS access key to use.",
          "aliases": [
            "aws_access_key_id"
          ]
        },
        {
          "key": "aws_iam_server_id",
          "fieldId": "AWS_IAM_SERVER_ID",
          "type": "text",
          "label": "aws iam server id",
          "description": "If specified, sets the value to use for the C(X-Vault-AWS-IAM-Server-ID) header as part of C(GetCallerIdentity) request."
        },
        {
          "key": "aws_profile",
          "fieldId": "AWS_PROFILE",
          "type": "text",
          "label": "aws profile",
          "description": "The AWS profile",
          "aliases": [
            "boto_profile"
          ]
        },
        {
          "key": "aws_secret_key",
          "fieldId": "AWS_SECRET_KEY",
          "type": "text",
          "label": "aws secret key",
          "description": "The AWS secret key that corresponds to the access key.",
          "aliases": [
            "aws_secret_access_key"
          ]
        },
        {
          "key": "aws_security_token",
          "fieldId": "AWS_SECURITY_TOKEN",
          "type": "text",
          "label": "aws security token",
          "description": "The AWS security token if using temporary access and secret keys."
        },
        {
          "key": "azure_client_id",
          "fieldId": "AZURE_CLIENT_ID",
          "type": "text",
          "label": "azure client id",
          "description": "The client ID (also known as application ID) of the Azure AD service principal or managed identity. Should be a UUID. If not specified, will use the system assigned managed identity."
        },
        {
          "key": "azure_client_secret",
          "fieldId": "AZURE_CLIENT_SECRET",
          "type": "text",
          "label": "azure client secret",
          "description": "The client secret of the Azure AD service principal."
        },
        {
          "key": "azure_resource",
          "fieldId": "AZURE_RESOURCE",
          "type": "text",
          "label": "azure resource",
          "default": "https://management.azure.com/",
          "description": "The resource URL for the application registered in Azure Active Directory. Usually should not be changed from the default."
        },
        {
          "key": "azure_tenant_id",
          "fieldId": "AZURE_TENANT_ID",
          "type": "text",
          "label": "azure tenant id",
          "description": "The Azure Active Directory Tenant ID (also known as the Directory ID) of the service principal. Should be a UUID. Required when using a service principal to authenticate to Vault, e.g. required when both I(azure_client_id) and I(azure_client_secret) are specified. Optional when using managed identity to authenticate to Vault."
        },
        {
          "key": "ca_cert",
          "fieldId": "CA_CERT",
          "type": "text",
          "label": "ca cert",
          "description": "Path to certificate to use for authentication. If not specified by any other means, the C(VAULT_CACERT) environment variable will be used.",
          "aliases": [
            "cacert"
          ]
        },
        {
          "key": "cert_auth_private_key",
          "fieldId": "CERT_AUTH_PRIVATE_KEY",
          "type": "text",
          "label": "cert auth private key",
          "description": "For C(cert) auth, path to the private key file to authenticate with, in PEM format."
        },
        {
          "key": "cert_auth_public_key",
          "fieldId": "CERT_AUTH_PUBLIC_KEY",
          "type": "text",
          "label": "cert auth public key",
          "description": "For C(cert) auth, path to the certificate file to authenticate with, in PEM format."
        },
        {
          "key": "data",
          "fieldId": "DATA",
          "type": "text",
          "label": "data",
          "yamlType": "dict",
          "description": "A dictionary to be serialized to JSON and then sent as the request body. If the dictionary contains keys named C(path) or C(wrap_ttl), the call will fail with C(hvac<1.2)."
        },
        {
          "key": "jwt",
          "fieldId": "JWT",
          "type": "text",
          "label": "jwt",
          "description": "The JSON Web Token (JWT) to use for JWT authentication to Vault."
        },
        {
          "key": "mount_point",
          "fieldId": "MOUNT_POINT",
          "type": "text",
          "label": "mount point",
          "description": "Vault mount point. If not specified, the default mount point for a given auth method is used. Does not apply to token authentication."
        },
        {
          "key": "namespace",
          "fieldId": "NAMESPACE",
          "type": "text",
          "label": "namespace",
          "description": "Vault namespace where secrets reside. This option requires HVAC 0.7.0+ and Vault 0.11+. Optionally, this may be achieved by prefixing the authentication mount point and/or secret path with the namespace (e.g C(mynamespace/secret/mysecret)). If environment variable C(VAULT_NAMESPACE) is set, its value will be used last among all ways to specify I(namespace)."
        },
        {
          "key": "path",
          "fieldId": "PATH",
          "type": "text",
          "label": "path",
          "description": "Vault path to be written to.",
          "required": true
        },
        {
          "key": "proxies",
          "fieldId": "PROXIES",
          "type": "text",
          "label": "proxies",
          "multiline": true,
          "description": "URL(s) to the proxies used to access the Vault service. It can be a string or a dict. If it's a dict, provide the scheme (eg. C(http) or C(https)) as the key, and the URL as the value. If it's a string, provide a single URL that will be used as the proxy for both C(http) and C(https) schemes. A string that can be interpreted as a dictionary will be converted to one (see examples). You can specify a different proxy for HTTP and HTTPS resources. If not specified, L(environment variables from the Requests library,https://requests.readthedocs.io/en/master/user/advanced/#proxies) are used."
        },
        {
          "key": "region",
          "fieldId": "REGION",
          "type": "text",
          "label": "region",
          "description": "The AWS region for which to create the connection."
        },
        {
          "key": "retries",
          "fieldId": "RETRIES",
          "type": "text",
          "label": "retries",
          "multiline": true,
          "description": "Allows for retrying on errors, based on the L(Retry class in the urllib3 library,https://urllib3.readthedocs.io/en/latest/reference/urllib3.util.html#urllib3.util.Retry). This collection defines recommended defaults for retrying connections to Vault. This option can be specified as a positive number (integer) or dictionary. If this option is not specified or the number is C(0), then retries are disabled. A number sets the total number of retries, and uses collection defaults for the other settings. A dictionary value is used directly to initialize the C(Retry) class, so it can be used to fully customize retries. For detailed information on retries, see the collection User Guide."
        },
        {
          "key": "retry_action",
          "fieldId": "RETRY_ACTION",
          "type": "dropdown",
          "label": "retry action",
          "default": "warn",
          "description": "Controls whether and how to show messages on I(retries). This has no effect if a request is not retried.",
          "options": [
            [
              "ignore",
              "ignore"
            ],
            [
              "warn",
              "warn"
            ]
          ]
        },
        {
          "key": "role_id",
          "fieldId": "ROLE_ID",
          "type": "text",
          "label": "role id",
          "description": "Vault Role ID or name. Used in C(approle), C(aws_iam), C(azure) and C(cert) auth methods. For C(cert) auth, if no I(role_id) is supplied, the default behavior is to try all certificate roles and return any one that matches. For C(azure) auth, I(role_id) is required."
        },
        {
          "key": "secret_id",
          "fieldId": "SECRET_ID",
          "type": "text",
          "label": "secret id",
          "description": "Secret ID to be used for Vault AppRole authentication."
        },
        {
          "key": "token",
          "fieldId": "TOKEN",
          "type": "text",
          "label": "token",
          "description": "Vault token. Token may be specified explicitly, through the listed [env] vars, and also through the C(VAULT_TOKEN) env var. If no token is supplied, explicitly or through env, then the plugin will check for a token file, as determined by I(token_path) and I(token_file). The order of token loading (first found wins) is C(token param -> ansible var -> ANSIBLE_HASHI_VAULT_TOKEN -> VAULT_TOKEN -> token file)."
        },
        {
          "key": "token_file",
          "fieldId": "TOKEN_FILE",
          "type": "text",
          "label": "token file",
          "default": ".vault-token",
          "description": "If no token is specified, will try to read the token from this file in I(token_path)."
        },
        {
          "key": "token_path",
          "fieldId": "TOKEN_PATH",
          "type": "text",
          "label": "token path",
          "description": "If no token is specified, will try to read the I(token_file) from this path."
        },
        {
          "key": "token_validate",
          "fieldId": "TOKEN_VALIDATE",
          "type": "boolean",
          "label": "token validate",
          "default": false,
          "description": "For token auth, will perform a C(lookup-self) operation to determine the token's validity before using it. Disable if your token does not have the C(lookup-self) capability."
        },
        {
          "key": "url",
          "fieldId": "URL",
          "type": "text",
          "label": "url",
          "description": "URL to the Vault service. If not specified by any other means, the value of the C(VAULT_ADDR) environment variable will be used. If C(VAULT_ADDR) is also not defined then an error will be raised."
        },
        {
          "key": "validate_certs",
          "fieldId": "VALIDATE_CERTS",
          "type": "boolean",
          "label": "validate certs",
          "description": "Controls verification and validation of SSL certificates, mostly you only want to turn off with self signed ones. Will be populated with the inverse of C(VAULT_SKIP_VERIFY) if that is set and I(validate_certs) is not explicitly provided. Will default to C(true) if neither I(validate_certs) or C(VAULT_SKIP_VERIFY) are set."
        },
        {
          "key": "wrap_ttl",
          "fieldId": "WRAP_TTL",
          "type": "text",
          "label": "wrap ttl",
          "description": "Specifies response wrapping token creation with duration. For example C(15s), C(20m), C(25h)."
        }
      ]
    }
  ]
}
